---
phase: 94-data-tables
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_webview_api.py
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "A hand-authored fixture provides a temp analysis_dir with peaks/{carbon_signals,hsqc,hmbc,cosy}.json matching the CONTEXT.md schema"
    - "A multi-iteration compound.lsd fixture includes a family-suffixed dir (iteration_02_foo) higher-numbered than a bare iteration_01"
    - "TestTablesEndpoint collects cleanly and skips (not errors) even before tables.py exists (WV-08 import-safety)"
  artifacts:
    - path: "tests/test_webview_api.py"
      provides: "TestTablesEndpoint class + tables_analysis_dir / tables_iterations_dir fixtures"
      contains: "class TestTablesEndpoint"
  key_links:
    - from: "tests/test_webview_api.py"
      to: "lucy_ng.webview.routers.tables"
      via: "try/except ImportError: pytest.skip"
      pattern: "except ImportError"
---

<objective>
Author the Wave-0 test scaffold for Phase 94: hand-built fixtures matching the
CONTEXT.md peaks-JSON schema plus a multi-iteration compound.lsd set, and a new
`TestTablesEndpoint` class that exercises TBL-01/02/03 and the SC4 degradation
contract. Tests are RED-by-skip until `tables.py` (Plan 02) exists — the WV-08
`try/except ImportError: pytest.skip(...)` guard makes the file collect cleanly now.

Purpose: Establish the executable acceptance target BEFORE any router code, so Plan
02 is written against a fixed contract (no field-name drift — RESEARCH Assumptions
A1-A5: no on-disk file matches the canonical schema, so fixtures MUST be hand-authored).
Output: `tests/test_webview_api.py` with fixtures + `TestTablesEndpoint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-data-tables/94-CONTEXT.md
@.planning/phases/94-data-tables/94-RESEARCH.md
@.planning/phases/94-data-tables/94-VALIDATION.md
@.planning/phases/94-data-tables/94-PATTERNS.md

<interfaces>
<!-- The router contract Plan 02 will implement, and this scaffold asserts against. -->
<!-- Endpoints (5, GET-only, prefix /api): -->
<!--   /api/tables/carbon      -> {"state":"ok"|"waiting", "note", "counts":{}, "rows":[]} -->
<!--   /api/tables/hsqc        -> same shape -->
<!--   /api/tables/hmbc        -> same shape (each row preserves its "flag" verbatim) -->
<!--   /api/tables/cosy        -> same shape -->
<!--   /api/tables/constraints -> {"state":"ok"|"waiting", "note", "inventory":{...}} -->
<!-- Factory: tables.make_router(analysis_dir: Path) -> APIRouter(prefix="/api") -->

<!-- CONTEXT.md peaks-JSON target schema (LOCKED — build fixtures to THIS, not to any on-disk file): -->
<!-- carbon_signals.json: top-level formula/dbe/solvent/note + signals[]:{atom,ppm,mult,nC,assignment,confidence} -->
<!-- hsqc.json: experiment/count/note + peaks[]:{carbon_ppm,proton_ppm,intensity,matched_real_carbon,one_bond} -->
<!-- hmbc.json: experiment/raw_count/kept_count/flag_rules/note + peaks[]:{carbon_ppm,carbon_ppm_observed,proton_ppm,intensity,flag} flag in {ok,potential_4J,1J_artifact} -->
<!-- cosy.json: experiment/count/note + peaks[]:{proton_a_ppm,proton_b_ppm,intensity} -->
<!-- compound.lsd CONSTRAINT INVENTORY v2 block: "; "-prefixed JSON between "; === CONSTRAINT INVENTORY v2 ===" and "; === END CONSTRAINT INVENTORY ===" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author Phase-94 fixtures (peaks JSON + multi-iteration compound.lsd)</name>
  <files>tests/test_webview_api.py</files>
  <read_first>
    - tests/test_webview_api.py — existing `TestStructuresEndpoint::test_ranked_with_null_rank_does_not_drop_tier` (~line 215) is the analog for building a bespoke `tmp_path`-scoped fixture inline via `mkdir` + `write_text(json.dumps(...))`.
    - tests/conftest.py — `live_analysis_dir` / `final_analysis_dir` / `empty_analysis_dir` fixtures (analog construction style: mkdir → write_text).
    - .planning/phases/94-data-tables/94-CONTEXT.md `<canonical_refs>` "Data shapes" — the exact peaks-JSON field names (LOCKED target schema).
    - .planning/phases/94-data-tables/94-RESEARCH.md "Code Examples → Real, on-disk CONSTRAINT INVENTORY v2 block" (CASE4) — adapt this block shape for the compound.lsd fixture; Assumptions A1-A5 (do NOT copy any on-disk peaks file — none matches).
  </read_first>
  <action>
    Add two pytest fixtures to `tests/test_webview_api.py`. (1) `tables_analysis_dir(tmp_path)` — creates `tmp_path/peaks/` and writes four hand-authored JSON files to the LOCKED CONTEXT.md schema: `carbon_signals.json` (top-level `formula`/`dbe`/`solvent`/`note` + `signals[]` with `atom,ppm,mult,nC,assignment,confidence`), `hsqc.json` (`experiment`/`count`/`note` + `peaks[]` with `carbon_ppm,proton_ppm,intensity,matched_real_carbon,one_bond`), `hmbc.json` (`experiment`/`raw_count`/`kept_count`/`flag_rules`/`note` + `peaks[]` with `carbon_ppm,carbon_ppm_observed,proton_ppm,intensity,flag`) — include at least one row of EACH flag value `ok`, `potential_4J`, `1J_artifact`; and `cosy.json` (`experiment`/`count`/`note` + `peaks[]` with `proton_a_ppm,proton_b_ppm,intensity`). Use a large `intensity` such as `5559614` in at least one row so the compact-intensity test target is exercisable downstream. Return the `tmp_path`. (2) `tables_iterations_dir(tmp_path)` — creates `iteration_01/compound.lsd` (bare) and `iteration_02_anchor_recovery/compound.lsd` (family-suffixed, HIGHER numeric prefix) each carrying a CONSTRAINT INVENTORY v2 block adapted from the CASE4 excerpt: `; `-prefixed JSON between `; === CONSTRAINT INVENTORY v2 ===` and `; === END CONSTRAINT INVENTORY ===`, with `iteration` set to 1 and 2 respectively and populated `mult_count`,`hsqc_count`,`hmbc_batches`,`hmbc_total`,`bond_constraints`,`cosy_equiv_pairs`,`applied_from_detection`,`pending_from_detection`,`deff_fexp`,`elim_budget`,`ring_exclusion_enabled`. Return the `tmp_path`. Fixtures are module-level in this test file (matching how bespoke fixtures are built inline elsewhere), not in conftest.py, to keep file ownership isolated to this plan.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/test_webview_api.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_webview_api.py` defines a fixture named `tables_analysis_dir` that writes `peaks/carbon_signals.json`, `peaks/hsqc.json`, `peaks/hmbc.json`, `peaks/cosy.json`.
    - The `hmbc.json` fixture `peaks[]` contains at least one row each with `flag` == `"ok"`, `"potential_4J"`, and `"1J_artifact"`.
    - At least one intensity value in the fixtures equals `5559614` (compact-format target).
    - `tables_iterations_dir` writes BOTH `iteration_01/compound.lsd` AND `iteration_02_anchor_recovery/compound.lsd` (a family-suffixed dir with a higher numeric prefix).
    - Each compound.lsd fixture contains the literal delimiters `; === CONSTRAINT INVENTORY v2 ===` and `; === END CONSTRAINT INVENTORY ===` with `; `-prefixed JSON lines between them.
    - File parses as valid Python (`ast.parse` succeeds).
  </acceptance_criteria>
  <done>Both fixtures present, hand-authored to the CONTEXT.md schema, file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add TestTablesEndpoint class (RED-by-skip until tables.py exists)</name>
  <files>tests/test_webview_api.py</files>
  <read_first>
    - tests/test_webview_api.py — `TestLogEndpoint` (~line 106) for the ok/waiting TestClient shape and the WV-08 `try/except ImportError: pytest.skip(...)` guard block copied verbatim into every method; `TestStructuresEndpoint::test_ranked_with_null_rank_does_not_drop_tier` for the inline-`tmp_path`-fixture assertion style.
    - .planning/phases/94-data-tables/94-VALIDATION.md "Per-Task Verification Map" + "Wave 0 Requirements" — the exact test method names required.
    - .planning/phases/94-data-tables/94-PATTERNS.md "tests/test_webview_api.py" section — import-safety pattern (non-negotiable).
  </read_first>
  <action>
    Add `class TestTablesEndpoint` to `tests/test_webview_api.py`, following `TestLogEndpoint`/`TestStructuresEndpoint`. Every method opens with the WV-08 guard: `try: from fastapi.testclient import TestClient; from lucy_ng.webview.routers import tables` / `except ImportError: pytest.skip("webview extra or tables router not yet available")`, then `from fastapi import FastAPI`, build `app = FastAPI(); app.include_router(tables.make_router(<dir>))`, and `with TestClient(app) as client:`. Methods: (1) `test_carbon_returns_rows(tables_analysis_dir)` — `GET /api/tables/carbon` → 200, `state=="ok"`, rows non-empty, each row exposes `ppm`/`mult`/`nC`/`assignment`/`confidence`. (2) `test_carbon_waiting_when_absent(empty_analysis_dir)` — `state=="waiting"`, HTTP 200. (3) `test_hsqc_cosy_return_ok(tables_analysis_dir)` — both `/hsqc` and `/cosy` → `state=="ok"` with their field sets. (4) `test_hmbc_preserves_flag_verbatim(tables_analysis_dir)` — `/hmbc` rows retain each `flag` value UNMODIFIED (assert the set of flags returned equals `{"ok","potential_4J","1J_artifact"}`; colouring is NOT a backend concern). (5) `test_malformed_json_returns_waiting(tmp_path)` — write `peaks/carbon_signals.json` containing invalid JSON (e.g. `"{not valid"`), assert `/carbon` → `state=="waiting"`, HTTP 200, never 500. (6) `test_constraints_selects_highest_iteration(tables_iterations_dir)` — `/constraints` → `state=="ok"` and the returned inventory's `iteration` == 2 (the family-suffixed `iteration_02_anchor_recovery` won by highest numeric prefix). (7) `test_constraints_waiting_when_malformed(tmp_path)` — write `iteration_01/compound.lsd` with a START delimiter but NO END delimiter, assert `/constraints` → `state=="waiting"`, HTTP 200. (8) `test_constraints_waiting_when_absent(empty_analysis_dir)` — `state=="waiting"`, HTTP 200. SC4 must be proven independently for ALL five panels, not just carbon/constraints — add dedicated absent-and-malformed methods for the remaining three peaks endpoints, mirroring the carbon waiting/malformed pair exactly: (9) `test_hsqc_waiting_when_absent(empty_analysis_dir)`, (10) `test_hsqc_waiting_when_malformed(tmp_path)` (write `peaks/hsqc.json` with invalid JSON), (11) `test_hmbc_waiting_when_absent(empty_analysis_dir)`, (12) `test_hmbc_waiting_when_malformed(tmp_path)` (write `peaks/hmbc.json` with invalid JSON), (13) `test_cosy_waiting_when_absent(empty_analysis_dir)`, (14) `test_cosy_waiting_when_malformed(tmp_path)` (write `peaks/cosy.json` with invalid JSON) — each asserting `state=="waiting"`, HTTP 200, never 500. Assertions must use exact string comparisons and include the response body in failure messages (match existing style).
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestTablesEndpoint -x -q</automated>
    <automated>ruff check tests/test_webview_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_webview_api.py::TestTablesEndpoint -x -q` collects without ImportError and reports SKIPPED (not FAILED, not ERROR) while `tables.py` is absent.
    - Class contains exactly these 14 methods: `test_carbon_returns_rows`, `test_carbon_waiting_when_absent`, `test_hsqc_cosy_return_ok`, `test_hmbc_preserves_flag_verbatim`, `test_malformed_json_returns_waiting`, `test_constraints_selects_highest_iteration`, `test_constraints_waiting_when_malformed`, `test_constraints_waiting_when_absent`, `test_hsqc_waiting_when_absent`, `test_hsqc_waiting_when_malformed`, `test_hmbc_waiting_when_absent`, `test_hmbc_waiting_when_malformed`, `test_cosy_waiting_when_absent`, `test_cosy_waiting_when_malformed`.
    - SC4 (absent + malformed → `state="waiting"`, HTTP 200, never 500) is independently asserted for ALL 5 panels: carbon, hsqc, hmbc, cosy, constraints.
    - Every method contains the literal `pytest.skip(` guard inside a `try/except ImportError`.
    - `test_constraints_selects_highest_iteration` asserts the returned inventory `iteration` equals `2`.
    - `test_hmbc_preserves_flag_verbatim` asserts the returned flag set equals `{"ok","potential_4J","1J_artifact"}`.
    - `ruff check tests/test_webview_api.py` passes (CLAUDE.md: tests are in lint scope).
    - Full existing suite still passes: `pytest tests/test_webview_api.py -q` shows no new failures/errors.
  </acceptance_criteria>
  <done>TestTablesEndpoint present with 14 named methods (SC4 proven for all 5 panels), all SKIP cleanly pre-tables.py; ruff clean; existing suite unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture files → router-under-test | Fixture content is authored by this plan; no external/untrusted input. Test-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01-01 | Tampering | Fixture drift vs LOCKED CONTEXT.md schema | mitigate | Fixtures hand-authored to CONTEXT.md `<canonical_refs>` field names verbatim; acceptance criteria pin exact field names and flag values. |
| T-94-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: zero new deps); nothing to audit. |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestTablesEndpoint -x -q` → all SKIPPED (tables.py absent).
- `pytest tests/test_webview_api.py -q` → no new failures introduced.
</verification>

<success_criteria>
- Two hand-authored fixtures (`tables_analysis_dir`, `tables_iterations_dir`) matching the LOCKED CONTEXT.md schema.
- `TestTablesEndpoint` with 14 methods covering TBL-01/02/03 + SC4 (absent+malformed proven for all 5 panels), all SKIP cleanly until Plan 02 lands.
</success_criteria>

<output>
Create `.planning/phases/94-data-tables/94-01-SUMMARY.md` when done.
</output>
