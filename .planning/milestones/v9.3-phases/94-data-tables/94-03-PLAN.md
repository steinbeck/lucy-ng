---
phase: 94-data-tables
plan: 03
type: execute
wave: 2
depends_on: [94-02]
files_modified:
  - src/lucy_ng/webview/static/index.html
  - src/lucy_ng/webview/static/webview.js
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "The Tables tab shows 5 stacked sections (¹³C Signals, HSQC, HMBC, COSY, LSD Constraint Inventory), each rendered and each able to independently show 'waiting for data' (SC4)"
    - "HMBC rows carry per-flag CSS classes (.row-ok / .row-potential-4j / .row-1j-artifact) applied by post-processing buildTable output, never innerHTML"
    - "Intensity cells render compact (5559614 -> 5.6M, 5559 -> 5.6K) client-side"
    - "TBL-03 renders as three subsections (Applied Constraints / Constraint Summary / Deferred-Pending), not one flat table"
  artifacts:
    - path: "src/lucy_ng/webview/static/index.html"
      provides: "5 Tables-tab sections + CSS tokens (table density, amber HMBC triad, .table-waiting)"
      contains: "data-panel=\"tables\""
    - path: "src/lucy_ng/webview/static/webview.js"
      provides: "5 fetch/render pairs registered in tick(), compact-intensity formatter, HMBC row colouring, TBL-03 structured render"
      contains: "api/tables/carbon"
  key_links:
    - from: "src/lucy_ng/webview/static/webview.js"
      to: "/api/tables/carbon,hsqc,hmbc,cosy,constraints"
      via: "fetch in refresh* registered in tick()"
      pattern: "api/tables"
    - from: "src/lucy_ng/webview/static/webview.js"
      to: "buildTable"
      via: "reuse + post-process querySelectorAll('tbody tr').className"
      pattern: "querySelectorAll"
---

<objective>
Populate the Tables tab: replace the `data-panel="tables"` placeholder with 5 stacked
sections + the required CSS tokens (UI-SPEC), and add 5 independent fetch→render→catch
functions to `webview.js` (registered in the existing ~3 s `tick()` poll) that reuse
`buildTable`/`appendInline`, apply HMBC flag row-classes by post-processing, format
intensity compactly, and render TBL-03 as the D-01 structured multi-section view.

Purpose: The frontend half of Phase 94 — turn the five backend routes into visible,
QC-grade tables while preserving the D-02/93 XSS discipline (no innerHTML of server content).
Output: modified `index.html` + `webview.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-data-tables/94-CONTEXT.md
@.planning/phases/94-data-tables/94-UI-SPEC.md
@.planning/phases/94-data-tables/94-PATTERNS.md

@src/lucy_ng/webview/static/index.html
@src/lucy_ng/webview/static/webview.js

<interfaces>
<!-- Backend payloads (from Plan 02) the render functions consume: -->
<!--   /api/tables/{carbon,hsqc,hmbc,cosy} -> {"state","note","counts":{},"rows":[ {..fields..} ]} -->
<!--   /api/tables/constraints             -> {"state","note","inventory":{...} | null} -->
<!-- Reusable frontend helpers (verbatim, do NOT modify): -->
<!--   buildTable(headerCells, rows)  — rows: string[][]; returns <table>; appendInline handles text safely. -->
<!--   appendInline(parent, text)     — textContent/createElement only (no innerHTML). -->
<!-- Poll driver: tick() (~line 385) calls refreshStatus/refreshStructures/refreshLog; register the 5 new refresh* here. -->
<!-- Tab wiring: initTabs() keys off data-panel attribute; keep data-panel="tables"; no initTabs change needed. -->

<!-- HMBC flag -> CSS class map (UI-SPEC, exact): -->
<!--   ok -> .row-ok ; potential_4J -> .row-potential-4j ; 1J_artifact -> .row-1j-artifact -->
<!-- Compact intensity (UI-SPEC, exact): abs>=1e6 -> (v/1e6).toFixed(1)+"M"; abs>=1e3 -> (v/1e3).toFixed(1)+"K"; else raw int. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Tables panel structure + CSS tokens in index.html</name>
  <files>src/lucy_ng/webview/static/index.html</files>
  <read_first>
    - src/lucy_ng/webview/static/index.html — the `<div class="placeholder" data-panel="tables">` placeholder (line 323); the `#log-panel table/th/td` density block (~lines 253-271); the `badge-*` colour-token block (~lines 43-46); `[data-panel]` container rule (~line 166); `#structure-hint` caption style.
    - .planning/phases/94-data-tables/94-UI-SPEC.md — "Tables-tab panel structure" (5 section order + headings), "HMBC Flag Color Map" (exact tokens), "Per-table waiting state" (`.table-waiting`), "Table density", "Per-table caption copy", "TBL-03 structured multi-section layout".
    - .planning/phases/94-data-tables/94-PATTERNS.md — index.html Analog 1 & 2.
  </read_first>
  <action>
    Replace the placeholder `<div class="placeholder" data-panel="tables">…</div>` with a real container that KEEPS `data-panel="tables"` (drop the `placeholder` class so it is not treated as a not-yet-built tab) and nests 5 `<section>` blocks in fixed order, each with a heading, a caption element, and a body container: `#table-carbon` ("¹³C Signals", 14px/600), `#table-hsqc` ("HSQC"), `#table-hmbc` ("HMBC"), `#table-cosy` ("COSY"), `#table-constraints` ("LSD Constraint Inventory", 16px/600 Display role). Add CSS in the existing inline `<style>` block, reusing shipped tokens only: (a) generalise the `#log-panel table/th/td` density rules to also apply under the tables panel — add a shared `.data-table` class selector (or scope the same rules under `[data-panel="tables"] table`) with `border-collapse:collapse; font-size:12px; cell padding 4px 8px; th background #f8f9fa/#495057/600; td color #212529; border 1px solid #dee2e6`; no zebra striping. (b) HMBC row classes per UI-SPEC exact tokens: `.row-ok { border-left:3px solid #28a745 }`, `.row-potential-4j { background:#fff3cd; color:#856404; border-left:3px solid #ffc107 }`, `.row-1j-artifact { background:#f1f3f5; color:#adb5bd; border-left:3px solid #adb5bd }`. (c) `.table-waiting { font-size:12px; font-style:italic; color:#6c757d; padding:12px 0 }`. (d) a caption class matching `#structure-hint` (12px/400/#6c757d). (e) 16px gap between the 5 sections, 24px between TBL-03's subsections. Introduce NO new colours/fonts/sizes beyond those enumerated in UI-SPEC.
  </action>
  <verify>
    <automated>python -c "s=open('src/lucy_ng/webview/static/index.html').read(); assert 'data-panel=\"tables\"' in s and 'placeholder\" data-panel=\"tables\"' not in s and all(x in s for x in ['table-carbon','table-hsqc','table-hmbc','table-cosy','table-constraints','row-potential-4j','row-1j-artifact','table-waiting']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `data-panel="tables"` is retained but no longer carries the `placeholder` class.
    - The panel contains 5 section containers with ids `table-carbon`, `table-hsqc`, `table-hmbc`, `table-cosy`, `table-constraints`.
    - CSS defines `.row-ok`, `.row-potential-4j`, `.row-1j-artifact` with exactly the UI-SPEC hex tokens (`#fff3cd`/`#856404`/`#ffc107` for potential-4j; `#f1f3f5`/`#adb5bd` for 1j-artifact; `#28a745` left border for ok).
    - CSS defines `.table-waiting` (italic, `#6c757d`).
    - No `innerHTML` introduced; no new colour outside the UI-SPEC enumerated set.
    - `pytest tests/test_webview_api.py::TestFrontend -x -q` still passes (index.html still served).
  </acceptance_criteria>
  <done>Tables panel has 5 sections + all required CSS tokens; frontend-served test green.</done>
</task>

<task type="auto">
  <name>Task 2: Render the 4 peaks tables (carbon/hsqc/hmbc/cosy) in webview.js</name>
  <files>src/lucy_ng/webview/static/webview.js</files>
  <read_first>
    - src/lucy_ng/webview/static/webview.js — URL consts (`STRUCTURES_URL`/`LOG_URL`, ~lines 5-6); `refreshStructures`/`renderStructures` fetch→render pair (~lines 74-79) + hint-text caption precedent (~lines 171-177); `buildTable` (~lines 270-294); `appendInline` (~lines 298-320); `tick()` (~line 385).
    - .planning/phases/94-data-tables/94-UI-SPEC.md — "HMBC Flag Color Map" (post-process option 2), "Compact intensity formatting" (exact algorithm), "Per-table caption copy" (`{note} — {counts}` format, drop dangling em-dash when note absent), "Copywriting Contract" waiting-state strings.
    - .planning/phases/94-data-tables/94-CONTEXT.md D-03/D-04/D-05; .planning/phases/94-data-tables/94-PATTERNS.md webview.js Analog 1 & 2 + Shared Pattern 6 (XSS discipline).
  </read_first>
  <action>
    Add near the existing URL consts: `CARBON_URL='/api/tables/carbon'`, `HSQC_URL`, `HMBC_URL`, `COSY_URL` (the constraints URL is added in Task 3). Add a `formatIntensity(v)` helper implementing the UI-SPEC algorithm exactly: `Math.abs(v) >= 1e6` → `(v/1e6).toFixed(1)+'M'`; `>= 1e3` → `(v/1e3).toFixed(1)+'K'`; else the raw integer as string. Add a caption helper that sets a caption element's `textContent` (no innerHTML), omitting a dangling ` — ` when note is empty (D-05). Add 4 `refreshX()/renderX()` pairs mirroring `refreshStructures`/`renderStructures`: each `refreshX` does `fetch(URL).then(json).then(renderX).catch(console.warn)`; each `renderX` clears its section body, and on `state !== 'ok'` (or empty rows) writes a `.table-waiting` element with the exact UI-SPEC copy ("Waiting for ¹³C signal data...", "Waiting for HSQC correlation data...", "Waiting for HMBC correlation data...", "Waiting for COSY correlation data...") via textContent, else builds the row matrix (`string[][]`) with columns per D-04 — carbon: `ppm|mult|nC|assignment|confidence`; hsqc: carbon/proton shifts + `matched_real_carbon` + `one_bond` + intensity(compact); hmbc: `carbon_ppm`(+`carbon_ppm_observed`) + proton_ppm + flag + intensity(compact); cosy: `proton_a_ppm|proton_b_ppm|intensity(compact)` — call `buildTable(headers, rows)`, render the caption from `note`+`counts` per D-05, and append the table to the section body. For HMBC ONLY: after `buildTable`, post-process `table.querySelectorAll('tbody tr')` and set each `tr.className` by index to the flag→class map (`ok`→`row-ok`, `potential_4J`→`row-potential-4j`, `1J_artifact`→`row-1j-artifact`) matching the fetched row order — do NOT fork buildTable, do NOT use innerHTML. Register `refreshCarbon()`, `refreshHsqc()`, `refreshHmbc()`, `refreshCosy()` inside `tick()`. Each panel renders and catches independently (SC4: one malformed source must not blank the other four).
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestMarkdownRendererSafety::test_no_innerhtml_in_source -x -q</automated>
    <automated>python -c "s=open('src/lucy_ng/webview/static/webview.js').read(); assert all(x in s for x in ['/api/tables/carbon','/api/tables/hsqc','/api/tables/hmbc','/api/tables/cosy','formatIntensity','row-potential-4j','querySelectorAll']) and 'innerHTML' not in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `webview.js` defines URL consts for `/api/tables/carbon`, `/hsqc`, `/hmbc`, `/cosy` and a `formatIntensity` function implementing the `M`/`K`/raw branches.
    - `formatIntensity(5559614)` yields `"5.6M"` and `formatIntensity(5559)` yields `"5.6K"` (verify by extracting/evaluating the function or a node one-liner).
    - `refreshCarbon`/`refreshHsqc`/`refreshHmbc`/`refreshCosy` are each called inside `tick()`.
    - HMBC render post-processes rows via `querySelectorAll('tbody tr')` and assigns `row-ok`/`row-potential-4j`/`row-1j-artifact` (no innerHTML, no fork of buildTable).
    - Waiting-state copy strings match the UI-SPEC Copywriting Contract exactly.
    - `test_no_innerhtml_in_source` still passes (no `innerHTML` substring anywhere in webview.js).
  </acceptance_criteria>
  <done>4 peaks tables render with captions, compact intensity, and HMBC flag colouring; innerHTML guard green.</done>
</task>

<task type="auto">
  <name>Task 3: Render TBL-03 structured LSD constraint inventory (3 subsections)</name>
  <files>src/lucy_ng/webview/static/webview.js</files>
  <read_first>
    - src/lucy_ng/webview/static/webview.js — `buildTable`/`appendInline`; the `refreshX`/`renderX` pattern added in Task 2; the shipped `#log-panel ul`/`li` list styling reused for the Deferred/Pending list; `tick()`.
    - .planning/phases/94-data-tables/94-UI-SPEC.md — "TBL-03 structured multi-section layout" (exact section order, empty-state copy, `Yes`/`No` rendering, 24px gaps) + "Per-table caption copy" (LSD caption = `Iteration {iteration} · {formula} · {timestamp}`).
    - .planning/phases/94-data-tables/94-CONTEXT.md D-01/D-02; .planning/phases/94-data-tables/94-RESEARCH.md "Code Examples" (inventory field names + `additionalProperties: true` → `.get()`-defensive) and Anti-Patterns (never assume a fixed key set).
  </read_first>
  <action>
    Add `CONSTRAINTS_URL='/api/tables/constraints'` and a `refreshConstraints()/renderConstraints()` pair, registered in `tick()`. `renderConstraints` clears `#table-constraints` body; on `state !== 'ok'` writes the `.table-waiting` copy "Waiting for LSD constraint data..." via textContent; else reads `data.inventory` (defensively via property access with fallbacks) and renders the caption `Iteration {iteration} · {formula} · {timestamp}` then THREE subsections in order (all textContent/createElement, no innerHTML): (1) "Applied Constraints" — `buildTable(['Type','Atom Indices','Note'], rows)` where rows come from `bond_constraints` (type `BOND`, each entry e.g. "1 14"), each `hmbc_batches[].correlations` entry (type `HMBC`), and `cosy_equiv_pairs` (type `COSY-equiv`); the Note column draws the matching `applied_from_detection` narrative where one maps, otherwise a genuinely empty cell; if all source arrays are empty render a single body row spanning 3 columns reading "No applied constraints recorded". (2) "Constraint Summary" — `buildTable(['Constraint','Value'], rows)` in fixed row order: `MULT`→`mult_count`, `HSQC`→`hsqc_count`, `HMBC total`→`hmbc_total`, `ELIM budget`→`elim_budget`, `Ring exclusion`→`ring_exclusion_enabled` rendered as `Yes`/`No` (never raw true/false), `DEFF/FEXP status`→`deff_fexp.status` (`.get()`-defensive). (3) "Deferred / Pending" — a `<ul>`/`<li>` list from `pending_from_detection`, one `<li>` per string via textContent; if empty render one `<li>` "Nothing deferred this iteration." Every inventory field access uses a defensive fallback (`||`/optional chaining) because the schema is `additionalProperties: true` and real files carry extra keys — a missing key must not throw. Subsections separated by the 24px (lg) gap from Task 1's CSS.
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestMarkdownRendererSafety::test_no_innerhtml_in_source -x -q</automated>
    <automated>python -c "s=open('src/lucy_ng/webview/static/webview.js').read(); assert all(x in s for x in ['/api/tables/constraints','Applied Constraints','Constraint Summary','Deferred']) and 'innerHTML' not in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `webview.js` defines `refreshConstraints` and it is called inside `tick()`.
    - `renderConstraints` produces THREE labelled subsections: "Applied Constraints", "Constraint Summary", "Deferred / Pending" (not one flat table).
    - `ring_exclusion_enabled` renders as `Yes`/`No`, never raw `true`/`false`.
    - Empty applied-constraint arrays render the "No applied constraints recorded" row; empty `pending_from_detection` renders "Nothing deferred this iteration.".
    - Inventory field access is defensive (missing key does not throw) — e.g. `deff_fexp.status` guarded.
    - `test_no_innerhtml_in_source` passes; no `innerHTML` in webview.js.
    - Full suite green: `pytest tests/test_webview_api.py -q`.
  </acceptance_criteria>
  <done>TBL-03 renders as the D-01 structured 3-subsection view, defensively parsed, innerHTML-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server file content → DOM | File-derived strings (`note`, `assignment`, narratives) rendered into the DOM; the XSS boundary. |
| HTTP client → tab UI | Client only reads; no interactive controls introduced (read-only dashboard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-01 | Tampering / Information Disclosure | Reflected XSS via a malicious `assignment`/`note`/narrative string containing `<img onerror=...>` | mitigate | All rendering uses `buildTable`/`appendInline` (`textContent`/`createElement`) and `.className` assignment; NO `innerHTML` of server content; enforced by the whole-file `test_no_innerhtml_in_source` scan re-run in every task verify. |
| T-94-03-02 | Denial of Service | A malformed backend payload blanks all 5 tables | accept/mitigate | Each panel fetches+catches independently (SC4); one failing source only shows its own `.table-waiting`. |
| T-94-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs; frontend is vanilla JS, no CDN, no build step (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestMarkdownRendererSafety::test_no_innerhtml_in_source -x -q` → green.
- `pytest tests/test_webview_api.py -q` → full webview suite green.
- index.html + webview.js contain the 5 sections, 5 fetch URLs, compact-intensity + HMBC-class post-process.
</verification>

<success_criteria>
- Tables tab shows 5 independently-rendering sections with per-table captions.
- HMBC rows colour-coded by flag via CSS classes (no innerHTML); intensity shown compact.
- TBL-03 renders as the D-01 structured 3-subsection layout.
- XSS discipline preserved (no innerHTML anywhere in webview.js).
</success_criteria>

<output>
Create `.planning/phases/94-data-tables/94-03-SUMMARY.md` when done.
</output>
