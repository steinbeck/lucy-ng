---
phase: 94-data-tables
plan: 04
type: execute
wave: 3
depends_on: [94-03]
files_modified: []
autonomous: false
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "In a live browser, the Tables tab shows all 5 tables with real data"
    - "HMBC rows are visibly colour-coded by flag (ok / potential_4J / 1J_artifact)"
    - "Each table shows its caption (note + counts) above it"
    - "Intensity cells display as compact 5.6M / 1.5M form, not raw integers"
    - "TBL-03 shows the structured 3-subsection layout with Deferred/Pending reasoning"
  artifacts: []
  key_links: []
---

<objective>
Human browser verification of the Phase 94 Tables tab — the visual/CSS behaviours that
Python assertions cannot cover: HMBC flag row-colouring, per-table captions, compact
intensity rendering, and the TBL-03 structured layout. Follows the 93-CONTEXT.md D-04
two-layer precedent (pytest static-scan + manual browser checkpoint) and the
94-VALIDATION.md "Manual-Only Verifications" row.

Purpose: Confirm the frontend renders correctly against a real (or fixture-backed)
`analysis_dir` before the phase is verified. No code changes.
Output: human approval (or a defect list feeding a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-data-tables/94-UI-SPEC.md
@.planning/phases/94-data-tables/94-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser check — HMBC colouring, captions, compact intensity, TBL-03 layout</name>
  <what-built>
    The Tables tab now renders five data tables from the `tables.py` router:
    ¹³C Signals, HSQC, HMBC, COSY, and the structured LSD Constraint Inventory.
    HMBC rows are colour-coded by flag; intensity is compacted (5.6M/1.5M); each
    table carries a caption (note + counts); TBL-03 is a 3-subsection structured view.
  </what-built>
  <how-to-verify>
    1. Serve a dashboard against a directory that has the Phase-94 data. Easiest path
       to guaranteed-schema data: point at a temp dir built from the Wave-0 fixtures,
       or run `lucy webview serve <analysis_dir>` on a live/recent CASE run's analysis
       directory. Command: `lucy webview serve <analysis_dir>` (note the URL printed).
       NOTE (RESEARCH A1-A5): older on-disk CASE runs use pre-schema peak filenames
       (`13c_curated.json`, `hmbc_curated.json`, ...) and will legitimately show
       "waiting" — that is NOT a defect. Verify against data matching the canonical
       `peaks/carbon_signals.json` + `peaks/{hsqc,hmbc,cosy}.json` schema (fixture-backed
       dir is the reliable option) so all five tables populate.
    2. Open the printed URL, click the "Tables" tab.
    3. Confirm all 5 sections appear in order: ¹³C Signals, HSQC, HMBC, COSY,
       LSD Constraint Inventory.
    4. HMBC table: confirm rows with `flag=ok` look normal (green left accent), rows
       with `flag=potential_4J` have the amber background (#fff3cd / dark amber text),
       rows with `flag=1J_artifact` look dimmed/grey. No rows are hidden.
    5. Each of the 4 peaks tables: confirm a small muted caption line appears ABOVE the
       table showing the note + counts (e.g. HMBC "… — 29 kept of 913").
    6. Intensity columns (HSQC/HMBC/COSY): confirm values render as `5.6M` / `1.5M` /
       `5.6K`, NOT raw `5559614`.
    7. LSD Constraint Inventory: confirm THREE subsections — "Applied Constraints"
       (Type/Atom Indices/Note table), "Constraint Summary" (Constraint/Value with
       Ring exclusion shown as Yes/No), "Deferred / Pending" (bullet-list reasoning).
    8. Optional negative check: point at an empty dir → each section shows its own
       italic "Waiting for … data..." line, page stays responsive, no error banner,
       browser console shows no uncaught exceptions (network warns via console.warn are ok).
  </how-to-verify>
  <read_first>
    - .planning/phases/94-data-tables/94-UI-SPEC.md — "HMBC Flag Color Map", "Per-table caption copy", "Compact intensity formatting", "TBL-03 structured multi-section layout", "Copywriting Contract" (exact waiting strings).
    - .planning/phases/94-data-tables/94-VALIDATION.md — "Manual-Only Verifications".
  </read_first>
  <action>
    Present the verification steps to the user, wait for their response, and record the
    outcome. If the user reports defects, capture them verbatim for a follow-up
    gap-closure plan; do not attempt fixes inside this checkpoint plan.
  </action>
  <acceptance_criteria>
    - User has viewed the Tables tab in a live browser against schema-matching data.
    - User confirms: 5 sections present; HMBC flag colours correct; captions present;
      intensity compacted; TBL-03 three-subsection layout present.
    - Any reported defects are recorded verbatim in the SUMMARY for gap-closure routing.
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete Phase 94, or describe the visual defects to route into a gap-closure plan.</resume-signal>
  <done>User approves the Tables-tab rendering, or a defect list is captured for follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human ↔ running dashboard | Read-only visual verification; no code or config change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04-01 | Spoofing | Verifying against pre-schema data yields false "waiting" | mitigate | Verify against schema-matching (fixture-backed) data; step 1 explicitly warns that older on-disk peak filenames are not a defect. |
| T-94-04-SC | Tampering | npm/pip/cargo installs | accept | No installs; verification-only checkpoint. |
</threat_model>

<verification>
- Human confirmation of the five UI behaviours above.
</verification>

<success_criteria>
- Tables tab visually verified: 5 tables, HMBC flag colours, captions, compact intensity, TBL-03 structured layout — or defects captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/94-data-tables/94-04-SUMMARY.md` when done (record approval or defect list).
</output>
