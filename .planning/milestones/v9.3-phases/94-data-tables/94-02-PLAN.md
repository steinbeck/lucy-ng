---
phase: 94-data-tables
plan: 02
type: execute
wave: 1
depends_on: [94-01]
files_modified:
  - src/lucy_ng/webview/routers/tables.py
  - src/lucy_ng/webview/app.py
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "GET /api/tables/carbon returns state=ok + rows (ppm/mult/nC/assignment/confidence) when carbon_signals.json present, state=waiting when absent"
    - "GET /api/tables/{hsqc,hmbc,cosy} return state=ok with their field sets; HMBC rows preserve the flag value verbatim"
    - "GET /api/tables/constraints selects the highest numeric iteration_(\\d+) across families (mtime tiebreak), strips '; ', json.loads-parses the inventory block"
    - "Every absent/partial/malformed source degrades to state=waiting, HTTP 200, never 500 (SC4)"
  artifacts:
    - path: "src/lucy_ng/webview/routers/tables.py"
      provides: "make_router(analysis_dir) with 5 GET routes under /api/tables + never-500 readers"
      exports: ["make_router"]
      contains: "def make_router"
      min_lines: 90
    - path: "src/lucy_ng/webview/app.py"
      provides: "include_router docking of the tables router"
      contains: "tables"
  key_links:
    - from: "src/lucy_ng/webview/app.py"
      to: "lucy_ng.webview.routers.tables"
      via: "lazy import + include_router"
      pattern: "include_router\\(_tables"
    - from: "src/lucy_ng/webview/routers/tables.py"
      to: "analysis_dir/iteration_*/compound.lsd"
      via: "prefix-match iteration selection"
      pattern: "re\\.match\\(r\"iteration_\\(\\\\d\\+\\)\""
---

<objective>
Create the `tables.py` FastAPI router (5 GET routes, mirroring `routers/log.py`
never-500 shape) and dock it into `create_app()` with one lazy import + one
`include_router` line. This turns the Plan 01 `TestTablesEndpoint` from SKIP to GREEN.

Purpose: The pure backend half of Phase 94 — read `analysis/peaks/*.json` and the
latest `iteration_NN/compound.lsd`, return per-source ok/waiting payloads (SC4).
Output: `src/lucy_ng/webview/routers/tables.py` (NEW) + modified `app.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-data-tables/94-CONTEXT.md
@.planning/phases/94-data-tables/94-RESEARCH.md
@.planning/phases/94-data-tables/94-PATTERNS.md

@src/lucy_ng/webview/routers/log.py
@src/lucy_ng/webview/routers/structures.py
@src/lucy_ng/webview/app.py

<interfaces>
<!-- Routes to expose (all GET, prefix /api, all never raise 500): -->
<!--   /api/tables/carbon      -> {"state","note","counts","rows"} -->
<!--   /api/tables/hsqc        -> {"state","note","counts","rows"} -->
<!--   /api/tables/hmbc        -> {"state","note","counts","rows"}  (row.flag preserved verbatim) -->
<!--   /api/tables/cosy        -> {"state","note","counts","rows"} -->
<!--   /api/tables/constraints -> {"state","note","inventory"}     (inventory = parsed dict or absent) -->

<!-- Selection helper (NEW — prefix match, NOT the $-anchored structures.py regex): -->
<!--   _newest_compound_lsd(analysis_dir): glob iteration_*/compound.lsd, re.match(r"iteration_(\d+)", parent.name), -->
<!--     max by (int(prefix), st_mtime); mtime is the tiebreak per D-02. -->

<!-- Inventory parse (NEW webview-local sibling of cli/lsd.py::_extract_inventory_block — DO NOT import cli/lsd.py, -->
<!--   it raises SystemExit): strip "; " via line[2:] after startswith("; "), map bare ";" to "", require both -->
<!--   START and END delimiters, return None on any failure; then json.loads inside try/except -> None. -->
<!--   Parse every field with .get(key, default): schema additionalProperties=true, real files carry extra keys. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tables.py with make_router + 4 peaks readers, dock in app.py</name>
  <files>src/lucy_ng/webview/routers/tables.py, src/lucy_ng/webview/app.py</files>
  <behavior>
    - GET /api/tables/carbon with carbon_signals.json present → state=ok, rows carry ppm/mult/nC/assignment/confidence.
    - GET /api/tables/carbon with file absent → state=waiting, HTTP 200.
    - GET /api/tables/hsqc and /api/tables/cosy present → state=ok with their field sets.
    - GET /api/tables/hmbc → each row's flag value returned UNMODIFIED.
    - Malformed/partial JSON in any peaks file → state=waiting, HTTP 200, never 500.
  </behavior>
  <read_first>
    - src/lucy_ng/webview/routers/log.py — the canonical `make_router(analysis_dir) -> APIRouter(prefix="/api")` factory + never-500 `_read_log` shape.
    - src/lucy_ng/webview/routers/structures.py — the BROADER JSON-parse except tuple `(json.JSONDecodeError, OSError, KeyError, TypeError, ValueError)` (RESEARCH Pitfall 1: log.py's narrow `(FileNotFoundError, OSError)` is INSUFFICIENT for JSON parsing).
    - src/lucy_ng/webview/app.py — the `create_app()` lazy-import + `include_router` docking block (~lines 47-55) and the module docstring route list.
    - .planning/phases/94-data-tables/94-CONTEXT.md `<canonical_refs>` "Data shapes" + D-04/D-05 (columns + caption note/counts).
    - .planning/phases/94-data-tables/94-PATTERNS.md "tables.py" Analog 1 & 2 + Shared Patterns 1, 2, 4.
  </read_first>
  <action>
    Create `src/lucy_ng/webview/routers/tables.py`. Module-level imports: `from __future__ import annotations`, `json`, `re`, `pathlib.Path`, `typing.Any`, `from fastapi import APIRouter` (module-level fastapi import is the established router convention; WV-08 forbids importing router modules from `webview/__init__.py`/`server.py`/`state.py`, NOT the fastapi import itself). Implement `make_router(analysis_dir: Path) -> APIRouter` returning `APIRouter(prefix="/api")` with four nested GET handlers: `/tables/carbon`, `/tables/hsqc`, `/tables/hmbc`, `/tables/cosy`, each delegating to a module-level reader `_read_carbon`/`_read_hsqc`/`_read_hmbc`/`_read_cosy`. Each reader: read `analysis_dir/"peaks"/<file>.json`, `json.loads`, and on success return `{"state":"ok","note":data.get("note"),"counts":{...salient counts per D-05...},"rows":data.get(<array key>, [])}`; on ANY failure return `{"state":"waiting","note":None,"counts":{},"rows":[]}`. Use the broad except tuple `(FileNotFoundError, json.JSONDecodeError, OSError, KeyError, TypeError, ValueError)`. Rows are returned verbatim (list of dicts) — do NOT reshape or drop fields; specifically the HMBC `flag` field passes through UNMODIFIED (colouring is a frontend concern per D-03). Per-source counts: carbon → `{"formula","dbe","solvent","count":len(signals)}`; hsqc/cosy → `{"count"}`; hmbc → `{"kept_count","raw_count"}` (all `.get()`-defensive). Array keys: carbon reads `signals`, the three correlations read `peaks`. Then modify `app.py`: add `from lucy_ng.webview.routers import tables as _tables  # noqa: PLC0415` next to the existing lazy router imports and `app.include_router(_tables.make_router(analysis_dir))` next to the existing include_router calls; extend the module docstring route list with the five `GET /api/tables/*` routes in the existing doc-comment style.
  </action>
  <verify>
    <automated>pytest "tests/test_webview_api.py::TestTablesEndpoint::test_carbon_returns_rows" "tests/test_webview_api.py::TestTablesEndpoint::test_carbon_waiting_when_absent" "tests/test_webview_api.py::TestTablesEndpoint::test_hsqc_cosy_return_ok" "tests/test_webview_api.py::TestTablesEndpoint::test_hmbc_preserves_flag_verbatim" "tests/test_webview_api.py::TestTablesEndpoint::test_malformed_json_returns_waiting" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tables.py` contains `def make_router(analysis_dir: Path) -> APIRouter` returning `APIRouter(prefix="/api")`.
    - `tables.py` registers routes `/tables/carbon`, `/tables/hsqc`, `/tables/hmbc`, `/tables/cosy`.
    - Each peaks reader uses the broad except tuple including `json.JSONDecodeError` (not log.py's narrow `(FileNotFoundError, OSError)` only).
    - `GET /api/tables/carbon` returns `{"state":"waiting"}` (HTTP 200) when `carbon_signals.json` absent, and `{"state":"ok"}` with rows exposing `ppm`/`mult`/`nC`/`assignment`/`confidence` when present.
    - `GET /api/tables/hmbc` response preserves each peak's `flag` field verbatim (returned flag set == `{"ok","potential_4J","1J_artifact"}` for the fixture).
    - Malformed JSON in any peaks file → `state=="waiting"`, HTTP 200, never 500.
    - `app.py` contains `include_router(_tables.make_router(analysis_dir))` and the module docstring lists the 5 `/api/tables/*` routes.
    - `ruff check src/lucy_ng/webview/routers/tables.py src/lucy_ng/webview/app.py` and `mypy src/lucy_ng/webview/routers/tables.py` pass.
  </acceptance_criteria>
  <done>Four peaks routes GREEN, docked in app.py, ruff+mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /api/tables/constraints — newest-iteration selection + inventory parse</name>
  <files>src/lucy_ng/webview/routers/tables.py</files>
  <behavior>
    - GET /api/tables/constraints selects iteration_02_anchor_recovery over iteration_01 (highest numeric prefix wins across family suffixes) → returned inventory.iteration == 2.
    - Two dirs with the same numeric prefix → mtime breaks the tie.
    - Missing compound.lsd, or a START delimiter without an END delimiter, or non-JSON block → state=waiting, HTTP 200, never 500.
  </behavior>
  <read_first>
    - src/lucy_ng/cli/lsd.py `_extract_inventory_block()` (~lines 175-205) — the LOGIC to mirror (delimiter pair, `line[2:]` 2-char strip, bare-`;`→"" mapping, START-without-END = malformed). DO NOT import it — its sibling validator raises SystemExit (RESEARCH Anti-Patterns / Pitfall 5).
    - src/lucy_ng/webview/routers/structures.py `_newest_solutions_smi` (~lines 163-175) — the near-analog; its `r"iteration_(\d+)$"` `$`-anchor is WRONG here (RESEARCH Pitfall 3 — will not match `iteration_07_anchor_recovery`).
    - .planning/phases/94-data-tables/94-RESEARCH.md "Pattern 2" (`_newest_compound_lsd` with prefix match + mtime tiebreak, verified against real CASE4 dirs) and "Code Examples → CONSTRAINT INVENTORY v2 block".
    - .planning/phases/94-data-tables/94-CONTEXT.md D-01 + D-02; .planning/phases/94-data-tables/94-PATTERNS.md Shared Pattern 3 & 5.
  </read_first>
  <action>
    Add to `tables.py`: (1) `_newest_compound_lsd(analysis_dir: Path) -> Path | None` — glob `analysis_dir/"iteration_*/compound.lsd"`, match each parent name with `re.match(r"iteration_(\d+)", name)` (PREFIX match, NO `$` anchor), collect `(int(group1), p.stat().st_mtime, p)`, return `max(..., key=lambda t: (t[0], t[1]))[2]` or `None` when no candidates. (2) A webview-local `_extract_inventory_block(content: str) -> str | None` that mirrors the cli/lsd.py logic exactly — iterate lines, set `in_block` on the START delimiter, break with `found_end=True` on the END delimiter, append `line[2:]` when `line.startswith("; ")` and `""` when `line == ";"`; return `None` if no lines collected OR END never found (START-without-END is malformed → None, never raise). (3) `_read_constraints(analysis_dir) -> dict[str, Any]` — call `_newest_compound_lsd`; if None → waiting; else read the file, `_extract_inventory_block`, if None → waiting; else `json.loads` the block inside `try/except (json.JSONDecodeError, TypeError, ValueError): return waiting`; on success return `{"state":"ok","note":inv.get("note"),"inventory":inv}`. Parse consumers must treat every inventory field with `.get(key, default)` (schema `additionalProperties: true`; real files carry `family`,`hmbc_active`,`ring_size_filter`, etc.). (4) Register `@router.get("/tables/constraints")` in `make_router` delegating to `_read_constraints`. The waiting payload shape is `{"state":"waiting","note":None,"inventory":None}`.
  </action>
  <verify>
    <automated>pytest "tests/test_webview_api.py::TestTablesEndpoint::test_constraints_selects_highest_iteration" "tests/test_webview_api.py::TestTablesEndpoint::test_constraints_waiting_when_malformed" "tests/test_webview_api.py::TestTablesEndpoint::test_constraints_waiting_when_absent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tables.py` contains the literal `re.match(r"iteration_(\d+)", ` (prefix match, no `$`).
    - `tables.py` strips the comment prefix with a fixed 2-char slice `line[2:]` (NOT `lstrip("; ")`) after a `startswith("; ")` check.
    - `tables.py` does NOT import from `lucy_ng.cli.lsd` (grep: `grep -c "cli.lsd\|cli import lsd" src/lucy_ng/webview/routers/tables.py` == 0).
    - `GET /api/tables/constraints` on the multi-iteration fixture returns `state=="ok"` and `inventory["iteration"] == 2` (family-suffixed higher-prefix dir wins).
    - Missing compound.lsd → `state=="waiting"`, HTTP 200; START-without-END block → `state=="waiting"`, HTTP 200; never 500.
    - `ruff check` and `mypy src/lucy_ng/webview/routers/tables.py` pass.
    - Full class GREEN: `pytest tests/test_webview_api.py::TestTablesEndpoint -q` all pass.
  </acceptance_criteria>
  <done>Constraints route selects highest iteration with mtime tiebreak, parses inventory defensively, never 500; whole TestTablesEndpoint class green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (analysis_dir) → router | The only "input" is file CONTENT under the server-startup `analysis_dir`; not client-controlled (no path/query params on any of the 5 GET routes). |
| router → HTTP client | Router returns file-derived JSON; the never-500 contract is the reliability boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-01 | Denial of Service | Partial/malformed JSON during a live run crashes a route (500) | mitigate | Broad except tuple in every reader collapses all failure modes to `{"state":"waiting"}` HTTP 200 (SC4); asserted by `test_malformed_json_returns_waiting` + `test_constraints_waiting_when_malformed`. |
| T-94-02-02 | Tampering | Path traversal via crafted request | accept | No request-supplied path/query input; all 5 routes read fixed relative paths (`peaks/*.json`, `iteration_*/compound.lsd`) under an already-resolved `analysis_dir` (server-startup arg). Same surface as the existing 3 routers; no new traversal introduced. |
| T-94-02-03 | Elevation of Privilege | Importing cli/lsd.py's SystemExit-raising validator into the router | mitigate | Inventory parser reimplemented webview-local, returns `None` on failure; acceptance grep asserts no `cli.lsd` import. |
| T-94-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestTablesEndpoint -q` → all pass (was all SKIP after Plan 01).
- `pytest tests/test_webview_api.py -q` → full webview suite green.
- `ruff check src tests` and `mypy src/lucy_ng/webview` clean.
</verification>

<success_criteria>
- Five GET routes under `/api/tables/*`, each degrading to `state=waiting` (HTTP 200) on any failure (SC4).
- Constraints route selects the highest numeric `iteration_(\d+)` across family suffixes with mtime tiebreak, parses the `; `-stripped inventory JSON defensively.
- Router docked in `app.py`; TestTablesEndpoint fully green.
</success_criteria>

<output>
Create `.planning/phases/94-data-tables/94-02-SUMMARY.md` when done.
</output>
