---
phase: 93-formatted-log-tab-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/webview/app.py
  - src/lucy_ng/webview/static/webview.js
  - src/lucy_ng/webview/static/index.html
  - tests/test_webview_api.py
autonomous: true
requirements: [TAB-01]

must_haves:
  truths:
    - "GET /webview.js returns 200 with a JavaScript content-type"
    - "The dashboard loads its script from the external /webview.js file, not an inline <script> block"
    - "webview.js sits at the flat static/ level so the existing hatch glob packages it into the wheel"
  artifacts:
    - path: "src/lucy_ng/webview/static/webview.js"
      provides: "Extracted dashboard client script (verbatim from the old inline block)"
      min_lines: 150
    - path: "src/lucy_ng/webview/app.py"
      provides: "GET /webview.js FileResponse route"
      contains: "@app.get(\"/webview.js\")"
    - path: "tests/test_webview_api.py"
      provides: "Route smoke test + packaging existence assertion"
      contains: "def test_webview_js_served"
  key_links:
    - from: "src/lucy_ng/webview/static/index.html"
      to: "/webview.js"
      via: "external <script src> tag"
      pattern: "script src=./webview.js"
    - from: "src/lucy_ng/webview/app.py"
      to: "static/webview.js"
      via: "FileResponse route"
      pattern: "FileResponse.*webview.js"
---

<objective>
Extract all inline JavaScript from `index.html` into a new `static/webview.js`, serve it via an
explicit `GET /webview.js` FastAPI route, wire `index.html` to load it externally, and add the
Wave 0 route + packaging tests. This is a **behavior-preserving** extraction — no tabs and no
markdown renderer are added in this plan (those land in Plan 02). It establishes the extraction +
route + packaging discipline that success criterion 4 (D-03) requires.

Purpose: Enables independent packaging verification of the JS asset and makes the client script
maintainable and testable on its own — the precondition for adding the tab bar and markdown
renderer in Plan 02.
Output: `static/webview.js` (new), `app.py` `/webview.js` route, `index.html` external `<script src>`,
two extended test classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-formatted-log-tab-framework/93-CONTEXT.md
@.planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md
@.planning/phases/93-formatted-log-tab-framework/93-RESEARCH.md
@.planning/phases/93-formatted-log-tab-framework/93-VALIDATION.md

<interfaces>
<!-- Real code the executor must mirror. Follow PATTERNS.md over RESEARCH.md where they differ. -->

The REAL app.py route pattern (src/lucy_ng/webview/app.py:56-65) — an inline `_static_file` Path
built inside create_app(), FileResponse imported lazily with `# noqa: PLC0415` (WV-08). Do NOT use
RESEARCH.md's `_static_dir` / module-level-decorator sketch; it does not match the file:

    from fastapi.responses import FileResponse  # noqa: PLC0415
    _static_file = Path(__file__).parent / "static" / "index.html"
    @app.get("/")
    def index() -> FileResponse:
        return FileResponse(str(_static_file), media_type="text/html")
    return app

The inline script to extract is index.html lines 199-425: an IIFE opening `(function () {` /
`'use strict';` and closing `}());`, ES5 `var`-style. It defines `refreshStatus`, `renderStatus`,
`refreshStructures`, `renderStructures`, `refreshLog`, `flashDot`, `tick`, and the bootstrap
`tick(); setInterval(tick, REFRESH_MS);`.

The route smoke-test analog is TestFrontend.test_index_html_served
(tests/test_webview_api.py:403-422): imports fastapi inside the body, try/except → pytest.skip,
`TestClient(create_app(live_analysis_dir)).get("/")`, asserts 200 + content-type. The packaging
analog is TestPackaging.test_hatch_artifacts_include_static (:463-484).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract webview.js, add /webview.js route, wire external script tag</name>
  <read_first>
    - src/lucy_ng/webview/static/index.html (the file being modified — inline script lines 199-425, script-tag swap)
    - src/lucy_ng/webview/app.py (the file being modified — real GET / route, lines 56-65)
    - .planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md (REAL app.py `_static_file` route pattern + WV-08 lazy-import discipline; the "important deviation from RESEARCH.md" note)
    - .planning/phases/93-formatted-log-tab-framework/93-RESEARCH.md (Pattern 1 explicit-media_type rationale, Pitfall 5 MIME confusion)
  </read_first>
  <action>
    Create `src/lucy_ng/webview/static/webview.js` containing the current inline `<script>` body
    from index.html (lines 199-425) VERBATIM — the whole IIFE from `(function () {` / `'use strict';`
    through the closing `}());`. Preserve the ES5 `var`/IIFE style exactly; do NOT convert to
    const/let/modules and do NOT add tabs or markdown logic in this plan.
    In index.html, delete the entire inline `<script>...</script>` block (lines 199-425) and replace
    it with a single line: an external script tag `<script src="/webview.js"></script>`.
    In app.py `create_app()`, next to the existing `_static_file` line, add
    `_webview_js = Path(__file__).parent / "static" / "webview.js"`, and add a second route
    `@app.get("/webview.js")` named `webview_js` returning
    `FileResponse(str(_webview_js), media_type="application/javascript")`. REUSE the FileResponse
    import already present for the `/` route — do NOT add a second `from fastapi.responses import
    FileResponse` line (WV-08). Add a `- GET /webview.js → extracted dashboard script (Phase 93)`
    bullet to the create_app docstring Returns list. Explicit `media_type` is mandatory (Pitfall 5 —
    never rely on mimetypes.guess_type for .js).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/webview/static/webview.js` exists; contains `'use strict';`, `function refreshLog(`, and `function tick(`.
    - `index.html` contains the literal `<script src="/webview.js"></script>`.
    - `index.html` does NOT contain `function tick(` (inline script fully removed): `grep -c 'function tick(' src/lucy_ng/webview/static/index.html` returns 0.
    - `app.py` contains `@app.get("/webview.js")` and `media_type="application/javascript"`.
    - `app.py` contains exactly one `from fastapi.responses import FileResponse` line: `grep -c 'from fastapi.responses import FileResponse' src/lucy_ng/webview/app.py` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_webview_api.py -q</automated>
  </verify>
  <done>webview.js extracted verbatim; index.html loads it via /webview.js; app.py serves it with the JS media type; existing suite still green (or gracefully skips when fastapi absent).</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 tests — /webview.js route smoke test + packaging existence assertion</name>
  <read_first>
    - tests/test_webview_api.py (the file being modified — TestFrontend :403-422 and TestPackaging :463-484 analogs)
    - .planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md (both test analogs quoted verbatim, module-level import discipline)
    - .planning/phases/93-formatted-log-tab-framework/93-VALIDATION.md (Wave 0 requirements, sampling rate)
  </read_first>
  <action>
    Add a `test_webview_js_served` method to the existing `TestFrontend` class, mirroring
    `test_index_html_served`: fastapi/create_app imports inside the method body, try/except ImportError
    → `pytest.skip(...)`, `with TestClient(create_app(live_analysis_dir)) as client: r = client.get("/webview.js")`.
    Assert `r.status_code == 200` and that the content-type starts with `application/javascript`
    (also accept `text/javascript` per RESEARCH A3).
    Extend `TestPackaging` (either inside `test_hatch_artifacts_include_static` after the existing
    glob-string assertion, or as a new method `test_webview_js_present_in_static`) with:
    `static_dir = Path(__file__).parent.parent / "src" / "lucy_ng" / "webview" / "static"` then
    `assert (static_dir / "webview.js").exists()`. Keep the existing glob-string assertion untouched
    (Pitfall 2 regression guard — do NOT change pyproject.toml). Keep module-level imports limited to
    the existing set (`from __future__ import annotations`, `pathlib.Path`, `pytest`, `tomllib`); no
    fastapi import at module scope.
  </action>
  <acceptance_criteria>
    - `tests/test_webview_api.py` contains `def test_webview_js_served`.
    - `tests/test_webview_api.py` contains a `webview.js` existence assertion referencing a `static` dir path.
    - `pyproject.toml` is unchanged (no diff) — the flat `src/lucy_ng/webview/static/*` glob already covers webview.js.
    - `pytest tests/test_webview_api.py::TestPackaging -q` exits 0.
    - `pytest tests/test_webview_api.py::TestFrontend -q` exits 0 (tests pass, or skip cleanly when fastapi absent — never fail).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_webview_api.py -q</automated>
  </verify>
  <done>Route smoke test and packaging existence assertion added under WV-08 import-safety; both pass or skip cleanly; pyproject.toml untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → FastAPI FileResponse → browser | Static asset (`webview.js`) read from disk and served to the browser |
| host mimetypes DB → browser MIME sniffing | Content-Type of the served `.js` governs whether the browser executes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03 | Denial of Service (soft) | `GET /webview.js` route | mitigate | Explicit `media_type="application/javascript"` on FileResponse — never rely on `mimetypes.guess_type()`, which can fall back to `application/octet-stream` and cause silent script-exec refusal under `nosniff` (Pitfall 5) |
| T-93-02 | Information Disclosure | static file serving | accept | Path is a fixed constant (`Path(__file__).parent / "static" / "webview.js"`), not user-derived; the existing "dumb" read-only loopback server already governs traversal — out of scope to re-solve this phase |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed this phase (no pyproject change); Package Legitimacy Gate N/A. No install task, so no slopcheck checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py -q` green (route + packaging classes pass or skip cleanly).
- `grep -c 'function tick(' src/lucy_ng/webview/static/index.html` returns 0 (inline script fully extracted).
- `git diff --stat pyproject.toml` shows no change.
</verification>

<success_criteria>
webview.js is extracted verbatim and served at `/webview.js` with the correct MIME type; index.html
loads it externally; the file lands at the flat `static/` level and the existing hatch glob packages
it; route smoke test and packaging assertion are green. Dashboard behavior is unchanged from v9.2.
</success_criteria>

<output>
Create `.planning/phases/93-formatted-log-tab-framework/93-01-SUMMARY.md` when done.
</output>
