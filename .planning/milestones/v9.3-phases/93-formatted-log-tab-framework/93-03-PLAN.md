---
phase: 93-formatted-log-tab-framework
plan: 03
type: execute
wave: 3
depends_on: [93-02]
files_modified: []
autonomous: false
requirements: [LOG-01, TAB-01]

must_haves:
  truths:
    - "The `# <img src=x onerror=alert(1)>` payload renders as literal escaped text in a real browser; no alert fires and no image element is created"
    - "Clicking each tab shows only that panel and hides the others with no page reload; the persistent Overview/Structures left column stays visible"
    - "The h3 agent-name headings read as a distinguishable level above body paragraph text"
  artifacts: []
  key_links: []
---

<objective>
Human-in-the-loop confirmation of the three behaviors that cannot be asserted from pytest because no
JS/browser runtime exists in the project stack (RESEARCH Open Question #2): (1) the XSS payload renders
as literal escaped text in a real browser, (2) tab switching works and the left column stays
persistent, and (3) the h3-vs-body typography hierarchy is visually distinguishable (UI-SPEC ~line 81
recommends confirming the 1px size delta reads as a level, not flat). This closes the manual half of
the D-04 two-layer verification strategy; the automated half already passed in Plans 01-02.

Purpose: The static-source innerHTML scan proves the code path is safe by construction, but only a
real browser proves the DOM renders the payload as text. This checkpoint is the final gate before
`/gsd:verify-work`.
Output: Human sign-off (or a defect list) on the three browser-observable behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-formatted-log-tab-framework/93-CONTEXT.md
@.planning/phases/93-formatted-log-tab-framework/93-UI-SPEC.md
@.planning/phases/93-formatted-log-tab-framework/93-VALIDATION.md
@.claude/commands/lucy-ng/references/progress-format.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser verification — XSS escaping, tab switching, typography hierarchy</name>
  <read_first>
    - .planning/phases/93-formatted-log-tab-framework/93-VALIDATION.md (Manual-Only Verifications table — the two documented manual checks)
    - .planning/phases/93-formatted-log-tab-framework/93-UI-SPEC.md (Manual hierarchy check note ~line 81: h3 13px/600/#495057 vs body 12px/400)
    - .claude/commands/lucy-ng/references/progress-format.md (a realistic CASE-PROGRESS.md shape: `# CASE Progress Log`, `## Iteration N`, `### <Agent>`, `**Field:** value` labels, the `## Timing Summary` pipe table, `---` separators, `### Team Models` bullet list)
  </read_first>
  <action>Human-driven browser verification only — no code is written in this task. The executor sets up the mock CASE-PROGRESS.md and serves the dashboard as described in <how-to-verify>, then a human confirms the three browser-observable behaviors (XSS escaping, tab switching, typography hierarchy) and either types "approved" or reports defects. If a defect is found, fix it in webview.js/index.html (per Plan 02 patterns) and re-verify before sign-off.</action>
  <what-built>
    Plans 01-02 extracted `static/webview.js` (served at `/webview.js`), added a persistent 4-tab bar
    over the right panel (Run Log / 1D Spectra / 2D Spectra / Tables; Run Log default-active; 1D/2D/
    Tables are docked-in placeholders), and a hand-rolled markdown-to-DOM renderer that converts the
    CASE-PROGRESS.md subset into DOM nodes using textContent/createElement only. The automated
    innerHTML static-source scan and the /webview.js route + packaging tests are already green.
  </what-built>
  <how-to-verify>
    1. Install the webview extra into the project venv: `pip install -e ".[webview]"` (fastapi +
       uvicorn). Confirm `lucy webview --help` runs.
    2. Create a throwaway mock analysis directory (e.g. under the scratchpad, NOT in the repo) with an
       `analysis/CASE-PROGRESS.md` that contains, at minimum: a `# CASE Progress Log` H1, a
       `## Iteration 1: ...` H2, a `### NMR-Chemist` H3, several `**Field:** value` bold-label lines,
       one inline `` `code` `` span, a `---` horizontal rule, a `### Team Models` bullet list (`- item`
       lines), a small pipe table (a `## Timing Summary`-style `| Phase | Agent | ... |` header + a
       `|---|---|` separator + one data row), AND this exact payload on its own line:
       `# <img src=x onerror=alert(1)>`
    3. Serve it: `lucy webview serve <mock-dir>` and open the printed loopback URL in a real browser
       (with the JS console open).
    4. XSS check (LOG-01 criterion #3): confirm NO `alert` dialog fires, NO broken-image element
       appears, and the literal text `# <img src=x onerror=alert(1)>` is visible as escaped characters
       in the Run Log. In DevTools, confirm the log panel contains a text node (not an `<img>` element)
       for that line.
    5. Markdown check (LOG-01): confirm headings, bold `**Field:**` labels, inline code, the `---`
       rule, the bullet list, and the pipe table all render with the UI-SPEC styling (not raw markdown
       characters).
    6. Tab check (TAB-01): confirm the right panel shows exactly 4 tabs in order Run Log / 1D Spectra /
       2D Spectra / Tables with Run Log active on load. Click each of the other three: only the clicked
       panel becomes visible, the others hide, there is NO page reload, and the placeholders read
       "1D Spectra — coming in Phase 95" / "2D Spectra — coming in Phase 96" / "Tables — coming in
       Phase 94". Confirm the LEFT column (Candidate Structures / Overview) stays visible across all
       tab clicks (D-01 persistent column).
    7. Typography check (UI-SPEC ~line 81): confirm the `### NMR-Chemist`-style H3 headings read as a
       distinguishable level ABOVE body paragraph text (the 13px/600/#495057 h3 vs 12px/400 body).
       If it reads flat/indistinguishable, report it rather than approving.
    8. Scroll check (D-13): let the ~3 s refresh tick a few times while scrolled mid-history; confirm
       the panel does NOT jump to top/bottom on each refresh.
  </how-to-verify>
  <acceptance_criteria>
    - No alert fires and no `<img>` element is created for the payload line; it appears as literal escaped text (a text node in the log panel).
    - All four tabs switch correctly with no page reload; only the active panel is visible; the left Overview/Structures column remains visible throughout.
    - The three placeholder panels show their exact "coming in Phase 9x" copy.
    - H3 agent-name headings are visually distinguishable as a level above body text (or the flatness is explicitly reported).
    - The log panel does not jump scroll position on the 3 s refresh while scrolled mid-history.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe the specific defect(s) observed (which check failed and how).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CASE-PROGRESS.md content → /api/log → browser DOM | The manual check exercises the exact XSS boundary Plan 02 mitigates, using a live browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering / Elevation of Privilege (browser XSS) | live-browser rendering of `renderLog` output | mitigate | This checkpoint is the runtime confirmation of the mitigation: a real browser must render the `# <img src=x onerror=alert(1)>` payload as literal escaped text with no alert. Blocking gate — the phase does not pass verification until a human confirms |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | The only install here is `pip install -e ".[webview]"` (fastapi/uvicorn), both already-shipped v9.2 dependencies declared in the existing `[webview]` extra — no new package, Package Legitimacy Gate N/A |
</threat_model>

<verification>
Human confirmation of all five acceptance criteria above. This is a blocking `checkpoint:human-verify`;
`workflow.auto_advance` does not apply — the phase cannot be marked verified without explicit sign-off
on the XSS-escaping behavior.
</verification>

<success_criteria>
A human has confirmed in a real browser that: the XSS payload renders as literal escaped text (no
alert, no image element), all four tabs switch with no page reload while the left column stays
persistent, and the h3-vs-body typography hierarchy reads correctly.
</success_criteria>

<output>
Create `.planning/phases/93-formatted-log-tab-framework/93-03-SUMMARY.md` when done (record the
sign-off outcome and any defects found/fixed).
</output>
