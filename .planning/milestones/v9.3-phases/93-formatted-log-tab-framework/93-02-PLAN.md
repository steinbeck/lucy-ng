---
phase: 93-formatted-log-tab-framework
plan: 02
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - src/lucy_ng/webview/static/index.html
  - src/lucy_ng/webview/static/webview.js
  - tests/test_webview_api.py
autonomous: true
requirements: [LOG-01, TAB-01]

must_haves:
  truths:
    - "The right panel shows a 4-tab bar (Run Log / 1D Spectra / 2D Spectra / Tables) with Run Log active by default"
    - "Clicking a tab shows that panel and hides the others with no page reload; the left column stays persistent"
    - "The run log renders the CASE-PROGRESS.md markdown subset (headings, bold, code, pipe-tables, hr, bullets) as DOM nodes"
    - "No function in webview.js ever assigns server content via innerHTML"
  artifacts:
    - path: "src/lucy_ng/webview/static/webview.js"
      provides: "initTabs/activate tab switching + renderLog/buildTable/appendInline markdown renderer"
      contains: "function renderLog"
    - path: "src/lucy_ng/webview/static/index.html"
      provides: "Tab bar markup, 4 data-panel divs, tab + markdown CSS"
      contains: "id=\"tab-bar\""
    - path: "tests/test_webview_api.py"
      provides: "Static-source innerHTML XSS-guard scan"
      contains: "class TestMarkdownRendererSafety"
  key_links:
    - from: "refreshLog"
      to: "renderLog"
      via: "call inside the fetch .then callback (replaces textContent assignment)"
      pattern: "renderLog\\(content"
    - from: "#tab-bar [data-tab] buttons"
      to: "[data-panel] divs"
      via: "activate() display + .active toggle"
      pattern: "function activate\\("
---

<objective>
Add the persistent 4-tab bar over the right panel (D-01) and the hand-rolled markdown-to-DOM
renderer for the run log (D-02), plus the automated XSS-discipline guard test (D-04a). The tab bar
docks in 3 placeholder tabs (1D Spectra / 2D Spectra / Tables — populated by Phases 94-96) alongside
the populated Run Log tab; the existing Overview + Structures widgets stay an untouched persistent
LEFT column. The markdown renderer converts raw CASE-PROGRESS.md text into DOM nodes using
`createElement`/`textContent` exclusively — never `innerHTML` — satisfying the phase's XSS invariant
by construction.

Purpose: Delivers LOG-01 (formatted log) and the client half of TAB-01 (tab navigation) while
preserving the v9.2 XSS discipline and the D-13 scroll-preserve behavior.
Output: Tab bar + placeholder panels + markdown CSS in index.html; tab switching + markdown renderer
in webview.js; a static-source innerHTML scan test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-formatted-log-tab-framework/93-CONTEXT.md
@.planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md
@.planning/phases/93-formatted-log-tab-framework/93-RESEARCH.md
@.planning/phases/93-formatted-log-tab-framework/93-UI-SPEC.md
@.planning/phases/93-formatted-log-tab-framework/93-VALIDATION.md
@.claude/commands/lucy-ng/references/progress-format.md

<interfaces>
<!-- Vetted code the executor transcribes. RESEARCH.md Pattern 2 (tabs) + Pattern 3 (renderer)
     are the target implementations; UI-SPEC.md is the visual/token contract. -->

Locked tab set + order (CONTEXT D-01, UI-SPEC Component Specs) — exactly these 4, Run Log active
by default:
  data-tab="log" → "Run Log"   |   data-tab="spectra-1d" → "1D Spectra"
  data-tab="spectra-2d" → "2D Spectra"   |   data-tab="tables" → "Tables"

Placeholder copy (UI-SPEC Copywriting Contract):
  "1D Spectra — coming in Phase 95" · "2D Spectra — coming in Phase 96" · "Tables — coming in Phase 94"

Tab switching (RESEARCH Pattern 2): initTabs() queries `#tab-bar [data-tab]` + `[data-panel]`;
activate(tabName) toggles `display` (flex/none) on panels and `.active` class on buttons; default
`activate('log')`; call `initTabs()` once at bootstrap next to `tick()`, NOT inside setInterval.

Markdown renderer (RESEARCH Pattern 3): renderLog(rawText, container), buildTable(headerCells, rows),
appendInline(parent, text). Block parser handles `^#{1,3}\s+` headings, `^---+$` hr, pipe tables
(require a `|[\s:-]+|` separator row before committing to table mode — Pitfall 4), `^-\s+` bullet
lists, ```-fenced code, blank-line skip, default paragraph. Inline tokenizer splits on `**bold**` and
`` `code` `` via one regex. Clears container with `while (container.firstChild)
container.removeChild(container.firstChild)` — NEVER innerHTML. Every leaf via textContent/createTextNode.

refreshLog rewire (RESEARCH "refreshLog() call site", PATTERNS D-13 note): keep `atBottom` capture
BEFORE and `scrollTop` restore AFTER; in the .then callback, empty content → removeChild loop +
appendChild a `Waiting for log data...` text node; else `renderLog(content, logEl)`. No more
`logEl.textContent = content` for populated content.

CSS tokens — reuse EXISTING hex only (UI-SPEC Color table): #212529 (ink/accent), #495057 (h3/hover),
#6c757d (muted/placeholder), #dee2e6 (borders/hr), #f8f9fa (code + th background). No new palette.
Markdown element sizes (UI-SPEC): h1 16px / h2 14px / h3 13px / body-p 12px / weights 400 & 600 only.
The `#log-panel` container loses its page-wide `white-space: pre-wrap` + monospace font; each rendered
child controls its own typography. Adjust `#log-panel` max-height to `calc(100vh - 156px)` to make
room for the tab-bar row.

XSS-guard test (PATTERNS test-analog): class TestMarkdownRendererSafety, method
test_no_innerhtml_in_source — read static/webview.js, `if not js_path.exists(): pytest.skip(...)`,
`assert "innerHTML" not in source`. Pure string scan; no fastapi import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab bar markup, placeholder panels, CSS, and tab-switching JS</name>
  <read_first>
    - src/lucy_ng/webview/static/index.html (the file being modified — #log-panel-wrapper block :191-195, #log-panel CSS :143-158, existing color/spacing tokens)
    - src/lucy_ng/webview/static/webview.js (the file being modified — extracted script from Plan 01; tick() bootstrap)
    - .planning/phases/93-formatted-log-tab-framework/93-UI-SPEC.md (Tab bar Component Specifications table, Placeholder panels, Color/Spacing/Typography tokens)
    - .planning/phases/93-formatted-log-tab-framework/93-RESEARCH.md (Architecture Patterns — Pattern 2 tab switching, lines 192-217)
    - .planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md (index.html "structure to replace" + "CSS tokens to extend, not invent"; webview.js initTabs bootstrap note)
  </read_first>
  <action>
    In index.html `#log-panel-wrapper`, replace `<h2>Run Log</h2>` with a `<div id="tab-bar">`
    containing four `<button data-tab="...">` elements in the LOCKED order: `data-tab="log"` labeled
    "Run Log", `data-tab="spectra-1d"` labeled "1D Spectra", `data-tab="spectra-2d"` labeled
    "2D Spectra", `data-tab="tables"` labeled "Tables". Replace the single
    `<pre id="log-panel">Waiting for log data...</pre>` with four sibling `[data-panel]` divs:
    `<div id="log-panel" data-panel="log">Waiting for log data...</div>` (now a `<div>`, was `<pre>`),
    `<div data-panel="spectra-1d">1D Spectra — coming in Phase 95</div>`,
    `<div data-panel="spectra-2d">2D Spectra — coming in Phase 96</div>`, and
    `<div data-panel="tables">Tables — coming in Phase 94</div>`. Leave the LEFT `#structure-panel`
    column completely untouched (D-01: it stays persistent, not a tab).
    Add CSS per UI-SPEC Component Specifications: `#tab-bar` (display flex, gap 4px, border-bottom
    1px solid #dee2e6, flex-shrink 0); `#tab-bar button` base (padding 8px 16px, font-size 13px,
    font-weight 600, color #6c757d, background transparent, border none, border-bottom 2px solid
    transparent, cursor pointer); hover → color #495057; `.active` → color #212529 + border-bottom-color
    #212529; `:focus-visible` → outline 2px solid #0c5460, outline-offset 2px. Give every `[data-panel]`
    the existing log-panel box model (background #fff, border 1px solid #dee2e6, border-radius 6px,
    padding 10px 12px, overflow-y scroll, min-height 200px, max-height calc(100vh - 156px)) and
    non-active panels `display: none`. Placeholder panels use the UI-SPEC placeholder style (font-size
    13px, color #6c757d, padding 16px 0, text-align center). REMOVE `white-space: pre-wrap` and the
    monospace `font-family` from the `#log-panel` container rule (typography moves to rendered children
    in Task 2). Use ONLY the existing hex tokens listed in the interfaces block — introduce no new colors.
    In webview.js add `initTabs()` and `activate(tabName)` per RESEARCH Pattern 2 (query
    `#tab-bar [data-tab]` + `[data-panel]`, toggle `display` flex/none + `.active` class, default
    `activate('log')`). Call `initTabs()` exactly once at bootstrap, next to the existing `tick()`
    call — NOT inside `setInterval` (tabs are click-driven, not polled; no fetch is triggered by a
    tab switch).
  </action>
  <acceptance_criteria>
    - index.html contains `data-tab="log"`, `data-tab="spectra-1d"`, `data-tab="spectra-2d"`, `data-tab="tables"` and the labels "Run Log", "1D Spectra", "2D Spectra", "Tables".
    - index.html contains `data-panel="log"`, `data-panel="spectra-1d"`, `data-panel="spectra-2d"`, `data-panel="tables"`.
    - index.html contains the exact strings `1D Spectra — coming in Phase 95`, `2D Spectra — coming in Phase 96`, `Tables — coming in Phase 94`.
    - index.html contains `id="tab-bar"` and a `.active` CSS rule with `border-bottom-color: #212529`.
    - index.html `#log-panel` container rule no longer contains `white-space: pre-wrap`: `grep -c 'white-space: pre-wrap' src/lucy_ng/webview/static/index.html` returns 0.
    - webview.js contains `function initTabs(`, `function activate(`, and `activate('log')`.
    - webview.js does NOT contain the literal `innerHTML`: `grep -c 'innerHTML' src/lucy_ng/webview/static/webview.js` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_webview_api.py -q</automated>
  </verify>
  <done>The right panel shows a 4-button tab bar (Run Log active) over 4 data-panel divs; 3 placeholders carry the "coming in Phase 9x" copy; the left column is untouched; tab switching toggles display + .active with no fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Markdown-to-DOM renderer, refreshLog rewire, and innerHTML XSS-guard test</name>
  <read_first>
    - src/lucy_ng/webview/static/webview.js (the file being modified — refreshLog :382-399 from the extracted script)
    - src/lucy_ng/webview/static/index.html (the file being modified — add markdown-child CSS to the <style> block)
    - tests/test_webview_api.py (the file being modified — class conventions, no fastapi at module scope)
    - .planning/phases/93-formatted-log-tab-framework/93-RESEARCH.md (Pattern 3 full renderLog/buildTable/appendInline code :236-367, and the refreshLog() call-site snippet :521-540; Anti-Patterns; Pitfalls 3 & 4)
    - .planning/phases/93-formatted-log-tab-framework/93-UI-SPEC.md ("Markdown-rendered Run Log panel" table — per-element styles; typography hierarchy note ~line 81)
    - .claude/commands/lucy-ng/references/progress-format.md (authoritative markdown subset the coordinator actually writes — headings, **Field:** labels, one pipe table, bullet lists, --- separators)
    - .planning/phases/93-formatted-log-tab-framework/93-PATTERNS.md (TestMarkdownRendererSafety analog; DOM-safety shared pattern)
  </read_first>
  <action>
    In webview.js add `renderLog(rawText, container)`, `buildTable(headerCells, rows)`, and
    `appendInline(parent, text)` exactly per RESEARCH Pattern 3. The block parser handles `#`/`##`/`###`
    headings (anchored `^(#{1,3})\s+`), `---` horizontal rules, pipe tables (only when the next line is
    a `|[\s:-]+|` separator — Pitfall 4 false-positive guard), `- ` bullet lists, ```-fenced code
    blocks, blank-line skip, and a default paragraph branch (covers `**Field:** value` lines). Clear the
    container with a `while (container.firstChild) container.removeChild(container.firstChild)` loop —
    NEVER `innerHTML`, never a regex→HTML-string→innerHTML converter. The inline tokenizer splits on
    `**bold**` (→ `<strong>`) and `` `code` `` (→ `<code>`) via one regex; every leaf is set with
    `textContent`/`createTextNode`.
    Rewire the existing `refreshLog()` to use the renderer while preserving the D-13 scroll contract:
    capture `atBottom` BEFORE the fetch, restore `scrollTop` AFTER in the `.then`; when
    `data.content` is empty, clear via the removeChild loop and appendChild a
    `document.createTextNode('Waiting for log data...')`; otherwise call `renderLog(content, logEl)`.
    Remove the old `logEl.textContent = content || ...` populated-content assignment.
    In index.html `<style>`, add per-element rules for rendered children of `#log-panel` per the
    UI-SPEC "Markdown-rendered Run Log panel" table: `#log-panel h1` (16px/600/#212529),
    `#log-panel h2` (14px/600/#212529), `#log-panel h3` (13px/600/#495057), `#log-panel p`
    (12px/400/#212529, line-height 1.6, margin 4px 0), `#log-panel strong` (600/#212529),
    `#log-panel code` (monospace stack, 12px, background #f8f9fa, padding 0 4px, border-radius 4px),
    `#log-panel pre`/`#log-panel pre code` (fenced block per spec), `#log-panel hr` (border-top 1px
    solid #dee2e6, margin 16px 0), `#log-panel table`/`th`/`td` (border-collapse, 12px, 1px solid
    #dee2e6 cells, #f8f9fa th background), `#log-panel ul`/`li` (per spec). Existing hex tokens only.
    In tests/test_webview_api.py add `class TestMarkdownRendererSafety` with method
    `test_no_innerhtml_in_source`: build the path to `src/lucy_ng/webview/static/webview.js`,
    `if not js_path.exists(): pytest.skip(...)`, read the source, `assert "innerHTML" not in source`
    with a message citing D-02. Pure string scan — no fastapi import, no TestClient.
  </action>
  <acceptance_criteria>
    - webview.js contains `function renderLog(`, `function buildTable(`, and `function appendInline(`.
    - webview.js contains `removeChild` and does NOT contain `innerHTML`: `grep -c 'innerHTML' src/lucy_ng/webview/static/webview.js` returns 0.
    - webview.js `refreshLog` calls `renderLog(content` and no longer assigns `logEl.textContent = content`: `grep -c 'logEl.textContent = content' src/lucy_ng/webview/static/webview.js` returns 0.
    - index.html contains a `#log-panel h3` rule (13px) and a `#log-panel p` rule (12px) — the typography hierarchy the manual checkpoint validates.
    - tests/test_webview_api.py contains `class TestMarkdownRendererSafety` and `assert "innerHTML" not in`.
    - `pytest tests/test_webview_api.py::TestMarkdownRendererSafety -q` exits 0.
    - `pytest tests/test_webview_api.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_webview_api.py -q</automated>
  </verify>
  <done>The run log renders the CASE-PROGRESS.md markdown subset as DOM nodes via textContent/createElement only; refreshLog keeps the scroll contract and routes through renderLog; the static-source innerHTML scan is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CASE-PROGRESS.md file content → /api/log → browser DOM | Run-derived free text (agent messages, SMILES/CXSMILES with legal `<`/`>`) is fetched and rendered into the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering / Elevation of Privilege (browser XSS) | `renderLog`/`appendInline`/`buildTable`/`refreshLog` in webview.js | mitigate | ALL server-derived strings reach the DOM only via `textContent`/`createTextNode`/`createElement`; container cleared with a `removeChild` loop; NEVER `innerHTML`, never a regex→HTML-string→innerHTML converter. Enforced automatically by `TestMarkdownRendererSafety::test_no_innerhtml_in_source` (static-source scan) plus the Plan 03 manual browser check of the `# <img src=x onerror=alert(1)>` payload |
| T-93-02 | Information Disclosure | /api/log endpoint | accept | Endpoint is unchanged from v9.2 (raw-text passthrough); the "dumb" read-only loopback server already governs which files are readable — out of scope to re-solve |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (no pyproject change); Package Legitimacy Gate N/A — no install task, no slopcheck checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py -q` green, including `TestMarkdownRendererSafety`.
- `grep -c 'innerHTML' src/lucy_ng/webview/static/webview.js` returns 0.
- Tab labels + placeholder copy + `#log-panel h3`/`p` CSS present in index.html.
</verification>

<success_criteria>
The right panel is a persistent 4-tab bar (Run Log default-active) over 4 panels, 3 of which are
docked-in placeholders; the left Overview/Structures column is untouched. The run log renders the
CASE-PROGRESS.md markdown subset (headings, bold labels, inline code, pipe-tables, hr, bullets,
fenced code) as DOM nodes via textContent/createElement only. The D-13 scroll-preserve behavior is
intact. The automated innerHTML static scan is green. (Browser-level XSS-escaping, tab switching, and
typography hierarchy are confirmed in Plan 03.)
</success_criteria>

<output>
Create `.planning/phases/93-formatted-log-tab-framework/93-02-SUMMARY.md` when done.
</output>
