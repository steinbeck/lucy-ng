---
phase: 95-1d-real-spectra-peak-overlay
plan: 05
type: execute
wave: 4
depends_on: [95-02]
gap_closure: true
files_modified:
  - src/lucy_ng/webview/routers/spectra.py
  - tests/test_webview_api.py
autonomous: true
requirements: [SP1-01]

must_haves:
  truths:
    - "Each picked peak carries ONE combined rotated label (ppm + assignment) instead of a separate horizontal assignment label, so dense-region assignment text no longer collides horizontally"
    - "The overlay still shows ppm value AND assignment (when present) per peak — D-03 satisfied"
    - "Reversed axis, trace, marker positions, and never-500 degradation are all unchanged"
  artifacts: []
  key_links: []
---

<objective>
Gap closure for the Wave-3 (95-04) human-verify checkpoint defect: assignment-label
collision in dense-peak regions. The ppm number labels are rotated 90° (legible), but the
assignment text labels are drawn horizontally at the top of the axes, so where peaks are
close in ppm (aromatic ~127–141, methyls ~18–22) the assignment labels overlap and become
unreadable. Fix `_render_1d_png` in `spectra.py` to draw ONE combined rotated label per
peak — the ppm value plus the assignment (when present) — eliminating the horizontal
collision while preserving D-03 (marker + ppm + assignment). Cosmetic/legibility only; no
change to trace rendering, axis direction, marker positions, or degradation behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-CONTEXT.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combine ppm + assignment into one rotated per-peak label in _render_1d_png</name>
  <read_first>
    - src/lucy_ng/webview/routers/spectra.py — `_render_1d_png` (~lines 188-269): the peak loop draws a rotated-90° ppm label at `marker_top` and a SEPARATE horizontal assignment label at `y_max`. The horizontal one is the collision source. Constants: `_ACCENT_COLOR = "#0c5460"`, `_TRACE_COLOR`, `_FIGSIZE = (9.0, 3.0)`, `_DPI = 100`.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md — D-03 overlay style (accent markers, ppm + assignment labels); the fix must keep both ppm and assignment visible per peak.
    - tests/test_webview_api.py — `test_peak_overlay_positions` (~line 1394, overlay render differs from bare trace) and the TestSpectraEndpoint fixtures; the change must keep these GREEN.
  </read_first>
  <action>
    In `_render_1d_png`, replace the two-label pattern with a single combined label per peak:
    - Remove the separate horizontal assignment `ax.text(... ppm, y_max, str(assignment), ha="center", va="bottom")` block.
    - Build one label string: `f"{ppm:.1f}  {assignment}"` when a truthy `assignment` is present, else `f"{ppm:.1f}"`.
    - Draw that single label at `marker_top` with `rotation=90, ha="center", va="bottom", fontsize=7, color=_ACCENT_COLOR` (same orientation the ppm label already uses), so every peak's text reads vertically along its marker and dense-region labels stack without horizontal overlap.
    - Keep the vertical marker line (`ax.plot([ppm,ppm],[y_min,marker_top], color=_ACCENT_COLOR, linewidth=0.75)`) unchanged.
    - Do NOT touch `_apply_nmr_axes`, the trace plot, marker geometry, the `annotate_missing_peaks` "peak positions unavailable" note, or any degradation/placeholder path.
    Add a focused test to `TestSpectraEndpoint` asserting the source no longer emits a separate horizontal assignment label — i.e. `_render_1d_png` produces exactly one `ax.text` call per peak (assignment folded into the ppm label). A pragmatic, robust assertion: render an overlay with two near-collinear peaks that both carry assignments and confirm the endpoint returns 200 image/png with non-empty bytes AND that the source contains no `va="bottom"`-anchored assignment-only text block (grep-style source assertion), keeping the check resilient to matplotlib internals.
  </action>
  <acceptance_criteria>
    - `spectra.py` `_render_1d_png` no longer contains a separate `str(assignment)` `ax.text(...)` call anchored at `y_max` (grep: no assignment-only horizontal label) — the assignment is folded into the single rotated ppm label.
    - The combined label string includes the assignment when present (source contains an f-string joining `ppm` and `assignment`).
    - `pytest tests/test_webview_api.py::TestSpectraEndpoint -q` exits 0 (existing overlay + never-500 tests still green, plus the new assertion).
    - `GET /api/spectra/1d/carbon` still returns 200 image/png on the manifest-backed fixture; reversed-axis test `ax.get_xlim()[0] > ax.get_xlim()[1]` still holds; no `invert_xaxis` introduced.
    - `mypy src/lucy_ng/webview/routers/spectra.py` and `ruff check src/lucy_ng/webview/routers/spectra.py tests/test_webview_api.py` exit 0 on the touched files.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Automated: TestSpectraEndpoint green (incl. new single-label assertion); mypy/ruff clean on touched files.
- Visual (orchestrator re-render): re-render the CASE1 ¹³C overlay and confirm assignment labels no longer collide in the aromatic (~127–141) and methyl (~18–22) regions.
</verification>

<success_criteria>
- One combined rotated label per peak (ppm + assignment); no horizontal assignment collision.
- All prior Phase 95 behaviour (trace, reversed axis, markers, degradation) unchanged and still green.
</success_criteria>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| HTTP client ↔ PNG route | Unchanged; read-only localhost image endpoint. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-95-05-01 | Denial of render | `_render_1d_png` label change | mitigate | Label change is inside the existing `try/finally` (figure always closed); never-500 route guard from 95-02 unchanged; overlay tests keep the render path green. |
| T-95-05-SC | Tampering | pip/npm installs | accept | No installs; label-only source change. |
</threat_model>

<output>
Create `.planning/phases/95-1d-real-spectra-peak-overlay/95-05-SUMMARY.md` when done.
</output>
