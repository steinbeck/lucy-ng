---
phase: 96-2d-real-spectra-peak-overlay
plan: 03
type: execute
wave: 2
depends_on: [96-02]
files_modified:
  - src/lucy_ng/webview/static/index.html
  - src/lucy_ng/webview/static/webview.js
autonomous: true
requirements: [SP2-01, SP-02]

must_haves:
  truths:
    - "The 2D Spectra tab shows three stacked plots (HSQC, HMBC, COSY), each an <img> sourced from its 2D route — the 'coming in Phase 96' placeholder is gone"
    - "refreshSpectra2D() cache-busts all three 2D <img> src attributes every ~3 s poll and is called from tick()"
    - "Each plot loads independently — a per-plot 'unavailable' placeholder PNG on one does not blank the others"
  artifacts:
    - path: "src/lucy_ng/webview/static/index.html"
      provides: "three stacked 2D <img> sections replacing the spectra-2d placeholder"
      contains: "img-spectrum-hsqc"
    - path: "src/lucy_ng/webview/static/webview.js"
      provides: "refreshSpectra2D + tick() call"
      contains: "refreshSpectra2D"
  key_links:
    - from: "src/lucy_ng/webview/static/webview.js"
      to: "/api/spectra/2d/hsqc"
      via: "img.src cache-buster in refreshSpectra2D, called from tick()"
      pattern: "spectra/2d/"
---

<objective>
Populate the 2D Spectra tab: replace the `data-panel="spectra-2d"` placeholder in
`index.html` with three stacked `<section>`/`<img>` blocks (HSQC/HMBC/COSY) mirroring the
1D-spectra markup, and add `refreshSpectra2D()` to `webview.js` (called from `tick()`) to
cache-bust the three `<img>` sources on the existing ~3 s poll. Pure frontend wiring against
the three routes shipped in Plan 02 — no new CSS class, no new design system (reuse
`.tables-section` / `.tables-heading` / `.spectrum-img` verbatim, D-09).

Purpose: Complete the user-facing half of SP2-01 — the chemist sees the three 2D contour
plots with cross-peaks overlaid, each degrading independently to a placeholder image (SP-02).
Output: `index.html` (three 2D `<img>` sections) + `webview.js` (`refreshSpectra2D` + tick wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-2d-real-spectra-peak-overlay/96-CONTEXT.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the spectra-2d placeholder with three stacked HSQC/HMBC/COSY img sections</name>
  <files>src/lucy_ng/webview/static/index.html</files>
  <read_first>
    - src/lucy_ng/webview/static/index.html — the `data-panel="spectra-1d"` block (~lines 421-434, the exact analog: `<section class="tables-section">` + `<h2 class="tables-heading">` + `<img class="spectrum-img" ...>`); the `data-panel="spectra-2d"` placeholder to replace (~line 435, `<div class="placeholder" data-panel="spectra-2d">2D Spectra — coming in Phase 96</div>`); the `.spectrum-img` CSS (~lines 371-379) and `.tables-section` box model — reuse verbatim, add NO new CSS.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md — "Layout & Interaction Contract" DOM structure block (the exact three-section markup, ids img-spectrum-hsqc/hmbc/cosy, alt text) and "Copywriting Contract" (section headings `HSQC`/`HMBC`/`COSY`).
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md — "src/lucy_ng/webview/static/index.html — replace the spectra-2d placeholder".
  </read_first>
  <action>
    Replace the single `<div class="placeholder" data-panel="spectra-2d">…</div>` line with a
    `<div data-panel="spectra-2d">` containing three stacked `<section class="tables-section">` blocks,
    exactly mirroring the `data-panel="spectra-1d"` block:
      - `id="spectrum-hsqc"`, `<h2 class="tables-heading">HSQC</h2>`, `<img id="img-spectrum-hsqc"
        class="spectrum-img" alt="HSQC contour plot with cross-peak overlay" src="/api/spectra/2d/hsqc">`
      - `id="spectrum-hmbc"`, heading `HMBC`, `<img id="img-spectrum-hmbc" class="spectrum-img"
        alt="HMBC contour plot with cross-peak overlay, colour-coded by flag" src="/api/spectra/2d/hmbc">`
      - `id="spectrum-cosy"`, heading `COSY`, `<img id="img-spectrum-cosy" class="spectrum-img"
        alt="COSY contour plot with cross-peak overlay and diagonal" src="/api/spectra/2d/cosy">`
    Reuse `.tables-section` / `.tables-heading` / `.spectrum-img` verbatim — add NO new CSS class and NO new
    style token (D-09: stacked vertically, all rendered; the existing `.tables-section { margin-bottom:16px }`
    box model produces the stack). Do NOT touch the tab bar `<button data-tab="spectra-2d">` (already present)
    or any other panel.
  </action>
  <verify>
    <automated>python -c "t=open('src/lucy_ng/webview/static/index.html',encoding='utf-8').read(); assert 'coming in Phase 96' not in t, 'placeholder still present'; assert all(s in t for s in ['id=\"img-spectrum-hsqc\"','id=\"img-spectrum-hmbc\"','id=\"img-spectrum-cosy\"','/api/spectra/2d/hsqc','/api/spectra/2d/hmbc','/api/spectra/2d/cosy']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` no longer contains the string `coming in Phase 96`.
    - `index.html` contains `<img>` elements with ids `img-spectrum-hsqc`, `img-spectrum-hmbc`, `img-spectrum-cosy`, each `class="spectrum-img"` sourced from `/api/spectra/2d/{hsqc,hmbc,cosy}` respectively.
    - The three sections reuse `class="tables-section"` + `class="tables-heading"` (headings `HSQC`/`HMBC`/`COSY`); no new CSS class or style token introduced.
    - The guard `python -c "..."` above exits 0.
  </acceptance_criteria>
  <done>The spectra-2d placeholder is replaced by three stacked HSQC/HMBC/COSY img sections reusing existing classes; guard passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add refreshSpectra2D and call it from tick()</name>
  <files>src/lucy_ng/webview/static/webview.js</files>
  <read_first>
    - src/lucy_ng/webview/static/webview.js — `refreshSpectra1D` (~lines 716-728, the exact analog: `var t = Date.now()`, per-img `getElementById` + `if (img) { img.src = '/api/...?t=' + t }`); `tick()` (~lines 767-778, the fire-and-forget call list where `refreshSpectra1D()` is invoked); `initTabs()` (~lines 733-755, generic data-panel toggling — no change needed).
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md — "Layout & Interaction Contract" `refreshSpectra2D` reference implementation + "Poll/refresh behaviour".
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md — "src/lucy_ng/webview/static/webview.js — add refreshSpectra2D".
  </read_first>
  <action>
    Add a `refreshSpectra2D()` function mirroring `refreshSpectra1D` exactly: one `var t = Date.now();`
    then three guarded assignments — `var h = document.getElementById('img-spectrum-hsqc'); if (h) { h.src =
    '/api/spectra/2d/hsqc?t=' + t; }` and the HMBC (`img-spectrum-hmbc` → `/api/spectra/2d/hmbc`) and COSY
    (`img-spectrum-cosy` → `/api/spectra/2d/cosy`) equivalents. The `?t=<timestamp>` cache-buster defeats the
    browser's `<img>` HTTP cache only; the server-side mtime cache ignores the query string (no conflict — see
    UI-SPEC Poll/refresh behaviour). No `fetch().then(json)` — same native `<img>` binary-payload pattern as
    `refreshSpectra1D`. Add `refreshSpectra2D();` to `tick()` immediately after the existing `refreshSpectra1D();`
    call (order-independent, fire-and-forget). Make NO change to `initTabs()` (it already toggles
    `data-panel="spectra-2d"` generically).
  </action>
  <verify>
    <automated>python -c "t=open('src/lucy_ng/webview/static/webview.js',encoding='utf-8').read(); assert 'function refreshSpectra2D' in t; assert t.count('refreshSpectra2D()') >= 1, 'not called from tick()'; assert all(s in t for s in ['/api/spectra/2d/hsqc?t=','/api/spectra/2d/hmbc?t=','/api/spectra/2d/cosy?t=','img-spectrum-hsqc','img-spectrum-hmbc','img-spectrum-cosy']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `webview.js` defines `function refreshSpectra2D()` that cache-busts (`?t=` + timestamp) the `src` of `img-spectrum-hsqc`/`img-spectrum-hmbc`/`img-spectrum-cosy` from `/api/spectra/2d/{hsqc,hmbc,cosy}`, each guarded by an `if (img)` null check.
    - `refreshSpectra2D();` appears in `tick()` (invoked once per poll).
    - `initTabs()` is unchanged.
    - The guard `python -c "..."` above exits 0.
  </acceptance_criteria>
  <done>refreshSpectra2D added (three cache-busted 2D img updates) and wired into tick(); guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server PNG bytes → browser `<img>` | Binary image payload rendered natively by the browser; no server content is injected into the DOM as HTML (no innerHTML), so no XSS surface (consistent with the v9.2/9.3 textContent discipline). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Tampering (XSS) | Rendering server data in the 2D tab | mitigate | The three plots are `<img src>` binary payloads only — no server string is ever assigned via innerHTML; the alt text and headings are static literals in index.html. No new DOM-injection path is introduced. |
| T-96-03-02 | Denial of Service | Poll re-fetching all three PNGs every ~3 s | accept | Cache-buster forces a network request each poll (needed for live updates), but the server-side mtime cache returns cached bytes without re-rendering on unchanged data (SC3, Plan 02) — the frontend load is three cheap image GETs, matching the existing 1D-tab load. |
| T-96-03-SC | Tampering | npm/pip/cargo installs | accept | No installs — vanilla JS/static HTML, no build step, no CDN, no dependency (96-UI-SPEC Registry Safety: not applicable). |
</threat_model>

<verification>
- Both `python -c "..."` structural guards exit 0.
- No new CSS class / no build step / no CDN introduced (visual verification deferred to Plan 04).
</verification>

<success_criteria>
- The 2D Spectra tab renders three stacked HSQC/HMBC/COSY `<img>` plots from the Plan 02 routes; the placeholder text is gone.
- `refreshSpectra2D()` cache-busts all three on the ~3 s poll via `tick()`; each plot loads/degrades independently (SP-02).
</success_criteria>

<output>
Create `.planning/phases/96-2d-real-spectra-peak-overlay/96-03-SUMMARY.md` when done.
</output>
</content>
