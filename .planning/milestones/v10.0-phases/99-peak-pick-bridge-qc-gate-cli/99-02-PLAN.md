---
phase: 99-peak-pick-bridge-qc-gate-cli
plan: 02
type: execute
wave: 2
depends_on: [99-01]
files_modified:
  - src/lucy_ng/nus/qc.py
autonomous: true
requirements: [QC-01, QC-02]
must_haves:
  truths:
    - "Six QC checks compute the documented algorithm on peak-list dicts only (never the raw spectrum matrix), honoring the lucy nus qc <peaks-dir> contract"
    - "aggregate_verdict() returns FAIL on any critical violation, PARTIAL on soft-only, PASS on none (D-02)"
    - "run_qc_checks(peaks_dir) globs HSQC/HMBC/COSY by keyword (not hardcoded _expN), reads the trusted 1D lists, returns a QcReport"
    - "Prot/quaternary classification uses the honest 3-tier resolution (DEPT → explicit override → insufficient_reference_data), never detection/'s hybridisation stats (D-03)"
    - "run_qc_checks on the known-bad fixture dir returns FAIL; on the synthetic-clean fixture dir returns PASS (QC-02)"
    - "Malformed/empty peak-list JSON reports a per-file error in QcReport.errors and never silently PASSes"
  artifacts:
    - path: "src/lucy_ng/nus/qc.py"
      provides: "6 check functions + aggregate_verdict + run_qc_checks + QcReferenceData + QC_CONFIG constants"
      exports: ["run_qc_checks", "aggregate_verdict", "ridge_fraction", "QcReferenceData", "QcConfig"]
      min_lines: 150
  key_links:
    - from: "src/lucy_ng/nus/qc.py"
      to: "lucy_ng.nus.postprocess.check_calibration"
      via: "import (reuse, not reimplement)"
      pattern: "from lucy_ng.nus.postprocess import"
    - from: "src/lucy_ng/nus/qc.py"
      to: "QcReport"
      via: "returns aggregated report"
      pattern: "QcReport"
---

<objective>
Build `nus/qc.py`: the headless, peak-list-only QC gate. Six checks split critical (quaternary-exclusion, ppm-calibration, signal-to-ridge, HSQC-coverage) vs soft (edited-sign self-consistency, COSY diagonal symmetry) per D-02, a single auditable `aggregate_verdict()`, a `QcReferenceData` resolver with the honest 3-tier prot/quaternary fallback (D-03), a central overridable `QcConfig` thresholds object (D-04), and `run_qc_checks(peaks_dir)` that globs the three experiment types, cross-checks against the trusted 1D lists, and returns a `QcReport`.

Purpose: This is the milestone's last line of defense against fabricated cross-peaks becoming hard LSD constraints. QC-02's discrimination (known-bad → FAIL, synthetic-clean → PASS) is the regression floor.

Output: src/lucy_ng/nus/qc.py, making test_qc_checks.py + test_qc_regression.py GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-CONTEXT.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-RESEARCH.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-PATTERNS.md

<interfaces>
From src/lucy_ng/nus/postprocess.py (REUSE — do not reimplement):
```python
GUIDE_S10_C13: list[float]  # 20 true 13C shifts (lines 45-66); first 5 rows include the quaternaries
DEFAULT_CALIBRATION_TOL = 0.5
def check_calibration(observed_ppms, reference_ppms=tuple(GUIDE_S10_C13), tol=DEFAULT_CALIBRATION_TOL) -> bool
```
From src/lucy_ng/models/nus.py (Plan 01):
```python
class QcVerdict(str, Enum): PASS; PARTIAL; FAIL
class QcCheckResult(BaseModel): name; passed; critical; details; value; threshold
class QcReport(BaseModel): verdict; checks; thresholds_used; errors; violated_checks(); critical_violations()
```
Known quaternaries (subset of GUIDE_S10_C13): 142.00, 135.86, 79.35, 36.23, 37.86.
Peak-list schemas (see Plan 01 interfaces): HSQC {c13_ppm,h1_ppm,edited_sign,multiplicity_hint,...}; HMBC {c13_ppm,h1_ppm,rel_intensity,rank_in_carbon,suspected_1J_artifact,...}; COSY {h1a_ppm,h1b_ppm,rel_intensity,...}; 1D {peaks:[{ppm,intensity,snr}]}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QcConfig + QcReferenceData + the six check functions</name>
  <files>src/lucy_ng/nus/qc.py</files>
  <behavior>
    - quaternary_exclusion: known-bad HSQC (≥4 peaks within ±0.5 of a quaternary shift) → violation True; synthetic-clean HSQC (zero) → clean.
    - ppm_calibration: reuse check_calibration on HSQC c13 shifts; a >0.5 ppm systematic offset → violation.
    - signal_to_ridge: ridge_fraction on known-bad COSY (h1a all 5.32) == 1.0 → violation at threshold 0.5; synthetic-clean → below threshold.
    - hsqc_coverage: fraction of trusted protonated carbons with ≥1 HSQC correlation ≥ floor (0.8) → clean; grossly incomplete → violation.
    - edited_sign_self_consistent: known-bad HSQC has mixed multiplicity_hint at 22.63/23.43/67.06 → soft violation; synthetic-clean consistent → clean.
    - cosy_diagonal_symmetry: synthetic-clean COSY (mirror pairs) → clean; a list with no mirror partners → soft violation.
    - Tier-3 (no DEPT, no override): quaternary_exclusion + hsqc_coverage return QcCheckResult with passed=... details="insufficient_reference_data", NOT a silent PASS.
  </behavior>
  <read_first>
    - src/lucy_ng/nus/postprocess.py (lines 42-69 GUIDE_S10_C13/DEFAULT_CALIBRATION_TOL; lines 462-490 check_calibration)
    - 99-PATTERNS.md lines 98-166 (qc.py analog section — ridge_fraction, edited_sign_self_consistent, quaternary-exclusion explicit-override, verdict aggregation)
    - 99-RESEARCH.md Common Pitfalls #1-#5 + Code Examples (concrete algorithms + known-bad numbers)
    - src/lucy_ng/detection/detector.py detect_hybridisation (lines 58-88) — READ ONLY to confirm it returns sp3/sp2/sp1 with NO CH-count; do NOT call it for classification
  </read_first>
  <action>
    Create src/lucy_ng/nus/qc.py (`from __future__ import annotations`; imports: Counter/defaultdict from collections, Sequence from collections.abc, pathlib.Path; `from lucy_ng.nus.postprocess import check_calibration, GUIDE_S10_C13, DEFAULT_CALIBRATION_TOL`; `from lucy_ng.models.nus import QcVerdict, QcCheckResult, QcReport`).

    Define a frozen/dataclass or Pydantic `QcConfig` centralizing all D-04 thresholds (overridable): `c13_tol=0.5`, `h1_tol=0.05`, `ridge_fraction_fail=0.5`, `hsqc_coverage_floor=0.8`, `known_quaternary_shifts=[142.00,135.86,79.35,36.23,37.86]`. Include a classmethod `default()`.

    `KNOWN_QUATERNARY_SHIFTS` derived as the config default. Compute `PROTONATED_REFERENCE = [s for s in GUIDE_S10_C13 if all(abs(s-q)>0.01 for q in KNOWN_QUATERNARY_SHIFTS)]` (the 15 protonated).

    `QcReferenceData` (dataclass): fields `trusted_c13: list[float]`, `trusted_h1: list[float]`, `quaternary_shifts: list[float] | None`, `classification_source: str` ("dept"|"override"|"insufficient_reference_data"). Classmethod `resolve(peaks_dir, config)` implementing the 3-tier D-03 resolution: (1) glob `*DEPT*`/`*dept*` JSON → if present derive prot/quaternary; (2) else config.known_quaternary_shifts (override) with source="override"; (3) else source="insufficient_reference_data", quaternary_shifts=None. Read trusted 1D shifts from the glob-matched `*13C*`/`*1H*` files' `peaks[].ppm` (D-03: read as-is, no re-pick). Never call detection.detect_hybridisation for CH-count.

    Six check functions, each returning a `QcCheckResult(name, passed, critical, details, value, threshold)`:
    - `check_quaternary_exclusion(hsqc_peaks, ref, config)` (critical): if ref.quaternary_shifts is None → passed as-is with details="insufficient_reference_data" (report honestly; per D-03/Pitfall-1 this must not become a silent PASS — set passed=False with the insufficient details so a missing reference cannot mask fabrication). Else violation if ANY hsqc c13_ppm within config.c13_tol of a quaternary shift.
    - `check_ppm_calibration(hsqc_peaks, config)` (critical): `passed = check_calibration([p["c13_ppm"] for p in hsqc_peaks], GUIDE_S10_C13, tol=config.c13_tol)`.
    - `check_signal_to_ridge(peaks_by_type, config)` (critical): compute `ridge_fraction` per experiment on its dominant axis (COSY h1a_ppm/h1b_ppm; HSQC/HMBC c13_ppm and h1_ppm), take the max; passed = max ≤ config.ridge_fraction_fail.
    - `check_hsqc_coverage(hsqc_peaks, ref, config)` (critical): if ref classification insufficient → passed=False details="insufficient_reference_data"; else fraction of PROTONATED_REFERENCE within c13_tol of some hsqc c13_ppm ≥ coverage_floor.
    - `check_edited_sign_consistency(hsqc_peaks, config)` (soft): the `edited_sign_self_consistent` bucket-by-carbon algorithm; passed = no carbon has >1 distinct multiplicity_hint.
    - `check_cosy_diagonal_symmetry(cosy_peaks, config)` (soft): fraction of (h1a,h1b) with a mirror (h1b,h1a) within h1_tol ≥ some symmetry floor (e.g. 0.5); passed accordingly. Empty COSY → passed=True details="no cosy peaks".
    Also expose module-level `ridge_fraction(peaks, axis_key, tol=0.05) -> float` (the Code-Example impl; empty → 0.0).
  </action>
  <acceptance_criteria>
    - `python -c "from lucy_ng.nus.qc import QcConfig, QcReferenceData, ridge_fraction, check_quaternary_exclusion"` succeeds.
    - `ridge_fraction` on 7 dicts all `h1a_ppm≈5.32` returns 1.0; on empty list returns 0.0.
    - `check_quaternary_exclusion` trips (passed=False) on the known-bad HSQC fixture cross_peaks with the override quaternary list; passes on the synthetic-clean HSQC.
    - `check_ppm_calibration` passes on synthetic-clean HSQC (calibrated to GUIDE_S10_C13).
    - `check_edited_sign_consistency` trips on known-bad HSQC (mixed hints at 22.63/23.43/67.06), passes on synthetic-clean.
    - With `QcReferenceData` source="insufficient_reference_data", `check_quaternary_exclusion` and `check_hsqc_coverage` return `passed=False` with `details` containing "insufficient_reference_data" (never a silent pass).
    - `mypy src/lucy_ng/nus/qc.py` passes.
    - No call to `detect_hybridisation` anywhere in qc.py: `! grep -n detect_hybridisation src/lucy_ng/nus/qc.py`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_qc_checks.py -q && python -c "from lucy_ng.nus.qc import ridge_fraction; assert ridge_fraction([{'h1a_ppm':5.32}]*7,'h1a_ppm')==1.0; assert ridge_fraction([],'h1a_ppm')==0.0; print('ok')" && ! grep -n detect_hybridisation src/lucy_ng/nus/qc.py && mypy src/lucy_ng/nus/qc.py</automated>
  </verify>
  <done>Six checks + ridge_fraction + QcConfig + QcReferenceData implemented; test_qc_checks.py green; no detection/ classification misuse; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: aggregate_verdict() + run_qc_checks(peaks_dir) orchestration + QC-02 discrimination</name>
  <files>src/lucy_ng/nus/qc.py</files>
  <behavior>
    - aggregate_verdict: any critical check failed → FAIL; only soft failed → PARTIAL; none failed → PASS.
    - run_qc_checks(known_bad_dir) → QcReport.verdict == FAIL (quaternary-exclusion + ridge both critical-trip).
    - run_qc_checks(clean_synthetic_dir) → QcReport.verdict == PASS.
    - A directory with a malformed JSON file → that file recorded in QcReport.errors; command does not crash; verdict never silently PASS if a critical input failed to load.
    - Glob is keyword-based (*HSQC*/*hsqc*, *HMBC*, *COSY*), not hardcoded _expN.
  </behavior>
  <read_first>
    - 99-PATTERNS.md lines 151-166 (aggregate_verdict CRITICAL/SOFT sets + error handling) + 99-RESEARCH.md Open Question 2 (glob-by-keyword recommendation)
    - src/lucy_ng/nus/runner.py run_stage (lines 51-118) — the typed-error, never-bare-traceback convention to mirror
    - tests/nus/test_qc_regression.py (Plan 01 stub — the exact assertions to satisfy)
  </read_first>
  <action>
    Add to nus/qc.py:
    - `CRITICAL_CHECKS = {"quaternary_exclusion","ppm_calibration","signal_to_ridge","hsqc_coverage"}` and `SOFT_CHECKS = {"edited_sign_consistency","cosy_diagonal_symmetry"}`.
    - `aggregate_verdict(checks: list[QcCheckResult]) -> QcVerdict`: FAIL if any check with `critical=True and not passed`; PARTIAL if any `critical=False and not passed`; else PASS. (Drive off the `critical` flag on each QcCheckResult, consistent with the name sets.)
    - `_load_peaks(peaks_dir) -> tuple[dict[str,list[dict]], list[str]]`: `Path(peaks_dir).resolve()`, glob by experiment-type keyword, `json.loads` each matched file's top-level `cross_peaks` (or `peaks` for 1D); wrap each file's parse in try/except appending a clear `"{filename}: {msg}"` to an errors list (never raise the whole run). Return ({"HSQC":[...],"HMBC":[...],"COSY":[...]}, errors).
    - `run_qc_checks(peaks_dir, config: QcConfig | None = None) -> QcReport`: resolve config default; `QcReferenceData.resolve(peaks_dir, config)`; load peaks; run all six checks (skip a check only when its experiment type is entirely absent — record that as a non-passing insufficient result for the critical ones, per Pitfall 1/T-99-03); `verdict = aggregate_verdict(checks)`; assemble `QcReport(verdict, checks, thresholds_used={...from config...}, errors)`. If load errors touched a critical experiment (e.g. HSQC failed to parse), ensure verdict cannot be PASS.
    - Keep qc.py PURE: it READS reference/peak JSON but performs NO write/quarantine (that is Plan 04's cli/nus.py boundary, per D-07 + Open Question 1).
  </action>
  <acceptance_criteria>
    - `lucy nus`-independent: `python -c "from lucy_ng.nus.qc import run_qc_checks; from pathlib import Path; r=run_qc_checks(Path('tests/fixtures/nus/known_bad_peaks')); print(r.verdict)"` prints `QcVerdict.FAIL`.
    - Same against `tests/fixtures/nus/clean_peaks_synthetic` prints `QcVerdict.PASS`.
    - aggregate_verdict returns FAIL/PARTIAL/PASS for hand-built check lists (one failing critical → FAIL; one failing soft only → PARTIAL; all passed → PASS).
    - A dir containing a deliberately malformed `HSQC_x.json` yields a QcReport whose `errors` names that file and whose verdict is NOT PASS.
    - Glob is keyword-based: `! grep -n "_exp[0-9]" src/lucy_ng/nus/qc.py` (no hardcoded experiment numbers).
    - `pytest tests/nus/test_qc_regression.py tests/nus/test_qc_checks.py -q` green; `mypy src/lucy_ng/nus/qc.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_qc_regression.py tests/nus/test_qc_checks.py -q && python -c "from lucy_ng.nus.qc import run_qc_checks; from pathlib import Path; assert str(run_qc_checks(Path('tests/fixtures/nus/known_bad_peaks')).verdict)=='QcVerdict.FAIL'; assert str(run_qc_checks(Path('tests/fixtures/nus/clean_peaks_synthetic')).verdict)=='QcVerdict.PASS'; print('discriminates')" && ! grep -nE "_exp[0-9]" src/lucy_ng/nus/qc.py && mypy src/lucy_ng/nus/qc.py</automated>
  </verify>
  <done>aggregate_verdict + run_qc_checks implemented; QC-02 discrimination proven (known-bad FAIL, clean PASS); malformed-JSON never silently PASSes; qc.py performs no writes; tests green; mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `<peaks-dir>` filesystem input → QC gate | Arbitrary, possibly externally-produced peak-list JSON crosses into the gate |
| reconstructed correlation → LSD constraint (downstream) | A cross-peak that passes the gate becomes a hard structure constraint; a false PASS silently corrupts a determination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03 | Integrity | A critical check silently no-ops (returns PASS) when its reference input (DEPT/override/1D) is missing → false PASS | mitigate | 3-tier resolver returns source="insufficient_reference_data"; quaternary-exclusion + hsqc-coverage return `passed=False` with that detail — verdict cannot reach PASS on missing reference (Task 1 acceptance asserts it) |
| T-99-01 | Integrity | Malformed/empty peak-list JSON crashes the gate mid-report or is treated as an empty (clean) list → false PASS | mitigate | `_load_peaks` per-file try/except → QcReport.errors; run_qc_checks ensures a critical-experiment load error blocks PASS (Task 2 acceptance) |
| T-99-06 | Tampering/Info-Disclosure | `<peaks-dir>` path escaping / glob over attacker-controlled paths | mitigate | `Path(peaks_dir).resolve()` before glob; no `shell=True`; qc.py introduces zero subprocess calls |
| T-99-07 | Integrity | ridge threshold too loose lets a t1-ridge pass | accept | Default 0.5 validated against the known-bad COSY (1.0) FAIL side; PASS side unvalidated until Phase 100 (Assumptions A1) — threshold is QcConfig-overridable (D-04) |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; nothing to verify |
</threat_model>

<verification>
- `pytest tests/nus/test_qc_checks.py tests/nus/test_qc_regression.py -q` green.
- QC-02 discrimination proven via direct run_qc_checks calls (known-bad FAIL / clean PASS).
- No `detect_hybridisation` call, no hardcoded `_expN`, no writes in qc.py.
- `pytest -q` full suite green; `mypy src/lucy_ng/nus/qc.py` clean.
</verification>

<success_criteria>
- Six checks + aggregate_verdict + run_qc_checks + QcReferenceData + QcConfig implemented in nus/qc.py.
- QC-01 satisfied (machine-readable PASS/PARTIAL/FAIL from the six named checks against trusted 1D).
- QC-02 discrimination floor satisfied (known-bad FAIL, synthetic-clean PASS).
- Honest 3-tier prot/quaternary resolution; no silent PASS on missing reference; reuse of Phase-98 check_calibration.
</success_criteria>

<output>
Create `.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-02-SUMMARY.md` when done
</output>
