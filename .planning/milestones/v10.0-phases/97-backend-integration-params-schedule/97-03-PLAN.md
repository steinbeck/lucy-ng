---
phase: 97-backend-integration-params-schedule
plan: 03
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/lucy_ng/nus/schedule.py
  - tests/test_nus_schedule.py
autonomous: true
requirements: [NUS-03]
must_haves:
  truths:
    - "nus/schedule.py builds a NusSchedule from the Bruker nuslist with 0-based, acquisition-order-preserved indexing (never sorted or regenerated)"
    - "The hard assertion n_sampled == len(nuslist), with n_sampled derived from FnMODE (QF→TD, echo-antiecho→TD/2), passes for all three real experiments and RAISES (never warns) on mismatch"
    - "An unrecognized FnMODE raises NotImplementedError rather than silently guessing"
  artifacts:
    - path: "src/lucy_ng/nus/schedule.py"
      provides: "read_nus_schedule(expdir) + expected_sample_count + validate_schedule"
      contains: "def expected_sample_count"
    - path: "tests/test_nus_schedule.py"
      provides: "FnMODE-derivation + acquisition-order regression tests"
      contains: "acquisition_order"
  key_links:
    - from: "src/lucy_ng/nus/schedule.py"
      to: "lucy_ng.models.nus.NusSchedule"
      via: "model construction after assertion passes"
      pattern: "NusSchedule\\("
---

<objective>
Implement `nus/schedule.py` — the sampling-schedule parser that reads the Bruker `nuslist` in acquisition order and enforces the FnMODE-derived `n_sampled == len(nuslist)` hard-fail assertion (the correctness crux of the phase) before any `NusSchedule` is considered valid.

Purpose: Closes NUS-03. A single wrong divisor would silently corrupt one of the three real experiments (exp2 QF vs exp3/exp4 echo-antiecho), so the assertion must raise, never warn.
Output: `src/lucy_ng/nus/schedule.py`, `tests/test_nus_schedule.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md
@.planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md
@src/lucy_ng/models/nus.py

<interfaces>
From lucy_ng.models.nus: NusSchedule (nuslist, fnmode_f1, td_f1, nus_td, n_sampled — see 97-01 <interfaces>).
Read entry point: ng.bruker.read(expdir)["nuslist"] → acquisition-ordered list[tuple[int,...]] (nmrglue, order-preserving, verified). Flatten 1-tuples to list[int].
Derivation: REAL_FNMODES={1,2}→n_sampled=TD; COMPLEX_FNMODES={4,5,6}→n_sampled=TD//2; else NotImplementedError.
Share the allowed-FnMODE constant with models/nus.py's fnmode validator — do not define a divergent set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: expected_sample_count + validate_schedule + read_nus_schedule + NUS-03 tests</name>
  <files>src/lucy_ng/nus/schedule.py, tests/test_nus_schedule.py</files>
  <read_first>
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "FnMODE → sampled-count derivation rule" table (exact values) + § "nuslist acquisition-order verification" + § "nus/schedule.py" code example
    - .planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md § nus/schedule.py (fail-loud precedent) + § "Test layout"
    - src/lucy_ng/models/nus.py (NusSchedule fields + shared FnMODE constant)
    - src/lucy_ng/readers/bruker.py (nmrglue read delegation convention)
  </read_first>
  <behavior>
    - expected_sample_count(1, 188) == 188; expected_sample_count(6, 100) == 50; expected_sample_count(6, 232) == 116.
    - expected_sample_count(3, 100) raises NotImplementedError (unrecognized mode — no silent guess).
    - validate_schedule passes for (1,188,nuslist[188]), (6,100,nuslist[50]), (6,232,nuslist[116]); raises ValueError when nuslist length mismatches the FnMODE-derived count.
    - read_nus_schedule(EXP2_COSY) → NusSchedule with n_sampled==188, len(nuslist)==188.
    - acquisition-order regression: read_nus_schedule(EXP2_COSY).nuslist[:8] == [0,124,431,670,369,53,211,120] (NOT sorted); assert nuslist != sorted(nuslist).
    - read_nus_schedule reads FnMODE from acqu2s and NusTD authoritatively (not max(nuslist)+1).
  </behavior>
  <action>
    Create module constants `REAL_FNMODES = {1, 2}` and `COMPLEX_FNMODES = {4, 5, 6}` (import/share the allowed
    set with models/nus.py so validator and assertion agree). Implement `expected_sample_count(fnmode, td_f1)` and
    `validate_schedule(fnmode, td_f1, nuslist)` verbatim per RESEARCH's code example — `NotImplementedError` for
    unknown FnMODE, `ValueError` for length mismatch with the "never sort, regenerate, or silently truncate/pad"
    message. Implement `read_nus_schedule(expdir) -> NusSchedule`: resolve+existence-check the path,
    `dic, _ = ng.bruker.read(str(expdir))`, flatten `dic["nuslist"]` (list of 1-tuples) to `list[int]` preserving
    order, read `fnmode_f1`/`td_f1`/`nus_td` from acqu2s, call `validate_schedule(...)` (raises on mismatch),
    then construct `NusSchedule(...)`. Document in the NusSchedule/module docstring the real-vs-complex grid note
    (exp3 max(nuslist)=199 vs NusTD=400 is expected, not a bug — RESEARCH § acquisition-order note) so a future
    maintainer does not "fix" it. Write `tests/test_nus_schedule.py` (DATA_DIR + class-per-concern), including a
    dedicated `test_..._acquisition_order` method and an unrecognized-FnMODE raise test.
  </action>
  <verify>
    <automated>pytest tests/test_nus_schedule.py -x && pytest tests/test_nus_schedule.py -k acquisition_order -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_nus_schedule.py -x` all green
    - `pytest tests/test_nus_schedule.py -k acquisition_order -x` green (order-preservation regression)
    - Test asserts expected_sample_count(3, 100) raises NotImplementedError
    - Test asserts validate_schedule with a truncated nuslist raises ValueError
    - `grep -n 'sorted(' src/lucy_ng/nus/schedule.py` shows no sorting applied to the parsed nuslist (only in a test negative-assertion, if any)
  </acceptance_criteria>
  <done>Schedule parsing preserves acquisition order and the FnMODE-derived hard assertion passes for all three fixtures and raises on mismatch; NUS-03 covered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/caller → schedule reader | Untrusted `expdir` path string crosses into filesystem access; nuslist length is safety-critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | `expdir` argument path traversal | mitigate | `Path(expdir).resolve()` + existence check before `ng.bruker.read` |
| T-97-03 | Tampering | Corrupted/mismatched nuslist silently accepted | mitigate | `validate_schedule` raises ValueError on `n_sampled != len(nuslist)` and NotImplementedError on unknown FnMODE — fail loud, never warn or pad |
| T-97-SC | Tampering | pip/package installs | accept | No installs; nmrglue/pydantic core deps only |
</threat_model>

<verification>
- `pytest tests/test_nus_schedule.py -x` green
- `pytest tests/test_nus_*.py -x` green
</verification>

<success_criteria>
NUS-03 met: acquisition-order-preserving nuslist parsing + FnMODE-derived hard-fail assertion validated across QF (exp2) and echo-antiecho (exp3/exp4) fixtures.
</success_criteria>

<output>
Create `.planning/phases/97-backend-integration-params-schedule/97-03-SUMMARY.md` when done.
</output>
