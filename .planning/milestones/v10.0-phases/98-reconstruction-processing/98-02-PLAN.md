---
phase: 98-reconstruction-processing
plan: 02
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - src/lucy_ng/nus/runner.py
  - src/lucy_ng/models/nus.py
  - tests/nus/test_runner_faillloud.py
  - tests/nus/test_fnmode_branching.py
autonomous: true
requirements: [RECON-04, RECON-03]

must_haves:
  truths:
    - "A single run_stage() helper raises a stage-named RuntimeError on any of: non-zero exit code, missing/empty output file, or an .fid/.ft2 output that parses but is all-zero"
    - "_ordering_for_fnmode returns 'expand_first' for echo-antiecho (COMPLEX_FNMODES), 'convert_first' for QF (REAL_FNMODES), and raises NotImplementedError for any other FnMODE"
    - "A per-FnMODE recipe captures stage order, bruk2pipe -yMODE, phase-mode (magnitude vs phase-sensitive), and whether SMILE's -EA flag applies — in one auditable place"
    - "D-01: every external tool runs as its own subprocess via the shared run_stage() fail-loud helper (exit code + output-file non-emptiness + non-all-zero data), never a single csh pipe chain"
  artifacts:
    - path: "src/lucy_ng/nus/runner.py"
      provides: "run_stage() fail-loud wrapper + FnMODE recipe table + _ordering_for_fnmode()"
      contains: "def run_stage("
      min_lines: 60
    - path: "src/lucy_ng/models/nus.py"
      provides: "NusReconstructionResult model"
      contains: "class NusReconstructionResult"
  key_links:
    - from: "src/lucy_ng/nus/runner.py"
      to: "lucy_ng.models.nus.REAL_FNMODES / COMPLEX_FNMODES"
      via: "import — never a second divergent FnMODE table"
      pattern: "REAL_FNMODES|COMPLEX_FNMODES"
    - from: "src/lucy_ng/nus/runner.py"
      to: "nmrglue.fileio.pipe.read"
      via: "output-file non-zero-data shape check in run_stage"
      pattern: "pipe\\.read"
---

<objective>
Build the foundation layer of `nus/runner.py`: the single fail-loud `run_stage()` subprocess
wrapper (RECON-04's correctness anchor), the FnMODE-driven stage-order recipe (RECON-03's
auditable branch table + `_ordering_for_fnmode()` refuse-to-guess helper), and the
`NusReconstructionResult` model. No orchestration or backend invocation yet — those consume
these primitives in Plans 03/05.

Purpose: every later external-tool call routes through one shared, tested fail-loud check;
FnMODE branching lives in one place reusing the shared `REAL_FNMODES`/`COMPLEX_FNMODES` constants.
Output: `src/lucy_ng/nus/runner.py` (new, helpers only), `NusReconstructionResult` in models/nus.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md
@src/lucy_ng/models/nus.py
@src/lucy_ng/nus/backends/__init__.py

<interfaces>
From src/lucy_ng/models/nus.py (Phase 97, reuse — do NOT redefine):
  REAL_FNMODES = {1, 2}          # QF/QSEQ magnitude-mode
  COMPLEX_FNMODES = {4, 5, 6}    # States/States-TPPI/Echo-AntiEcho phase-sensitive
  VALID_FNMODES = REAL_FNMODES | COMPLEX_FNMODES
  class NusAcquisitionParams(BaseModel)  # grpdly, nus_td, fnmode_f1, f1/f2 SF/OFFSET, ...
  class NusSchedule(BaseModel)           # nuslist, nus_td, n_sampled

run_stage() target signature (98-PATTERNS.md Pattern 2 / 98-RESEARCH.md Pattern 2):
  run_stage(name: str, argv: list[str], cwd: Path, expected_output: Path, timeout: int = 600) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_stage() fail-loud subprocess wrapper (RECON-04)</name>
  <files>src/lucy_ng/nus/runner.py, tests/nus/test_runner_faillloud.py</files>
  <read_first>
    - src/lucy_ng/lsd/runner.py lines 262-352 (the "success derives from the on-disk artefact, not just returncode" precedent)
    - src/lucy_ng/nus/backends/nmrpipe_smile.py lines 75-90 (fixed-arg-list, never shell=True, capture_output, timeout)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "Core run_stage() fail-loud wrapper" (verbatim target implementation)
    - tests/nus/conftest.py (make_valid/empty/truncated_intermediate + mock_subprocess_run fixtures from Plan 01)
    - tests/nus/test_runner_faillloud.py (the Plan-01 skipped stubs to implement)
  </read_first>
  <behavior>
    - Non-zero returncode → RuntimeError naming the stage and including truncated stderr
    - returncode 0 but expected_output missing or `stat().st_size == 0` → RuntimeError
    - expected_output suffix in {.fid,.ft2} that parses via `nmrglue.fileio.pipe.read` but has `data.size == 0` or `not data.any()` → RuntimeError
    - returncode 0 + non-empty, non-all-zero output → returns None (no raise)
  </behavior>
  <action>
    Create `src/lucy_ng/nus/runner.py` with module docstring framing it as the NUS analog of
    `LSDRunner`. Implement module-level `run_stage(name, argv, cwd, expected_output, timeout=600)
    -> None` exactly per 98-PATTERNS.md Pattern 2: run `subprocess.run(argv, capture_output=True,
    text=True, timeout=timeout, cwd=cwd)` (never `shell=True`); raise stage-named `RuntimeError`
    on non-zero returncode, on missing/empty `expected_output`, and — for `.fid`/`.ft2` outputs —
    on all-zero/empty parsed data via `nmrglue.fileio.pipe.read(str(expected_output))`. Import
    nmrglue as `ng`. Then remove the `pytest.mark.skip` from the four RECON-04 stubs and implement
    them using the conftest fixtures (drive returncode via `mock_subprocess_run`, drive the output
    file via the make_*_intermediate factories).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/runner.py` contains `def run_stage(`
    - `grep -c 'shell=True' src/lucy_ng/nus/runner.py` returns 0
    - `pytest tests/nus/test_runner_faillloud.py -q` exits 0 with 4 passed, 0 skipped
    - test_nonzero_exit_raises, test_empty_output_raises, test_truncated_all_zero_output_raises each assert a RuntimeError whose message contains the stage name
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_runner_faillloud.py -q</automated>
  </verify>
  <done>run_stage() aborts loud on exit-code, missing/empty, and all-zero-data failures; all four RECON-04 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FnMODE recipe table + _ordering_for_fnmode() (RECON-03)</name>
  <files>src/lucy_ng/nus/runner.py, tests/nus/test_fnmode_branching.py</files>
  <read_first>
    - src/lucy_ng/nus/schedule.py (the expected_sample_count() refuse-to-guess NotImplementedError convention to mirror)
    - src/lucy_ng/models/nus.py lines 24-30 (REAL_FNMODES/COMPLEX_FNMODES/VALID_FNMODES to import, not redefine)
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Critical Finding 1 (FnMODE-dependent stage order) and § Pattern 3 (per-FnMODE recipe table)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "FnMODE-driven stage-order branch"
    - tests/nus/test_fnmode_branching.py (Plan-01 skipped stubs)
  </read_first>
  <behavior>
    - _ordering_for_fnmode(6) == "expand_first"; _ordering_for_fnmode(4) == "expand_first"; _ordering_for_fnmode(5) == "expand_first"
    - _ordering_for_fnmode(1) == "convert_first"; _ordering_for_fnmode(2) == "convert_first"
    - _ordering_for_fnmode(3) raises NotImplementedError (3 is not in REAL_FNMODES∪COMPLEX_FNMODES for this project's supported set)
    - The recipe for FnMODE 6 marks phase-sensitive / -EA applies; the recipe for FnMODE 1 marks magnitude-mode / no phase / no -EA
  </behavior>
  <action>
    In `src/lucy_ng/nus/runner.py` add: `_ordering_for_fnmode(fnmode: int) -> str` returning
    "expand_first" for `fnmode in COMPLEX_FNMODES`, "convert_first" for `fnmode in REAL_FNMODES`,
    else `raise NotImplementedError(f"FnMODE={fnmode} has no known stage-order recipe")` — importing
    the constants from `lucy_ng.models.nus`, never redefining them. Add a small immutable recipe
    structure (a frozen dataclass or NamedTuple keyed by fnmode) named e.g. `FnModeRecipe` with
    fields: `stage_order` (the string above), `bruk2pipe_ymode` (e.g. "Echo-AntiEcho" for 6;
    a QF/magnitude value flagged provisional per 98-RESEARCH.md Assumptions Log A3 for 1),
    `phase_sensitive` (True for COMPLEX_FNMODES, False for REAL_FNMODES), and `smile_ea` (True for 6).
    Provide `recipe_for_fnmode(fnmode) -> FnModeRecipe` that also refuses unknown FnMODEs. Then
    implement the RECON-03 stubs.
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/runner.py` contains `def _ordering_for_fnmode(` and references `COMPLEX_FNMODES`
    - `grep -cE '^(REAL_FNMODES|COMPLEX_FNMODES|VALID_FNMODES) *=' src/lucy_ng/nus/runner.py` returns 0 (imported, never redefined)
    - `pytest tests/nus/test_fnmode_branching.py -q` exits 0 with all tests passed, 0 skipped
    - The QF (FnMODE 1) recipe's `bruk2pipe_ymode` is documented in-source as provisional per Assumptions Log A3
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_fnmode_branching.py -q</automated>
  </verify>
  <done>FnMODE stage-order + recipe branching lives in one place, reuses the shared constants, refuses unknown FnMODEs; RECON-03 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: NusReconstructionResult model</name>
  <files>src/lucy_ng/models/nus.py</files>
  <read_first>
    - src/lucy_ng/models/nus.py lines 129-171 (NusSchedule to_dict/from_dict + ConfigDict convention to mirror)
    - src/lucy_ng/lsd/runner.py lines 90-128 (LSDResult field set + summary() convention)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "src/lucy_ng/models/nus.py (model)"
  </read_first>
  <action>
    Add `NusReconstructionResult(BaseModel)` to `src/lucy_ng/models/nus.py` with
    `model_config = ConfigDict(arbitrary_types_allowed=True)` and explicitly typed fields:
    `success: bool`, `backend: str`, `fnmode_f1: int`, `stage_dir: str` (the
    `analysis/nus_recon/<expN>/` path), `stage_outputs: dict[str, str]` (stage name → output file
    path under stage_dir), `processed_spectrum: str | None` (final .ft2 path), and
    `smile_iterations: int | None`. Provide `to_dict()`/`from_dict()` matching the existing
    convention and a `summary()` string method mirroring `LSDResult.summary()`. Store paths as
    strings for JSON-serialisability (the `--format json` CLI convention). Do not add a
    field_validator unless a real invariant exists (fnmode recognition may reuse VALID_FNMODES).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/models/nus.py` contains `class NusReconstructionResult`
    - `python -c "from lucy_ng.models.nus import NusReconstructionResult; NusReconstructionResult(success=True, backend='nmrpipe_smile', fnmode_f1=6, stage_dir='/tmp/x', stage_outputs={}, processed_spectrum=None, smile_iterations=None).to_dict()"` exits 0 and round-trips via from_dict
    - `mypy src/lucy_ng/models/nus.py` passes (strict mode)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from lucy_ng.models.nus import NusReconstructionResult as R; d=R(success=True,backend='nmrpipe_smile',fnmode_f1=6,stage_dir='/tmp/x',stage_outputs={'bruk2pipe':'/tmp/x/test.fid'},processed_spectrum=None,smile_iterations=None).to_dict(); assert R.from_dict(d).to_dict()==d"</automated>
  </verify>
  <done>NusReconstructionResult exists with the LSDResult-style field set + to_dict/from_dict/summary, round-trips cleanly, passes strict mypy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner.py → external subprocess | run_stage builds a fixed argv list and invokes an external tool; the argv is assembled by callers (Plans 03/05) from typed Pydantic fields, never raw user strings |
| runner.py → filesystem | run_stage reads/stats `expected_output`; path supplied by callers, resolved before dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | run_stage subprocess invocation | mitigate | Fixed arg-list `subprocess.run`, never `shell=True` (grep-gated in acceptance criteria); mirrors NmrPipeSmileBackend/LSDRunner |
| T-98-02 | Tampering | expected_output path handling | mitigate | run_stage only stats/reads the caller-supplied resolved path; no string concatenation of user input here |
| T-98-SC | Tampering | pip installs | accept | No new package installs; nmrglue already core (98-RESEARCH.md audit N/A) |
</threat_model>

<verification>
- `pytest tests/nus/test_runner_faillloud.py tests/nus/test_fnmode_branching.py -q` green (0 skipped for these)
- `mypy src/lucy_ng/nus/runner.py src/lucy_ng/models/nus.py` passes
- `pytest -q` still green
</verification>

<success_criteria>
run_stage() is the single fail-loud subprocess check (exit code + output non-emptiness + non-zero
data); FnMODE stage-order branching + recipe live in one auditable place reusing the shared
constants and refusing unknown FnMODEs; NusReconstructionResult follows the models/nus.py convention.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-02-SUMMARY.md` when done
</output>
