---
phase: 101-jcamp-dx-reader
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - src/lucy_ng/readers/_jcampdx_decode.py
autonomous: true
requirements: [JC-04]

must_haves:
  truths:
    - "The vendored decoder decodes the hand-oracle line '0A00N%Sl' to [100.0, 105.0, 105.0, 102.0] with dtype 'R', independently of nmrglue"
    - "parse_data handles all four pseudo-digit modes (SQZ, DIF, DUP, PAC/AFFN) present in real Bruker DIFDUP pages"
    - "The decoder module imports NO nmrglue symbol (satisfies JC-04's 'without depending on nmrglue's private API')"
  artifacts:
    - path: "src/lucy_ng/readers/_jcampdx_decode.py"
      provides: "Vendored DIFDUP/SQZ/DUP/PAC line decoder (parse_data + 9-object dependency closure) with New-BSD attribution"
      contains: "def parse_data"
      min_lines: 200
  key_links:
    - from: "src/lucy_ng/readers/_jcampdx_decode.py"
      to: "(nothing external beyond re + numpy)"
      via: "self-contained pure-Python module"
      pattern: "^import numpy"
---

<objective>
Vendor nmrglue's DIFDUP/SQZ/DUP/PAC line decoder into a self-contained lucy-ng
module so JC-04 is satisfied literally: a CI-runnable decoder with no dependency
on nmrglue's private API. This is the one genuinely-novel-to-lucy-ng numeric
kernel; it is copied byte-identical (not refactored) so the Plan-01 hand-oracle
test is a valid independent check of the copy.

Purpose: give `read_2d` (Plan 04) a decode kernel it owns, and make the JC-04
oracle GREEN.
Output: `src/lucy_ng/readers/_jcampdx_decode.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-jcamp-dx-reader/101-RESEARCH.md
@.planning/phases/101-jcamp-dx-reader/101-PATTERNS.md

Vendored source (read the exact 9-object closure, lines 208-453):
`/opt/miniconda3/lib/python3.12/site-packages/nmrglue/fileio/jcampdx.py`
License text: `/opt/miniconda3/lib/python3.12/site-packages/nmrglue-0.12.dev0.dist-info/LICENSE.txt`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the 9-object decoder closure with New-BSD attribution</name>
  <read_first>
    - /opt/miniconda3/lib/python3.12/site-packages/nmrglue/fileio/jcampdx.py lines 208-453 (the full dependency closure of `_parse_data`)
    - /opt/miniconda3/lib/python3.12/site-packages/nmrglue-0.12.dev0.dist-info/LICENSE.txt (full 4-clause New-BSD text to reproduce)
    - .planning/phases/101-jcamp-dx-reader/101-PATTERNS.md § "_jcampdx_decode.py" (the exact 9-object list, provenance line, required imports)
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "Code Examples — Vendored decoder" (corrected object list + line budget)
  </read_first>
  <files>src/lucy_ng/readers/_jcampdx_decode.py</files>
  <action>
    Create `src/lucy_ng/readers/_jcampdx_decode.py`. Reproduce the full New-BSD license block
    (Copyright (c) 2010-2015 Jonathan J. Helmus, all 4 clauses verbatim) in the module docstring plus a
    one-line provenance note: `# Vendored from nmrglue.fileio.jcampdx (0.12-dev), lines 208-453.` (per
    D-05). Copy these 9 objects BYTE-IDENTICAL from the source — do not clean up, rename internals, or
    refactor (a byte-identical copy is what makes the Plan-01 hand-oracle a valid independent check):
    `_DIGITS`, `_SQZ_DIGITS`, `_DIF_DIGITS`, `_DUP_DIGITS`, `_detect_format`, `_parse_affn_pac`,
    `_append_value`, `_finish_value`, `_parse_pseudo`, and the entry point (source name `_parse_data`).
    Expose the entry point as a public `parse_data` (Claude's Discretion per PATTERNS — either rename or
    add `parse_data = _parse_data` alias) since the reader and the oracle import `parse_data`. Imports:
    `import re`, `from warnings import warn`, `import numpy as np` ONLY — the module MUST NOT import
    nmrglue (that is the whole point of JC-04). Note the return contract: `parse_data` returns raw
    decoded integers as `(np.ndarray float64, "R"|"I")`; Y-FACTOR scaling is applied later by the reader,
    NOT here (Pitfall 2).
  </action>
  <acceptance_criteria>
    - `pytest tests/readers/test_jcampdx_decode.py -x` PASSES (both the mini-vector oracle and the DIF/DUP-heavy test).
    - `python -c "import lucy_ng.readers._jcampdx_decode as m; r,d=m.parse_data('(F2++(Y..Y))\n0A00N%Sl'); assert list(r)==[100.0,105.0,105.0,102.0] and d=='R'; print('OK')"` prints OK.
    - `grep -c nmrglue src/lucy_ng/readers/_jcampdx_decode.py` returns 0.
    - The module docstring contains the New-BSD copyright line (`grep -q "Jonathan J. Helmus" src/lucy_ng/readers/_jcampdx_decode.py`).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/readers/test_jcampdx_decode.py -x && test "$(grep -c nmrglue src/lucy_ng/readers/_jcampdx_decode.py)" = "0" && grep -q "Jonathan J. Helmus" src/lucy_ng/readers/_jcampdx_decode.py && echo "JC-04 decoder OK"</automated>
  </verify>
  <done>Vendored decoder passes the independent hand-oracle, decodes DIF/DUP/SQZ/PAC, imports no nmrglue, and carries verbatim New-BSD attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encoded `##DATA TABLE=` text → decoder | Untrusted compressed line text decoded to integers in-process. No I/O, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02 | Tampering (scientific-data integrity) | `parse_data` decode correctness | mitigate | Byte-identical vendoring of an already-tested upstream kernel; the Plan-01 hand-oracle (independent of nmrglue) proves the copy is correct, so a vendoring-introduced decode bug fails CI rather than silently corrupting intensities. |
| T-101-SC | Tampering (supply chain) | package installs | accept | N/A — no new packages; code copied from the existing audited `nmrglue` core dependency under its New-BSD license. |
</threat_model>

<verification>
- `pytest tests/readers/test_jcampdx_decode.py -x` green.
- Decoder module contains no `nmrglue` reference.
- New-BSD attribution present.
</verification>

<success_criteria>
The JC-04 decoder is CI-verified GREEN by an oracle independent of nmrglue, with no nmrglue import
and full license attribution.
</success_criteria>

<output>
Create `.planning/phases/101-jcamp-dx-reader/101-02-SUMMARY.md` when done.
</output>
