---
phase: 101-jcamp-dx-reader
plan: 04
type: execute
wave: 3
depends_on: ["101-02", "101-03"]
files_modified:
  - src/lucy_ng/readers/jcamp.py
autonomous: true
requirements: [JC-01, JC-02]

must_haves:
  truths:
    - "A 2D HSQC NTUPLES .dx decodes into a Spectrum2D with data.shape == (n_f1, n_f2) == (16, 2048) on the fixture (JC-01)"
    - "F1 (¹³C) and F2 (¹H) ppm axes are reversed and match the 1D reference peaks within tolerance (¹H ≤0.05 ppm, ¹³C ≤0.10 ppm) — the load-bearing cross-check that catches the 0.447 ppm naive-divisor bug (JC-02)"
    - "Y-FACTOR ≠ 1 is applied to decoded intensities before stacking (Pitfall 2)"
    - "JcampReader.read() dispatches to read_1d/read_2d on ##NUM DIM"
  artifacts:
    - path: "src/lucy_ng/readers/jcamp.py"
      provides: "read_2d (page assembly + ppm axes + fail-loud) and the read() dispatcher"
      contains: "def read_2d"
      min_lines: 80
  key_links:
    - from: "src/lucy_ng/readers/jcamp.py::read_2d"
      to: "lucy_ng.readers._jcampdx_decode.parse_data"
      via: "per-page DIFDUP decode + (n_f1, n_f2) stacking"
      pattern: "parse_data"
    - from: "src/lucy_ng/readers/jcamp.py::read_2d"
      to: "_detect_experiment_type (bruker.py)"
      via: ".PULSE SEQUENCE → experiment_type"
      pattern: "_detect_experiment_type"
    - from: "src/lucy_ng/readers/jcamp.py::read_2d F2 axis"
      to: "1D ¹H reference peaks"
      via: "cross-check test within tolerance"
      pattern: "PPM_CROSS_CHECK_TOLERANCE"
---

<objective>
Implement the genuinely-novel capability of this phase: assemble the
DIFDUP-compressed per-F1-row NTUPLES pages into a full `(n_f1, n_f2)` intensity
matrix (closing the exact gap where nmrglue returns `data=None`), derive both
reversed ppm axes with the verified formula, and gate everything with the
fail-loud assertion plus the load-bearing 1D cross-check. Add the `read()`
dispatcher so Phase-102 CLI wiring is trivial.

Purpose: land JC-01 (2D → Spectrum2D) and JC-02 (correct, cross-checked reversed
ppm axes) — the milestone's one real technical risk.
Output: `read_2d` + `read` in `src/lucy_ng/readers/jcamp.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/101-jcamp-dx-reader/101-RESEARCH.md
@.planning/phases/101-jcamp-dx-reader/101-PATTERNS.md

<interfaces>
Available from Plan 03 (src/lucy_ng/readers/jcamp.py):
  _read_metadata(path)->dict  (inner _datatype_* bucket)
  _strip_caret(v)->str ; _ppm_scale(first_hz,last_hz,offset_ppm,sf,n)->np.ndarray
  _resolve_dim(inner, target_nucleus)->(offset_ppm, sf)  (via .NUCLEUS, len-asserted)
  _assert_plausible_ppm_axis(scale, nucleus)  (reversed + bounds, raises)
  class JcampReader: @staticmethod read_1d(path)->Spectrum1D
Available from Plan 02 (src/lucy_ng/readers/_jcampdx_decode.py):
  parse_data(datastring)->(np.ndarray float64, "R"|"I")   # raw integers, NO Y-FACTOR applied
Reuse (bruker.py): _detect_experiment_type(pulse_program, f1_nucleus, f2_nucleus)->str
Target model (spectrum.py): Spectrum2D(data, f1_ppm_scale, f2_ppm_scale, f1_nucleus, f2_nucleus,
  experiment_type, frequency, metadata)  — nucleus set {1H,13C,...}; experiment set {HSQC,HMBC,COSY,TOCSY,NOESY,ROESY}
NTUPLES layout (RESEARCH): inner["PAGE"] = list of "F1=<hz>" per row; inner["DATATABLE"] = DIFDUP row
  strings; SYMBOL="F1,F2,Y" fixes VAR_DIM/FACTOR/FIRST/LAST column order; Y index → Y_FACTOR (Pitfall 2);
  F2 axis from global FIRST[0]/LAST[0]; F1 axis from the per-page PAGE Hz values (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: read_2d — page assembly + Y-FACTOR + reversed ppm axes + fail-loud (JC-01, JC-02 reader-internal)</name>
  <read_first>
    - src/lucy_ng/readers/bruker.py::read_2d (call-site shape, metadata idiom, Spectrum2D construction; note lines 246-255 are the WRONG axis path for JCAMP — do not imitate the guess_udic math)
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "Pattern 2" (F1 from PAGE Hz), § "Pitfall 2" (Y-FACTOR), § "Pitfall 3" (per-page vs global FIRST), § "Pitfall 4" (.NUCLEUS mapping)
    - .planning/phases/101-jcamp-dx-reader/101-PATTERNS.md § "Core 2D ppm-axis pattern to REPLACE" and § "Constructor call pattern"
    - tests/readers/test_jcamp.py (TestJcampReader2D targets: test_read_2d_shape, test_read_2d_ppm_axis_assertion, test_read_2d_yfactor_scaling)
  </read_first>
  <files>src/lucy_ng/readers/jcamp.py</files>
  <action>
    Add `@staticmethod read_2d(path) -> Spectrum2D` to JcampReader. Guard missing path
    (FileNotFoundError). Load `inner = _read_metadata(path)`. Parse SYMBOL ("F1,F2,Y") to fix column
    order; assert `len(inner["PAGE"]) == len(inner["DATATABLE"])` and equals VAR_DIM's F1 entry (fail-loud,
    T-101-04). Locate the Y index in SYMBOL and read that column's FACTOR as `y_factor` (Pitfall 2 — do
    NOT assume 1). For each page: `arr, _ = parse_data(inner["DATATABLE"][i])`, multiply by `y_factor`,
    and stack into a `(n_f1, n_f2)` float64 matrix (n_f1 = number of pages, n_f2 = decoded row length).
    Dimensions via `.NUCLEUS` (comma-split → F1 nucleus, F2 nucleus; caret-stripped). Derive:
      • F2 axis: `_ppm_scale(FIRST[0]_F2, LAST[0]_F2, offset_F2, sf_F2, n_f2)` where offset/sf come from
        `_resolve_dim(inner, f2_nucleus)` (Pitfall 4).
      • F1 axis: extract the per-page Hz from each `inner["PAGE"][i]` ("F1=<hz>"), then
        `_ppm_scale(page_hz[0], page_hz[-1], offset_F1, sf_F1, n_f1)` with `_resolve_dim(inner, f1_nucleus)`
        (Pitfall 3 — use PAGE Hz, NOT the global triple, so the trimmed fixture's axis has the right count).
    Call `_assert_plausible_ppm_axis` on BOTH axes (D-04). `experiment_type = _detect_experiment_type(
    pulse_program_from_.PULSE SEQUENCE, f1_nucleus, f2_nucleus)`. frequency = `$SF` of F2. Build metadata
    (pulse_program, solvent, f1/f2 frequency) per bruker.py idiom. Return Spectrum2D with exact field names.
  </action>
  <acceptance_criteria>
    - `pytest tests/readers/test_jcamp.py::TestJcampReader2D::test_read_2d_shape -x` PASSES (data.shape == (16, 2048)).
    - `pytest tests/readers/test_jcamp.py::TestJcampReader2D::test_read_2d_ppm_axis_assertion -x` PASSES.
    - `pytest tests/readers/test_jcamp.py::TestJcampReader2D::test_read_2d_yfactor_scaling -x` PASSES.
    - `python -c "from lucy_ng.readers.jcamp import JcampReader; s=JcampReader.read_2d('tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx'); assert s.data.shape==(16,2048); assert s.f1_nucleus=='13C' and s.f2_nucleus=='1H'; assert s.experiment_type=='HSQC'; assert s.f1_ppm_scale[0]>s.f1_ppm_scale[-1] and s.f2_ppm_scale[0]>s.f2_ppm_scale[-1]; print('2D ok', s.data.shape)"` prints `2D ok (16, 2048)`.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/readers/test_jcamp.py::TestJcampReader2D -x</automated>
  </verify>
  <done>2D NTUPLES pages assemble into a correctly-shaped, Y-FACTOR-scaled Spectrum2D with reversed, plausibility-gated ppm axes and correct experiment type.</done>
</task>

<task type="auto">
  <name>Task 2: read() dispatcher + JC-02 load-bearing 1D cross-check</name>
  <read_first>
    - .planning/phases/101-jcamp-dx-reader/101-CONTEXT.md § D-09 (read dispatcher on ##NUM DIM), D-03 (cross-check vs 1D reference)
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "JC-02 cross-check tolerance" + § "D-08 layer 2" (verified oracle peak coordinates)
    - tests/readers/test_jcamp.py (TestJcampReaderPpmCrossCheck::test_read_2d_ppm_axes_match_1d_reference, TestJcampReaderErrors)
  </read_first>
  <files>src/lucy_ng/readers/jcamp.py</files>
  <action>
    Add `@staticmethod read(path) -> Spectrum1D | Spectrum2D` that reads `##NUM DIM` from
    `_read_metadata` and dispatches to read_1d (NUM DIM == 1) or read_2d (== 2), raising ValueError for
    unsupported dimensionality (fail-loud, D-09). Ensure the JC-02 load-bearing cross-check test is GREEN:
    it reads the 2D fixture via read_2d and the two 1D references via read_1d, projects the 2D onto each
    axis (or uses the verified oracle cross-peaks: ¹³C 21.7–23.5 ppm / ¹H 0.96–0.99 ppm), and asserts each
    matched pair is within PPM_CROSS_CHECK_TOLERANCE_1H (0.05) / PPM_CROSS_CHECK_TOLERANCE_13C (0.10) of
    the corresponding 1D reference peak — the check that would fail under the naive 0.447 ppm divisor bug.
    If the cross-check reveals an axis error, the bug is in the Task-1 formula/dimension mapping, not the
    test — fix the reader, never loosen the tolerance. Confirm TestJcampReaderErrors (nonexistent path →
    FileNotFoundError) passes for read/read_1d/read_2d.
  </action>
  <acceptance_criteria>
    - `pytest tests/readers/test_jcamp.py::TestJcampReaderPpmCrossCheck::test_read_2d_ppm_axes_match_1d_reference -x` PASSES within the stated tolerances.
    - `python -c "from lucy_ng.readers.jcamp import JcampReader; from lucy_ng.models import Spectrum1D, Spectrum2D; assert isinstance(JcampReader.read('tests/fixtures/jcamp/C20H32O2_1H.dx'), Spectrum1D); assert isinstance(JcampReader.read('tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx'), Spectrum2D); print('dispatch ok')"` prints `dispatch ok`.
    - `pytest tests/readers/ -x` is fully green.
    - `pytest -q` (full suite) shows no regressions.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/readers/ -x</automated>
  </verify>
  <done>read() dispatches on ##NUM DIM; the load-bearing 1D cross-check passes within tolerance (proving axes are calibrated, not just plausible); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2D `.dx` NTUPLES file → reader | Local scientific data parsed into a Spectrum2D. No network/auth/session/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02 | Tampering (scientific-data integrity, WR-04 class) | 2D ppm-axis derivation (Hz-vs-ppm; naive divisor gives 0.447 ppm error) | mitigate | Verified `OFFSET + SF` formula + `.NUCLEUS`-based dimension mapping + fail-loud plausibility/reversal assertion on BOTH axes, AND the load-bearing 1D cross-check test (¹H ≤0.05 / ¹³C ≤0.10 ppm) that the naive bug cannot pass. |
| T-101-04 | Tampering | Malformed/truncated NTUPLES (page/datatable count mismatch, missing Y-FACTOR) | mitigate | Assert `len(PAGE)==len(DATATABLE)==VAR_DIM[F1]` and apply the SYMBOL-indexed Y_FACTOR (never assume 1) before stacking; refuse to build a partially-decoded matrix. |
| T-101-SC | Tampering (supply chain) | package installs | accept | N/A — no new packages; `nmrglue` is an existing audited core dependency. |
</threat_model>

<verification>
- `pytest tests/readers/ -x` fully green (all JC-01..04 tests).
- `pytest -q` full suite green (no regressions).
- 2D fixture read yields shape (16, 2048), reversed axes, HSQC type, F1=13C/F2=1H.
</verification>

<success_criteria>
JC-01 (2D → Spectrum2D, correct shape, no external binary) and JC-02 (reversed, cross-checked ppm
axes catching the Hz-vs-ppm bug class) are both satisfied and CI-verified on committed real data.
</success_criteria>

<output>
Create `.planning/phases/101-jcamp-dx-reader/101-04-SUMMARY.md` when done.
</output>
