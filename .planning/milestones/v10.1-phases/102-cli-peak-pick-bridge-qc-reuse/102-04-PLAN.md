---
phase: 102-cli-peak-pick-bridge-qc-reuse
plan: 04
type: execute
wave: 3
depends_on: ["102-03"]
files_modified:
  - tests/test_cli_jcamp.py
  - tests/test_skill_files_unchanged.py
autonomous: true
requirements: [JCLI-01, JCLI-02]
must_haves:
  truths:
    - "A single `lucy jcamp <dir>` run over the six committed JCAMP fixtures produces the CASE-consumable per-experiment JSON for HSQC/HMBC/COSY plus 13C.json/1H.json, or quarantines all of them -- proven on real (trimmed) committed data, not on mocks (JCLI-01)"
    - "The unchanged Phase-99 QC gate runs over the JCAMP-derived peak lists and returns a PASS/PARTIAL/FAIL verdict that is surfaced in the peak JSON's additive reconstruction metadata block, exactly as it does for NUS-reconstructed peaks (JCLI-02, D-05)"
    - "PASS/PARTIAL and FAIL are both mechanically reachable and produce different, asserted write behaviour -- D-05's 'wired and discriminating' bar; driving the REAL C20H32O2-jcamp dataset to a green §8 verdict is explicitly Phase 103 / JVAL and is not attempted here"
    - "The edited-HSQC sign survives the JCAMP round-trip: the committed trimmed HSQC fixture is multiplicity-edited (33 negative cross-peaks) and the emitted cross_peaks carry both CH2 and CH_or_CH3 multiplicity hints with zero ambiguous hints (JCLI-02, ROADMAP criterion 3)"
    - "The NOESY fixture is read but not picked -- it appears in the run's skipped list with a stated reason, the run still succeeds for the supported experiments, and no NOESY.json is written (D-06)"
    - "case.md and the five lucy-* agent files are byte-unchanged after this phase, asserted by a committed SHA-256 golden-hash test plus a roster-completeness check, not by trust (JCLI-02, ROADMAP criterion 4)"
    - "The inherited QcConfig.default() quaternary-override behaviour (classification_source='override' with the five compiled-in §8 shifts whenever no DEPT exists) is recorded honestly as observed behaviour rather than presented as a choice or silently fixed (D-04, Pitfall 4)"
    - "Every claim states its proof level: committed-trimmed-fixture, synthetic, or monkeypatched -- a green CI suite on 16 F1 rows is never worded as 'verified on real data' (Pitfall 6, the Phase-100 meta-lesson)"
  artifacts:
    - path: "tests/test_cli_jcamp.py"
      provides: "Fixture-backed end-to-end integration suite for lucy jcamp"
      contains: "class TestJcampEndToEnd"
    - path: "tests/test_skill_files_unchanged.py"
      provides: "SHA-256 golden-hash guard for case.md + the 5 lucy-* agent files"
      min_lines: 40
  key_links:
    - from: "tests/test_cli_jcamp.py"
      to: "tests/fixtures/jcamp/"
      via: "copying all six committed .dx fixtures into tmp_path and invoking the real CLI"
      pattern: "C20H32O2_(HSQC|HMBC|COSY|NOESY)_trimmed\\.dx"
    - from: "tests/test_skill_files_unchanged.py"
      to: ".claude/commands/lucy-ng/case.md"
      via: "hashlib.sha256 comparison against a frozen baseline table"
      pattern: "sha256"
---

<objective>
Prove the phase actually works end-to-end on real committed data, prove the
QC gate discriminates, and freeze the CASE orchestrator against drift.

Purpose: this is where JCLI-02 and ROADMAP success criteria 2, 3 and 4 are
actually earned. Plan 03 built the command; this plan runs it over the six
committed JCAMP fixtures and asserts what really happens -- including honest
labelling of what the 16-row trimmed fixtures can and cannot prove
(102-RESEARCH.md Pitfall 6). It also builds the repo's FIRST committed
byte-unchanged test: no prior pytest test anywhere asserts a hash or diff for
`case.md` or the agent files (verified by searching the whole `tests/` tree).

Output: the `TestJcampEndToEnd` / `TestJcampQcDiscrimination` suites appended
to `tests/test_cli_jcamp.py`, and the new `tests/test_skill_files_unchanged.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-CONTEXT.md
@.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-RESEARCH.md
@.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-PATTERNS.md
@.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-VALIDATION.md
@.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-03-SUMMARY.md
@src/lucy_ng/cli/jcamp.py
@tests/nus/test_write_boundary.py

<interfaces>
<!-- Verified by direct execution against the committed fixtures on 2026-07-25.
     These are measured facts, not estimates. -->

Committed fixture set after plan 102-01 (tests/fixtures/jcamp/):
  C20H32O2_HSQC_trimmed.dx   -> Spectrum2D (16, 2048), experiment_type "HSQC",
                                f1 13C 22.5166..21.1983 ppm, f2 1H 7.0506..-0.4469 ppm
  C20H32O2_HMBC_trimmed.dx   -> Spectrum2D (16, 2048), experiment_type "HMBC"
  C20H32O2_COSY_trimmed.dx   -> Spectrum2D (16, 2048), experiment_type "COSY"
  C20H32O2_NOESY_trimmed.dx  -> Spectrum2D (16, 2048), experiment_type "NOESY"
  C20H32O2_1H.dx             -> Spectrum1D, nucleus "1H"   (whole real file)
  C20H32O2_13C.dx            -> Spectrum1D, nucleus "13C"  (whole real file)

Measured edited-sign facts for the trimmed HSQC fixture (real data, snr_floor=5.0):
  detect_multiplicity_edited(spectrum.data) == (True, 33)
  data.min() == -55253700.0, data.max() == 372616478.0
  bridge_peak_pick(..., experiment="HSQC") -> n_cross_peaks == 115 with
  multiplicity_hint distribution {"CH_or_CH3": 70, "CH2": 45} and ZERO
  "CH_or_CH2_or_CH3" (ambiguous) entries.
  => The edited-sign round-trip (ROADMAP criterion 3) is FIXTURE-COVERED on real
     committed data. Assert the distribution shape (both hints present, zero
     ambiguous), not the exact 70/45 counts, to stay robust.

QcReport construction for the monkeypatched-verdict tests
(lucy_ng.models.nus): QcReport(verdict=QcVerdict.PASS, checks=[...],
thresholds_used={...}, errors=[]); QcCheckResult(name=..., passed=...,
critical=..., details=...). The CLI resolves run_qc_checks via a DEFERRED
import inside the command body, so monkeypatching the source attribute
`lucy_ng.nus.qc.run_qc_checks` intercepts it correctly.

Write-boundary assertion shape to mirror (tests/nus/test_write_boundary.py
lines 29-64): on FAIL, assert `not (root / "analysis" / "nmr_peaks").exists()`,
assert the quarantine directory exists, and assert `result.exit_code != 0`.

Frozen skill-file baseline, RE-VERIFIED with `shasum -a 256` on 2026-07-25
(working tree clean; identical to the values in 102-RESEARCH.md Pitfall 5):

  .claude/commands/lucy-ng/case.md
      8299791ead74294fa31424bae990de62d7bf73260d5dbdbe1e776539e7148d8b
  .claude/agents/lucy-nmr-chemist.md
      4dd7766e3746074062e5f05cefc4462ce85ee444c264c426298fb830c2760839
  .claude/agents/lucy-lsd-engineer.md
      0e9ffcbe4856f9980ed19b5384fb9c7050b20d6427901d0e1ae3ffc1a8507f3b
  .claude/agents/lucy-solution-analyst.md
      dbe9da127ed576aca22fd9d34bf6b599b2e7765b29dffb90fcae83e29dc290f2
  .claude/agents/lucy-devils-advocate.md
      ee80ace79e5785b810e6d9da295f1d31e01ecfa6758f71a3b77d7768c5cbb34f
  .claude/agents/lucy-diagnostic.md
      74bd725c4067be5f076c78424632b7f9d6b4111322d9947fbcbe804a8cfcdbb2

`.claude/agents/` currently contains exactly these five `lucy-*.md` files plus
`supervisor.md`. `supervisor.md` is deliberately EXCLUDED: it is not part of "the
5-agent team" per CLAUDE.md's roster nor CONTEXT.md's list (102-RESEARCH.md
Assumption A2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture-backed end-to-end suite for lucy jcamp (JCLI-01/02, D-04/D-05/D-06)</name>

  <read_first>
    - tests/test_cli_jcamp.py (the file being extended -- plan 102-03 Task 2
      created `TestJcampCliSurface` and `TestJcampImportSafety`; keep them and
      append, do not rewrite)
    - src/lucy_ng/cli/jcamp.py (the command under test -- read the whole body so
      the assertions match the real directory layout, JSON keys and exit rules)
    - tests/nus/test_write_boundary.py in full (154 lines) -- the PASS/PARTIAL vs
      FAIL write-boundary assertion shape to mirror
    - tests/nus/test_cli_pipeline.py -- the staged/final assertion conventions
    - src/lucy_ng/nus/qc.py lines 618-692 (`run_qc_checks`) and lines 100-137
      (`QcReferenceData.resolve`) -- to write correct expectations about
      `classification_source` and about which checks are critical vs soft
    - src/lucy_ng/models/nus.py -- `QcReport`, `QcCheckResult`, `QcVerdict`
      constructor signatures for the synthetic-report tests
  </read_first>

  <files>tests/test_cli_jcamp.py</files>

  <action>
    Append two classes to `tests/test_cli_jcamp.py`. Add a module-scope helper
    `_copy_fixtures(tmp_path, names)` that copies the named committed `.dx` files
    from `tests/fixtures/jcamp/` into `tmp_path` -- ALWAYS copy, never invoke the
    CLI against the tracked fixture directory, because the command creates
    `analysis/nmr_peaks/` and `analysis/jcamp_ingest/` side effects (the same
    `_copy_fixture` discipline Phase 98 Plan 06 established).

    Class `TestJcampEndToEnd` -- everything here is FIXTURE-COVERED on real
    committed (trimmed) data, no mocks:

      1. `test_directory_run_write_boundary_invariant` -- copy all six fixtures,
         run `runner.invoke(jcamp, [str(tmp_path)])`. Assert the branch-agnostic
         invariant so the test cannot be wrong about which verdict a 16-row
         fixture happens to produce:
           * `result.exit_code` is 0 or 1 (never a traceback -- assert
             `result.exception is None or isinstance(result.exception, SystemExit)`)
           * EITHER exit 0 AND `tmp_path/"analysis"/"nmr_peaks"` exists containing
             exactly `{HSQC.json, HMBC.json, COSY.json, 13C.json, 1H.json}`
           * OR exit 1 AND `tmp_path/"analysis"/"nmr_peaks"` does NOT exist AND
             `tmp_path/"analysis"/"jcamp_ingest"/"qc_failed"` exists containing
             `qc_report.json` plus the five payloads
           * In BOTH branches: no `NOESY.json` exists anywhere under
             `tmp_path/"analysis"` (D-06 -- read, not picked)

      2. `test_noesy_is_skipped_not_failed` (D-06) -- run with `--format json`,
         parse stdout, assert the `skipped` list contains exactly one entry whose
         `file` is the NOESY fixture name and whose `reason` mentions `NOESY`, and
         assert the `failed` list is EMPTY (a skip is not a failure).

      3. `test_all_six_fixtures_are_read_without_read_failures` -- same run;
         assert `failed == []`, proving all four 2D fixtures (including the two
         homonuclear ones fixed in plan 102-01) and both 1D files decoded.

      4. `test_edited_hsqc_sign_survives_round_trip` (ROADMAP criterion 3) --
         locate the emitted `HSQC.json` in whichever directory the run wrote to
         (consumable or quarantine), load it, and assert: `n_cross_peaks > 0`;
         the set of `multiplicity_hint` values across `cross_peaks` contains BOTH
         `"CH2"` and `"CH_or_CH3"`; and contains NO `"CH_or_CH2_or_CH3"`. Add a
         comment recording the measured baseline (115 cross-peaks, 70/45 split,
         zero ambiguous, from a spectrum with 33 detected negative cross-peaks) and
         stating that this is real committed data, not synthetic.

      5. `test_qc_verdict_is_embedded_in_peak_json` (JCLI-02) -- load the emitted
         `HSQC.json`; assert `payload["reconstruction"]["qc_verdict"]` is one of
         `"PASS"`, `"PARTIAL"`, `"FAIL"`; assert
         `payload["reconstruction"]["backend"] == "jcamp"`; assert
         `payload["reconstruction"]["thresholds_used"]` is non-empty; assert the
         additive `payload["source"]["format"] == "JCAMP-DX"`.

      6. `test_1d_lists_are_qc_discoverable_in_the_output_directory` (D-03/D-04) --
         from whichever directory was written, call the REAL
         `QcReferenceData.resolve(that_dir)` and assert `trusted_c13` and
         `trusted_h1` are both non-empty. Then assert the observed
         `classification_source` value and add a comment naming it as INHERITED
         byte-unchanged-`qc.py` behaviour (expected `"override"` with the five
         compiled-in §8 shifts, because the dataset has no DEPT -- 102-RESEARCH.md
         Pitfall 4), explicitly NOT a choice this phase makes.

      7. `test_explicit_file_list_mode` (D-01) -- invoke with three explicit file
         paths (HSQC + 1H + 13C) instead of a directory; assert the run reaches a
         verdict and writes into `<parent-of-first-file>/analysis/nmr_peaks/` or
         quarantines, per the same invariant as test 1.

      8. `test_out_override` (D-02) -- invoke with `--out <tmp_path/custom>`;
         assert nothing is created under `tmp_path/"analysis"` and that the
         consumable or quarantine artefacts land relative to the custom path
         (`custom` itself for consumables, `custom.parent/"jcamp_ingest"` for the
         work root).

      9. `test_format_json_shape` -- assert `json.loads(result.output)` has the
         keys `verdict`, `out_dir`, `written`, `skipped`, `failed`, `quarantine`,
         `report`, and that `report["checks"]` has 6 entries (the six named QC
         checks).

      10. `test_observed_trimmed_fixture_verdict` -- RUN the command first, observe
          the actual verdict, then pin it with an explicit assertion AND a comment
          explaining why that verdict is expected for 16 F1 rows (a trimmed HSQC
          covering ~1.3 ppm of 13C cannot reach the 0.8 `hsqc_coverage` floor
          against the whole-file 1D 13C reference). State in the comment that
          driving the REAL 2048x2048 dataset to a green §8 verdict is Phase 103 /
          JVAL (D-05), NOT this phase's bar. Do not invent the expected value --
          write down what actually happened.

    Class `TestJcampQcDiscrimination` (D-05: PASS/PARTIAL/FAIL all reachable) --
    label this class MOCK-COVERED in its docstring: the peaks are real
    fixture-derived, but the VERDICT is injected so the CLI's own branch logic can
    be exercised without a §8-quality spectrum.

      11. `test_pass_verdict_writes_consumable_peaks` -- `monkeypatch.setattr(
          "lucy_ng.nus.qc.run_qc_checks", lambda *a, **k: <synthetic PASS QcReport
          with six passing QcCheckResults>)`. Run over the six fixtures; assert
          exit 0, `analysis/nmr_peaks/HSQC.json` exists, its
          `reconstruction.qc_verdict == "PASS"`, every `cross_peaks[].confidence ==
          "high"`, and no quarantine directory was created.
      12. `test_partial_verdict_writes_and_warns` -- synthetic PARTIAL report (one
          failing SOFT check, e.g. `cosy_diagonal_symmetry`); assert exit 0,
          consumable peaks written, `confidence == "low"` on every cross-peak, the
          violated check named in the CLI output, and `reconstruction.violated_checks`
          containing that name.
      13. `test_fail_verdict_quarantines_and_exits_nonzero` -- synthetic FAIL report
          (one failing CRITICAL check, e.g. `quaternary_exclusion`); mirror
          `tests/nus/test_write_boundary.py`: assert `exit_code != 0`, assert
          `not (root/"analysis"/"nmr_peaks").exists()`, assert the quarantine dir
          holds `qc_report.json` and the payloads, and assert the quarantined
          HSQC payload's `reconstruction.qc_verdict == "FAIL"` with a `caveat`
          naming the violated check. This also proves `confidence_from_verdict()`
          was never called for FAIL (it raises by design).
      14. `test_read_failure_forces_nonzero_exit` -- copy the fixtures, then write
          a deliberately malformed `broken.dx` (e.g. a few lines of non-JCAMP
          text) into `tmp_path`; monkeypatch a PASS verdict so the verdict cannot
          mask the effect; assert exit code is non-zero and the `--format json`
          `failed` list names `broken.dx`. This pins the "malformed input is never
          silently treated as clean" rule (T-102-07).

    Every new test must copy fixtures into `tmp_path` first. Add a module-level
    docstring section stating the proof level of each class explicitly.
  </action>

  <verify>
    <automated>pytest tests/test_cli_jcamp.py -q &amp;&amp; pytest -q &amp;&amp; ruff check src tests &amp;&amp; test -z "$(git status --porcelain tests/fixtures/jcamp/)"</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_cli_jcamp.py -q` exits 0 with at least 23 tests collected
      (the 9 surface/import tests from plan 102-03 plus the 14 added here)
    - `grep -c "class TestJcampEndToEnd\|class TestJcampQcDiscrimination" tests/test_cli_jcamp.py` prints `2`
    - Every `TestJcampEndToEnd` test copies fixtures into `tmp_path`:
      `git status --porcelain tests/fixtures/jcamp/` is empty after the run (the
      tracked fixture tree is never polluted with `analysis/` output)
    - The `--format json` output parses and contains all seven documented top-level
      keys, with `report["checks"]` of length 6
    - The emitted `HSQC.json` cross-peaks contain both `"CH2"` and `"CH_or_CH3"`
      multiplicity hints and zero `"CH_or_CH2_or_CH3"` hints
    - The JSON `skipped` list contains exactly one NOESY entry and the `failed`
      list is empty for the clean six-fixture run
    - `TestJcampQcDiscrimination` contains one test per verdict (PASS, PARTIAL,
      FAIL) and the FAIL test asserts
      `not (root / "analysis" / "nmr_peaks").exists()` and a non-zero exit code
    - `grep -c "monkeypatch" tests/test_cli_jcamp.py` is `0` inside
      `TestJcampEndToEnd` (that class is fully un-mocked) and non-zero inside
      `TestJcampQcDiscrimination`
    - Both class docstrings state their proof level in words (fixture-covered vs
      mock-covered verdict)
    - Full suite `pytest -q` exits 0; `ruff check src tests` exits 0
  </acceptance_criteria>

  <done>
    A single `lucy jcamp` invocation over the six committed JCAMP fixtures is
    asserted end-to-end: all six files read, NOESY skipped non-fatally, the
    unchanged QC gate reached a verdict that is embedded in the peak JSON, the
    edited-HSQC sign survived on real data, all three verdict branches are proven
    reachable with their distinct write behaviour, and a malformed file forces a
    non-zero exit.
  </done>
</task>

<task type="auto">
  <name>Task 2: SHA-256 byte-unchanged guard for case.md and the 5-agent team</name>

  <read_first>
    - tests/test_case_md_wv07.py in full (187 lines) -- ONLY for its repo-relative
      path convention; its testing strategy (substring content-contract) is NOT
      the pattern to copy here
    - .planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-RESEARCH.md Pitfall 5
      (the precomputed baseline table and the rationale for hashing over a
      git-ref diff)
    - .planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-PATTERNS.md section
      "`tests/test_case_byte_unchanged.py` (NEW -- no true analog, build fresh)"
    - CLAUDE.md section "CASE skill location" (the authoritative 5-agent roster)
  </read_first>

  <files>tests/test_skill_files_unchanged.py</files>

  <action>
    Create `tests/test_skill_files_unchanged.py`. There is no prior analog in this
    repo -- no committed pytest test anywhere performs a hash or diff equality
    check (verified by searching the whole `tests/` tree for `sha256`, `hashlib`
    and `git diff`). Build it fresh.

    Resolve paths deterministically from the test file rather than from the
    process cwd: `REPO_ROOT = Path(__file__).resolve().parents[1]`. (The existing
    `test_case_md_wv07.py` uses bare relative paths and therefore silently depends
    on pytest being launched from the repo root -- do not inherit that fragility.)

    Define a module-level `EXPECTED_SHA256: dict[str, str]` mapping the six
    repo-relative paths to the baseline hashes reproduced verbatim in this plan's
    `<interfaces>` block. Define `EXPECTED_AGENT_FILES: tuple[str, ...]` listing
    exactly the five `lucy-*.md` filenames.

    Tests:
      - `test_skill_files_are_byte_unchanged` -- parametrized over
        `EXPECTED_SHA256.items()`; assert each file exists and that
        `hashlib.sha256(path.read_bytes()).hexdigest()` equals the baseline. The
        assertion message must tell a future maintainer exactly what to do: this
        is not a bug to work around; either revert the edit, or update the baseline
        DELIBERATELY as part of a phase whose scope includes changing the CASE
        orchestrator, and record that decision in the phase SUMMARY.
      - `test_agent_roster_is_complete` -- glob `.claude/agents/lucy-*.md` and
        assert the sorted filenames equal `EXPECTED_AGENT_FILES`. This catches a
        NEWLY ADDED agent file, which a pure hash table over known paths cannot.
        Add a comment explaining that `supervisor.md` is deliberately excluded --
        it is not part of the 5-agent team per CLAUDE.md's roster or CONTEXT.md's
        list (102-RESEARCH.md Assumption A2) -- and that the glob pattern
        `lucy-*.md` is what makes that exclusion explicit rather than accidental.
      - `test_case_md_is_a_regular_readable_file` -- assert
        `.claude/commands/lucy-ng/case.md` exists and `read_bytes()` is non-empty,
        so a broken symlink or an empty file fails loudly with a clear message
        rather than as a confusing hash mismatch.

    Module docstring must state: WHY this exists (ROADMAP Phase-102 success
    criterion 4 / JCLI-02: "case.md and the 5-agent team agent files are
    byte-unchanged after this phase, verifiable by diff"); that this is the repo's
    first committed byte-unchanged test; that a hash table was chosen over
    `git diff --exit-code <ref>` because content hashing needs no git subprocess
    and is immune to shallow-clone and history-rewrite concerns; and the exact
    procedure for updating the baseline (`shasum -a 256 <path>`) when a future
    milestone legitimately changes these files.

    Do NOT add hashes for `src/lucy_ng/nus/qc.py`, `nus/bridge.py`,
    `cli/pick.py` or the pickers. Those modules are byte-unchanged for THIS phase
    only and are already verified by the `git diff --exit-code 22f2b52 -- ...`
    acceptance gates in plans 102-01/02/03; freezing them by committed hash would
    make a legitimate future edit fail confusingly. Say so in the docstring.
  </action>

  <verify>
    <automated>pytest tests/test_skill_files_unchanged.py -q &amp;&amp; git diff --exit-code 22f2b52 -- .claude/ src/lucy_ng/nus/ src/lucy_ng/cli/pick.py src/lucy_ng/processing/peak_picker.py src/lucy_ng/processing/peak_picker_2d.py &amp;&amp; pytest -q &amp;&amp; ruff check src tests</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_skill_files_unchanged.py` exists and contains `hashlib.sha256`
      and a dict literal with the six baseline hashes
    - `pytest tests/test_skill_files_unchanged.py -q` exits 0 with at least 8 tests
      collected (6 parametrized hash checks + the roster check + the readable-file check)
    - `grep -c "supervisor" tests/test_skill_files_unchanged.py` is at least 1 (the
      exclusion is documented, not silent)
    - `grep -c "shasum -a 256" tests/test_skill_files_unchanged.py` is at least 1
      (the baseline-update procedure is written down)
    - The test resolves paths via `Path(__file__).resolve().parents[1]`, not via
      bare cwd-relative strings: `pytest tests/test_skill_files_unchanged.py -q`
      passes when run from a different working directory
      (`cd /tmp && pytest <abs-repo>/tests/test_skill_files_unchanged.py -q` exits 0)
    - `git diff --exit-code 22f2b52 -- .claude/` exits 0
    - Full suite `pytest -q` exits 0; `ruff check src tests` exits 0
  </acceptance_criteria>

  <done>
    The CASE orchestrator prompt and the five team-agent files are frozen by a
    committed, cwd-independent SHA-256 test with a roster-completeness check and a
    documented deliberate-update procedure; ROADMAP criterion 4 is now verifiable
    by running the suite rather than by trust.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QC verdict -> consumable `analysis/nmr_peaks/` | The single barrier preventing unvalidated JCAMP-derived peaks from becoming hard LSD constraints in a CASE run |
| repository working tree -> CASE orchestrator prompt | `case.md` and the agent files are the highest-value tamper target in this repo: they are the instructions an autonomous agent team executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-08 | Tampering / Repudiation | D-07 write boundary under a FAIL verdict | mitigate | `test_fail_verdict_quarantines_and_exits_nonzero` asserts `not (root/"analysis"/"nmr_peaks").exists()`, a populated quarantine directory containing `qc_report.json`, and a non-zero exit -- mirroring `tests/nus/test_write_boundary.py` |
| T-102-07 | Tampering / Denial of Service | A malformed `.dx` in directory mode being silently absorbed as "clean" | mitigate | `test_read_failure_forces_nonzero_exit` injects a deliberately broken file with a PASS verdict monkeypatched in, and asserts the run still exits non-zero and names the file in `failed` |
| T-102-10 | Tampering | Undetected modification of `case.md` or any `lucy-*.md` agent file (prompt injection / silent behaviour change in the autonomous CASE team) | mitigate | Committed SHA-256 golden-hash test over all six files, plus a `lucy-*.md` roster-completeness glob so a NEWLY ADDED agent file is also caught, plus a documented deliberate-update procedure so drift can only happen as an explicit, reviewed decision |
| T-102-11 | Tampering | Silent drift in the reused byte-protected production modules (`nus/qc.py`, `nus/bridge.py`, `cli/pick.py`, both pickers) | mitigate | `git diff --exit-code 22f2b52 -- ...` gate in every plan of this phase; deliberately NOT frozen by committed hash so a legitimate future-phase edit does not fail confusingly |
| T-102-14 | Spoofing (of evidence) | A green CI suite on 16-row trimmed fixtures being reported as "verified on real data" | mitigate | Every test class states its proof level in its docstring; the manual-only real-dataset run is recorded in 102-VALIDATION.md's Manual-Only table and explicitly assigned to Phase 103 / JVAL |
| T-102-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed by this plan (102-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `pytest tests/test_cli_jcamp.py tests/test_skill_files_unchanged.py -q` green.
- Full suite `pytest -q` green with no regression against the Phase-101 baseline
  of 1408 passed.
- `mypy src/lucy_ng` and `ruff check src tests` green.
- `git diff --exit-code 22f2b52 -- .claude/ src/lucy_ng/nus/ src/lucy_ng/cli/pick.py src/lucy_ng/processing/peak_picker.py src/lucy_ng/processing/peak_picker_2d.py` exits 0.
- `git status --porcelain tests/fixtures/jcamp/` empty after the suite runs.

**Proof level (honesty gate) -- state this verbatim in the SUMMARY:**
- FIXTURE-COVERED on real committed data (16 F1 rows per 2D file, whole real 1D
  files): file discovery, 1D/2D routing, homonuclear COSY/NOESY reading, the D-06
  NOESY skip, the edited-HSQC sign round-trip, QC-gate execution and verdict
  embedding, 1D reference discoverability, both output formats.
- MOCK-COVERED (real peaks, injected verdict): the PASS and PARTIAL write
  branches, and the malformed-file exit rule under a forced PASS.
- NOT PROVEN HERE: peak-count plausibility, SNR-floor behaviour on a full
  2048x2048 matrix, and any claim that the QC verdict is chemically correct.
  Those require the real, external, uncommitted `C20H32O2-jcamp` dataset and are
  Phase 103 / JVAL's job (D-05). A green suite here must never be described as
  "verified on real data".
</verification>

<success_criteria>
- ROADMAP criterion 1: one `lucy jcamp <dir>` run produces the per-experiment JSON
  for HSQC/HMBC/COSY plus the two 1D lists (or quarantines them), asserted on
  committed fixtures.
- ROADMAP criterion 2: the unchanged QC gate returns a verdict for JCAMP-derived
  peaks, and all three verdicts drive distinct, asserted write behaviour.
- ROADMAP criterion 3: the edited-HSQC sign survives, proven on real data by both
  `CH2` and `CH_or_CH3` hints appearing with zero ambiguous hints.
- ROADMAP criterion 4: `case.md` and the five agent files are frozen by a
  committed test, not by trust.
- Every claim carries its proof level.
</success_criteria>

<output>
Create `.planning/phases/102-cli-peak-pick-bridge-qc-reuse/102-04-SUMMARY.md` when done.
Record: the OBSERVED verdict and the six per-check results for the committed
six-fixture run (with the observed `classification_source`, expected `"override"`
per Pitfall 4); the observed HSQC cross-peak count and multiplicity-hint split;
which claims are fixture-covered vs mock-covered vs deferred to Phase 103; and
any deviation from this plan's literal instructions.
</output>
</content>
