---
context: default
phase: none — v10.1 closed PARTIAL and archived 2026-07-28; awaiting next milestone
task: n/a — documentation reconciliation + one CLI feature + compute-host ops
total_tasks: n/a
status: paused
last_updated: 2026-09-07T08:20:00.000Z
---

# Continue here — end-of-window sprint running, 118 of 258 done, TWO SETTINGS TO REVERT WEDNESDAY

Nothing is half-built, nothing is uncommitted, nothing is unpushed. The benchmark is **running**
an end-of-window sprint and needs no attention until Wednesday — but Wednesday it does, see the
block immediately below.

# ⚠ REVERT THESE ON WEDNESDAY 2026-09-09

Two settings in `scripts/uat_watchdog.py` are deliberate, temporary end-of-window exceptions.
Left in place they let the benchmark eat the user's entire weekly allowance — which is exactly
what the three reductions of 2026-08-27/09-01 existed to prevent.

- [ ] `DEFAULT_SEVEN_DAY_MAX` — **95.0 → 30.0**
- [ ] `STALE_HARD_LIMIT_S` — **30 h → 12 h**
- [ ] Then **restart the watchdog** — editing the file alone changes nothing, the running
      process holds the values it read at startup. See the restart recipe below.

Both carry the same note at their definition sites, so the file tells you too.

## Critical Anti-Patterns

Carried forward from the 2026-08-08 handoff (still true, all discovered through real
failure), plus three found this session.

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| **A broken measurement setup returns a plausible number, not an error** | Three times this session. (1) `ControlPath` over the 104-byte Unix-socket limit → eight ssh calls failed and the timing loop printed `0 ms`, which reads as "blazing fast". (2) An empty PID list reached `ps -p`, which then silently summed **all 969 processes** on the host — reported as "149 % CPU, 24 GB" for 38 webview servers. (3) `pgrep -f "lucy-ng:case"` found nothing on a host where two runs were healthily in progress, because resumed runs carry `--resume … -p Continue the in-progress CASE workflow` instead. | blocking | Every measurement needs a positive control: prove the setup reports the expected value in a case where the answer is known, before trusting it where it is not. A suspiciously clean number (0 ms, exactly 100 %, a round total) is a failure signal, not a result. |
| **The pgrep pattern that matches nothing** | `pgrep -f "[u]atchdog\.py"` matches the literal "uatchdog.py" — but the file is `uat_watchdog.py`, so it matched nothing, `kill` got no argument, and the follow-up count (using the same broken pattern) reported "0 remaining". The launch script then correctly refused to start a second watchdog beside the survivor. An empty result and a successful kill look identical. | blocking | Bracket a letter that leaves the string intact: `[u]at_watchdog`. Verify a kill by PID, never by re-running the same pattern that produced the target list. |
| **The pgrep self-match trap, again** | On 2026-09-02 a check reported *five* watchdog instances where exactly one was running: the command line itself contained the literal `uat_watchdog.py` (inside a `grep -c "python3 -u scripts/uat_watchdog.py"`), so the shell matched its own process list. The bracket idiom in the *other* half of the same command was not enough. The project's own `uat_run_remaining.sh` has the same hazard: it guards with a bare `pgrep -f "uat_watchdog.py"`, so **invoking it from a command line mentioning that filename makes it refuse to start**. | blocking | Never let a process-matching command line contain the literal pattern anywhere — bracket every occurrence, or match on a PID list captured first. Cross-check any surprising count against `ps` on the specific PIDs. |
| **A quota reading can be badly wrong even when fresh** | On 2026-09-07 the snapshot file, freshly written both times, said **17 %** at 08:34 and **56 %** at 08:47. Thirty-nine points in thirteen minutes is not usage. The reported percentage lags and catches up in jumps, so the earlier apparent *drops* (38→4 on 09-05, 26→17 overnight) were lagging readings, not real decreases and not a changed plan. | blocking | Treat a single fresh reading as one noisy sample. The watchdog's extrapolation is often the better estimate — it measured 2.05 pt/h against the 2.0 it assumes. Never conclude a trend from two readings. |
| **Verifying a running process by reading its source file** | After changing `STALE_HARD_LIMIT_S` the check imported the *file* and printed "30 h" — proving nothing about the process, which had started before the edit and still held 12 h. Compounded by a kill that silently did nothing (see below), so the change was not live while being reported as done. | blocking | A constant is live only if the process started *after* the file's mtime. Compare the two explicitly, or restart and confirm from the process's own log line. |
| **Tests that spawn real processes** | The first revision of the opt-out tests called `serve` without patching `server.start`, leaving **eight live uvicorn servers** on the dev machine — while the feature under test existed to stop exactly that leak. | blocking | Patch the side-effecting entry point in a fixture shared by every test in the class, so no test can start a real server regardless of implementation state. Runtime is the tell: 4.35 s → 0.40 s once nothing spawned. |
| **Reading a stale planning doc as current fact** | `STATE.md` was reported to the user verbatim, including "CR-02/CR-03 filed, not fixed". Both had been fixed on 2026-08-03 in `7dfe2ce`. The file's own header date was wrong too. | blocking | Before citing a planning document as the current state, check it against git: `git log --since=<doc date>` for the paths it describes. The doc is a snapshot with a date, not a source of truth. |
| **Framing a settled decision as an open defect** | The benchmark's mixed provenance (two skill states, two team models) was written up as a to-do proposing re-runs. It was in fact a deliberate, discussed decision on cost grounds. | advisory | Before filing something as a defect, ask whether it was chosen. A trade-off someone accepted belongs in the decision record with its rationale, not in a to-do list that reopens it. |
| **Symptom-to-cause invention** (from 2026-08-08) | `Execution error` in a run log → declared the CLI broken, killed a healthy run, committed a false claim. It is what `claude -p` prints when `timeout` kills it. | blocking | Locate and read the code that emits the symptom before asserting a cause. Name the file and line. Without that you have a guess, not a cause. |
| **Grading against an unverified reference** (from 2026-08-08) | `NUS-RECONSTRUCTION-GUIDE.md` §8/§10 was cited as "ground truth" across three phases and two source modules. It is a prior agent's hypothesis about an unsolved sample, written into a failed run's own output directory. | blocking | Verify a reference's provenance where it is first cited — who produced it, from what, when. An artefact sitting in a run's own output directory is a suspect, not a source. |
| **Diagnosing long runs by elapsed time or log lines** (from 2026-08-08) | A CASE run takes 1.5–2.5 h and hits the 3600 s call timeout once per attempt by design. | advisory | Judge progress by artefacts appearing in `analysis/` (iteration dirs, `timing.jsonl`), never by log text or elapsed time. |

<current_state>
No GSD phase is active. Repo clean and pushed — `origin/master == master == 908365f` — and the
compute host sits on the same commit.

**The campaign is running an end-of-window sprint.** The weekly window resets 2026-09-08 03:00Z
and unused quota expires, so the ceiling was raised twice today (30 → 80 → 95 %) and the
stale-snapshot limit from 12 h to 30 h. Both revert Wednesday — see the block at the top.

| | |
|---|---|
| Datasets finished | **118 of 258** |
| Open in this campaign | **21** of 154 (133 attempted, 14 exhausted after ≥600 s) |
| Graded in `scorecard.tsv` | **65**, generated 2026-08-24 — 53 runs behind |
| Weekly window | 59 %, ceiling 95 %, resets 2026-09-08T03:00Z |
| Watchdog | alive, restarted 2026-09-07 09:26 |
| Leaked dashboards | 0 — the opt-out works in production |

**Why nothing ran 2026-09-02 → 09-06, and it was not the quota:** Claude Code on the compute
host was **logged out**. The watchdog reported it plainly once restarted
(`HALT: … 'Not logged in · Please run /login'`), but for four days no watchdog was running to
report anything. The user re-authenticated on 09-06 at 11:31 and the run resumed by itself ten
minutes later. Check authentication before blaming the ceiling.
</current_state>

<completed_work>

- Weekly ceiling raised for the end-of-window sprint, 30 → 80 → 95 % (`8d67688`, `908365f`) and
  the stale-snapshot limit 12 h → 30 h (`008822d`) — done, **both revert Wednesday**
- Diagnosed the four-day standstill: the compute host was logged out, not quota-blocked — done
- **C20H32O2 constitution independently reconfirmed** — done, but written up only in conversation
- Weekly quota ceiling lowered 60 → 50 → 40 → 30 % (`d7d397e`, `00c49c9`, `8a1d84e`) — done
- Compute host's checkout brought up to date; it had sat at 2026-08-13 for three weeks, so the
  webview opt-out shipped on 08-27 had **never taken effect there** and servers kept leaking — done
- Planning reconciliation: `STATE.md` + `PROJECT.md` corrected and extended — done
- Benchmark mixed-provenance recorded as a decision under `## Key Decisions (2026-08)` — done
- Auto-memory updated (`MEMORY.md`, benchmark ops, C20H32O2 reference) — done, outside repo
- 39 orphaned webview servers killed on the compute host — done
- `LUCY_NO_WEBVIEW` opt-out + 14 tests + harness export — done, suite green
</completed_work>

<remaining_work>

- **⚠ Wednesday 2026-09-09: revert both sprint settings** (see the block at the top).
- **After the Tuesday reset the watchdog will NOT resume on its own.** It deliberately waits for
  a real snapshot reading rather than trusting the extrapolation, so it needs one keystroke in an
  interactive Claude Code session before it starts a chunk.
- **Write up the C20H32O2 re-analysis** in `.planning/analysis/` — it exists only in conversation.
- **Regrade the campaign** — `scorecard.tsv` covers 65 runs and dates from 2026-08-24, while 118
  have finished. Needs rdkit, which is not in the compute host's system python; use the venv at
  `/mnt/raid_drive/chris/lucy-ng`.
- **Decide whether the campaign gets its own quota, or gets cut down** — see Blockers.
- PROV-01 behaviour decision on `DEFAULT_QUATERNARY_SHIFTS` — open since 2026-07-31
- Review spec `49057ef` — waiting since 2026-08-10
- Decide what to do with the modified `uv.lock`
</remaining_work>

<decisions_made>

- **Opt-out lives in `lucy webview serve`, not in `case.md`.** `case.md` is byte-frozen with a
  committed SHA-256 guard, and its launch block explicitly forbids extra arguments — so an
  environment variable is the only switch that reaches through the frozen call. A CLI flag
  would have been useless here. `case.md` verified byte-unchanged via git.
- **Only `1`/`true`/`yes`/`on` disable.** `0`/`false`/`no`/empty keep the dashboard, so setting
  the variable to a negative value can never silently cost someone their dashboard.
- **The opt-out is checked before `_require_webview()`** — doing nothing needs no dependency,
  so a disabled run also succeeds where the `[webview]` extra is absent.
- **Interactive runs keep their post-run dashboard.** `case.md` leaves it up on purpose
  (WV-07, line 945); only headless benchmark runs were leaking.
- **The weekly ceiling is 30 % and the benchmark is a guest with a small allowance.** Three
  steps in six days; the trigger for the last one was 21 % of the week gone twelve hours after
  the reset. It will spend much of each week blocked, and that is the intended trade. ⚠ **The
  lever for more progress is not this number** — the compute host runs on the user's personal
  quota, so the ceiling only redistributes a shortage. The real options are a separate quota,
  or a smaller justified subsample.
- **Keep the compute host's checkout in sync.** Verified before pulling that `.claude/` was
  unchanged across the missing commits, so the running campaign did **not** gain a third skill
  state. Check this again before any future pull mid-campaign.
- **The benchmark's mixed provenance is an accepted decision, not a defect.** Runs span two
  skill states *and* two team models (4.8 → Opus 5 via `f9aa7b3`). Keeping them was chosen on
  cost grounds. The obligation is to state the mixed provenance beside every number — never a
  single unqualified percentage.
</decisions_made>

<blockers>

- **Weekly window at 38 %, ceiling 30 %** → nothing launches until it falls back under 30 % or
  the window resets on **2026-09-08T03:00Z**. Working as designed, not a fault.
- **The campaign is quota-bound, and that is the real constraint.** 147 datasets remain; at the
  observed ~20/week that is roughly seven weeks, while the user's own consumption keeps growing.
  Lowering the ceiling further does not help — it only moves the shortage around.
- ~~Skill-file byte-freeze guard red since 2026-08-09~~ — **RESOLVED 2026-08-27** in
  `8554948`. `d62d833` had changed two agent files without refreshing the frozen hashes.
  Resolution was (b) update, not (a) revert: the edit is deliberate and correct, and
  `case.md` plus the other three agents still matched byte for byte, so the drift was fully
  explained by that one commit. Re-verified by mutation — one extra byte makes the guard
  fail again. Worth remembering: the commit that split the benchmark into two skill states
  was exactly the one where this control failed to fire, and the red test sat unnoticed for
  18 days.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` § *Post-Milestone Validation Work* — the only place the August work is
   catalogued. Everything above that section was written at the v10.1 close and is older.
2. `.planning/todos/pending/2026-07-31-prov-01-*.md` — PROV-01 is analytically finished
   (2026-08-02) but has an open behaviour decision. It also changes how Phase 103's PARTIAL
   must be read: 37.86 ppm is a **CH**, so `quaternary_exclusion` was correctly reporting a
   wrong input assumption, and **JVAL-F2 is mis-scoped** as filed.
3. `tests/test_skill_files_unchanged.py` — read before touching anything under `.claude/`.

## How to restart the watchdog (this cost time twice)

`./scripts/uat_run_remaining.sh` — but **from a command line that does not contain the string
`uat_watchdog.py`**, or its own bare `pgrep -f` guard matches your shell and refuses to start.

Two things that do *not* work:

- Editing `scripts/uat_watchdog.py` alone. The running process holds the value it read at
  startup; a ceiling change needs a restart to take effect. Confirm it in the log — a fresh
  start prints `thresholds: 7d < NN %`.
- `launchctl kickstart -k`. The LaunchAgent runs the *launch script*, which detaches the
  watchdog (PPID 1), so kickstart never reaches the running process — it just logs
  "A watchdog is already running. Not starting a second."

The healthy state is a **pair**: the python process plus its `caffeinate -i -m` parent. On
2026-09-02 only the caffeinate half had been killed; the watchdog was still polling, so it
looked fine, but the Mac could then idle-sleep and stall it — which is exactly what cost
11.5 hours overnight on 2026-08-31.

## Infrastructure State

- **Compute host (`ssh -p 2222 chris@35.198.180.5`, alias `sshsheldon`)**: idle, quota-blocked.
  **111** runs carry `final_results.md`; the graded scorecard still covers **65** (2026-08-24),
  so it trails by 46. Checkout now at `8a1d84e`, same as origin — it had been three weeks stale.
  Webview orphans: 0, and the opt-out is now verified working there
  (`LUCY_NO_WEBVIEW=1` → "Webview dashboard not started"; without it a server really starts).
  ⚠ The ssh link drops occasionally — the watchdog treats that as HALT and retries, by design.
- **Quota watchdog**: running on the Mac (PID pair since 2026-09-02 13:41; LaunchAgent loaded).
  Ceilings are **two**, not one: 7-day **30 %**, 5-hour **70 %**; a running chunk is aborted
  5 pt above either, i.e. at 35 % weekly. Snapshot freshness depends on an interactive session
  rendering its status line — so the watchdog only learns about a quota reset once someone
  types. That, plus an overnight network outage, is why the 2026-09-01 reset at 05:00 only
  produced a launch at **09:13**, seven minutes after the Mac woke.
- **Grading**: `grade_blind.py` needs rdkit, which is **not** in the compute host's system
  python — use the venv at `/mnt/raid_drive/chris/lucy-ng`.
- **Known cosmetic defect**: `meta.json` carries no timestamp and its `top_smiles` is
  sometimes garbage (`"3645"`); `summary.tsv` truncates the column. Grading is unaffected —
  `grade_blind.py` re-extracts from `final_results.md`. (Re-discovered this session; it was
  already recorded in the 2026-08-08 handoff.)

<context>
The through-line of this session was that several confident conclusions rested on measurement
setups that were themselves broken — and broken in the worst way, returning a plausible number
instead of an error. Each was caught only by asking "is the thing that produced this number
itself verified?". Apply that before trusting any figure you did not both generate and check.

The user checks work and runs parallel sessions, so a wrong intermediate result propagates
into PDFs and other sessions quickly. Name errors explicitly and retract them out loud rather
than quietly correcting them.
</context>

## What the C20H32O2 re-analysis established (2026-09-05/06, not yet written up)

Reached from integrated ¹³C areas and edited-HSQC signs — an independent route to the same
answer the 2026-08-02 analysis got from weaker evidence:

- **The minor component is ~16.5 % of the ¹³C signal area, not 40 %.** The 40 % came from a ¹H
  integral whose normalisation was never checked.
- **The carbon list is not ambiguous after all.** Integrated areas separate the components:
  15 signals at ~1.0, one at **2.08** (22.63 ppm = two coincident carbons, confirming the user's
  own hypothesis and the earlier 2.02), 51.63 at 0.65, everything else ≤0.4. 15 + 2 + 1 plus the
  two olefinics from `exp7` = **exactly 20**.
- **51.63 is quaternary**, confirmed: its HSQC response is 45σ where every other carbon runs
  166σ–55 611σ, and it is isolated so nothing bleeds into it.
- **No weak carbon is a hidden quaternary** — every sub-0.4 signal shows a strong HSQC
  correlation, so they are protonated carbons of the minor component.
- **The CH₃/CH split needs no integral argument.** Six positive-sign carbons must carry
  32 − 22 = 10 H, and x·3 + (6−x)·1 = 10 forces x = 2. The formula decides it.
- Result: **11 CH₂ + 4 CH + 2 CH₃ + 3 Cq**.

Open: **35.23 ppm** has a full-carbon area (0.84) but an HSQC response of only 166σ, ~25× weaker
than comparable neighbours. Either a weak CH₂ correlation or a fourth quaternary; the H balance
argues for CH₂. And 13 of 39 signals sit closer than the HSQC's F1 resolution (0.088 ppm/point),
so those pairs cannot be separated at all.

⚠ Also measured: `zgpg30` with D1 = 2 s means NOE favours protonated carbons and quaternaries
are systematically weak. That is why "weak signal" and "quaternary" are hard to tell apart here.

Context worth keeping: all 5817 PubChem entries with formula C20H32O2 were checked. 66 match the
skeleton profile (4 rings, 1 C=C, no OH, no C=O), 34 as distinct constitutions — but **none**
matches the full carbon-type profile. That is not proof of novelty; a one-carbon error in the
assignment would change the hit list.

<next_action>
**Wednesday 2026-09-09: revert the two sprint settings and restart the watchdog.** That is the
one dated obligation; everything else can wait.

Before then: after the Tuesday-morning reset the watchdog needs one keystroke in an interactive
session before it will start a chunk.

Then, in order: whether the campaign gets its own quota or a smaller subsample (still the
binding constraint on everything else), writing up the C20H32O2 re-analysis, the PROV-01
behaviour decision, and `/gsd-new-milestone`. Regrade before quoting any benchmark number — the
scorecard is 53 runs behind.
</next_action>
