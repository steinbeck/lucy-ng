---
phase: 16-database-schema
plan: 01
type: execute
---

<objective>
Add hose_stats table to compounds.db for HOSE-based shift prediction with precomputed statistics.

Purpose: Enable fast O(1) lookups of shift statistics (mean, std, count) by HOSE code and radius, replacing in-memory lookup table with database-backed prediction.
Output: Extended database schema, Pydantic models, and manager methods for HOSE statistics.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-database-schema/16-CONTEXT.md

# Prior phase establishing patterns:
@.planning/phases/14-cli-integration/14-01-SUMMARY.md

# Existing database module (extend these):
@src/lucy_ng/database/schema.py
@src/lucy_ng/database/models.py
@src/lucy_ng/database/manager.py

# Current HOSE prediction (will consume this schema in Phase 18):
@src/lucy_ng/prediction/lookup.py
@src/lucy_ng/prediction/predictor.py

**Tech stack available:** SQLite, Pydantic v2
**Established patterns:**
- SCHEMA_VERSION tracking in schema_meta table
- Pydantic models with database conversion methods
- DatabaseManager with context manager protocol
- Batch insert methods for performance

**Constraining decisions:**
- Phase 16 CONTEXT: Precomputed statistics approach (mean, std, count per HOSE code at each radius)
- Phase 16 CONTEXT: Query speed is top priority, use proper indexing
- Phase 11: Schema versioning via schema_meta table
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hose_stats schema and bump version</name>
  <files>src/lucy_ng/database/schema.py</files>
  <action>
Add CREATE_HOSE_STATS_TABLE SQL statement:
- hose_code TEXT NOT NULL - the HOSE code string
- radius INTEGER NOT NULL - sphere radius (1-6)
- mean REAL NOT NULL - mean shift in ppm
- std REAL NOT NULL - standard deviation
- count INTEGER NOT NULL - number of observations
- PRIMARY KEY (hose_code, radius) - composite key for uniqueness

Add CREATE_HOSE_STATS_INDEX for fast lookup on hose_code (the primary query pattern).

Bump SCHEMA_VERSION from 1 to 2.

Add new statements to SCHEMA_STATEMENTS list.

Do NOT create a separate table for individual HOSE code observations - we only store aggregated statistics as per 16-CONTEXT.md (precomputed stats approach).
  </action>
  <verify>python -c "from lucy_ng.database.schema import SCHEMA_VERSION, CREATE_HOSE_STATS_TABLE; print(SCHEMA_VERSION); print(CREATE_HOSE_STATS_TABLE)"</verify>
  <done>SCHEMA_VERSION is 2, CREATE_HOSE_STATS_TABLE contains correct columns and primary key</done>
</task>

<task type="auto">
  <name>Task 2: Add HOSEStatsRecord model</name>
  <files>src/lucy_ng/database/models.py</files>
  <action>
Add HOSEStatsRecord Pydantic model with fields:
- hose_code: str
- radius: int (1-6)
- mean: float - predicted shift in ppm
- std: float - standard deviation
- count: int - observation count for confidence scoring

Add to __init__.py exports.

This model will be used by Phase 18 (Prediction API) to return query results with all the statistics needed for confidence scoring.
  </action>
  <verify>python -c "from lucy_ng.database.models import HOSEStatsRecord; r = HOSEStatsRecord(hose_code='C(CC)', radius=3, mean=25.5, std=2.1, count=150); print(r)"</verify>
  <done>HOSEStatsRecord model created and importable with all required fields</done>
</task>

<task type="auto">
  <name>Task 3: Add HOSE stats methods to DatabaseManager</name>
  <files>src/lucy_ng/database/manager.py, tests/test_database.py</files>
  <action>
Add to DatabaseManager:

1. insert_hose_stats_batch(stats: list[HOSEStatsRecord], batch_size: int = 10000) -> int
   - Batch insert for Phase 17's bulk generation
   - Use INSERT OR REPLACE to handle reruns
   - Return count of inserted rows

2. get_hose_stats(hose_code: str, radius: int) -> HOSEStatsRecord | None
   - Single lookup for exact (hose_code, radius) match
   - This is the primary query for prediction

3. get_hose_stats_all_radii(hose_code: str) -> list[HOSEStatsRecord]
   - Get stats at all available radii for a HOSE code
   - Useful for fallback queries (try radius 6, then 5, etc.)

4. get_hose_stats_count() -> int
   - Return total number of HOSE stats entries

Add tests for all new methods using in-memory SQLite.
  </action>
  <verify>pytest tests/test_database.py -v -k hose</verify>
  <done>All HOSE stats methods implemented, tests pass</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest tests/test_database.py -v` passes
- [ ] `python -c "from lucy_ng.database import DatabaseManager, HOSEStatsRecord"` succeeds
- [ ] Schema version is 2
- [ ] HOSE stats table has correct columns and index
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript/mypy errors
- Database schema extended with hose_stats table
- HOSEStatsRecord model available for Phase 17 and 18
- Manager methods ready for bulk insert (Phase 17) and query (Phase 18)
</success_criteria>

<output>
After completion, create `.planning/phases/16-database-schema/16-01-SUMMARY.md`:

# Phase 16-01 Summary: Database Schema

**[Substantive one-liner about what was built]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [Files with descriptions]

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

- Phase 16 complete
- Ready for Phase 17 (HOSE Generation) to populate the hose_stats table
</output>
