---
phase: 95-1d-real-spectra-peak-overlay
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_webview_api.py
autonomous: true
requirements: [SP1-01, SP-02]

must_haves:
  truths:
    - "matplotlib>=3.7 is declared in the [project.optional-dependencies].webview extra ONLY, never in base dependencies or dev"
    - "from lucy_ng.cli import cli succeeds (base CLI import path pulls in no matplotlib) — SC3 guard"
    - "TestSpectraEndpoint collects cleanly and SKIPS (not ERROR) before spectra.py exists (WV-08 import-safety)"
    - "The reversed-axis contract is expressed as an assertion ax.get_xlim()[0] > ax.get_xlim()[1] on a descending ppm_scale (SC2)"
    - "The 2D(acqu2s)/DEPT(pulse_program) experiment-exclusion contract is expressed as a unit test target"
  artifacts:
    - path: "pyproject.toml"
      provides: "matplotlib in the webview extra"
      contains: "matplotlib>=3.7"
    - path: "tests/test_webview_api.py"
      provides: "TestSpectraEndpoint class + spectra fixtures"
      contains: "class TestSpectraEndpoint"
  key_links:
    - from: "tests/test_webview_api.py"
      to: "lucy_ng.webview.routers.spectra"
      via: "try/except ImportError: pytest.skip"
      pattern: "except ImportError"
---

<objective>
Ship the Wave-0 acceptance target for Phase 95 BEFORE any router code: add
`matplotlib>=3.7` to the `[webview]` optional-dependency extra (D-04), and author a new
`TestSpectraEndpoint` test class in `tests/test_webview_api.py` covering SP1-01 (real
¹³C/¹H trace, reversed axis, peak overlay, 2D/DEPT experiment exclusion) and SP-02
(never-500 "unavailable" on absent manifest / stale raw path / missing peaks). Tests are
RED-by-skip via the WV-08 `try/except ImportError: pytest.skip(...)` guard until Plan 02's
`spectra.py` lands.

Purpose: Fix the executable contract (endpoint shapes, helper signatures, the two
RESEARCH-verified discriminators: `set_xlim`-only reversal and `acqu2s`/`dept` exclusion)
so Plan 02 is written against a frozen target with zero field/behaviour drift.
Output: `pyproject.toml` matplotlib line + `tests/test_webview_api.py::TestSpectraEndpoint`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-CONTEXT.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-RESEARCH.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-PATTERNS.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-VALIDATION.md

<interfaces>
<!-- The router contract Plan 02 will implement, and this scaffold asserts against. -->
<!-- Endpoints (2, GET-only, prefix /api), BOTH always return image/png HTTP 200: -->
<!--   /api/spectra/1d/carbon  -> PNG bytes (real ¹³C trace + overlay, or placeholder) -->
<!--   /api/spectra/1d/proton  -> PNG bytes (real ¹H trace + overlay, or placeholder) -->
<!-- Factory: spectra.make_router(analysis_dir: Path) -> APIRouter(prefix="/api"); matplotlib imported LAZILY inside make_router only. -->
<!-- Module-level helpers Plan 02 will expose (unit-testable, no matplotlib import needed for the pure ones): -->
<!--   _read_manifest(analysis_dir: Path) -> dict | None      (None on any failure) -->
<!--   _select_experiment(bruker_data_dir: Path, nucleus: str) -> Spectrum1D | None -->
<!--   _apply_nmr_axes(ax, ppm_scale) -> None                  (set_xlim ONLY, NO invert_xaxis) -->
<!-- .run_manifest.json contract: {"bruker_data_dir": "<abs>", "formula": "<formula>"} at analysis/.run_manifest.json -->
<!-- carbon_signals.json overlay rows (Phase 94 LOCKED schema): signals[]:{atom,ppm,mult,nC,assignment,confidence} -->
<!-- Real CASE1 dataset root (7 numbered exp dirs): ~/Dropbox/.../active-lucy-ng-testprojects/CASE1 -->
<!--   exp 2 = zgpg30 13C (WANT) · exp 3/4 = dept135/dept90 (EXCLUDE) · exp 5/6/7 = 2D, acqu2s present (EXCLUDE) · exp 1 = zg30 1H -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matplotlib to the [webview] optional-dependency extra</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — the existing `[project.optional-dependencies]` `webview = ["fastapi>=0.100", "uvicorn>=0.20"]` block (95-PATTERNS.md pins it at ~lines 61-64) and the `[tool.hatch.build.targets.wheel]` artifacts list (DO NOT touch — no new static globs needed).
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-RESEARCH.md "Standard Stack" + "Package Legitimacy Audit" — matplotlib>=3.7, slopcheck [OK], PyPI-approved.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-CONTEXT.md D-04 — webview extra ONLY, never base/dev.
  </read_first>
  <action>
    In `pyproject.toml`, add the string `"matplotlib>=3.7"` as a third entry to the
    `[project.optional-dependencies].webview` list (alongside `fastapi>=0.100` and
    `uvicorn>=0.20`). Do NOT add it to base `[project].dependencies`, to `dev`, or anywhere
    else (D-04). Do NOT modify the hatch wheel `artifacts` glob list — matplotlib ships its
    own package data. `nmrglue`/`numpy` are already base deps; do not re-declare them.
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); wv=d['project']['optional-dependencies']['webview']; base=d['project'].get('dependencies',[]); assert any(s.startswith('matplotlib') for s in wv), wv; assert not any(s.startswith('matplotlib') for s in base), base; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `[project.optional-dependencies].webview` contains an entry beginning `matplotlib` with the specifier `>=3.7`.
    - Base `[project].dependencies` contains NO matplotlib entry (D-04).
    - `python -c "import tomllib; ..."` guard above exits 0.
    - `pyproject.toml` remains valid TOML (parses).
  </acceptance_criteria>
  <done>matplotlib>=3.7 present in the [webview] extra only; base deps unchanged; TOML valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add TestSpectraEndpoint + spectra fixtures (RED-by-skip until spectra.py exists)</name>
  <files>tests/test_webview_api.py</files>
  <read_first>
    - tests/test_webview_api.py — `TestTablesEndpoint` (Phase 94) for the WV-08 `try/except ImportError: pytest.skip(...)` guard block copied verbatim into every method, the `TestClient` build shape, and the `TestDepiction` class (~lines 337-401) for the direct-helper-import unit-test style (assert on a function's return, not only through HTTP).
    - tests/conftest.py — `empty_analysis_dir` (line 264) reused for the absent-manifest test.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-PATTERNS.md "tests/test_webview_api.py::TestSpectraEndpoint" — the PNG-endpoint assertion set (`content-type == image/png`, `len(r.content) > 0`), the reversed-axis unit-test approach (assert on the Axes, not on PNG bytes), and the two spectra fixture variants (real-CASE1 skipif + synthetic Bruker tree).
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" (8 rows) + "Pattern 2" (verified acqu2s/dept discriminators, CASE1 exp layout) + Code Examples for `_read_manifest`/`_select_experiment`/`_apply_nmr_axes`.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-VALIDATION.md "Wave 0 Requirements" + "Manual-Only Verifications".
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md "Copywriting Contract" — the 6 empty-state copy strings, so placeholder-content assertions (if any) use the locked wording.
  </read_first>
  <action>
    Add three module-level pytest fixtures plus `class TestSpectraEndpoint` to
    `tests/test_webview_api.py`. Every test method opens with the WV-08 guard:
    `try: from fastapi.testclient import TestClient; from lucy_ng.webview.routers import spectra`
    `except ImportError: pytest.skip("webview extra or spectra router not yet available")` — and,
    for methods that call `spectra.make_router(...)` (which lazily imports matplotlib), the guard
    must also wrap that call so an absent-matplotlib env SKIPS rather than errors.

    Fixtures (module-level in this file, not conftest.py — file-ownership isolation):
    (1) `spectra_stale_manifest_dir(tmp_path)` — writes `tmp_path/.run_manifest.json` with
        `{"bruker_data_dir": "/nonexistent/path/does/not/exist", "formula": "C13H18O2"}` (a well-formed
        manifest pointing at a missing dir — the D-05 stale-path case).
    (2) `spectra_case1_manifest_dir(tmp_path)` — writes `tmp_path/.run_manifest.json` pointing
        `bruker_data_dir` at the REAL CASE1 root (`Path.home()/"Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1"`)
        and writes a `tmp_path/peaks/carbon_signals.json` with a couple of hand-authored signals (LOCKED
        Phase-94 schema: `signals[]` each with `atom,ppm,mult,nC,assignment,confidence`), including one
        near 181 ppm labelled `C=O`. Guard with `pytest.mark.skipif`/an in-body `pytest.skip` when the
        CASE1 root is absent (do NOT hard-fail CI on a missing local dataset).
    (3) `synthetic_bruker_dir(tmp_path)` — builds numbered experiment subdirs with fake `acqus`
        (text) files and, for the 2D ones, a fake `acqu2s` file present — NO real FID/pdata needed,
        since the `acqu2s`-exclusion branch runs BEFORE any `read_1d` call.

    `class TestSpectraEndpoint` methods (each an independent RED-by-skip target — 8 map rows plus the
    SC3 import guard and the 2D/DEPT exclusion unit test):
    1. `test_apply_nmr_axes_reverses_descending_scale` — import `_apply_nmr_axes` from spectra; build a
       `Figure`+subplot; call `_apply_nmr_axes(ax, numpy_descending_ppm_scale)` (e.g. `np.array([231.0, ..., -12.0])`);
       assert `ax.get_xlim()[0] > ax.get_xlim()[1]` (SC2 axis direction — proves reversal WITHOUT
       `invert_xaxis`). No real data needed.
    2. `test_carbon_returns_png_on_case1` — build `TestClient(app)` with `spectra.make_router(spectra_case1_manifest_dir)`;
       `GET /api/spectra/1d/carbon` → 200, `content-type == "image/png"`, `len(content) > 0` (SP1-01 happy path; skipif CASE1 absent).
    3. `test_case1_carbonyl_left_of_aliphatic` — using the real CASE1 dataset via `_select_experiment(CASE1_root, "13C")`
       + `_apply_nmr_axes`, assert `ax.get_xlim()[0] > ax.get_xlim()[1]` AND the carbonyl (~181 ppm) sits at a
       higher ppm (left) than the aliphatic CH₃ region (~14–23 ppm) i.e. `xlim[0] > 181 > 14 > xlim[1]` on the
       reversed axis (SC2; skipif CASE1 absent).
    4. `test_select_experiment_excludes_2d_and_dept` — against the real CASE1 root, assert `_select_experiment(CASE1_root, "13C")`
       returns a `Spectrum1D` whose `metadata["pulse_program"]` does NOT contain `"dept"` and is the standard
       decoupled experiment (`zgpg30`); and that a directory containing `acqu2s` is never selected. (Skipif CASE1 absent;
       the `synthetic_bruker_dir` fixture additionally exercises the pure acqu2s-skip path with no external dependency.)
    5. `test_proton_present_and_carbon_independent` — `GET /api/spectra/1d/proton` and `/carbon` are each 200 image/png
       and independently expressible (a per-nucleus "unavailable" on one does not affect the other). Skipif CASE1 absent
       for the "present" assertion; the independence/never-500 assertion runs unconditionally.
    6. `test_peak_overlay_positions` — assert the overlay is driven by the `carbon_signals.json` ppm list: either
       assert the render helper is invoked with the expected ppm values (e.g. monkeypatch/spy on the overlay-drawing
       helper) OR assert a non-zero PNG-byte delta between an overlay render and a no-peaks render for the same trace
       (unit-level, NOT pixel-diff).
    7. `test_missing_manifest_returns_placeholder` — `spectra.make_router(empty_analysis_dir)`; `GET /api/spectra/1d/carbon`
       → 200, `content-type == "image/png"`, `len(content) > 0` (D-01; never 500, never a JSON body).
    8. `test_stale_bruker_path_returns_placeholder` — `spectra.make_router(spectra_stale_manifest_dir)`; `GET /api/spectra/1d/carbon`
       → 200 image/png placeholder (D-05; never 500).
    9. `test_missing_peaks_json_renders_bare_trace` — manifest+raw present but NO `peaks/carbon_signals.json`;
       `GET /api/spectra/1d/carbon` → 200 image/png (trace still renders, overlay omitted; SP-02 partial degradation).
       Skipif CASE1 absent.
    10. `test_no_module_level_matplotlib_import` — read the SOURCE of `src/lucy_ng/webview/routers/spectra.py`; assert
        no line (after `.lstrip()`, excluding comment lines beginning `#`) that starts with `import matplotlib` or
        `from matplotlib` appears BEFORE the `def make_router` line — i.e. every matplotlib import lives inside the
        factory (WV-08/D-04). This test does NOT skip on ImportError; it skips only if the source file does not yet exist.
    11. `test_cli_imports_without_matplotlib` — `from lucy_ng.cli import cli` succeeds (SC3: base CLI import path
        pulls in no matplotlib eagerly). Runs GREEN immediately (not RED-by-skip) — it is a standing guard.

    Assertions use exact comparisons and include the response body/text in failure messages (match the existing
    `TestTablesEndpoint` style). Do NOT assert on JSON `state` for the PNG routes — these routes are binary-only.
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestSpectraEndpoint -q</automated>
    <automated>ruff check tests/test_webview_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_webview_api.py::TestSpectraEndpoint -q` collects without ImportError; the spectra-router-dependent methods report SKIPPED (not FAILED/ERROR) while `spectra.py` is absent.
    - `test_cli_imports_without_matplotlib` PASSES immediately (it does not skip) — `from lucy_ng.cli import cli` raises no ImportError.
    - The class contains a method asserting `ax.get_xlim()[0] > ax.get_xlim()[1]` on a descending ppm_scale (SC2 reversed-axis).
    - The class contains a method asserting `_select_experiment(..., "13C")` excludes DEPT (`"dept"` not in `metadata["pulse_program"]`) and never selects an `acqu2s`-bearing (2D) directory.
    - SP-02 never-500 is independently asserted for: absent manifest, stale bruker path, and missing peaks JSON — each expecting HTTP 200 `content-type == image/png`, never a JSON body, never 500.
    - `test_no_module_level_matplotlib_import` reads the spectra.py source and asserts matplotlib is imported only inside `make_router` (excluding comment lines from the check).
    - CASE1-real-data methods use `skipif`/in-body `pytest.skip` when the CASE1 root is absent (no hard CI failure).
    - `ruff check tests/test_webview_api.py` passes.
    - Existing suite unbroken: `pytest tests/test_webview_api.py -q` shows no new failures/errors.
  </acceptance_criteria>
  <done>TestSpectraEndpoint present with the 8 map rows + SC3 guard + 2D/DEPT-exclusion unit test; all spectra-dependent methods SKIP cleanly pre-spectra.py; SC3 cli-import test GREEN; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture files → router-under-test | Fixture content authored by this plan; the CASE1 path is a trusted local dataset. Test-only, no HTTP-supplied input. |
| pyproject dependency declaration → install surface | Adds one dependency (matplotlib) to the optional [webview] extra. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | Tampering | Fixture/contract drift vs LOCKED schema + RESEARCH-verified discriminators | mitigate | Fixtures hand-authored to the Phase-94 LOCKED carbon_signals schema + `.run_manifest.json` contract; acceptance criteria pin the reversed-axis assertion and the acqu2s/dept exclusion behaviour verbatim. |
| T-95-01-SC | Tampering | pip install (matplotlib) via the [webview] extra | accept | matplotlib audited in 95-RESEARCH.md Package Legitimacy Audit — `slopcheck [OK]`, PyPI, ~20-year industry-standard library, disposition Approved. [OK] packages need no blocking human checkpoint (checkpoints are only for [ASSUMED]/[SUS]). |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestSpectraEndpoint -q` → spectra-dependent methods SKIP, `test_cli_imports_without_matplotlib` PASSES.
- `python -c "import tomllib; ..."` matplotlib-in-webview-only guard exits 0.
- `pytest tests/test_webview_api.py -q` → no new failures.
</verification>

<success_criteria>
- `matplotlib>=3.7` in the `[webview]` extra only (base deps untouched).
- `TestSpectraEndpoint` covers SP1-01 (reversed axis, overlay, 2D/DEPT exclusion, per-nucleus render) and SP-02 (never-500 on absent manifest / stale path / missing peaks), all RED-by-skip until Plan 02.
- SC3 base-install import guard (`from lucy_ng.cli import cli`) green from day one.
</success_criteria>

<output>
Create `.planning/phases/95-1d-real-spectra-peak-overlay/95-01-SUMMARY.md` when done.
</output>
