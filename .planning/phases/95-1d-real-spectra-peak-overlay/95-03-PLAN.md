---
phase: 95-1d-real-spectra-peak-overlay
plan: 03
type: execute
wave: 2
depends_on: [95-02]
files_modified:
  - src/lucy_ng/webview/static/index.html
  - src/lucy_ng/webview/static/webview.js
  - .claude/commands/lucy-ng/case.md
autonomous: true
requirements: [SP1-01, SP-02]

must_haves:
  truths:
    - "The 1D Spectra tab shows a ¹³C section and a ¹H section, each an <img> fed by its PNG endpoint"
    - "refreshSpectra1D() cache-busts both <img> sources on every ~3s tick (no SMILES-diff dedupe, per D-06)"
    - "case.md writes analysis/.run_manifest.json at run-start with bruker_data_dir + formula, before the webview launch"
  artifacts:
    - path: "src/lucy_ng/webview/static/index.html"
      provides: "spectra-1d panel with img-spectrum-carbon/proton + .spectrum-img CSS"
      contains: "img-spectrum-carbon"
    - path: "src/lucy_ng/webview/static/webview.js"
      provides: "refreshSpectra1D + tick registration"
      contains: "refreshSpectra1D"
    - path: ".claude/commands/lucy-ng/case.md"
      provides: "run-start .run_manifest.json write block"
      contains: ".run_manifest.json"
  key_links:
    - from: "src/lucy_ng/webview/static/index.html"
      to: "/api/spectra/1d/carbon"
      via: "img src"
      pattern: "spectra/1d/carbon"
    - from: "src/lucy_ng/webview/static/webview.js"
      to: "refreshSpectra1D"
      via: "tick() registration"
      pattern: "refreshSpectra1D"
    - from: ".claude/commands/lucy-ng/case.md"
      to: "analysis/.run_manifest.json"
      via: "cat > heredoc at run-start"
      pattern: "bruker_data_dir"
---

<objective>
Wire the frontend and the path source: populate the 1D Spectra tab with two `<img>` mounts
(¹³C + ¹H) fed by the Plan-02 PNG endpoints, add `refreshSpectra1D()` to the ~3s poll, and add
the run-start `.run_manifest.json` write to `case.md` so the router can locate the raw Bruker
data. Completes the user-visible SP1-01 surface and the SP-02 "unavailable" surface end-to-end.

Purpose: Make the real trace + peak overlay actually appear in the dashboard, and supply the
trusted-local path that makes raw-data access possible.
Output: `index.html` + `webview.js` frontend edits + one `case.md` manifest-write block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-CONTEXT.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-PATTERNS.md
@.planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md
@src/lucy_ng/webview/static/index.html
@src/lucy_ng/webview/static/webview.js

<interfaces>
<!-- Endpoints from Plan 02 (both always image/png, HTTP 200): -->
<!--   /api/spectra/1d/carbon  ·  /api/spectra/1d/proton -->
<!-- DOM contract (95-UI-SPEC): inside <div data-panel="spectra-1d"> two <section class="tables-section"> -->
<!--   with <h2 class="tables-heading">¹³C Spectrum / ¹H Spectrum</h2> and <img class="spectrum-img" -->
<!--   id="img-spectrum-carbon" / "img-spectrum-proton">. Both sections ALWAYS present (never hidden). -->
<!-- .spectrum-img CSS: width:100%; height:auto; display:block; background:#fff; border:1px solid #dee2e6; border-radius:6px; -->
<!-- Poll: refreshSpectra1D() reassigns each <img>.src = URL + '?t=' + Date.now() every tick (no dedupe). -->
<!-- .run_manifest.json: {"bruker_data_dir": "<compound_path abs>", "formula": "<formula>"} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html — replace the spectra-1d placeholder with ¹³C/¹H img mounts + .spectrum-img CSS</name>
  <files>src/lucy_ng/webview/static/index.html</files>
  <read_first>
    - src/lucy_ng/webview/static/index.html — the `spectra-1d`/`spectra-2d` placeholder divs (95-PATTERNS.md pins them at ~lines 405-412) and the sibling `tables` panel `<section class="tables-section">`/`tables-heading` structure to imitate; the inline `<style>` token block (reuse `#dee2e6` border + `6px` radius; add NO new design system).
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md "Layout & Interaction Contract" — the exact DOM structure + the `.spectrum-img` CSS rule (verbatim) + the ¹³C/¹H heading copy.
  </read_first>
  <action>
    Replace ONLY the `<div class="placeholder" data-panel="spectra-1d">...</div>` line with the
    95-UI-SPEC DOM: a `<div data-panel="spectra-1d">` containing two `<section class="tables-section">`
    blocks — `#spectrum-carbon` (`<h2 class="tables-heading">¹³C Spectrum</h2>` + `<img id="img-spectrum-carbon"
    class="spectrum-img" alt="13C 1D spectrum with peak overlay" src="/api/spectra/1d/carbon">`) and
    `#spectrum-proton` (¹H equivalent, `id="img-spectrum-proton"`, `src="/api/spectra/1d/proton"`). Keep the
    `data-panel="spectra-1d"` attribute so `initTabs()` finds the panel by attribute (no ID coupling). Add the
    `.spectrum-img` rule (width:100%; height:auto; display:block; background:#fff; border:1px solid #dee2e6;
    border-radius:6px;) to the existing inline `<style>` block — reuse existing tokens only. Leave the
    `spectra-2d` placeholder line completely untouched (Phase 96 scope). Both ¹³C and ¹H sections are ALWAYS
    present — absence of a ¹H experiment is shown by the placeholder PNG inside the `<img>`, not by hiding the section.
  </action>
  <verify>
    <automated>python -c "s=open('src/lucy_ng/webview/static/index.html').read(); assert 'img-spectrum-carbon' in s and 'img-spectrum-proton' in s; assert '/api/spectra/1d/carbon' in s and '/api/spectra/1d/proton' in s; assert '.spectrum-img' in s; assert 'coming in Phase 96' in s, 'spectra-2d placeholder must stay untouched'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `[data-panel="spectra-1d"]` contains two `<img>` elements with ids `img-spectrum-carbon` and `img-spectrum-proton`, each `src`-ed at its `/api/spectra/1d/{carbon,proton}` route.
    - The `.spectrum-img` CSS rule exists in the inline `<style>` block and reuses existing tokens (`#dee2e6`, `6px`) — no new color/spacing tokens introduced.
    - The `spectra-2d` "coming in Phase 96" placeholder is unchanged.
    - Both sections use the existing `tables-section`/`tables-heading` classes (no new design system).
  </acceptance_criteria>
  <done>1D Spectra tab has ¹³C + ¹H img mounts wired to the PNG routes; .spectrum-img added; 2D placeholder untouched.</done>
</task>

<task type="auto">
  <name>Task 2: webview.js — refreshSpectra1D() cache-bust + tick() registration</name>
  <files>src/lucy_ng/webview/static/webview.js</files>
  <read_first>
    - src/lucy_ng/webview/static/webview.js — `renderStructures`'s cache-busted `img2.src = '/api/structure/' + idx + '.svg?t=' + Date.now()` image-tag idiom (95-PATTERNS.md ~lines 111-145) and the `tick()` function (~lines 752-762) listing every `refreshXxx()` call.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-UI-SPEC.md "Poll/refresh behaviour" — the exact `refreshSpectra1D()` body (cache-bust both imgs every tick; NO SMILES-diff dedupe per D-06; no `.catch()` needed since the PNG endpoint never errors).
  </read_first>
  <action>
    Add `function refreshSpectra1D()` to `webview.js` that reads `img-spectrum-carbon` and
    `img-spectrum-proton` by id and, when present, reassigns each `.src` to
    `'/api/spectra/1d/carbon?t=' + Date.now()` and `'/api/spectra/1d/proton?t=' + Date.now()`
    respectively (unconditional every tick — D-06 says the cheap re-render is acceptable, so no
    `lastSmiles`-style dedupe). No `fetch().then(json)` and no `.catch()` — the browser's native
    `<img>` loading handles the binary payload, and the endpoint is never-500 so the placeholder
    state is baked into the returned pixels. Register `refreshSpectra1D();` in `tick()` alongside
    the existing `refreshCarbon()`/`refreshHsqc()`/etc. calls (any position).
  </action>
  <verify>
    <automated>python -c "s=open('src/lucy_ng/webview/static/webview.js').read(); import re; assert 'function refreshSpectra1D' in s; assert s.count('refreshSpectra1D')>=2, 'must be defined AND called in tick()'; assert 'img-spectrum-carbon' in s and 'img-spectrum-proton' in s; assert '/api/spectra/1d/carbon' in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `refreshSpectra1D` is both DEFINED and CALLED (appears >=2 times); the call sits inside `tick()`.
    - The function reassigns `img-spectrum-carbon.src` and `img-spectrum-proton.src` with a `?t=` + `Date.now()` cache-buster each tick.
    - No SMILES-diff dedupe and no `.catch()` branch is added for these images (D-06 + never-500 contract).
  </acceptance_criteria>
  <done>refreshSpectra1D defined and registered in tick(); both imgs cache-busted every poll.</done>
</task>

<task type="auto">
  <name>Task 3: case.md — write analysis/.run_manifest.json at run-start</name>
  <files>.claude/commands/lucy-ng/case.md</files>
  <read_first>
    - .claude/commands/lucy-ng/case.md — the `spawn_case_team` Step 5 `run_start` timing stamp + `mkdir -p <compound_path>/analysis` prefix and the webview-launch `WEBVIEW_OUTPUT=$(lucy webview serve ...)` block (95-PATTERNS.md pins the window at ~lines 232-268 / 317-346); confirm `<compound_path>` and `<formula>` are in scope there (they are — interpolated into the SendMessage prompts a few lines below).
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-RESEARCH.md "Pattern 3: .run_manifest.json contract + case.md write site" — the exact insertion window (after `mkdir -p .../analysis`, before the webview launch) and the heredoc form.
    - .planning/phases/95-1d-real-spectra-peak-overlay/95-CONTEXT.md D-07 — trusted-local absolute path; no CLI signature or `.webview.json` model change.
  </read_first>
  <action>
    Insert ONE new Bash write block into `case.md`'s `spawn_case_team` Step 5, immediately AFTER
    the `run_start` timing stamp (whose `mkdir -p <compound_path>/analysis` prefix guarantees the
    dir exists) and BEFORE the `lucy webview serve` launch call (so the dashboard reads it from the
    first poll tick). The block writes `<compound_path>/analysis/.run_manifest.json` via a
    `cat > ... <<JSON` heredoc containing `{"bruker_data_dir": "<compound_path (absolute)>", "formula": "<formula>"}`,
    using the `<compound_path>` and `<formula>` variables already in scope. Change NOTHING else — no CLI
    signature change, no `.webview.json` schema change, no new step numbering. Keep it to this single added
    write block (project-skill code — minimize the diff).
  </action>
  <verify>
    <automated>python -c "s=open('.claude/commands/lucy-ng/case.md').read(); assert '.run_manifest.json' in s; assert 'bruker_data_dir' in s and 'formula' in s; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `case.md` contains a single new Bash block writing `analysis/.run_manifest.json` with `bruker_data_dir` and `formula` keys.
    - The write is positioned after the `mkdir -p .../analysis` run-start step and before the `lucy webview serve` launch.
    - No `lucy` CLI signature change and no `.webview.json` model change is introduced (diff is one added write block).
  </acceptance_criteria>
  <done>case.md writes the run-manifest at run-start with bruker_data_dir + formula; diff limited to one write block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM `<img>` → PNG endpoint | The `<img src>` is a browser-native GET of a never-500 image route; the placeholder state is baked into the pixels, so no JS error branch is needed. |
| case.md (trusted-local process) → analysis/.run_manifest.json | The manifest is written by the trusted local orchestrator, not over HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-01 | Tampering (XSS) | Frontend rendering of server content | mitigate | This phase adds no server-string-into-DOM injection — the new surface is an `<img src>` pointing at a fixed same-origin route (binary payload); the v9.2/93 textContent-only discipline is unaffected (no `innerHTML` of server content added). |
| T-95-03-02 | Information Disclosure | Absolute `bruker_data_dir` path written into the manifest | accept | D-07: trusted-local single-user tool; the manifest lives under the run's own `analysis/` dir, written by the trusted case.md process. No HTTP-facing exposure of the path beyond the localhost image it renders. |
| T-95-03-SC | Tampering | pip/npm installs | accept | No package installs in this plan (frontend + skill-script edits only); nothing to audit. |
</threat_model>

<verification>
- The three per-task grep guards exit 0.
- `pytest tests/test_webview_api.py -q` → no regressions (frontend/script edits do not touch the router).
- Manual browser verification is deferred to Plan 95-04 (checkpoint).
</verification>

<success_criteria>
- 1D Spectra tab renders the ¹³C + ¹H PNGs and refreshes them each ~3s tick.
- `case.md` writes `.run_manifest.json` at run-start so the router can locate raw data.
- No new design system, no CLI/`.webview.json` change, no XSS surface introduced.
</success_criteria>

<output>
Create `.planning/phases/95-1d-real-spectra-peak-overlay/95-03-SUMMARY.md` when done.
</output>
