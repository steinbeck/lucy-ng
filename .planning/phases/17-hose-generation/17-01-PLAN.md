---
phase: 17-hose-generation
plan: 01
type: execute
---

<objective>
Generate HOSE codes for all database compounds and populate hose_stats table with aggregated statistics.

Purpose: Enable database-backed 13C shift prediction with precomputed statistics per HOSE code.
Output: Populated hose_stats table with ~1M+ statistics entries across radii 1-6.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase providing database schema:
@.planning/phases/16-database-schema/16-01-SUMMARY.md

# Key source files:
@src/lucy_ng/database/manager.py
@src/lucy_ng/database/models.py
@src/lucy_ng/prediction/hose.py
@src/lucy_ng/prediction/lookup.py

**Tech stack available:** SQLite, hosegen library, RDKit, tqdm
**Established patterns:**
- INSERT OR REPLACE for idempotent batch operations
- Context manager pattern for DatabaseManager
- HOSECodeGenerator for HOSE code generation
- Progress bars with tqdm for batch operations

**Constraining decisions:**
- Phase 16: Store precomputed statistics (mean, std, count), not raw observations
- Phase 16: Composite primary key (hose_code, radius) for uniqueness
- Phase 16: INSERT OR REPLACE enables idempotent reruns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compound iteration method to DatabaseManager</name>
  <files>src/lucy_ng/database/manager.py, tests/test_database.py</files>
  <action>
Add `iter_compounds_with_shifts()` method to DatabaseManager that yields (compound_id, smiles, shifts) tuples for efficient batch processing.

Implementation:
- Query compounds table for id and smiles
- Join with shifts table to get atom_index and shift_ppm
- Use server-side cursor (fetchmany) for memory efficiency with 895K compounds
- Return Iterator[tuple[int, str, list[tuple[int, float]]]] - (compound_id, smiles, [(atom_idx, shift), ...])

Add tests verifying:
- Iterator yields correct data structure
- Handles compounds with no shifts (should skip)
- Memory-efficient (doesn't load all compounds at once)
  </action>
  <verify>pytest tests/test_database.py -v -k "iter_compounds" passes</verify>
  <done>DatabaseManager has iter_compounds_with_shifts() method with tests</done>
</task>

<task type="auto">
  <name>Task 2: Create HOSEStatsGenerator service</name>
  <files>src/lucy_ng/prediction/stats_generator.py, src/lucy_ng/prediction/__init__.py, tests/test_hose_stats_generator.py</files>
  <action>
Create HOSEStatsGenerator class that processes compounds and generates aggregated HOSE statistics.

Implementation:
- __init__(db_manager: DatabaseManager, max_radius: int = 6)
- generate_all() -> dict[tuple[str, int], list[float]] that:
  1. Iterates through all compounds via iter_compounds_with_shifts()
  2. For each compound, parse SMILES to RDKit mol
  3. For each carbon with known shift, generate HOSE codes at radii 1-6
  4. Aggregate shifts by (hose_code, radius) key
  5. Show progress with tqdm
- compute_stats() -> list[HOSEStatsRecord] that converts aggregates to stats
- populate_database() -> int that inserts stats and returns count

Use HOSECodeGenerator.prepare_mol() for SMILES parsing.
Handle failures gracefully - skip compounds that fail parsing, log count at end.
Use statistics.mean() and statistics.stdev() for aggregation.
  </action>
  <verify>pytest tests/test_hose_stats_generator.py -v passes</verify>
  <done>HOSEStatsGenerator class implemented with tests</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI command for HOSE stats generation</name>
  <files>src/lucy_ng/cli/database.py, tests/test_cli.py</files>
  <action>
Add `lucy database generate-hose-stats` CLI command to run batch HOSE generation.

Implementation:
- Command: `lucy database generate-hose-stats [--db PATH] [--max-radius INT]`
- Default db path: data/reference/compounds.db
- Default max-radius: 6
- Use HOSEStatsGenerator.populate_database()
- Show progress via tqdm (already in generator)
- Report final statistics: compounds processed, stats generated, time taken

Output format:
```
Generating HOSE statistics...
Processing compounds: 100%|████████████| 895000/895000 [XX:XX<00:00, XXXX/s]
Generated 1,234,567 statistics from 895,000 compounds
Time: XX min
```
  </action>
  <verify>lucy database generate-hose-stats --help shows command, pytest tests/test_cli.py -v -k "generate_hose" passes</verify>
  <done>CLI command works and can process database</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/test_database.py -v passes (all database tests)
- [ ] pytest tests/test_hose_stats_generator.py -v passes
- [ ] pytest tests/test_cli.py -v -k database passes
- [ ] lucy database generate-hose-stats --help shows correct usage
- [ ] mypy src/lucy_ng --strict passes (no type errors)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- iter_compounds_with_shifts() efficiently iterates database
- HOSEStatsGenerator processes compounds and generates statistics
- CLI command ready to run batch generation
- No type errors or test failures
</success_criteria>

<output>
After completion, create `.planning/phases/17-hose-generation/17-01-SUMMARY.md` with:
- Accomplishments (methods added, tests written)
- Files created/modified
- Decisions made
- Issues encountered
- Next phase readiness
</output>
