---
phase: 96-2d-real-spectra-peak-overlay
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_webview_api.py
autonomous: true
requirements: [SP2-01, SP-02]

must_haves:
  truths:
    - "TestSpectraEndpoint2D collects cleanly and SKIPS (not ERROR) for every 2D-router-dependent method before the 2D routes exist (WV-08 import-safety)"
    - "The 2D reversed-axis contract is expressed as an assertion ax.get_xlim()[0] > ax.get_xlim()[1] AND ax.get_ylim()[0] > ax.get_ylim()[1] on descending scales (SC1)"
    - "The HMBC flag→colour mapping is asserted against the LOCKED hex palette (#28a745 / #ffc107 / #adb5bd) via inspect.getsource on the overlay helper (SC2)"
    - "Cache-hit-no-rerender (SC3) and cache-bounded ≤3 (SC4) are expressed as unit tests (monkeypatch render spy + len(_png_cache) assertion)"
    - "SP-02 never-500 is asserted for absent manifest / stale path / no matching 2D experiment — each expecting HTTP 200 image/png"
    - "Hand-authored analysis/peaks/{hsqc,hmbc,cosy}.json fixtures follow the LOCKED Phase-94 schema, including at least one HMBC row per flag value ok/potential_4J/1J_artifact"
  artifacts:
    - path: "tests/test_webview_api.py"
      provides: "TestSpectraEndpoint2D class + 2D peaks fixtures"
      contains: "class TestSpectraEndpoint2D"
  key_links:
    - from: "tests/test_webview_api.py"
      to: "lucy_ng.webview.routers.spectra"
      via: "try/except ImportError: pytest.skip"
      pattern: "except ImportError"
---

<objective>
Ship the Wave-0 acceptance target for Phase 96 BEFORE any 2D router code: author a new
`TestSpectraEndpoint2D` class (or 2D sibling methods) in `tests/test_webview_api.py`
covering SP2-01 (real HSQC/HMBC/COSY PNG routes, both-axes-reversed, HMBC flag-colour
palette), SC3 (render < 1 s, cache-hit-no-rerender), SC4 (cache bounded ≤ 3), and SP-02
(never-500 "unavailable" on absent manifest / stale raw path / no matching 2D experiment).
Tests are RED-by-skip via the WV-08 `try/except ImportError: pytest.skip(...)` guard until
Plan 02's 2D routes land. Also hand-author the `analysis/peaks/{hsqc,hmbc,cosy}.json`
overlay fixtures to the LOCKED Phase-94 schema.

Purpose: Freeze the executable 2D contract (three route paths, the `_apply_nmr_axes_2d` /
`_select_experiment_2d` / `_render_2d_png` / `_png_cache` signatures, the HMBC flag palette,
the cache behaviour) so Plan 02 is written against a fixed target with zero field/behaviour
drift.
Output: `tests/test_webview_api.py::TestSpectraEndpoint2D` + hand-authored 2D peak fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-CONTEXT.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-VALIDATION.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md

<interfaces>
<!-- The 2D router contract Plan 02 will implement, and this scaffold asserts against. -->
<!-- Endpoints (3, GET-only, prefix /api), all ALWAYS return image/png HTTP 200: -->
<!--   /api/spectra/2d/hsqc  -> PNG bytes (real HSQC contour + open-circle overlay, or placeholder) -->
<!--   /api/spectra/2d/hmbc  -> PNG bytes (real HMBC contour + flag-coloured overlay, or placeholder) -->
<!--   /api/spectra/2d/cosy  -> PNG bytes (real COSY contour + overlay + diagonal, or placeholder) -->
<!-- Factory: spectra.make_router(analysis_dir: Path) -> APIRouter; the SAME make_router as Phase 95 (routes added in place). matplotlib imported LAZILY inside make_router only. -->
<!-- Module-level 2D helpers Plan 02 will expose (unit-testable): -->
<!--   _apply_nmr_axes_2d(ax, f1_ppm_scale, f2_ppm_scale) -> None   (set_xlim from f2, set_ylim from f1; NO invert_*axis) -->
<!--   _select_experiment_2d(bruker_data_dir: Path, experiment_type: str) -> Spectrum2D | None   (keeps ONLY dirs WITH acqu2s) -->
<!--   _read_peaks_2d(analysis_dir: Path, kind: str) -> list[dict]   (kind in {"hsqc","hmbc","cosy"}) -->
<!--   _block_max_decimate(data, scale_a, scale_b, max_dim=512) -> (data, scale_a, scale_b) -->
<!--   _geometric_levels(floor, factor=1.4, count=8) -> ndarray -->
<!--   _plot_hmbc_overlay(ax, peaks) -> None    (uses _HMBC_FLAG_COLORS dict) -->
<!--   _png_cache: dict[str, tuple[float, bytes]]   module-level; <= 3 entries under one-slot-per-route eviction -->
<!--   _cached_or_render(cache_key, source_mtime, render_fn) -> bytes -->
<!-- LOCKED HMBC flag palette (D-06): ok=#28a745, potential_4J=#ffc107, 1J_artifact=#adb5bd -->
<!-- Real CASE1 dataset root (2D exps): CASE1/6 = HSQC, CASE1/7 = HMBC, CASE1/5 = COSY (verified in 96-RESEARCH.md Pattern 2). -->
<!-- 2D peaks JSON schema (Phase 94 LOCKED, 96-CONTEXT.md canonical_refs): -->
<!--   hsqc.json: peaks[]:{carbon_ppm,proton_ppm,intensity,matched_real_carbon,one_bond} -->
<!--   hmbc.json: peaks[]:{carbon_ppm,carbon_ppm_observed,proton_ppm,intensity,flag} -->
<!--   cosy.json: peaks[]:{proton_a_ppm,proton_b_ppm,intensity} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author analysis/peaks/{hsqc,hmbc,cosy}.json fixtures to the LOCKED schema</name>
  <files>tests/test_webview_api.py</files>
  <read_first>
    - tests/test_webview_api.py — the existing `tables_analysis_dir` fixture (Phase 94, ~line 550 per 96-PATTERNS.md) which already hand-authors `peaks/hsqc.json`/`hmbc.json`/`cosy.json` to the locked schema (including one HMBC row per flag) — reuse its exact field names and shape; and the `spectra_case1_manifest_dir` fixture (Phase 95, ~lines 1119-1175) for the `.run_manifest.json` + `peaks/` writing shape.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-CONTEXT.md `<canonical_refs>` "Cross-peak JSON schemas (from Phase 94)" — the exact field lists for hsqc/hmbc/cosy peaks.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md "Validation Architecture → Wave 0 Gaps" — the note NOT to hand-author a fake 2D pdata/1/2rr; test 2D selection against real CASE1/repo data with a skip guard instead.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-VALIDATION.md "Wave 0 Requirements".
  </read_first>
  <action>
    Add a module-level pytest fixture `spectra_case1_manifest_dir_2d(tmp_path)` (or extend the
    existing `spectra_case1_manifest_dir` shape) that writes `tmp_path/.run_manifest.json` pointing
    `bruker_data_dir` at the REAL CASE1 root (`Path.home()/"Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/CASE1"`,
    reuse the existing `CASE1_ROOT` constant), plus hand-authored `tmp_path/peaks/hsqc.json`,
    `tmp_path/peaks/hmbc.json`, `tmp_path/peaks/cosy.json` to the LOCKED Phase-94 schema:
      - `hsqc.json`: top-level `experiment`/`count`/`note` + `peaks[]` each with `carbon_ppm`,
        `proton_ppm`, `intensity`, `matched_real_carbon`, `one_bond`. Include a few realistic
        aromatic/aliphatic rows (e.g. carbon_ppm ~127/proton_ppm ~7.2 aromatic; carbon_ppm ~22/proton_ppm ~1.2 aliphatic).
      - `hmbc.json`: top-level `experiment`/`raw_count`/`kept_count`/`flag_rules`/`note` + `peaks[]`
        each with `carbon_ppm`, `carbon_ppm_observed`, `proton_ppm`, `intensity`, `flag` — include
        AT LEAST ONE row for EACH flag value: `ok`, `potential_4J`, `1J_artifact`.
      - `cosy.json`: top-level `experiment`/`count`/`note` + `peaks[]` each with `proton_a_ppm`,
        `proton_b_ppm`, `intensity`.
    Guard the fixture with the existing `if not CASE1_ROOT.is_dir(): pytest.skip(...)` idiom used
    throughout the file (real-data tests skip when the local dataset is absent — never hard-fail CI).
    Do NOT create any fake `pdata/1/2rr` — real HSQC/HMBC/COSY 2D data comes from `CASE1_ROOT/{6,7,5}`.
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py --collect-only -q</automated>
    <automated>ruff check tests/test_webview_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_webview_api.py` defines a fixture writing `.run_manifest.json` + `peaks/hsqc.json` + `peaks/hmbc.json` + `peaks/cosy.json`.
    - The `hmbc.json` fixture `peaks[]` contains at least one row each with `flag` == `"ok"`, `"potential_4J"`, and `"1J_artifact"`.
    - Fixture field names match the LOCKED schema exactly (hsqc: carbon_ppm/proton_ppm/intensity/matched_real_carbon/one_bond; hmbc: carbon_ppm/carbon_ppm_observed/proton_ppm/intensity/flag; cosy: proton_a_ppm/proton_b_ppm/intensity).
    - Real-data fixture skips (not errors) when `CASE1_ROOT` is absent.
    - `ruff check tests/test_webview_api.py` passes; the collect-only run reports no ImportError.
  </acceptance_criteria>
  <done>2D peak fixtures hand-authored to the locked schema (HMBC covers all three flag values); collects cleanly; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add TestSpectraEndpoint2D (RED-by-skip until the 2D routes exist)</name>
  <files>tests/test_webview_api.py</files>
  <read_first>
    - tests/test_webview_api.py — `TestSpectraEndpoint` (Phase 95, ~lines 1221-1660) for: the WV-08 `try/except ImportError: pytest.skip(...)` guard copied into every method; the `TestClient(FastAPI())` + `include_router(spectra.make_router(...))` build shape; `test_apply_nmr_axes_reverses_descending_scale` (the direct-Axes assertion style); `test_missing_manifest_returns_placeholder` / `test_stale_bruker_path_returns_placeholder` (never-500 style); the monkeypatch-spy idiom (`_spy_read_1d`, ~lines 1309-1316); and `test_no_module_level_matplotlib_import` (source-scan style).
    - tests/test_webview_api.py — `CASE1_ROOT` constant (~lines 1091-1099), `empty_analysis_dir` / `spectra_stale_manifest_dir` fixtures reused verbatim for the 2D placeholder tests.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" (the 9 rows) + Pattern 2 (CASE1 6=HSQC/7=HMBC/5=COSY) + Pattern 5 (`_png_cache`, `_cached_or_render`) + Pattern 6 (`_plot_hmbc_overlay`, `_HMBC_FLAG_COLORS`).
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md "tests/test_webview_api.py — extend TestSpectraEndpoint" — the 2D method mirrors and the cache-test guidance.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md "Copywriting Contract" — the per-plot "No HSQC/HMBC/COSY experiment found in this dataset." strings.
  </read_first>
  <action>
    Add `class TestSpectraEndpoint2D` to `tests/test_webview_api.py`. Every 2D test METHOD name MUST
    contain the literal substring `spectra_2d` (the class name `TestSpectraEndpoint2D` does NOT — no underscore —
    so `-k spectra_2d` matches via method names only), while also embedding its sub-selector word
    (`real` / `apply_nmr_axes_2d` / `hmbc_flag_color` / `render_under_budget` / `cache_hit_no_rerender` /
    `cache_bounded` / `placeholder` / `select_experiment_2d`) so the word-based `-k` sub-selectors keep working. Every method opens with the
    WV-08 guard `try: from fastapi.testclient import TestClient; from lucy_ng.webview.routers import spectra`
    `except ImportError: pytest.skip("webview extra or spectra 2D routes not yet available")`; methods
    calling `spectra.make_router(...)` (lazy matplotlib import) must wrap that call in the same guard so
    an absent-matplotlib env SKIPS rather than errors. Methods (each an independent RED-by-skip target):

    1. `test_spectra_2d_apply_nmr_axes_2d_reverses_both_scales` — import `_apply_nmr_axes_2d`; build a Figure+subplot;
       call it with two DISTINCT descending arrays (e.g. f1=np.array([160.,120.,40.,0.]), f2=np.array([9.,6.,3.,0.]));
       assert `ax.get_xlim()[0] > ax.get_xlim()[1]` AND `ax.get_ylim()[0] > ax.get_ylim()[1]` (SC1). No real data.
    2. `test_spectra_2d_hsqc_returns_png_on_case1_real` / `test_spectra_2d_hmbc_returns_png_on_case1_real` / `test_spectra_2d_cosy_returns_png_on_case1_real`
       (three methods) — build `TestClient` with `spectra.make_router(spectra_case1_manifest_dir_2d)`;
       `GET /api/spectra/2d/{hsqc,hmbc,cosy}` → status 200, `content-type == "image/png"`, `len(content) > 0`
       (SP2-01 happy path; skip when CASE1 absent). Method names/`-k` must contain `spectra_2d` and `real`.
    3. `test_spectra_2d_hmbc_flag_color_palette` — `import inspect; from lucy_ng.webview.routers import spectra;`
       assert `inspect.getsource(spectra._plot_hmbc_overlay)` (or `spectra._HMBC_FLAG_COLORS`) contains the
       three LOCKED hex values `#28a745`, `#ffc107`, `#adb5bd` keyed on `ok`/`potential_4J`/`1J_artifact`
       (SC2). `-k` name contains `hmbc_flag_color`.
    4. `test_spectra_2d_select_experiment_2d_keeps_only_acqu2s` — against the REAL CASE1 root, assert
       `_select_experiment_2d(CASE1_ROOT, "HSQC")` returns a `Spectrum2D` with `experiment_type == "HSQC"`
       and that a 1D (no-`acqu2s`) dir is never returned; likewise `"COSY"` returns a `Spectrum2D`
       (skip when CASE1 absent). Per 96-RESEARCH Wave-0 note, do NOT use a synthetic fake 2D pdata here.
    5. `test_spectra_2d_render_under_budget` — time a single real 2D render (either via `_render_2d_png` on a
       `_select_experiment_2d` result, or a `TestClient` GET) and assert the wall-clock delta < 1.0 s
       (SC3 perf; skip when CASE1 absent). `-k` name contains `render_under_budget`.
    6. `test_spectra_2d_cache_hit_no_rerender` — monkeypatch/spy the module-level 2D render function (the one
       `_cached_or_render` invokes) to count calls; issue two identical GETs (same unchanged source mtime)
       and assert the render function was called exactly ONCE (SC3 cache). `-k` name contains `cache_hit_no_rerender`.
    7. `test_spectra_2d_cache_bounded` — clear `spectra._png_cache`; issue N (e.g. 6) repeated GETs across all three
       routes; assert `len(spectra._png_cache) <= 3` afterward (SC4). `-k` name contains `cache_bounded`.
       Reset `spectra._png_cache.clear()` at the start of this test (and any cache test) to avoid cross-test bleed.
    8. `test_spectra_2d_missing_manifest_placeholder` — `spectra.make_router(empty_analysis_dir)`;
       `GET /api/spectra/2d/hsqc` → 200 image/png, non-empty (SP-02; never 500, never a JSON body).
       `-k` name contains `spectra_2d` and `placeholder`.
    9. `test_spectra_2d_stale_path_placeholder` — `spectra.make_router(spectra_stale_manifest_dir)`;
       `GET /api/spectra/2d/hmbc` → 200 image/png placeholder (SP-02).
    10. `test_spectra_2d_no_matching_experiment_placeholder` — a manifest pointing at a real directory
        that contains NO matching 2D experiment (e.g. an empty tmp dir as `bruker_data_dir`); assert
        `GET /api/spectra/2d/cosy` → 200 image/png (SP-02 "No COSY experiment found" branch).

    The existing `test_no_module_level_matplotlib_import` (WV-08) already scans all of `spectra.py`
    up to `def make_router`; confirm the new module-top `_compute_2d_noise_sigma`/`Spectrum2D` imports
    (matplotlib-free) do not violate it — if a dedicated 2D assertion is desired, add
    `test_spectra_2d_no_module_level_matplotlib_import` asserting no `import matplotlib`/`from matplotlib` line
    (excluding `#` comment lines) precedes `def make_router`. Use exact comparisons; include response
    body/text in failure messages. Do NOT assert JSON `state` on these binary PNG routes.
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q</automated>
    <automated>ruff check tests/test_webview_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q` collects without ImportError; all 2D-route-dependent methods report SKIPPED (not FAILED/ERROR) while the 2D routes are absent.
    - The class contains a method asserting BOTH `ax.get_xlim()[0] > ax.get_xlim()[1]` AND `ax.get_ylim()[0] > ax.get_ylim()[1]` (SC1).
    - The class contains a method asserting the LOCKED HMBC hex palette (`#28a745`/`#ffc107`/`#adb5bd`) via source/dict inspection (SC2).
    - The class contains a cache-hit-no-rerender test (render fn called once across two unchanged-mtime requests, SC3) and a cache-bounded test (`len(spectra._png_cache) <= 3`, SC4).
    - SP-02 never-500 is asserted for absent manifest, stale path, and no-matching-2D-experiment — each HTTP 200 `image/png`, never a JSON body.
    - CASE1-real-data methods skip when `CASE1_ROOT` is absent; `-k spectra_2d`, `-k "spectra_2d and real"`, `-k apply_nmr_axes_2d`, `-k hmbc_flag_color`, `-k render_under_budget`, `-k cache_hit_no_rerender`, `-k cache_bounded`, `-k "spectra_2d and placeholder"` each select at least one test.
    - `ruff check tests/test_webview_api.py` passes; existing suite unbroken (`pytest tests/test_webview_api.py -q` shows no new failures/errors).
  </acceptance_criteria>
  <done>TestSpectraEndpoint2D present covering all 9 test-map rows + WV-08; all 2D-route-dependent methods SKIP cleanly pre-implementation; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture files → 2D routes-under-test | Fixture content authored by this plan; the CASE1 path is a trusted local dataset. Test-only, no HTTP-supplied input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-01 | Tampering | Fixture/contract drift vs the LOCKED Phase-94 2D peak schema + LOCKED HMBC flag palette | mitigate | Fixtures hand-authored to the exact hsqc/hmbc/cosy field lists; the HMBC fixture covers all three flag values; acceptance criteria pin both-axes-reversed and the three hex values verbatim. |
| T-96-01-02 | Denial of Service | Cache-growth regression escaping test coverage (SC4) | mitigate | A dedicated `test_spectra_2d_cache_bounded` asserts `len(_png_cache) <= 3` after N repeated polls; a `test_spectra_2d_cache_hit_no_rerender` proves the render fn is not re-invoked on unchanged mtime. |
| T-96-01-SC | Tampering | npm/pip/cargo installs | accept | No installs — matplotlib/numpy/nmrglue already approved (95-RESEARCH Package Legitimacy Audit); this phase adds zero packages (96-RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q` → 2D-route-dependent methods SKIP, no ERROR/collection failure.
- `pytest tests/test_webview_api.py -q` → no new failures.
- `ruff check tests/test_webview_api.py` passes.
</verification>

<success_criteria>
- `TestSpectraEndpoint2D` covers SP2-01 (real HSQC/HMBC/COSY PNG, both-axes-reversed, HMBC flag palette), SC3 (render<1s, cache-hit-no-rerender), SC4 (cache ≤3), and SP-02 (never-500 on absent manifest / stale path / no matching 2D experiment) — all RED-by-skip until Plan 02.
- 2D peak fixtures hand-authored to the LOCKED schema, HMBC covering all three flag values.
</success_criteria>

<output>
Create `.planning/phases/96-2d-real-spectra-peak-overlay/96-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
