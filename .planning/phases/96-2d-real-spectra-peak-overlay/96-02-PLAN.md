---
phase: 96-2d-real-spectra-peak-overlay
plan: 02
type: execute
wave: 1
depends_on: [96-01]
files_modified:
  - src/lucy_ng/webview/routers/spectra.py
  - src/lucy_ng/webview/app.py
autonomous: true
requirements: [SP2-01, SP-02]

must_haves:
  truths:
    - "GET /api/spectra/2d/{hsqc,hmbc,cosy} each return valid PNG bytes at HTTP 200, image/png, never a 500, never a JSON body"
    - "On real CASE1 data both contour axes are reversed: ax.get_xlim()[0] > ax.get_xlim()[1] AND ax.get_ylim()[0] > ax.get_ylim()[1]"
    - "HMBC cross-peak markers are colour-coded by flag using the LOCKED palette ok=#28a745 / potential_4J=#ffc107 / 1J_artifact=#adb5bd"
    - "A single 2D render completes in under 1 s (decimation ≤512×512 + geometric MAD-threshold levels)"
    - "Repeated polls with unchanged source mtime return the cached PNG without re-rendering; _png_cache holds at most one entry per route (≤3 total)"
    - "matplotlib is imported ONLY inside make_router(); base `from lucy_ng.cli import cli` raises no ImportError (WV-08)"
  artifacts:
    - path: "src/lucy_ng/webview/routers/spectra.py"
      provides: "three 2D contour routes + 2D helpers + mtime PNG cache"
      contains: "def _select_experiment_2d"
    - path: "src/lucy_ng/webview/app.py"
      provides: "docstring listing the three 2D routes (router already docked)"
      contains: "GET /api/spectra/2d/hsqc"
  key_links:
    - from: "src/lucy_ng/webview/routers/spectra.py"
      to: "lucy_ng.processing.peak_picker_2d._compute_2d_noise_sigma"
      via: "module-top import (matplotlib-free)"
      pattern: "_compute_2d_noise_sigma"
    - from: "src/lucy_ng/webview/routers/spectra.py"
      to: "lucy_ng.readers.bruker.BrukerReader.read_2d"
      via: "_select_experiment_2d scan of acqu2s dirs"
      pattern: "read_2d"
---

<objective>
Extend the Phase 95 `spectra.py` router IN PLACE with three new PNG routes
`/api/spectra/2d/{hsqc,hmbc,cosy}` that render real HSQC/HMBC/COSY contour plots from raw
Bruker 2D data (`BrukerReader.read_2d`) with the picked cross-peaks overlaid, plus an
mtime-keyed PNG cache. This is a mechanical mirror of the 1D implementation: reuse
`_read_manifest`/`_render_placeholder_png`/`_JSON_READ_ERRORS`/the styling constants
verbatim, add 2D siblings (`_select_experiment_2d`, `_apply_nmr_axes_2d`, `_render_2d_png`,
`_read_peaks_2d`), and turn the Plan 01 RED-by-skip tests GREEN. Leave every existing 1D
function untouched (no regression to Phase 95's passing tests).

Purpose: Complete the backend half of SP2-01 (real 2D contour + overlay, both axes reversed,
HMBC flag colours) under the SC1-SC4 constraints (reversed axes, <1 s render, bounded cache,
no memory growth) and the SP-02 never-500 contract.
Output: `spectra.py` (+3 routes, 2D helpers, cache) + `app.py` docstring update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-CONTEXT.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md
@.planning/phases/96-2d-real-spectra-peak-overlay/96-VALIDATION.md

<interfaces>
<!-- The file being extended (read in full before editing): src/lucy_ng/webview/routers/spectra.py -->
<!-- Reuse VERBATIM (do NOT modify): _read_manifest, _render_placeholder_png, _JSON_READ_ERRORS, -->
<!--   _MSG_NO_MANIFEST, _MSG_STALE_PATH, _MSG_PEAKS_UNAVAILABLE, _DPI, _TRACE_COLOR, _ACCENT_COLOR, -->
<!--   _PLACEHOLDER_COLOR, _apply_nmr_axes (1D), _select_experiment (1D), _render_1d_png, get_carbon_1d, get_proton_1d -->
<!-- Spectrum2D (lucy_ng.models): .data (2D ndarray), .f1_ppm_scale, .f2_ppm_scale, .f1_nucleus, .f2_nucleus, .experiment_type, .frequency, .metadata -->
<!--   VERIFIED (96-RESEARCH Pattern 2): data.shape == (len(f1_ppm_scale), len(f2_ppm_scale)); BOTH scales already descending; NO transpose, NO [::-1]. -->
<!-- _compute_2d_noise_sigma (lucy_ng.processing.peak_picker_2d, line 11) — matplotlib-free; import at module top; snr_floor default 5.0. -->
<!-- ax.contour(f2_ppm_scale, f1_ppm_scale, data, levels=..., colors=_TRACE_COLOR, linewidths=0.5)  # X=f2, Y=f1, Z=data native -->
<!-- LOCKED HMBC flag palette (D-06): {"ok":"#28a745","potential_4J":"#ffc107","1J_artifact":"#adb5bd"}, default "#495057" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add module-level 2D helpers (select / axes / decimate / levels / overlays / render / cache)</name>
  <files>src/lucy_ng/webview/routers/spectra.py</files>
  <behavior>
    - `_apply_nmr_axes_2d(ax, f1_ppm_scale, f2_ppm_scale)` leaves BOTH axes reversed: xlim[0]>xlim[1] AND ylim[0]>ylim[1] on descending scales.
    - `_select_experiment_2d(CASE1_ROOT, "HSQC")` returns a Spectrum2D with experiment_type=="HSQC"; a 1D (no acqu2s) dir is never selected; returns None (not raise) when no match / unreadable dir.
    - `_block_max_decimate(data, sa, sb, max_dim=512)` returns arrays ≤512 in each axis with consistently-decimated scales (all three shrink together); a narrow apex survives (block max, not stride).
    - `_geometric_levels(floor, 1.4, 8)` returns 8 strictly increasing positive levels starting at `floor`.
    - `_read_peaks_2d(analysis_dir, "hmbc")` returns the `peaks` list from analysis/peaks/hmbc.json, or [] on any failure (never raises).
    - `_plot_hmbc_overlay` maps flag→colour via the LOCKED palette; unknown/missing flag → default #495057; malformed peak rows skipped, never raise.
    - `_cached_or_render(key, mtime, fn)` calls `fn` on a miss/changed-mtime, returns cached bytes on an unchanged-mtime hit, keeps one slot per key.
  </behavior>
  <read_first>
    - src/lucy_ng/webview/routers/spectra.py — READ IN FULL. The functions to mirror: `_select_experiment` (lines 100-141, INVERT the acqu2s filter), `_apply_nmr_axes` (lines 175-184, add a 2D sibling — do NOT modify the 1D one), `_render_1d_png` (lines 187-265, the try/finally figure-release + defensive per-peak casting), `_render_placeholder_png` (267-295, reuse), `_read_peaks` (144-160, mirror as `_read_peaks_2d`). The constants block (56-72) and `_JSON_READ_ERRORS` (44-54).
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md — Pattern 3 (`_block_max_decimate` full verified body, ceil-division + edge-pad), Pattern 4 (`_compute_2d_noise_sigma` reuse + `_geometric_levels`, compute sigma on FULL-RES data), Pattern 5 (`_png_cache` dict + `_cached_or_render` + `_source_mtime`), Pattern 6 (`_plot_hmbc_overlay` + `_HMBC_FLAG_COLORS` + HSQC uniform scatter + `_plot_cosy_diagonal`), Pattern 7 (`_select_experiment_2d` full verified body), Pattern 1 (`_render_2d_png` contour call).
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md — the per-function analog mapping and the "reuse verbatim" list.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-UI-SPEC.md — Layout & Interaction Contract (figsize (9.0,6.0), dpi 100, contour linewidth 0.5, marker s=30 linewidths=1.0 facecolors="none", COSY diagonal 0.5pt #adb5bd, axis labels δH/δC per plot type, HMBC 7pt flag legend top-right), Contour level constants (floor=5.0×sigma, factor=1.4, count=8, positive-only), Color contract.
    - src/lucy_ng/processing/peak_picker_2d.py lines 11-48 — `_compute_2d_noise_sigma` signature (to import, not duplicate).
  </read_first>
  <action>
    Extend `spectra.py` module-level code (all ABOVE `make_router`, keeping matplotlib out of module
    scope — WV-08). Add to the imports: `Spectrum2D` to the `from lucy_ng.models import ...` line, and a
    new module-top `from lucy_ng.processing.peak_picker_2d import _compute_2d_noise_sigma` (plain numpy,
    matplotlib-free — permitted at module top per 96-RESEARCH Pattern 4). Add `Callable` to typing imports
    if needed for `_cached_or_render`.

    Add sibling constants (do NOT reword existing 1D ones): `_FIGSIZE_2D = (9.0, 6.0)`;
    `_MSG_NO_HSQC = "No HSQC experiment found in this dataset."`,
    `_MSG_NO_HMBC = "No HMBC experiment found in this dataset."`,
    `_MSG_NO_COSY = "No COSY experiment found in this dataset."` (exact UI-SPEC Copywriting wording);
    `_HMBC_FLAG_COLORS = {"ok": "#28a745", "potential_4J": "#ffc107", "1J_artifact": "#adb5bd"}` and
    `_DEFAULT_FLAG_COLOR = "#495057"`.

    Add these module-level functions (bodies per the verified 96-RESEARCH patterns cited above):
    - `_select_experiment_2d(bruker_data_dir: Path, experiment_type: str) -> Spectrum2D | None` — mirror of
      `_select_experiment` with the INVERTED filter: skip dirs WITHOUT `acqu2s`; call `BrukerReader.read_2d`
      catching `(FileNotFoundError, ValueError, OSError)`; filter `spectrum.experiment_type != experiment_type`;
      lowest-experiment-number tiebreak via `min(candidates, key=lambda t: t[0])[1]`. No DEPT filter.
    - `_apply_nmr_axes_2d(ax, f1_ppm_scale, f2_ppm_scale) -> None` — `ax.set_xlim(float(f2[0]), float(f2[-1]))`
      (F2=direct/x), `ax.set_ylim(float(f1[0]), float(f1[-1]))` (F1=indirect/y); both scales already
      descending so NO invert/[::-1]. Set xlabel `"δH (ppm)"`; ylabel passed by caller (`"δC (ppm)"` for
      HSQC/HMBC, `"δH (ppm)"` for COSY).
    - `_read_peaks_2d(analysis_dir: Path, kind: str) -> list[dict[str, Any]]` — read `analysis/peaks/{kind}.json`
      (kind in hsqc/hmbc/cosy), return its `peaks` list or [] on any `_JSON_READ_ERRORS`.
    - `_block_max_decimate(data, scale_a, scale_b, max_dim=512)` — ceil-division step, edge-pad, reshape
      `.max(axis=(1,3))`, return decimated data + both consistently-decimated scales (single call so scales
      never drift — Pitfall 1).
    - `_geometric_levels(floor, factor=1.4, count=8) -> ndarray` — `floor * (factor ** np.arange(count))`.
    - `_selected_2d_pdata_path(bruker_data_dir, experiment_type) -> Path | None` — re-resolves the SAME
      lowest-numbered `acqu2s`-bearing dir matching `experiment_type` as `_select_experiment_2d` and returns its
      `pdata/1/2rr` path (or the exp dir if `2rr` is absent); lets the mtime cache key on the real source file
      WITHOUT changing the frozen `_select_experiment_2d(...) -> Spectrum2D | None` return contract.
    - `_source_mtime(exp_dir, peaks_path) -> float` — `max(2rr_mtime_or_exp_dir_mtime, peaks_mtime)`, each in
      its own try/except OSError → 0.0 (Pitfall 5 — must not raise).
    - `_png_cache: dict[str, tuple[float, bytes]] = {}` (module-level) + `_cached_or_render(cache_key, source_mtime, render_fn)`.
    - Overlay helpers: `_plot_hmbc_overlay(ax, peaks)` (open circles `facecolors="none"`, `edgecolors` from
      `_HMBC_FLAG_COLORS.get(flag, _DEFAULT_FLAG_COLOR)`, `s=30`, `linewidths=1.0`, per-peak
      `try/except (KeyError, TypeError, ValueError): continue`, x=`proton_ppm` y=`carbon_ppm`); an HSQC-uniform
      scatter (x=`proton_ppm` y=`carbon_ppm`, `edgecolors=_ACCENT_COLOR`, same open-circle style — inline or a
      `_plot_uniform_overlay(ax, peaks, xkey, ykey, color)` helper); `_plot_cosy_diagonal(ax, f1_scale, f2_scale)`
      (thin `#adb5bd` line over the overlapping ppm range) plus a COSY scatter (x=`proton_a_ppm` y=`proton_b_ppm`,
      `_ACCENT_COLOR`).
    - `_render_2d_png(figure_cls, canvas_cls, spectrum, peaks, experiment_type) -> bytes` — mirror `_render_1d_png`'s
      try/finally: `fig = figure_cls(figsize=_FIGSIZE_2D, dpi=_DPI)`; compute
      `sigma = _compute_2d_noise_sigma(spectrum.data)` on FULL-RES data (Pitfall 2 — BEFORE decimating);
      `decimated, f1s, f2s = _block_max_decimate(spectrum.data, spectrum.f1_ppm_scale, spectrum.f2_ppm_scale)`;
      `levels = _geometric_levels(5.0 * sigma)`; `ax.contour(f2s, f1s, decimated, levels=levels, colors=_TRACE_COLOR, linewidths=0.5)`;
      `_apply_nmr_axes_2d(ax, f1s, f2s)` + set the per-type ylabel; dispatch the overlay by `experiment_type`
      with EXPLICIT mutual exclusivity (never both, mirrors 1D): IF `peaks` is non-empty → draw the overlay
      markers (HMBC → `_plot_hmbc_overlay` PLUS the 7pt flag legend top-right; HSQC → uniform accent open-circle
      scatter; COSY → `_plot_cosy_diagonal` + accent scatter) and do NOT draw the peaks-unavailable note; ELSE
      (`peaks` empty) → draw ONLY the `_MSG_PEAKS_UNAVAILABLE` top-right annotation and NO markers/legend (the
      HMBC flag legend appears exclusively in the non-empty-peaks branch); `canvas.print_png(buf)`;
      `finally: del canvas; del fig`.

    Do NOT touch any existing 1D function or the 1D constants. Do NOT add any matplotlib import at module
    scope (Figure/FigureCanvasAgg stay injected as `figure_cls`/`canvas_cls` params, exactly like `_render_1d_png`).
    Document the chosen decimation method (block-max) and level constants (factor 1.4, count 8, k=5.0) in the
    relevant docstrings per D-01/D-04 "document the chosen values".
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py -k "apply_nmr_axes_2d or hmbc_flag_color or select_experiment_2d" -q</automated>
    <automated>mypy src/lucy_ng/webview/routers/spectra.py</automated>
    <automated>ruff check src/lucy_ng/webview/routers/spectra.py</automated>
  </verify>
  <acceptance_criteria>
    - `spectra.py` contains `def _select_experiment_2d(`, `def _apply_nmr_axes_2d(`, `def _render_2d_png(`, `def _read_peaks_2d(`, `def _block_max_decimate(`, `def _geometric_levels(`, `def _plot_hmbc_overlay(`, `def _cached_or_render(` and a module-level `_png_cache: dict`.
    - `_HMBC_FLAG_COLORS` maps `ok`→`#28a745`, `potential_4J`→`#ffc107`, `1J_artifact`→`#adb5bd`; `pytest -k hmbc_flag_color` passes.
    - `pytest -k apply_nmr_axes_2d` passes (both axes reversed); `pytest -k select_experiment_2d` passes (or skips when CASE1 absent).
    - `_compute_2d_noise_sigma` is imported at module top from `lucy_ng.processing.peak_picker_2d`; no `import matplotlib`/`from matplotlib` line appears before `def make_router`.
    - Every existing 1D function/constant is byte-for-byte unchanged (Phase 95 tests still pass).
    - `mypy src/lucy_ng/webview/routers/spectra.py` (strict) passes; `ruff check src/lucy_ng/webview/routers/spectra.py` passes.
  </acceptance_criteria>
  <done>All 2D helper functions + cache primitives + palette added at module scope (matplotlib-free); the axes/palette/selection unit tests pass; mypy+ruff clean; 1D code untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three 2D routes into make_router + update app.py docstring</name>
  <files>src/lucy_ng/webview/routers/spectra.py, src/lucy_ng/webview/app.py</files>
  <read_first>
    - src/lucy_ng/webview/routers/spectra.py — `make_router` (lines 303-372): the lazy matplotlib import block (322-323, REUSE — do NOT add a second import block), the `_render_nucleus` never-500 closure shape (327-360), and the `@router.get(...)` registration (362-370). The router docstring (304-320) to extend.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-PATTERNS.md "make_router() — lazy matplotlib import + never-500 guard shape" — the `_render_2d(experiment_type, no_experiment_message)` closure to add and the three `@router.get("/spectra/2d/{hsqc,hmbc,cosy}")` registrations inside the SAME router.
    - .planning/phases/96-2d-real-spectra-peak-overlay/96-RESEARCH.md — System Architecture Diagram (the render flow: manifest → select_2d → cache lookup → decimate/noise/contour → cache store → Response), Pitfall 5 (the `_source_mtime` lookup must sit INSIDE the never-500 guard).
    - src/lucy_ng/webview/app.py lines 37-38 area — the two existing `GET /api/spectra/1d/{carbon,proton}` docstring bullets to mirror.
  </read_first>
  <action>
    In `spectra.py::make_router`, reusing the `Figure`/`FigureCanvasAgg` already lazily imported at the top
    of the factory (do NOT add a second `from matplotlib...` block), add a `_render_2d(experiment_type: str,
    no_experiment_message: str) -> bytes` closure with the SAME broad never-500 structure as `_render_nucleus`:
    `try:` read manifest (→ `_MSG_NO_MANIFEST` placeholder if None); check `bruker_dir.is_dir()` (→ `_MSG_STALE_PATH`);
    `spectrum = _select_experiment_2d(bruker_dir, experiment_type)` (→ `no_experiment_message` if None); locate the
    selected experiment's directory + `peaks_path = analysis_dir / "peaks" / f"{kind}.json"`; compute
    `source_mtime = _source_mtime(exp_dir, peaks_path)` (inside the guard — Pitfall 5); then
    `return _cached_or_render(cache_key, source_mtime, lambda: _render_2d_png(Figure, FigureCanvasAgg, spectrum,
    _read_peaks_2d(analysis_dir, kind), experiment_type))` where `cache_key` is the route name (e.g. `"hsqc"`);
    `except Exception: return _render_placeholder_png(Figure, FigureCanvasAgg, no_experiment_message)`
    (`# noqa: BLE001`). Map kind↔experiment_type: hsqc→"HSQC", hmbc→"HMBC", cosy→"COSY".

    `_select_experiment_2d(bruker_data_dir, experiment_type) -> Spectrum2D | None` MUST keep its Wave-0-frozen
    public signature UNCHANGED — do NOT return a tuple, do NOT store the dir on the returned object (changing it
    would break Plan 01's frozen `test_spectra_2d_select_experiment_2d_keeps_only_acqu2s`, which is out of this
    plan's scope — `tests/test_webview_api.py` is NOT in this plan's files_modified). To obtain the source `2rr`
    path for the mtime key WITHOUT touching that contract, add a small internal module-level helper
    `_selected_2d_pdata_path(bruker_data_dir, experiment_type) -> Path | None` that re-resolves the SAME
    lowest-numbered `acqu2s`-bearing directory matching `experiment_type` (identical scan + tiebreak rule as
    `_select_experiment_2d`) and returns its `pdata/1/2rr` path (or that exp dir if `2rr` is absent). Then
    `source_mtime = _source_mtime(selected_pdata_path_or_exp_dir, peaks_path)` inside the guard. Keep the cache
    key independent of the `?t=` query string — key on `(route_name, source_mtime)` only (Pitfall 4).

    Register three sync `def` routes inside the SAME `router` (no new APIRouter, no new include_router):
    `@router.get("/spectra/2d/hsqc") def get_hsqc_2d() -> Response: return Response(content=_render_2d("HSQC", _MSG_NO_HSQC), media_type="image/png")`
    and the HMBC/COSY equivalents (`_MSG_NO_HMBC`/`_MSG_NO_COSY`). Extend the `make_router` docstring to
    describe the three new routes and their contract.

    In `app.py`, add three docstring bullets after the two existing 1D-spectra bullets (lines ~37-38), mirroring
    the exact format: ``GET /api/spectra/2d/hsqc`` → real HSQC contour + cross-peak overlay (SP2-01, Phase 96),
    and the HMBC/COSY equivalents. No `include_router` change — `_spectra.make_router` is already docked.
  </action>
  <verify>
    <automated>pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q</automated>
    <automated>pytest tests/test_webview_api.py::TestSpectraEndpoint -q</automated>
    <automated>mypy src/lucy_ng/webview/routers/spectra.py src/lucy_ng/webview/app.py</automated>
    <automated>ruff check src/lucy_ng/webview/routers/spectra.py src/lucy_ng/webview/app.py</automated>
  </verify>
  <acceptance_criteria>
    - `spectra.py` registers `@router.get("/spectra/2d/hsqc")`, `.../hmbc`, `.../cosy`, each a sync `def` returning `Response(..., media_type="image/png")`.
    - `pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q` is GREEN (real-data methods pass when CASE1 present, else skip): both-axes-reversed, HMBC flag palette, render<1s, cache-hit-no-rerender, cache-bounded ≤3, and the three SP-02 placeholder branches all pass.
    - `pytest tests/test_webview_api.py::TestSpectraEndpoint -q` (Phase 95) still fully GREEN — no 1D regression.
    - The `_source_mtime` lookup is inside the `try/except Exception` never-500 guard; every 2D route returns HTTP 200 image/png on absent manifest / stale path / no matching experiment (never 500, never JSON).
    - `_select_experiment_2d(...) -> Spectrum2D | None` keeps its Wave-0-frozen signature (no tuple/path return); the source `2rr` mtime is obtained via `_selected_2d_pdata_path(...)`; `tests/test_webview_api.py` is NOT modified by this plan and `pytest -k select_experiment_2d` still passes.
    - `app.py` docstring lists `GET /api/spectra/2d/hsqc`, `.../hmbc`, `.../cosy` (SP2-01, Phase 96); no new `include_router` line added.
    - Only the lazy `Figure`/`FigureCanvasAgg` import from Phase 95 is present in `make_router` (no second matplotlib import block); `from lucy_ng.cli import cli` still raises no ImportError.
    - `mypy` (strict) + `ruff check` on both files pass.
  </acceptance_criteria>
  <done>Three 2D routes docked in the existing router with the never-500 + mtime-cache flow; TestSpectraEndpoint2D GREEN; Phase 95 1D tests still GREEN; app.py docstring updated; mypy+ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest `.run_manifest.json` `bruker_data_dir` → filesystem scan | Absolute path written by the trusted local `case.md` process (D-07); read-only, no HTTP-supplied input crosses here. |
| `analysis/peaks/*.json` → render overlay | Read-only JSON produced by the local peak-picker; individual peak rows are defensively cast. |
| browser poll (`?t=`) → route handler | Query param is untrusted but IGNORED server-side (never enters the cache key or filesystem paths). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-01 | Tampering | Path traversal / unreadable `bruker_data_dir` in the manifest | mitigate | Any missing/non-2D/unreadable path collapses to the `_MSG_STALE_PATH`/`no_experiment_message` placeholder inside the broad `except Exception` guard — never a 500, never a stack trace leak (SP-02). No path is constructed from HTTP input. |
| T-96-02-02 | Denial of Service | Unbounded Figure/cache allocation under the ~3 s poll | mitigate | Every render releases the Figure via `try/finally: del canvas; del fig` (no pyplot global registry); `_png_cache` keeps one slot per route (≤3 entries) keyed on `(route, source_mtime)` — SC3/SC4. `test_cache_bounded` guards the ≤3 invariant. |
| T-96-02-03 | Denial of Service | `pdata/1/2rr` mtime lookup raising outside the guard (Pitfall 5) | mitigate | `_source_mtime` wraps each stat in `try/except OSError → 0.0` AND is called inside the route's `try/except Exception` guard, with a documented fallback to the experiment-dir mtime. |
| T-96-02-04 | Information Disclosure | Malformed/huge peaks JSON crashing the render | mitigate | `_read_peaks_2d` returns [] on any `_JSON_READ_ERRORS`; per-peak overlay casting skips malformed rows; peaks lists are O(10-100) rows for real NMR data (96-RESEARCH Security Domain). |
| T-96-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (matplotlib/numpy/nmrglue already approved; 96-RESEARCH "Package Legitimacy Audit: Not applicable"). |

No auth/session/network surface exists — localhost single-user read-only tool (96-RESEARCH Security Domain V2/V3/V4 = No).
</threat_model>

<verification>
- `pytest tests/test_webview_api.py::TestSpectraEndpoint2D -q` GREEN (or skip when CASE1 absent).
- `pytest tests/test_webview_api.py::TestSpectraEndpoint -q` still GREEN (no 1D regression).
- `pytest -k "spectra_2d and placeholder"` → HTTP 200 image/png on every degradation path.
- `mypy src/lucy_ng` strict + `ruff check src tests` pass.
</verification>

<success_criteria>
- Three 2D contour routes render real HSQC/HMBC/COSY from raw Bruker data with cross-peaks overlaid, both axes reversed, HMBC colour-coded by flag (SP2-01/SC1/SC2).
- Render < 1 s via block-max decimation ≤512×512 + geometric MAD-threshold levels; mtime cache prevents re-render on unchanged polls; `_png_cache` ≤ 3 entries; figures released via try/finally (SC3/SC4).
- Every route degrades to a placeholder PNG at HTTP 200 on any failure (SP-02); matplotlib stays lazy inside make_router (WV-08).
</success_criteria>

<output>
Create `.planning/phases/96-2d-real-spectra-peak-overlay/96-02-SUMMARY.md` when done.
</output>
</content>
