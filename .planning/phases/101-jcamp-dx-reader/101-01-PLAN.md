---
phase: 101-jcamp-dx-reader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/readers/__init__.py
  - tests/readers/test_jcampdx_decode.py
  - tests/readers/test_jcamp.py
  - tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx
  - tests/fixtures/jcamp/C20H32O2_1H.dx
  - tests/fixtures/jcamp/C20H32O2_13C.dx
  - tests/fixtures/jcamp/_generate_fixture.py
autonomous: true
requirements: [JC-01, JC-02, JC-03, JC-04]

must_haves:
  truths:
    - "A trimmed real 2D HSQC .dx fixture exists under tests/fixtures/jcamp/, small enough to commit and CI-runnable, containing real DIFDUP pages with genuine (non-noise) cross-peaks"
    - "The two real 1D reference .dx files (¹H, ¹³C) are available under tests/fixtures/jcamp/ for the JC-02 cross-check"
    - "Both RED test files collect individually and each test fails (target modules not yet implemented)"
    - "COSY and NOESY 2D pages decode with the same NTUPLES/PAGE/DATATABLE structure as HSQC (RESEARCH Open Question 1 / Assumption A3 resolved before Plan 04 commits to one 2D code path)"
  artifacts:
    - path: "tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx"
      provides: "Trimmed real 2D HSQC NTUPLES fixture (~16 real signal pages, header pruned to consumed keys)"
    - path: "tests/fixtures/jcamp/C20H32O2_1H.dx"
      provides: "Real 1D ¹H reference for JC-02 cross-check and JC-03 read_1d"
    - path: "tests/fixtures/jcamp/C20H32O2_13C.dx"
      provides: "Real 1D ¹³C reference for JC-02 cross-check and JC-03 read_1d"
    - path: "tests/readers/test_jcampdx_decode.py"
      provides: "D-08 layer-1 hand-oracle unit test (RED)"
      contains: "test_hand_authored_mini_vector"
    - path: "tests/readers/test_jcamp.py"
      provides: "D-08 layer-2 integration tests (RED)"
      contains: "test_read_2d_shape"
  key_links:
    - from: "tests/readers/test_jcampdx_decode.py"
      to: "lucy_ng.readers._jcampdx_decode"
      via: "import inside test body (module created in Plan 02)"
      pattern: "from lucy_ng.readers._jcampdx_decode import parse_data"
    - from: "tests/readers/test_jcamp.py"
      to: "lucy_ng.readers.jcamp"
      via: "import inside test body (module created in Plans 03-04)"
      pattern: "from lucy_ng.readers.jcamp import JcampReader"
---

<objective>
Ship the Nyquist Wave-0 scaffolding for the JCAMP-DX reader: the committed CI
fixture (trimmed real 2D HSQC + the two real 1D references) and the RED test
files that every downstream implementation plan turns GREEN. This is the plan
that makes "verified means verified" possible for the whole phase (the Phase-100
mock-only-verification lesson): the correctness oracle is committed real data,
not a mock.

Purpose: give Plans 02/03/04 a failing, executable target and an independent
correctness oracle before any reader code is written.
Output: `tests/fixtures/jcamp/` (3 committed .dx + generation script) and two RED
test modules under `tests/readers/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-jcamp-dx-reader/101-RESEARCH.md
@.planning/phases/101-jcamp-dx-reader/101-PATTERNS.md
@.planning/phases/101-jcamp-dx-reader/101-VALIDATION.md

Source data (real, external — NOT committed as-is; source for the trimmed fixture):
`~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2-jcamp/`
(C20H32O2_HSQC.dx, C20H32O2_1H.dx, C20H32O2_13C.dx, C20H32O2_COSY.dx, C20H32O2_NOESY.dx)

Test-shape analog: `tests/test_bruker_reader.py` (class-per-concern, method-per-assertion).
Import convention (this codebase, WV-08): imports of the module under test go INSIDE test
function bodies so collection succeeds while the target module is still absent (RED per-test).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and commit the trimmed HSQC fixture + 1D references + COSY/NOESY spot-check</name>
  <read_first>
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "Code Examples" (fixture size measurement, verified peak-oracle coordinates, retained-key list)
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "Open Questions" #1 and § "Assumptions Log" A3 (COSY/NOESY spot-check requirement)
    - The real source file `~/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2-jcamp/C20H32O2_HSQC.dx` (structure only — grep the header keys and locate the ##NTUPLES / ##PAGE=/##DATA TABLE= blocks; do NOT read all 18 MB)
  </read_first>
  <files>tests/fixtures/jcamp/_generate_fixture.py, tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx, tests/fixtures/jcamp/C20H32O2_1H.dx, tests/fixtures/jcamp/C20H32O2_13C.dx</files>
  <action>
    Write a reproducible generation script `tests/fixtures/jcamp/_generate_fixture.py` that reads
    the real source HSQC `.dx` and emits `C20H32O2_HSQC_trimmed.dx`. It MUST:
    (a) Retain only the header keys the reader consumes (per RESEARCH § Code Examples list): DATATYPE,
        DATACLASS, NUM DIM, SYMBOL, VAR_DIM, FACTOR, FIRST, LAST, UNITS, .NUCLEUS, .PULSE SEQUENCE,
        .OBSERVE FREQUENCY, .OBSERVE NUCLEUS, .SHIFT REFERENCE, $SF, $SFO1, $SFO2, $BF1, $BF2, $OFFSET,
        $NUC1, TITLE — plus every JCAMP structural line required for nmrglue to still parse it
        (##TITLE, ##JCAMP-DX, ##DATA TYPE, the ##NTUPLES/##VAR_NAME/##END NTUPLES framing, ##END).
        Prune the bulk Bruker `$`-arrays the reader never reads ($P, $D, $CNST, $SPNAM, $PLW, $SP,
        $GPNAM, AUDITTRAIL, etc.). Byte-for-byte preserve the data-carrying ##PAGE=/##DATA TABLE= lines.
    (b) Keep only F1 page-index window [1735:1751] (16 real pages) — this window is verified by RESEARCH
        to contain the genuine gem-dimethyl/methyl cross-peaks (¹³C ~21.7–23.5 ppm, ¹H ~0.96–0.99 ppm)
        that Plan 04's integration oracle asserts on.
    (c) Update `##VAR_DIM=` so its F1 entry (first, per SYMBOL="F1,F2,Y" order) equals the trimmed page
        count (16), keeping F2 and Y at 2048 — so a downstream `len(PAGE)==VAR_DIM[F1]` consistency
        assertion holds on the fixture.
    Target committed size ~50–150 KB. Then copy `C20H32O2_1H.dx` and `C20H32O2_13C.dx` whole into the
    fixture dir (RESEARCH verified they are small enough to commit; they are the JC-02 cross-check /
    JC-03 references). Finally, in the SAME generation script (throwaway/dev use of nmrglue is fine here —
    it is not shipped reader code), decode ONE page from the real COSY and one from the real NOESY via
    nmrglue's own `_parse_data` and assert both return a non-None `(array, dtype)` with the same page
    structure as HSQC — this resolves RESEARCH Open Question 1 before Plan 04 assumes one 2D code path.
    Print the EXACT single line `COSY/NOESY SPOTCHECK PASS` (and nothing else on that line) on success so
    the verify command below can grep for it; fail loud (non-zero exit) if either decode returns None.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx` exists, is < 200 KB, and is parseable:
      `python -c "import nmrglue.fileio.jcampdx as jc; d=jc._readrawdic('tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx'); k=[x for x in d if x.startswith('_datatype_')][0]; inner=d[k][0]; assert len(inner['PAGE'])==16, len(inner['PAGE']); print('pages',len(inner['PAGE']),'vardim',inner['VAR_DIM'][0])"`
      exits 0 and prints `pages 16`.
    - `tests/fixtures/jcamp/C20H32O2_1H.dx` and `tests/fixtures/jcamp/C20H32O2_13C.dx` exist.
    - Running `python tests/fixtures/jcamp/_generate_fixture.py` prints the exact line `COSY/NOESY SPOTCHECK PASS` and exits 0 (this is the runnable Open-Question-1 resolution, now folded into the verify command below).
  </acceptance_criteria>
  <verify>
    <automated>python tests/fixtures/jcamp/_generate_fixture.py | grep -qx "COSY/NOESY SPOTCHECK PASS" && python -c "import nmrglue.fileio.jcampdx as jc; d=jc._readrawdic('tests/fixtures/jcamp/C20H32O2_HSQC_trimmed.dx'); k=[x for x in d if x.startswith('_datatype_')][0]; inner=d[k][0]; assert len(inner['PAGE'])==16; assert len(inner['DATATABLE'])==16; print('OK')"</automated>
  </verify>
  <done>Trimmed HSQC fixture (16 real pages, pruned header, parseable) + both 1D references committed; the COSY/NOESY spot-check runs inside the verify command and prints `COSY/NOESY SPOTCHECK PASS`, confirming one 2D code path is safe.</done>
</task>

<task type="auto">
  <name>Task 2: Author the RED test scaffolding (hand-oracle unit test + integration test)</name>
  <read_first>
    - tests/test_bruker_reader.py (class-per-concern / method-per-assertion shape, fixture-path constants)
    - tests/test_webview_api.py (WV-08 imports-inside-test-body convention)
    - .planning/phases/101-jcamp-dx-reader/101-PATTERNS.md § "tests/readers/test_jcamp.py" and § "tests/readers/test_jcampdx_decode.py" (exact test IDs, tolerance constants, hand-oracle vector)
    - .planning/phases/101-jcamp-dx-reader/101-RESEARCH.md § "Code Examples" (D-08 layer-1 mini-vector "0A00N%Sl"→[100,105,105,102]; Y-FACTOR≠1 case; peak-oracle coordinates)
  </read_first>
  <files>tests/readers/__init__.py, tests/readers/test_jcampdx_decode.py, tests/readers/test_jcamp.py</files>
  <action>
    Create `tests/readers/__init__.py` (empty package marker). Author two RED test modules; ALL imports
    of `lucy_ng.readers._jcampdx_decode` and `lucy_ng.readers.jcamp` go INSIDE the test function bodies
    (WV-08 convention) so collection succeeds and each test fails individually until implemented.

    test_jcampdx_decode.py (D-08 layer 1 — must NEVER import nmrglue): `test_hand_authored_mini_vector`
    calls the vendored `parse_data("(F2++(Y..Y))\n0A00N%Sl")` and asserts `list(result)==[100.0,105.0,105.0,102.0]`
    and `dtype=="R"` (hand-computed from the JCAMP pseudo-digit spec, independent of our decoder). Add a
    second test exercising a DIF/DUP-heavy line so a vendoring regression cannot hide.

    test_jcamp.py: module-level fixture-path constants FIXTURE_DIR / HSQC_TRIMMED / REF_1H / REF_13C and
    tolerance constants PPM_CROSS_CHECK_TOLERANCE_1H = 0.05, PPM_CROSS_CHECK_TOLERANCE_13C = 0.10.
    Classes and the exact test names from RESEARCH's Requirements→Test map:
      • TestJcampReader2D::test_read_2d_shape — asserts `read_2d(HSQC_TRIMMED)` returns Spectrum2D with
        data.shape == (16, 2048) (JC-01).
      • TestJcampReader2D::test_read_2d_ppm_axis_assertion — asserts the reader-internal fail-loud
        assertion (descending + plausible bounds) is present/reachable (JC-02 reader assertion).
      • TestJcampReaderPpmCrossCheck::test_read_2d_ppm_axes_match_1d_reference — projects the 2D onto each
        axis and matches the verified oracle cross-peaks (¹³C 21.7–23.5 / ¹H 0.96–0.99) against 1D
        reference peaks within tolerance (JC-02 load-bearing).
      • TestJcampReader1D::test_read_1d — `read_1d(REF_1H)` → Spectrum1D nucleus "1H"; `read_1d(REF_13C)` →
        nucleus "13C" (JC-03).
      • TestJcampReader2D::test_read_2d_yfactor_scaling — asserts a Y_FACTOR≠1 row is multiplied through
        (Pitfall 2). Where the multiplication lives (reader) determines placement — target the reader.
      • TestJcampReaderErrors — mirrors TestBrukerReaderErrors (nonexistent path → FileNotFoundError).
    Do NOT implement any production module — these are RED targets only.
  </action>
  <acceptance_criteria>
    - `pytest tests/readers/ --collect-only -q` succeeds (collection does not error).
    - `pytest tests/readers/test_jcampdx_decode.py::test_hand_authored_mini_vector -x` FAILS with an
      import/module-not-found error (RED) — the vendored module does not exist yet.
    - `pytest tests/readers/test_jcamp.py::TestJcampReader2D::test_read_2d_shape -x` FAILS (RED).
    - `grep -q "import nmrglue" tests/readers/test_jcampdx_decode.py` returns non-zero (the oracle test
      must not import nmrglue).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/readers/ --collect-only -q && ! grep -q "import nmrglue" tests/readers/test_jcampdx_decode.py && echo "RED-scaffold OK (collects, oracle independent of nmrglue)"</automated>
  </verify>
  <done>Both test modules collect individually, every target test is RED (targets unimplemented), and the hand-oracle test is provably independent of nmrglue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.dx` file → reader | Local scientific data file crosses into the parser. No network/auth/session/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Tampering (scientific-data integrity, not a security boundary) | Committed fixture correctness | mitigate | Fixture is REAL trimmed data with a verified page count (`len(PAGE)==16`) and known oracle peaks; the RED tests assert against these, so a wrong-data fixture is caught by Plan 04's cross-check rather than trusted. |
| T-101-SC | Tampering (supply chain) | package installs | accept | N/A — zero new packages this phase; `nmrglue` is an already-audited existing core dependency. No slopcheck/legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/readers/ --collect-only -q` collects with no errors.
- Fixture parseable by nmrglue `_readrawdic` with 16 pages / 16 datatables.
- The generation script's COSY/NOESY spot-check prints `COSY/NOESY SPOTCHECK PASS` (asserted in the Task 1 verify command).
- Hand-oracle test does not import nmrglue.
</verification>

<success_criteria>
Committed CI fixture (trimmed HSQC + two 1D refs) plus two RED test modules exist; COSY/NOESY
structural spot-check passed (auditable in the verify command); all downstream implementation targets have a failing executable test.
</success_criteria>

<output>
Create `.planning/phases/101-jcamp-dx-reader/101-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
