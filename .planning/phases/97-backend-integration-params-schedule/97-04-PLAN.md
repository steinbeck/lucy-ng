---
phase: 97-backend-integration-params-schedule
plan: 04
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/lucy_ng/nus/backends/__init__.py
  - src/lucy_ng/nus/backends/nmrpipe_smile.py
  - tests/test_nus_backends.py
autonomous: true
requirements: [NUS-01]
must_haves:
  truths:
    - "NmrPipeSmileBackend detects the real, which()-able tools (nmrPipe, bruk2pipe, nusExpand.tcl) and probes the SMILE plugin via `nmrPipe -fn SMILE -help` (NOT shutil.which('smileNus'))"
    - "diagnose() distinguishes 'not_installed' from 'installed_not_sourced' with actionable install/source guidance"
    - "On this dev machine (no NMRPipe) is_available() correctly returns False without raising"
    - "A NusBackend protocol + registry (get_backend / list_available_backends) exposes backends generically"
  artifacts:
    - path: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      provides: "NmrPipeSmileBackend detection class"
      contains: "REQUIRED_TOOLS"
    - path: "src/lucy_ng/nus/backends/__init__.py"
      provides: "NusBackend protocol + registry"
      contains: "def get_backend"
    - path: "tests/test_nus_backends.py"
      provides: "availability + diagnostic tests"
      contains: "NmrPipeSmileBackend"
  key_links:
    - from: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      to: "shutil.which / subprocess.run"
      via: "tool detection + SMILE capability probe"
      pattern: "shutil.which|subprocess.run"
---

<objective>
Implement the NUS reconstruction backend-detection half: `NmrPipeSmileBackend` (mirroring `LSDRunner`) with the RESEARCH-corrected tool list and the SMILE-plugin capability probe, plus a `NusBackend` protocol + registry in `backends/__init__.py`.

Purpose: Closes NUS-01's detection logic (the CLI `check` command is wired in plan 05). Fully independent of the params/schedule/models data path — no fixtures needed.
Output: `src/lucy_ng/nus/backends/nmrpipe_smile.py`, `src/lucy_ng/nus/backends/__init__.py` (upgraded from the plan-01 marker), `tests/test_nus_backends.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md
@.planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md
@src/lucy_ng/lsd/runner.py

<interfaces>
NmrPipeSmileBackend (nmrpipe_smile.py):
  REQUIRED_TOOLS = ["nmrPipe", "bruk2pipe", "nusExpand.tcl"]   # real, which()-able — NEVER "smileNus"
  missing_tools() -> list[str]                                  # classmethod, shutil.which per tool
  smile_plugin_available() -> bool                              # classmethod, subprocess `nmrPipe -fn SMILE -help` capability probe
  is_available() -> bool                                        # not missing_tools() and smile_plugin_available()
  diagnose() -> dict                                            # {status, missing_tools, smile_available, hint}; status ∈ {available, smile_plugin_missing, installed_not_sourced, not_installed}

NusBackend protocol + registry (backends/__init__.py):
  get_backend(name: str = "nmrpipe_smile") -> NusBackend
  list_available_backends() -> list[str]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NmrPipeSmileBackend detection + SMILE capability probe</name>
  <files>src/lucy_ng/nus/backends/nmrpipe_smile.py, tests/test_nus_backends.py</files>
  <read_first>
    - src/lucy_ng/lsd/runner.py (SEARCH_PATHS + shutil.which + is_available classmethod idiom, subprocess list-arg convention — PATTERNS §nmrpipe_smile.py)
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "Pattern 1" (Critical correction: smileNus is NOT a binary) + § diagnose() code example + § "Security Domain"
    - tests/test_lsd_runner.py (TestLSDRunnerAvailability class shape)
  </read_first>
  <behavior>
    - missing_tools() returns a list; on this dev machine (no NMRPipe) it includes "nmrPipe".
    - smile_plugin_available() returns False when nmrPipe is absent (short-circuits, no subprocess).
    - is_available() returns a bool and is False on this machine — never raises.
    - diagnose() returns a dict whose status is "not_installed" or "installed_not_sourced" on this machine, with a non-empty "hint" containing an install URL.
    - The SMILE probe uses a fixed arg list ["nmrPipe","-fn","SMILE","-help"] with no shell=True (assert via source inspection test / monkeypatched subprocess).
  </behavior>
  <action>
    Create `NmrPipeSmileBackend` per the RESEARCH Pattern 1 / PATTERNS reference implementation. `REQUIRED_TOOLS =
    ["nmrPipe", "bruk2pipe", "nusExpand.tcl"]` — do NOT include `smileNus`. `missing_tools()` = classmethod using
    `shutil.which`. `smile_plugin_available()` = classmethod that short-circuits False if `shutil.which("nmrPipe")`
    is None, else `subprocess.run(["nmrPipe","-fn","SMILE","-help"], capture_output=True, text=True, timeout=10)`
    inside try/except `(OSError, subprocess.TimeoutExpired)`, returning `"smile" in combined and "unknown function"
    not in combined`. `is_available()` = `not missing_tools() and smile_plugin_available()`. Add `diagnose() -> dict`
    per RESEARCH's code example distinguishing `not_installed` vs `installed_not_sourced` (probe `~/.nmrpipe`,
    `~/nmrpipe`, `/opt/nmrpipe`) with the actionable `.cshrc`/source hint + install URL. Write `tests/test_nus_backends.py`
    with a TestNusBackendAvailability class asserting bool return types, False-on-this-machine, and that the probe
    is not invoked when nmrPipe is absent (monkeypatch shutil.which to simulate presence/absence for the diagnostic
    branches).
  </action>
  <verify>
    <automated>pytest tests/test_nus_backends.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_nus_backends.py -x` all green
    - `grep -n 'smileNus' src/lucy_ng/nus/backends/nmrpipe_smile.py` returns nothing
    - `grep -n 'shell=True' src/lucy_ng/nus/backends/nmrpipe_smile.py` returns nothing
    - `grep -c 'nmrPipe.*-fn.*SMILE.*-help\|"-fn", "SMILE", "-help"' src/lucy_ng/nus/backends/nmrpipe_smile.py` ≥ 1
    - Test asserts NmrPipeSmileBackend.is_available() is False on this machine and does not raise
  </acceptance_criteria>
  <done>NmrPipeSmileBackend detects real tools + SMILE plugin via capability probe, diagnoses installed-vs-sourced, returns False cleanly on this machine; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: NusBackend protocol + registry</name>
  <files>src/lucy_ng/nus/backends/__init__.py, tests/test_nus_backends.py</files>
  <read_first>
    - src/lucy_ng/nus/backends/nmrpipe_smile.py (the backend to register — from Task 1)
    - .planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md § backends/__init__.py (registry role — first Protocol in repo, no exact analog)
    - .planning/phases/97-backend-integration-params-schedule/97-CONTEXT.md § Claude's Discretion (Protocol vs ABC, registry API names)
  </read_first>
  <action>
    Upgrade `backends/__init__.py` (currently the plan-01 docstring marker) to define a `NusBackend`
    `typing.Protocol` capturing the detection surface (`is_available()`, `missing_tools()`, `diagnose()`), a
    `get_backend(name: str = "nmrpipe_smile") -> NusBackend` factory returning the `NmrPipeSmileBackend`, and
    `list_available_backends() -> list[str]` returning the names whose `is_available()` is True (empty on this
    machine). Register `NmrPipeSmileBackend` under the key `"nmrpipe_smile"`. Keep it dependency-light (stdlib +
    the sibling module only). Extend `tests/test_nus_backends.py` with a class asserting `get_backend()` returns an
    object exposing `is_available`, an unknown name raises a clear error, and `list_available_backends()` returns a
    list (empty here).
  </action>
  <verify>
    <automated>pytest tests/test_nus_backends.py -x && python -c "from lucy_ng.nus.backends import get_backend, list_available_backends; print(list_available_backends())"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_nus_backends.py -x` all green
    - `python -c "from lucy_ng.nus.backends import get_backend, list_available_backends"` exits 0
    - get_backend("nmrpipe_smile") returns an object with an is_available() method
    - get_backend("does_not_exist") raises (KeyError or ValueError with a clear message)
    - list_available_backends() returns a list ([] on this machine)
  </acceptance_criteria>
  <done>NusBackend protocol + registry exposes backends generically; NmrPipeSmileBackend registered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend detection → external process | A subprocess is launched to probe the NMRPipe SMILE plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Tampering | `nmrPipe -fn SMILE -help` capability probe | mitigate | Fixed hard-coded arg list `["nmrPipe","-fn","SMILE","-help"]`, `shell=False`, no user input interpolated, `timeout=10`, exceptions caught — matches LSDRunner subprocess convention |
| T-97-SC | Tampering | pip/package installs | accept | No installs; detection targets an already-trusted, externally-installed toolchain via PATH — the backend is never a pip dependency |
</threat_model>

<verification>
- `pytest tests/test_nus_backends.py -x` green
- `grep -n smileNus src/lucy_ng/nus/backends/*.py` empty; `grep -n shell=True src/lucy_ng/nus/backends/*.py` empty
</verification>

<success_criteria>
NUS-01 detection logic met: corrected tool list + SMILE capability probe + installed-vs-sourced diagnostic + generic backend registry; correct False on the (NMRPipe-less) dev machine.
</success_criteria>

<output>
Create `.planning/phases/97-backend-integration-params-schedule/97-04-SUMMARY.md` when done.
</output>
