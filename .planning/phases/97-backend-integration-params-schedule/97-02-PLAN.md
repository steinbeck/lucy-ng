---
phase: 97-backend-integration-params-schedule
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - src/lucy_ng/nus/params.py
  - tests/test_nus_params.py
autonomous: true
requirements: [NUS-02]
must_haves:
  truths:
    - "nus/params.py extracts the full D-04 superset of Bruker acquisition + calibration params for any NUS experiment dir, per-experiment, never hard-coded"
    - "FnMODE is read from acqu2s (F1), never the vestigial acqus (F2) FnMODE=0"
    - "SF/OFFSET are read from pdata/1/procs and pdata/1/proc2s, not acqus/acqu2s"
    - "Parsing all three real fixtures yields the research-verified values and a validated NusAcquisitionParams"
  artifacts:
    - path: "src/lucy_ng/nus/params.py"
      provides: "read_nus_params(expdir) -> NusAcquisitionParams"
      contains: "def read_nus_params"
    - path: "tests/test_nus_params.py"
      provides: "per-fixture param extraction assertions"
      contains: "read_nus_params"
  key_links:
    - from: "src/lucy_ng/nus/params.py"
      to: "lucy_ng.readers.bruker"
      via: "reuse _get_param_2d / _strip_brackets"
      pattern: "from lucy_ng.readers.bruker import"
    - from: "src/lucy_ng/nus/params.py"
      to: "lucy_ng.models.nus.NusAcquisitionParams"
      via: "model construction"
      pattern: "NusAcquisitionParams\\("
---

<objective>
Implement `nus/params.py` — the pure-Python reader that turns a Bruker NUS experiment directory into a validated `NusAcquisitionParams` (D-04 superset), reusing `readers/bruker.py`'s helpers and reading calibration params from the correct `procs`/`proc2s` files.

Purpose: Closes NUS-02. Phase 98 processing consumes this model directly (no second parse pass).
Output: `src/lucy_ng/nus/params.py`, `tests/test_nus_params.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md
@.planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md
@src/lucy_ng/models/nus.py

<interfaces>
From lucy_ng.readers.bruker (reuse, do NOT duplicate — CONTEXT Claude's Discretion):
  _strip_brackets(value: str) -> str
  _get_param_2d(dic: dict, param_dict: str, key: str, default=None) -> Any   # param_dict generalizes to "procs"/"proc2s"

From lucy_ng.models.nus:
  NusAcquisitionParams  (field names defined in plan 01 — see 97-01 <interfaces>)

Read entry point: ng.bruker.read(expdir) — returns (dic, data); dic has keys acqus, acqu2s, procs, proc2s, nuslist, pprog.
NEVER ng.bruker.read_pdata() (NUS dirs have no 1r/2rr → raises OSError). NEVER infer nus_td from max(nuslist)+1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read_nus_params + NUS-02 fixture tests</name>
  <files>src/lucy_ng/nus/params.py, tests/test_nus_params.py</files>
  <read_first>
    - src/lucy_ng/readers/bruker.py (`_strip_brackets`, `_get_param_2d`, `_get_2d_params` dict-build shape, `read_pdata` usage to AVOID — PATTERNS §nus/params.py)
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "Acquisition parameters" + "Processing/calibration parameters" tables (exact expected values) + § Anti-Patterns
    - src/lucy_ng/models/nus.py (target model field names)
    - tests/test_bruker_reader.py (DATA_DIR + class-per-concern test layout, PATTERNS §"Test layout")
  </read_first>
  <behavior>
    - read_nus_params(EXP2_COSY) → NusAcquisitionParams with fnmode_f1==1, f1_td==188, nus_td==750, f1_nucleus=="1H", f2_td==2048, byte_order==0, grpdly==67.9851531982422 (float, not rounded), nus_amount_pct==25.
    - read_nus_params(EXP3_HSQC) → fnmode_f1==6, f1_td==100, nus_td==400, f1_nucleus=="13C", f1_sfo1≈125.715668907639, nus_amount_pct==25.
    - read_nus_params(EXP4_HMBC) → fnmode_f1==6, f1_td==232, nus_td==700, f1_nucleus=="13C", nus_amount_pct==33.
    - SF/OFFSET populated from procs/proc2s: exp3 f1_sf≈125.704983984, f1_offset≈174.9902 (research table).
    - fnmode_f1 is read from acqu2s: a regression test asserts it is NOT 0 for exp3/exp4 (guards the acqus-vs-acqu2s trap).
    - A nonexistent expdir raises FileNotFoundError before any nmrglue call.
  </behavior>
  <action>
    Create `read_nus_params(expdir: str | Path) -> NusAcquisitionParams`. Resolve the path
    (`Path(expdir).resolve()`) and raise `FileNotFoundError` if it does not exist (BrukerReader precedent).
    Call `dic, _data = ng.bruker.read(str(expdir))` — never `read_pdata()`. Build the flat field dict following
    `_get_2d_params`' shape and RESEARCH's verified code example: acqus keys (PULPROG, NUC1, SFO1, SW_h, TD,
    BYTORDA, DTYPA, DECIM, DSPFVS, GRPDLY, NusAMOUNT, NusSEED), acqu2s keys (NUC1, SFO1, SW_h, O1, TD, FnMODE,
    NusTD) — `fnmode_f1` MUST come from `dic["acqu2s"]["FnMODE"]`, never `dic["acqus"]`. SF/OFFSET via
    `_get_param_2d(dic, "procs", "SF")` / `_get_param_2d(dic, "proc2s", ...)`; leave them None if procs/proc2s
    absent (do not raise). Keep GRPDLY as float. Construct and return `NusAcquisitionParams(**fields)` so Pydantic
    validates. Raise `ValueError` with an explicit message for any genuinely-required field (e.g. FnMODE, TD)
    that is missing. Write `tests/test_nus_params.py` with a DATA_DIR constant + one class per experiment covering
    the behaviors, plus the acqu2s-not-acqus regression assertion.
  </action>
  <verify>
    <automated>pytest tests/test_nus_params.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_nus_params.py -x` all green
    - `grep -n 'read_pdata' src/lucy_ng/nus/params.py` returns nothing (must use ng.bruker.read)
    - `grep -n 'acqu2s.*FnMODE\|FnMODE.*acqu2s' src/lucy_ng/nus/params.py` shows fnmode read from acqu2s
    - Test asserts read_nus_params(EXP3_HSQC).fnmode_f1 == 6 (not 0) and .nus_td == 400
    - Test asserts read_nus_params(EXP4_HMBC).nus_amount_pct == 33
  </acceptance_criteria>
  <done>read_nus_params returns a validated NusAcquisitionParams matching research values for all three fixtures; NUS-02 covered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/caller → params reader | Untrusted `expdir` path string crosses into filesystem access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | `expdir` argument path traversal | mitigate | `Path(expdir).resolve()` + existence check before any `ng.bruker.read` call (BrukerReader.read_2d precedent); parsed values re-validated by NusAcquisitionParams |
| T-97-SC | Tampering | pip/package installs | accept | No installs; uses already-audited nmrglue/pydantic core deps only |
</threat_model>

<verification>
- `pytest tests/test_nus_params.py -x` green
- `pytest tests/test_nus_*.py -x` green (no regression against schedule/models sibling files if already landed)
</verification>

<success_criteria>
NUS-02 met: validated per-experiment param extraction, FnMODE from acqu2s, SF/OFFSET from procs/proc2s, verified against all three real fixtures.
</success_criteria>

<output>
Create `.planning/phases/97-backend-integration-params-schedule/97-02-SUMMARY.md` when done.
</output>
