---
phase: 97-backend-integration-params-schedule
plan: 05
type: execute
wave: 3
depends_on: [97-02, 97-03, 97-04]
files_modified:
  - src/lucy_ng/cli/nus.py
  - src/lucy_ng/cli/main.py
  - src/lucy_ng/nus/__init__.py
  - pyproject.toml
  - tests/test_cli_nus.py
  - tests/test_cli_main.py
autonomous: true
requirements: [NUS-01, NUS-04, NUS-05]
must_haves:
  truths:
    - "`lucy nus check` reports NMRPipe+SMILE availability, fails loud (exit 1) when unusable, and prints the installed-vs-not-sourced diagnostic hint; supports --format json"
    - "`lucy nus params <expdir> --format json` emits schema-valid NusAcquisitionParams JSON for all three fixtures"
    - "`lucy nus schedule <expdir> --format json` emits schema-valid NusSchedule JSON for all three fixtures"
    - "The `nus` group is registered on the root CLI and `lucy_ng.cli` imports without any [nus] extra installed (import-safe)"
    - "An empty-but-present `[nus]` optional-dependencies extra exists in pyproject.toml"
  artifacts:
    - path: "src/lucy_ng/cli/nus.py"
      provides: "lucy nus group: check/params/schedule, import-safe, --format json"
      contains: "def check"
    - path: "src/lucy_ng/cli/main.py"
      provides: "nus group registration"
      contains: "add_command(nus)"
    - path: "pyproject.toml"
      provides: "[nus] optional extra scaffold"
      contains: "nus = ["
  key_links:
    - from: "src/lucy_ng/cli/main.py"
      to: "src/lucy_ng/cli/nus.py"
      via: "import + add_command"
      pattern: "from lucy_ng.cli.nus import nus"
    - from: "src/lucy_ng/cli/nus.py"
      to: "lucy_ng.nus.params / schedule / backends"
      via: "deferred imports inside command bodies"
      pattern: "from lucy_ng.nus"
---

<objective>
Wire the `lucy nus` CLI group (`check`, `params`, `schedule`) over the params/schedule/backend layers, register it on the root CLI import-safely, add the empty `[nus]` pyproject extra, and finalize `nus/__init__.py` public re-exports.

Purpose: Closes NUS-04 (JSON CLI over all three fixtures), NUS-05 (core CLI stays dependency-free / import-safe + `[nus]` scaffold), and completes NUS-01 (the `check` command surface). This is the integration wave — depends on plans 02/03/04.
Output: `src/lucy_ng/cli/nus.py`, `cli/main.py` registration, `nus/__init__.py` re-exports, `pyproject.toml` `[nus]` extra, `tests/test_cli_nus.py`, `tests/test_cli_main.py` additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md
@.planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md
@src/lucy_ng/cli/webview.py
@src/lucy_ng/cli/lsd.py
@src/lucy_ng/cli/main.py

<interfaces>
Consumes (deferred-import inside command bodies):
  from lucy_ng.nus.params import read_nus_params        # -> NusAcquisitionParams (plan 02)
  from lucy_ng.nus.schedule import read_nus_schedule    # -> NusSchedule (plan 03)
  from lucy_ng.nus.backends import get_backend          # -> NmrPipeSmileBackend.diagnose()/is_available() (plan 04)

Model serialization: model.to_dict() → json.dumps(data, indent=2)  (single canonical --format json path).
CLI conventions (PATTERNS §cli/nus.py): @click.group()/@nus.command(); @click.argument("expdir", type=click.Path(exists=True));
  --format Choice(["text","json"]) default "text"; `lucy nus check` copies cli/lsd.py:lsd_check shape (err=True line + raise SystemExit(1) when unusable), plus diagnostic hint + --format json (a superset of lsd check).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lucy nus group (check/params/schedule), import-safe, --format json</name>
  <files>src/lucy_ng/cli/nus.py, tests/test_cli_nus.py</files>
  <read_first>
    - src/lucy_ng/cli/webview.py (import-safe module docstring + deferred-import-in-command-body convention — PATTERNS §cli/nus.py Analog 1)
    - src/lucy_ng/cli/lsd.py (lsd_check fail-loud shape + lsd_run --format json branching + click.Path(exists=True) — PATTERNS §cli/nus.py Analog 2)
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "Security Domain" (click.Path + resolve mitigation)
    - tests/test_cli_lsd.py + tests/test_cli_webview.py (TestLSDCheck + TestImportSafety subprocess leak-check patterns)
  </read_first>
  <behavior>
    - `lucy nus check` on this machine (no NMRPipe): exits 1, stderr mentions the missing tool, stdout includes the diagnostic hint (install URL). With --format json: emits diagnose() dict as JSON and still exits 1 when unusable.
    - `lucy nus params tests/fixtures/nus/exp3_hsqc --format json` exits 0 and emits JSON parseable to a dict with fnmode_f1==6, f1_td==100, nus_td==400.
    - `lucy nus schedule tests/fixtures/nus/exp2_cosy --format json` exits 0 and emits JSON with n_sampled==188 and a 188-length nuslist in acquisition order.
    - `lucy nus params <nonexistent>` exits non-zero via click.Path(exists=True).
    - Importing lucy_ng.cli.nus performs no top-level import of lucy_ng.nus.* (deferred inside command bodies).
  </behavior>
  <action>
    Create `cli/nus.py` with the import-safety docstring (webview convention) and an `@click.group() nus`. Add three
    commands. `check`: call `get_backend().diagnose()` (deferred import), print human-readable tool/SMILE status +
    the installed-vs-sourced hint following the `lsd_check` shape (`click.echo(..., err=True)` for the missing line),
    support `--format json` (dump the diagnose dict), and `raise SystemExit(1)` when not usable. `params` /
    `schedule`: each takes `@click.argument("expdir", type=click.Path(exists=True))` and `--format` Choice; resolve
    the path, call `read_nus_params`/`read_nus_schedule` (deferred imports), and branch output — `json` →
    `click.echo(json.dumps(model.to_dict(), indent=2))`, `text` → a concise human summary. Keep ALL `lucy_ng.nus.*`
    imports inside command bodies (establishes the Phase-98 deferred-import convention even though nothing forces it
    yet; a one-line `_require_nus()` no-op or comment is optional per discretion). Write `tests/test_cli_nus.py`
    using CliRunner covering the behaviors above, plus a subprocess-based import-safety check (mirror
    test_cli_webview.py TestImportSafety) that `python -c "import lucy_ng.cli.nus"` succeeds with nothing extra.
  </action>
  <verify>
    <automated>pytest tests/test_cli_nus.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cli_nus.py -x` all green
    - `pytest tests/test_cli_nus.py -k check -x` green (exit-1 + diagnostic)
    - `pytest tests/test_cli_nus.py -k "params or schedule" -x` green (JSON over fixtures)
    - `grep -n "^from lucy_ng.nus\|^import lucy_ng.nus" src/lucy_ng/cli/nus.py` returns nothing (imports are deferred, not top-level)
    - Test asserts `lucy nus schedule tests/fixtures/nus/exp3_hsqc --format json` JSON has n_sampled == 50
  </acceptance_criteria>
  <done>lucy nus check/params/schedule implemented, import-safe, JSON for all three fixtures; NUS-04 covered, NUS-01 CLI surface complete; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register nus group + [nus] extra + finalize nus/__init__ re-exports + import-safety test</name>
  <files>src/lucy_ng/cli/main.py, pyproject.toml, src/lucy_ng/nus/__init__.py, tests/test_cli_main.py</files>
  <read_first>
    - src/lucy_ng/cli/main.py (import block + add_command block + docstring command list — PATTERNS §cli/main.py)
    - pyproject.toml lines 45-65 ([project.optional-dependencies]: empty `prediction = []` + populated `webview` precedents)
    - src/lucy_ng/lsd/__init__.py (re-export + __all__ style for finalizing nus/__init__.py)
    - tests/test_cli_main.py (test_all_command_groups_registered — extend it)
  </read_first>
  <action>
    In `cli/main.py`: add `from lucy_ng.cli.nus import nus` to the import block (near `lsd`/`pylsd`), add
    `cli.add_command(nus)` after `add_command(webview)`, and add a one-line `nus` entry to the `\b` command-summary
    docstring block. In `pyproject.toml`: add an empty-but-present `nus = [ ... ]` extra under
    `[project.optional-dependencies]` with the explanatory comment from RESEARCH Pattern 2 (no dependencies yet —
    reserved for Phase 98/99). Finalize `src/lucy_ng/nus/__init__.py`: replace the plan-01 docstring-only marker with
    the full re-export list (`read_nus_params`, `NusAcquisitionParams`, `read_nus_schedule`, `NusSchedule`,
    `get_backend`, `list_available_backends`) grouped under `__all__` with comment headers (`# Params`, `# Schedule`,
    `# Backends`) per lsd/__init__.py — safe now that all submodules exist. In `tests/test_cli_main.py`: extend the
    registration test to assert `nus` is a registered group, and add a `test_nus_import_safe` (or add to the existing
    collect-safety pattern) asserting `lucy_ng.cli` imports without the [nus]/any optional extra.
  </action>
  <verify>
    <automated>pytest tests/test_cli_main.py -k "nus_import_safe or registered" -x && pip install -e . && python -c "from lucy_ng.cli import cli; from lucy_ng.nus import read_nus_params, read_nus_schedule, get_backend"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cli_main.py -k "nus_import_safe or registered" -x` green
    - `pip install -e .` succeeds (core, no extras)
    - `python -c "from lucy_ng.cli import cli"` exits 0 (import-safe)
    - `lucy nus --help` lists check, params, schedule (and NOT reconstruct/pipeline — D-02)
    - `grep -n "nus = \[" pyproject.toml` present; `grep -n "add_command(nus)" src/lucy_ng/cli/main.py` present
  </acceptance_criteria>
  <done>nus group registered on root CLI, [nus] empty extra scaffolded, nus package public API finalized, core CLI proven import-safe; NUS-05 covered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/user → CLI | Untrusted `<expdir>` argument crosses into filesystem access |
| CLI → external process | `check` triggers the backend SMILE capability probe (via plan-04 code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | `<expdir>` argument on `nus params`/`nus schedule` | mitigate | `type=click.Path(exists=True)` + `Path(expdir).resolve()` before file access (cli/lsd.py precedent); parsed values re-validated by the Pydantic models |
| T-97-02 | Tampering | `nus check` → SMILE capability probe | mitigate | Delegates to plan-04's fixed-arg-list, shell-free `subprocess.run` — no user input reaches the probe |
| T-97-SC | Tampering | pip/package installs | accept | `[nus]` extra is empty (zero packages); `pip install -e .` pulls only already-audited core deps — no new package to vet |
</threat_model>

<verification>
- `pytest tests/test_cli_nus.py tests/test_cli_main.py -x` green
- `pytest` (full suite) green
- `lucy nus check` exits 1 with diagnostic on this machine; `lucy nus params tests/fixtures/nus/exp3_hsqc --format json` exits 0 with valid JSON
- `pip install -e .` (core only) succeeds and `from lucy_ng.cli import cli` imports clean
</verification>

<success_criteria>
NUS-01 (check surface) + NUS-04 (JSON CLI over all three fixtures) + NUS-05 (import-safe core CLI + empty [nus] extra) met; only the three implemented subcommands registered (D-02); the diff to detection/fragments/lsd/ranking/cli/pick.py stays empty (CASE-pipeline-unchanged invariant).
</success_criteria>

<output>
Create `.planning/phases/97-backend-integration-params-schedule/97-05-SUMMARY.md` when done.
</output>
