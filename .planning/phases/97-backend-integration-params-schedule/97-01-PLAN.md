---
phase: 97-backend-integration-params-schedule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/nus/exp2_cosy/acqus
  - tests/fixtures/nus/exp2_cosy/acqu2s
  - tests/fixtures/nus/exp2_cosy/nuslist
  - tests/fixtures/nus/exp2_cosy/pdata/1/procs
  - tests/fixtures/nus/exp2_cosy/pdata/1/proc2s
  - tests/fixtures/nus/exp3_hsqc/acqus
  - tests/fixtures/nus/exp3_hsqc/acqu2s
  - tests/fixtures/nus/exp3_hsqc/nuslist
  - tests/fixtures/nus/exp3_hsqc/pdata/1/procs
  - tests/fixtures/nus/exp3_hsqc/pdata/1/proc2s
  - tests/fixtures/nus/exp4_hmbc/acqus
  - tests/fixtures/nus/exp4_hmbc/acqu2s
  - tests/fixtures/nus/exp4_hmbc/nuslist
  - tests/fixtures/nus/exp4_hmbc/pdata/1/procs
  - tests/fixtures/nus/exp4_hmbc/pdata/1/proc2s
  - src/lucy_ng/nus/__init__.py
  - src/lucy_ng/nus/backends/__init__.py
  - src/lucy_ng/models/nus.py
  - src/lucy_ng/models/__init__.py
  - tests/test_nus_models.py
autonomous: true
requirements: [NUS-02, NUS-03]
must_haves:
  truths:
    - "The three real C20H32O2 NUS metadata experiments (exp2 COSY, exp3 HSQC, exp4 HMBC) are available as self-contained, CI-portable text fixtures"
    - "A validated NusAcquisitionParams Pydantic model exists that captures the D-04 superset and rejects an unknown nucleus and an unrecognized FnMODE"
    - "A validated NusSchedule Pydantic model exists holding an acquisition-ordered nuslist and the full-grid NusTD"
    - "lucy_ng.nus and lucy_ng.nus.backends are importable packages"
  artifacts:
    - path: "src/lucy_ng/models/nus.py"
      provides: "NusAcquisitionParams + NusSchedule Pydantic v2 models"
      contains: "class NusAcquisitionParams"
    - path: "tests/fixtures/nus/exp2_cosy/acqu2s"
      provides: "Real COSY F1 acquisition params (FnMODE=1, TD=188, NusTD=750)"
    - path: "tests/fixtures/nus/exp3_hsqc/acqu2s"
      provides: "Real HSQC F1 acquisition params (FnMODE=6, TD=100, NusTD=400)"
    - path: "tests/fixtures/nus/exp4_hmbc/acqu2s"
      provides: "Real HMBC F1 acquisition params (FnMODE=6, TD=232, NusTD=700)"
    - path: "src/lucy_ng/nus/__init__.py"
      provides: "nus package marker"
  key_links:
    - from: "src/lucy_ng/models/__init__.py"
      to: "src/lucy_ng/models/nus.py"
      via: "re-export of NusAcquisitionParams, NusSchedule"
      pattern: "from lucy_ng.models.nus import"
---

<objective>
Lay the shared foundation for Phase 97: copy the real C20H32O2 metadata fixtures (D-03 + the RESEARCH SF/OFFSET-in-procs correction), define the validated `NusAcquisitionParams` and `NusSchedule` Pydantic v2 contracts that every downstream plan builds against, and create the `nus`/`nus.backends` package markers so the wave-2 plans can fill in single modules without touching shared `__init__` files.

Purpose: Interface-first — the models are the contract for NUS-02/NUS-03; downstream `params.py`/`schedule.py`/`cli/nus.py` construct and serialize these exact models. Fixtures make the data half fully unit-testable with zero external binary.
Output: `tests/fixtures/nus/` tree (3 experiments), `models/nus.py`, `models/__init__.py` exports, `nus/__init__.py`, `nus/backends/__init__.py`, `tests/test_nus_models.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-backend-integration-params-schedule/97-CONTEXT.md
@.planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md
@.planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md

<interfaces>
<!-- Contracts this plan DEFINES for downstream plans 02/03/05. Executor: these are the field names and signatures the rest of the phase depends on. -->

NusAcquisitionParams (models/nus.py) — D-04 superset. Fields (names are load-bearing; the F1/F2 split MUST be unambiguous — RESEARCH § "Trap"):
  pulse_program: str
  f2_nucleus: str; f2_sfo1: float; f2_sw_h: float; f2_td: int
  byte_order: int; dtype_code: int; decim: float; dspfvs: int; grpdly: float   # grpdly stays float, never rounded
  nus_amount_pct: int; nus_seed: int
  f1_nucleus: str; f1_sfo1: float; f1_sw_h: float; f1_o1: float; f1_td: int
  fnmode_f1: int          # read from acqu2s, NEVER acqus (acqus FnMODE is vestigial 0)
  nus_td: int             # authoritative full-grid size; never inferred from max(nuslist)+1
  f2_sf: float | None; f2_offset: float | None; f1_sf: float | None; f1_offset: float | None  # from procs/proc2s; None allowed pre-reconstruction

NusSchedule (models/nus.py):
  nuslist: list[int]      # acquisition order, never sorted
  fnmode_f1: int
  td_f1: int
  nus_td: int
  n_sampled: int          # == len(nuslist), asserted at build time in schedule.py

Both models expose to_dict() -> dict and from_dict(cls, d) classmethod (Spectrum1D convention, models/spectrum.py:39-53) for the single canonical --format json path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy real C20H32O2 metadata fixtures + create nus package markers</name>
  <files>tests/fixtures/nus/exp2_cosy/{acqus,acqu2s,nuslist,pdata/1/procs,pdata/1/proc2s}, tests/fixtures/nus/exp3_hsqc/{...}, tests/fixtures/nus/exp4_hmbc/{...}, src/lucy_ng/nus/__init__.py, src/lucy_ng/nus/backends/__init__.py</files>
  <read_first>
    - .planning/phases/97-backend-integration-params-schedule/97-CONTEXT.md § D-03 (fixture strategy)
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "Wave 0 Gaps" note + "SF/OFFSET live in procs" correction
    - src/lucy_ng/lsd/__init__.py (package-init docstring style for nus/__init__.py)
  </read_first>
  <action>
    Copy the text metadata (NOT the binary `ser`) for the three NUS experiments from
    `/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2/{2,3,4}/` into
    `tests/fixtures/nus/{exp2_cosy,exp3_hsqc,exp4_hmbc}/`. Per experiment copy: `acqus`, `acqu2s`, `nuslist`,
    and the two small processing-param text files `pdata/1/procs` + `pdata/1/proc2s` (RESEARCH found SF/OFFSET
    live in procs, not acqus — D-04's SF/OFFSET fields need these). Preserve the `pdata/1/` sub-path exactly so
    `ng.bruker.read(expdir)` resolves procs/proc2s. exp2→exp2_cosy (dir 2), exp3→exp3_hsqc (dir 3), exp4→exp4_hmbc (dir 4).
    Then create `src/lucy_ng/nus/__init__.py` as a package marker with a module docstring ONLY (no top-level
    re-imports yet — submodules do not exist until wave 2; the full re-export list is added in plan 05). Create
    `src/lucy_ng/nus/backends/__init__.py` likewise as a docstring-only package marker (the NusBackend Protocol +
    registry are added by plan 04). Do not copy `ser` (large binary, deferred to Phase 98).
  </action>
  <verify>
    <automated>test -f tests/fixtures/nus/exp2_cosy/acqu2s && test -f tests/fixtures/nus/exp3_hsqc/pdata/1/procs && test -f tests/fixtures/nus/exp4_hmbc/nuslist && python -c "import lucy_ng.nus, lucy_ng.nus.backends"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l tests/fixtures/nus/exp2_cosy/nuslist` reports 188; exp3_hsqc/nuslist reports 50; exp4_hmbc/nuslist reports 116
    - `grep '\$FnMODE=' tests/fixtures/nus/exp2_cosy/acqu2s` yields 1; exp3_hsqc and exp4_hmbc yield 6
    - `grep '\$NusTD=' tests/fixtures/nus/exp2_cosy/acqu2s` yields 750; exp3_hsqc 400; exp4_hmbc 700
    - No `ser` file present under tests/fixtures/nus/
    - `python -c "import lucy_ng.nus, lucy_ng.nus.backends"` exits 0
  </acceptance_criteria>
  <done>15 fixture files copied under tests/fixtures/nus/ with correct values; nus + nus.backends importable as packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NusAcquisitionParams + NusSchedule Pydantic models + model tests</name>
  <files>src/lucy_ng/models/nus.py, src/lucy_ng/models/__init__.py, tests/test_nus_models.py</files>
  <read_first>
    - src/lucy_ng/models/spectrum.py (ConfigDict, field_validator, to_dict/from_dict conventions — PATTERNS §"Pydantic v2 model conventions")
    - src/lucy_ng/models/__init__.py (existing re-export list to extend)
    - .planning/phases/97-backend-integration-params-schedule/97-RESEARCH.md § "Exact Bruker Field Names" tables (field values) + § "Trap" (F1/F2 fnmode disambiguation)
    - .planning/phases/97-backend-integration-params-schedule/97-PATTERNS.md § models/nus.py
  </read_first>
  <behavior>
    - NusAcquisitionParams accepts a full valid exp3 param set (f1_nucleus="13C", fnmode_f1=6, ...) and round-trips through to_dict()/from_dict() unchanged.
    - f1_nucleus/f2_nucleus validator rejects an unknown nucleus (e.g. "Xx") with ValueError; accepts {"1H","13C","15N","31P","19F","2H"}.
    - fnmode_f1 validator rejects a value outside {1,2,4,5,6} with ValueError (agrees with schedule.py's REAL/COMPLEX sets — do not diverge).
    - SF/OFFSET fields default to None and a model constructed without them validates (pre-reconstruction case).
    - NusSchedule holds nuslist as list[int] preserving input order (a deliberately unsorted list stays unsorted after construction); to_dict() emits the list verbatim.
  </behavior>
  <action>
    Create `models/nus.py` with two Pydantic v2 `BaseModel`s exactly matching the `<interfaces>` block above.
    Keep `model_config = ConfigDict(arbitrary_types_allowed=True)` for consistency with Spectrum1D even though
    all fields are scalar/list. Add `field_validator` guards on `f1_nucleus`/`f2_nucleus` (reuse the
    `{"1H","13C","15N","31P","19F","2H"}` set from spectrum.py) and on `fnmode_f1` (restrict to {1,2,4,5,6};
    define the allowed set as a module constant so `schedule.py` in plan 03 can share/agree with it, not duplicate
    divergent logic). SF/OFFSET fields are `float | None = None` — do NOT validate-reject None. Give both models
    `to_dict()`/`from_dict()` per the spectrum.py pattern (nuslist is JSON-native, no numpy coercion). Add both
    names to `models/__init__.py` `__all__` and its import block. Write `tests/test_nus_models.py` covering the
    behaviors above (construct-from-fixture-values, validator rejections, round-trip, unsorted-order preservation).
  </action>
  <verify>
    <automated>pytest tests/test_nus_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_nus_models.py -x` all green
    - Constructing NusAcquisitionParams with f1_nucleus="Xx" raises ValueError
    - Constructing NusAcquisitionParams with fnmode_f1=3 raises ValueError
    - NusAcquisitionParams(...).to_dict() then from_dict() returns an equal model
    - NusSchedule(nuslist=[0,124,431], ...).to_dict()["nuslist"] == [0,124,431] (order preserved)
    - `python -c "from lucy_ng.models import NusAcquisitionParams, NusSchedule"` exits 0
  </acceptance_criteria>
  <done>models/nus.py defines both validated models; re-exported from models package; test_nus_models.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → parser | Bruker metadata text files are read and coerced into typed models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | Parsed Bruker param values feeding NusAcquisitionParams/NusSchedule | mitigate | Pydantic v2 field validators (nucleus set, fnmode_f1 ∈ {1,2,4,5,6}, typed scalars) reject malformed values at construction — no raw dict flows downstream |
| T-97-SC | Tampering | pip/package installs | accept | No package installs in this plan; fixtures are static local text copied from the trusted project dataset, no `ser` binary copied |
</threat_model>

<verification>
- `pytest tests/test_nus_models.py -x` green
- `python -c "import lucy_ng.nus, lucy_ng.nus.backends; from lucy_ng.models import NusAcquisitionParams, NusSchedule"` exits 0
- Fixtures present with research-verified nuslist lengths / FnMODE / NusTD values
</verification>

<success_criteria>
Fixtures copied (3 experiments × 5 text files, no binary), both Pydantic contracts defined and validated, packages importable — downstream wave-2 plans can construct these models without ambiguity.
</success_criteria>

<output>
Create `.planning/phases/97-backend-integration-params-schedule/97-01-SUMMARY.md` when done.
</output>
