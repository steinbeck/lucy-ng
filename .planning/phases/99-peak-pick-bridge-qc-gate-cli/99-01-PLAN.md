---
phase: 99-peak-pick-bridge-qc-gate-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/models/nus.py
  - tests/fixtures/nus/known_bad_peaks/HSQC_exp3.json
  - tests/fixtures/nus/known_bad_peaks/HMBC_exp4.json
  - tests/fixtures/nus/known_bad_peaks/COSY_exp2.json
  - tests/fixtures/nus/known_bad_peaks/13C_exp7_wide.json
  - tests/fixtures/nus/known_bad_peaks/13C_exp6_narrow.json
  - tests/fixtures/nus/known_bad_peaks/1H_exp1.json
  - tests/fixtures/nus/clean_peaks_synthetic/HSQC_exp3.json
  - tests/fixtures/nus/clean_peaks_synthetic/HMBC_exp4.json
  - tests/fixtures/nus/clean_peaks_synthetic/COSY_exp2.json
  - tests/fixtures/nus/clean_peaks_synthetic/13C_exp7_wide.json
  - tests/fixtures/nus/clean_peaks_synthetic/1H_exp1.json
  - tests/nus/conftest.py
  - tests/nus/test_qc_checks.py
  - tests/nus/test_qc_regression.py
  - tests/nus/test_bridge.py
  - tests/nus/test_bridge_metadata.py
  - tests/nus/test_write_boundary.py
  - tests/nus/test_cli_pipeline.py
autonomous: true
requirements: [QC-01, QC-02, PICK-03]
must_haves:
  truths:
    - "The QcReport/QcCheckResult/QcVerdict Pydantic contract exists and round-trips via to_dict/from_dict (the shared interface Plans 02-04 implement against)"
    - "The real known-bad home-IST peak lists are committed as a repo fixture (QC-02 FAIL side)"
    - "A hand-authored synthetic clean peak-list set exists that respects §8 (15 protonated carbons, 5 quaternaries with zero HSQC, no ridge, symmetric COSY) — the load-bearing QC-02 PASS side"
    - "Every QC/bridge/CLI behaviour has a RED-by-skip stub test collectable on a machine with no NMRPipe backend"
  artifacts:
    - path: "src/lucy_ng/models/nus.py"
      provides: "QcVerdict enum + QcCheckResult + QcReport models"
      contains: "class QcReport"
    - path: "tests/fixtures/nus/known_bad_peaks/HSQC_exp3.json"
      provides: "QC-02 FAIL-side regression fixture (real home-IST)"
    - path: "tests/fixtures/nus/clean_peaks_synthetic/HSQC_exp3.json"
      provides: "QC-02 PASS-side synthetic clean fixture"
    - path: "tests/nus/test_qc_checks.py"
      provides: "RED-by-skip stubs, one class per QC check"
  key_links:
    - from: "tests/nus/conftest.py"
      to: "tests/fixtures/nus/known_bad_peaks"
      via: "fixture path resolver"
      pattern: "known_bad_peaks"
    - from: "src/lucy_ng/models/nus.py"
      to: "QcReport.to_dict"
      via: "JSON-serializable contract"
      pattern: "def to_dict"
---

<objective>
Ship the Nyquist Wave 0 scaffold for Phase 99: the shared QC data-model contract (QcVerdict/QcCheckResult/QcReport in models/nus.py), the two load-bearing test-fixture sets (the real known-bad home-IST lists for QC-02's FAIL side and a hand-authored synthetic clean set for QC-02's PASS side), and RED-by-skip stub test files covering every downstream behaviour (QC checks, QC regression discrimination, bridge, bridge metadata, write-boundary enforcement, pipeline CLI).

Purpose: Plans 02-04 implement against a fixed data contract and a committed regression floor. Without the synthetic clean fixture, QC-02's "PASS on a clean reconstruction" cannot be proven at all in Phase 99 (no real clean C20H32O2 reconstruction exists until Phase 100). This plan front-loads the interface + fixtures so the QC engine (Plan 02) and bridge (Plan 03) run in parallel.

Output: models/nus.py QC models; tests/fixtures/nus/{known_bad_peaks,clean_peaks_synthetic}/; extended tests/nus/conftest.py; six RED-by-skip stub test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-CONTEXT.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-RESEARCH.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-PATTERNS.md

<interfaces>
<!-- Existing models/nus.py conventions the new QC models must mirror. -->
From src/lucy_ng/models/nus.py (NusReconstructionResult, lines 174-233):
- Pydantic v2 BaseModel, `model_config = ConfigDict(arbitrary_types_allowed=True)`
- fields + `to_dict()` (returns plain JSON-serializable dict) + `@classmethod from_dict(cls, d)` + optional `summary()`
- str-choice convention elsewhere: `click.Choice(["text", "json"])`

Real per-peak schema (verified on disk, the exact shape fixtures use):
- HSQC cross_peak: {c13_ppm, h1_ppm, edited_sign, multiplicity_hint, confidence, note}
- HMBC cross_peak: {c13_ppm, h1_ppm, rel_intensity, rank_in_carbon, suspected_1J_artifact, confidence, note}
- COSY cross_peak: {h1a_ppm, h1b_ppm, rel_intensity, confidence, note}
- top-level (all three): {experiment, caveat, n_cross_peaks, cross_peaks}
- 1D list (13C_exp*/1H_exp1): {count, noise_sigma, negative_detected, snr_floor_used, peaks:[{ppm, intensity, snr}]}

Existing Phase-98 conftest factories to mirror (tests/nus/conftest.py):
- `nus_fixture_dir(name)` resolver; `make_valid_intermediate`/`make_empty_intermediate`/`make_truncated_intermediate`; `mock_run_stage`/`mock_subprocess_run` (string monkeypatch targets, raising=False, deferred — NOT module-level imports).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QcVerdict / QcCheckResult / QcReport models to models/nus.py</name>
  <files>src/lucy_ng/models/nus.py</files>
  <read_first>
    - src/lucy_ng/models/nus.py (full — mirror NusReconstructionResult lines 174-233: ConfigDict, to_dict/from_dict, summary())
    - .planning/phases/99-peak-pick-bridge-qc-gate-cli/99-PATTERNS.md (lines 170-190, the models section)
    - 99-CONTEXT.md D-02 (critical/soft verdict semantics), D-05 (metadata block fields), D-06 (confidence mapping)
  </read_first>
  <action>
    Append three additive classes to models/nus.py (do NOT modify existing classes):
    - `QcVerdict(str, Enum)` with members `PASS = "PASS"`, `PARTIAL = "PARTIAL"`, `FAIL = "FAIL"` (str-Enum so JSON-serializes to the plain string, matching the project's plain-string-choice convention).
    - `QcCheckResult(BaseModel)`: fields `name: str`, `passed: bool`, `critical: bool`, `details: str = ""`, plus optional `value: float | None = None` (the computed metric, e.g. ridge_fraction) and `threshold: float | None = None`. Include `to_dict()`/`from_dict()`.
    - `QcReport(BaseModel)`: fields `verdict: QcVerdict`, `checks: list[QcCheckResult]`, `thresholds_used: dict[str, float] = {}`, `errors: list[str] = []` (per-file JSON-parse errors, Security V5). Add `to_dict()` (serialize verdict via `self.verdict.value`, checks via each `to_dict()`), `from_dict()`, and `summary()` (human-readable, mirroring NusReconstructionResult.summary()). Add a convenience `violated_checks(self) -> list[str]` returning names of failed checks, and `critical_violations(self) -> list[str]`.
    - Do NOT import numpy/nmrglue here; models stay dependency-light.
    - Keep `from enum import Enum` import added at top.
  </action>
  <acceptance_criteria>
    - `python -c "from lucy_ng.models.nus import QcVerdict, QcCheckResult, QcReport"` succeeds.
    - `QcVerdict.FAIL.value == "FAIL"`; `json.dumps(QcReport(verdict=QcVerdict.PASS, checks=[]).to_dict())` produces `"verdict": "PASS"` (plain string, not `"QcVerdict.PASS"`).
    - Round-trip: `QcReport.from_dict(r.to_dict()).verdict == r.verdict` for a report containing one QcCheckResult.
    - `mypy src/lucy_ng/models/nus.py` passes (strict mode).
    - Existing NusReconstructionResult/NusSchedule/NusAcquisitionParams classes are byte-unchanged (git diff shows only additions).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; from lucy_ng.models.nus import QcVerdict, QcCheckResult, QcReport; r=QcReport(verdict=QcVerdict.PASS, checks=[QcCheckResult(name='x', passed=True, critical=True)]); assert json.loads(json.dumps(r.to_dict()))['verdict']=='PASS'; assert QcReport.from_dict(r.to_dict()).verdict==QcVerdict.PASS; print('ok')" && mypy src/lucy_ng/models/nus.py</automated>
  </verify>
  <done>QcVerdict/QcCheckResult/QcReport exist, JSON-round-trip cleanly, mypy-clean, existing models untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit known-bad + hand-author synthetic-clean peak-list fixtures</name>
  <files>tests/fixtures/nus/known_bad_peaks/HSQC_exp3.json, tests/fixtures/nus/known_bad_peaks/HMBC_exp4.json, tests/fixtures/nus/known_bad_peaks/COSY_exp2.json, tests/fixtures/nus/known_bad_peaks/13C_exp7_wide.json, tests/fixtures/nus/known_bad_peaks/13C_exp6_narrow.json, tests/fixtures/nus/known_bad_peaks/1H_exp1.json, tests/fixtures/nus/clean_peaks_synthetic/HSQC_exp3.json, tests/fixtures/nus/clean_peaks_synthetic/HMBC_exp4.json, tests/fixtures/nus/clean_peaks_synthetic/COSY_exp2.json, tests/fixtures/nus/clean_peaks_synthetic/13C_exp7_wide.json, tests/fixtures/nus/clean_peaks_synthetic/1H_exp1.json</files>
  <read_first>
    - 99-RESEARCH.md "Validation Architecture" > "Wave 0 Gaps" (the two fixture sets) and Common Pitfalls #3/#5 (the exact known-bad numbers: HSQC 4/27 quaternary hits, COSY 7/7 share h1a=5.32)
    - 99-CONTEXT.md canonical_refs (the real fixture paths + the 5 named quaternaries 142.0/135.86/79.35/36.23/37.86)
    - src/lucy_ng/nus/postprocess.py lines 45-66 (GUIDE_S10_C13 — the 20 true 13C shifts; 5 are quaternary, 15 protonated)
  </read_first>
  <action>
    Copy the six real files verbatim from `/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2/analysis/nmr_peaks/{HSQC_exp3,HMBC_exp4,COSY_exp2,13C_exp7_wide,13C_exp6_narrow,1H_exp1}.json` into `tests/fixtures/nus/known_bad_peaks/` (use the Read tool to load each, Write to persist — small files 3-13 KB, safe to commit; do NOT copy any `ser`/binary). These are the QC-02 FAIL-side regression floor, unchanged.

    Hand-author `tests/fixtures/nus/clean_peaks_synthetic/` — the load-bearing PASS-side set. Respect §8 exactly so Plan 02's aggregate_verdict returns PASS:
    - `HSQC_exp3.json`: top-level {experiment:"HSQC", caveat, n_cross_peaks, cross_peaks}. Include one cross-peak per PROTONATED carbon = the 15 non-quaternary shifts from GUIDE_S10_C13 (all except 142.00, 135.86, 79.35, 36.23, 37.86). Each cross-peak {c13_ppm=<shift>, h1_ppm=<plausible 0.8-5.4>, edited_sign, multiplicity_hint (consistent per carbon), confidence:"high", note}. ZERO cross-peaks within ±0.5 ppm of any of the 5 quaternary shifts. Give 2 diastereotopic peaks to a couple of CH2 carbons but keep their multiplicity_hint IDENTICAL between the two (edited-sign self-consistent). Ensure no single h1_ppm coordinate is shared by >50% of peaks (ridge_fraction < 0.5).
    - `HMBC_exp4.json`: {experiment:"HMBC", ...}; cross_peaks {c13_ppm, h1_ppm, rel_intensity, rank_in_carbon, suspected_1J_artifact:false, confidence, note}. Spread H1 coordinates so no single-axis bucket exceeds 50% of peaks (ridge_fraction < 0.5). ~15-25 peaks.
    - `COSY_exp2.json`: {experiment:"COSY", ...}; cross_peaks {h1a_ppm, h1b_ppm, rel_intensity, confidence, note}. Include diagonal-SYMMETRIC pairs — for each (a,b) also include (b,a) within tolerance — a real aliphatic network of ~6-10 pairs, NOT all sharing one h1a coordinate.
    - `13C_exp7_wide.json` / `1H_exp1.json`: the trusted-1D reference lists in the same {count, noise_sigma, negative_detected, snr_floor_used, peaks:[{ppm, intensity, snr}]} shape. 13C: include all 20 GUIDE_S10_C13 shifts as `ppm` values (this is the calibration + coverage reference). 1H: ~15-30 plausible proton shifts.
    - Round every ppm to 2 decimals. Keep files small.
  </action>
  <acceptance_criteria>
    - All 11 files are valid JSON (`python -c "import json; json.load(open(f))"` for each).
    - known_bad HSQC has ≥4 cross_peaks whose c13_ppm is within ±0.5 of {142.0,135.86,79.35,36.23,37.86}; known_bad COSY has all 7 cross_peaks sharing h1a_ppm≈5.32 (assert max same-coordinate fraction == 1.0).
    - synthetic-clean HSQC has ZERO cross_peaks within ±0.5 of any of the 5 quaternary shifts; has ≥12 unique c13_ppm values matching (±0.5) protonated GUIDE_S10_C13 shifts.
    - synthetic-clean COSY: for every (h1a,h1b) there is a mirror (h1b,h1a) within ±0.05; no single h1a bucket (0.05 ppm) holds >50% of peaks.
    - synthetic-clean 13C reference lists all 20 GUIDE_S10_C13 shifts within ±0.05.
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
import json,glob
from collections import Counter
Q={142.0,135.86,79.35,36.23,37.86}
kb="tests/fixtures/nus/known_bad_peaks";cl="tests/fixtures/nus/clean_peaks_synthetic"
for f in glob.glob(kb+"/*.json")+glob.glob(cl+"/*.json"): json.load(open(f))
h=json.load(open(kb+"/HSQC_exp3.json"))["cross_peaks"]
assert sum(any(abs(p["c13_ppm"]-q)<=0.5 for q in Q) for p in h)>=4
c=json.load(open(kb+"/COSY_exp2.json"))["cross_peaks"]
b=Counter(round(p["h1a_ppm"]/0.05) for p in c); assert max(b.values())/len(c)==1.0
ch=json.load(open(cl+"/HSQC_exp3.json"))["cross_peaks"]
assert all(not any(abs(p["c13_ppm"]-q)<=0.5 for q in Q) for p in ch)
cc=json.load(open(cl+"/COSY_exp2.json"))["cross_peaks"]
bb=Counter(round(p["h1a_ppm"]/0.05) for p in cc); assert max(bb.values())/len(cc)<=0.5
print("fixtures ok")
PY</automated>
  </verify>
  <done>Six real known-bad files copied unchanged; five synthetic-clean files authored satisfying §8; all assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Extend conftest + write six RED-by-skip stub test files</name>
  <files>tests/nus/conftest.py, tests/nus/test_qc_checks.py, tests/nus/test_qc_regression.py, tests/nus/test_bridge.py, tests/nus/test_bridge_metadata.py, tests/nus/test_write_boundary.py, tests/nus/test_cli_pipeline.py</files>
  <read_first>
    - tests/nus/conftest.py (full — mirror the deferred-string-monkeypatch, raising=False, no-top-level-lucy_ng.nus-import collection-safety convention)
    - tests/nus/test_reconstruct_chain.py + tests/nus/test_runner_faillloud.py (Phase-98 stub + fail-loud-branch test style to mirror)
    - 99-RESEARCH.md "Phase Requirements → Test Map" (the 6 test files + their req IDs)
    - 99-VALIDATION.md "Per-Task Verification Map" + "Wave 0 Requirements"
  </read_first>
  <action>
    Add to tests/nus/conftest.py (deferred, collection-safe — NO module-level `lucy_ng.nus` import):
    - `known_bad_peaks_dir` fixture → resolves `tests/fixtures/nus/known_bad_peaks` (Path).
    - `clean_peaks_dir` fixture → resolves `tests/fixtures/nus/clean_peaks_synthetic` (Path).
    - `KNOWN_QUATERNARY_SHIFTS = [142.00, 135.86, 79.35, 36.23, 37.86]` module constant re-exported for tests.

    Write six stub test files. Each test skips RED-by-skip using `pytest.importorskip("lucy_ng.nus.qc")` / `pytest.importorskip("lucy_ng.nus.bridge")` at the top (so tests are collectable before Plans 02/03 create those modules, then auto-activate once they exist). Structure per file:
    - `test_qc_checks.py`: one class per check — `TestQuaternaryExclusion`, `TestPpmCalibration`, `TestSignalToRidge`, `TestHsqcCoverage`, `TestEditedSignConsistency`, `TestCosyDiagonalSymmetry` — each with a `test_clean_passes` and a `test_violation_trips` method (bodies may `pytest.skip("Plan 02")` but MUST reference the intended callable name so the contract is documented). Covers QC-01.
    - `test_qc_regression.py`: `test_known_bad_dir_fails` (run_qc_checks(known_bad_peaks_dir).verdict == FAIL) and `test_synthetic_clean_dir_passes` (== PASS). Covers QC-02.
    - `test_bridge.py`: `test_build_spectrum2d_from_ft2` and `test_bridge_peak_pick_emits_schema` (asserts HSQC keys {c13_ppm,h1_ppm,edited_sign,multiplicity_hint,confidence,note}). Covers PICK-01.
    - `test_bridge_metadata.py`: `test_metadata_block_present` (top-level reconstruction/nus_metadata block with backend/iterations/verdict) and `test_confidence_derived_from_verdict` (PASS→high/medium, PARTIAL→low). Covers PICK-03.
    - `test_write_boundary.py`: `test_fail_verdict_quarantines_and_exits_nonzero`, `test_pass_writes_consumable_peaks`, `test_partial_writes_with_warning`. Covers QC-03/D-07.
    - `test_cli_pipeline.py`: `test_qc_command_json_format`, `test_pipeline_command_wires_stages`, `test_all_nus_subcommands_support_format_json`. Covers PICK-02/D-08.
  </action>
  <acceptance_criteria>
    - `pytest tests/nus/ --collect-only -q` collects all new tests with zero collection errors on this backend-less machine.
    - `pytest tests/nus/test_qc_checks.py tests/nus/test_qc_regression.py tests/nus/test_bridge.py tests/nus/test_bridge_metadata.py tests/nus/test_write_boundary.py tests/nus/test_cli_pipeline.py -q` runs with every test SKIPPED (RED-by-skip), zero passed, zero failed, zero errors.
    - conftest.py has NO top-level `from lucy_ng.nus` import (grep -v comment lines returns 0 matches for `^from lucy_ng.nus` / `^import lucy_ng.nus`).
    - Full suite still collects: `pytest --collect-only -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/ --collect-only -q && pytest tests/nus/test_qc_checks.py tests/nus/test_qc_regression.py tests/nus/test_bridge.py tests/nus/test_bridge_metadata.py tests/nus/test_write_boundary.py tests/nus/test_cli_pipeline.py -q 2>&1 | tail -5 && ! grep -nE '^(from|import) lucy_ng\.nus' tests/nus/conftest.py</automated>
  </verify>
  <done>All six stub files + conftest additions collectable and RED-by-skip; no collection-safety violation; full suite still collects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → repo tree | Externally-produced JSON copied into the committed fixture tree; must be data-only (no code, no binaries) |
| QcReport model → downstream consumers | The report is the machine-readable trust signal; its schema must not lose the verdict/critical distinction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Integrity | Synthetic clean fixture accidentally trips a real violation → false regression floor | mitigate | Task 2 acceptance asserts the clean fixture has zero quaternary hits + no ridge, so a correct Plan-02 engine MUST return PASS on it |
| T-99-02 | Integrity | QcVerdict serialized as `"QcVerdict.PASS"` (repr) instead of `"PASS"` → CASE/LLM misreads verdict | mitigate | str-Enum + Task 1 acceptance asserts `to_dict()["verdict"] == "PASS"` |
| T-99-03 | Tampering | Copying a `ser`/binary or oversized blob into the fixture tree | mitigate | Task 2 copies only the six named JSON files (3-13 KB); explicit "no binary" instruction |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit = N/A); nothing to verify |
</threat_model>

<verification>
- `pytest tests/nus/ --collect-only -q` clean.
- QC models import + round-trip + mypy clean.
- All 11 fixtures valid JSON with the asserted structural properties.
- `pytest -q` full suite still green (no regressions introduced by the additive model + stub tests).
</verification>

<success_criteria>
- QcVerdict/QcCheckResult/QcReport contract exists (interface for Plans 02-04).
- known-bad (FAIL side) + synthetic-clean (PASS side) fixtures committed.
- Six RED-by-skip stub test files + conftest fixtures collectable, all skipped, no errors.
- Existing Phase-97/98 nus models + tests unchanged.
</success_criteria>

<output>
Create `.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-01-SUMMARY.md` when done
</output>
