---
phase: 99-peak-pick-bridge-qc-gate-cli
plan: 04
type: execute
wave: 3
depends_on: [99-02, 99-03]
files_modified:
  - src/lucy_ng/cli/nus.py
autonomous: true
requirements: [PICK-02, QC-03]
must_haves:
  truths:
    - "lucy nus qc <peaks-dir> runs run_qc_checks against an arbitrary peaks directory, supports --format json, and exits non-zero on FAIL"
    - "lucy nus pipeline <expdir> runs the whole chain (reconstruct → staged peak-pick → QC → verdict-annotated rebuild → write) in one process and supports --format json"
    - "The consumable file written by the pipeline carries the REAL computed verdict: reconstruction.qc_verdict equals the QcReport verdict (D-05) and per-peak confidence is verdict-derived (D-06) — the payload is rebuilt with the verdict AFTER run_qc_checks returns, never written from the pre-QC staged payload"
    - "On QC PASS/PARTIAL the pipeline writes consumable analysis/nmr_peaks/*.json; on FAIL it writes nothing consumable, quarantines to analysis/nus_recon/<expN>/qc_failed/, and exits non-zero (D-07/QC-03)"
    - "QC thresholds are CLI-overridable (D-04); every lucy nus subcommand supports --format json"
    - "case.md and the CASE pipeline are untouched — enforcement lives only at the pipeline write boundary"
  artifacts:
    - path: "src/lucy_ng/cli/nus.py"
      provides: "qc + pipeline commands (optionally peak-pick), deferred-import + write-boundary enforcement"
      contains: "def pipeline"
  key_links:
    - from: "src/lucy_ng/cli/nus.py"
      to: "lucy_ng.nus.qc.run_qc_checks"
      via: "deferred import in qc + pipeline command bodies"
      pattern: "from lucy_ng.nus.qc import run_qc_checks"
    - from: "src/lucy_ng/cli/nus.py::pipeline"
      to: "analysis/nus_recon/<expN>/qc_failed"
      via: "FAIL-verdict quarantine branch"
      pattern: "qc_failed"
---

<objective>
Add the two Phase-99 CLI commands to the import-safe `lucy nus` group: `lucy nus qc <peaks-dir>` (standalone QC gate, the QC-02 discrimination proof surface) and `lucy nus pipeline <expdir>` (the full chain: `NusRunner.reconstruct()` → `bridge_peak_pick()` → `run_qc_checks()` → write-boundary enforcement). Implement D-07: PASS/PARTIAL writes consumable `analysis/nmr_peaks/*.json`; FAIL quarantines to `analysis/nus_recon/<expN>/qc_failed/` and exits non-zero — extending the FIX-10 constraint-hardness guard to reconstruction-derived peaks so a fabricated cross-peak can never silently become a hard LSD constraint. `case.md` is NOT touched (single pipeline-boundary barrier). Thresholds are CLI-overridable (D-04); every subcommand supports `--format json` (D-08).

Purpose: PICK-02 (reusable end-to-end pipeline command + `--format json` everywhere) + QC-03 (handoff refuses to start on FAIL).

Output: extended src/lucy_ng/cli/nus.py, making test_write_boundary.py + test_cli_pipeline.py GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-CONTEXT.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-RESEARCH.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-PATTERNS.md

<interfaces>
From src/lucy_ng/nus/qc.py (Plan 02):
```python
def run_qc_checks(peaks_dir, config: QcConfig | None = None) -> QcReport
class QcConfig: c13_tol; h1_tol; ridge_fraction_fail; hsqc_coverage_floor; known_quaternary_shifts
```
From src/lucy_ng/nus/bridge.py (Plan 03):
```python
def build_spectrum2d(processed_ft2, params, experiment_type) -> Spectrum2D
def bridge_peak_pick(spectrum, *, experiment, qc_report=None, recon_meta=None, ...) -> dict
def write_peak_json(out_dir, experiment, payload) -> Path
```
From src/lucy_ng/nus/runner.py (Phase 98, unchanged):
```python
class NusRunner:
    def reconstruct(self, expdir, *, max_iter=500, threshold=0.8, n_sigma=5,
                    virtual_echo=True, f1_p0=90.0, f1_p1=0.0, f2_p0=0.0, f2_p1=0.0,
                    timeout=600) -> NusReconstructionResult
# NusReconstructionResult: success, backend, fnmode_f1, stage_dir, stage_outputs,
#                          processed_spectrum (final .ft2 path), smile_iterations
```
From src/lucy_ng/models/nus.py: QcVerdict {PASS, PARTIAL, FAIL}; QcReport.to_dict().

Existing cli/nus.py conventions (lines 1-16, 31-143, 146-254):
- module is import-safe: NO top-level `lucy_ng.nus.*` import; all deferred into command bodies.
- every command: `@click.option("--format","output_format",type=click.Choice(["text","json"]),default="text",show_default=True,...)`.
- `Path(expdir).resolve()`; `raise SystemExit(1)` on failure (see check command).
- reconstruct command already exposes --iterations/--threshold/--virtual-echo/--f1-p0/--f1-p1/--f2-p0/--f2-p1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lucy nus qc &lt;peaks-dir&gt; command (standalone gate, --format json, exit-1 on FAIL)</name>
  <files>src/lucy_ng/cli/nus.py</files>
  <behavior>
    - `lucy nus qc <known-bad-dir> --format json` emits QcReport JSON with "verdict":"FAIL" and exits non-zero.
    - `lucy nus qc <clean-synthetic-dir>` emits/reports PASS and exits 0.
    - Text format prints verdict + violated check names.
    - Threshold override flags (--ridge-fail, --coverage-floor, --c13-tol, --h1-tol) construct a QcConfig (D-04).
    - Import-safe: no module-level lucy_ng.nus import added.
  </behavior>
  <read_first>
    - src/lucy_ng/cli/nus.py (full — mirror params/schedule command skeleton lines 72-143 + reconstruct lines 146-254; deferred-import + --format json + SystemExit convention)
    - 99-PATTERNS.md lines 194-251 (cli/nus.py analog: qc command shape, thin-CLI-over-fat-library)
    - 99-CONTEXT.md D-04 (CLI-overridable thresholds), D-08 (qc runnable against arbitrary peaks-dir)
    - tests/nus/test_cli_pipeline.py (Plan 01 stub — the qc/format assertions to satisfy)
  </read_first>
  <action>
    Add a `qc` command to the `nus` group in cli/nus.py, following the params/schedule skeleton:
    - `@nus.command("qc")`, `@click.argument("peaks_dir", type=click.Path(exists=True))`, threshold override options `--ridge-fail`(float, default None), `--coverage-floor`(float, default None), `--c13-tol`(float, default None), `--h1-tol`(float, default None), plus the standard `--format` option block verbatim.
    - Body: deferred `from lucy_ng.nus.qc import run_qc_checks, QcConfig`; `resolved = Path(peaks_dir).resolve()`; build a QcConfig by starting from `QcConfig.default()` and overriding only the flags the user set (None → keep default); `report = run_qc_checks(resolved, config)`; branch on output_format (`json.dumps(report.to_dict(), indent=2)` vs a text summary printing verdict + `report.violated_checks()`); finally `if report.verdict == QcVerdict.FAIL: raise SystemExit(1)`.
    - Preserve import-safety: the `from lucy_ng.nus.qc import ...` stays INSIDE the command body.
  </action>
  <acceptance_criteria>
    - `python -m lucy_ng.cli.main nus qc tests/fixtures/nus/known_bad_peaks --format json` (or the project's `lucy` entrypoint) prints JSON with `"verdict": "FAIL"` and exits non-zero.
    - Same against `tests/fixtures/nus/clean_peaks_synthetic` prints `"verdict": "PASS"` and exits 0.
    - `lucy nus qc --help` lists --ridge-fail/--coverage-floor/--c13-tol/--h1-tol/--format.
    - Import-safety preserved: `python -c "import lucy_ng.cli.nus"` succeeds with the `[nus]` engine importable, and `grep -nE '^(from|import) lucy_ng\.nus' src/lucy_ng/cli/nus.py` returns 0 lines (all nus imports deferred).
    - `pytest tests/nus/test_cli_pipeline.py -q -k qc` green.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_cli_pipeline.py -q -k qc && ! grep -nE '^(from|import) lucy_ng\.nus' src/lucy_ng/cli/nus.py && python -c "from click.testing import CliRunner; from lucy_ng.cli.nus import nus; r=CliRunner().invoke(nus,['qc','tests/fixtures/nus/known_bad_peaks','--format','json']); assert r.exit_code!=0 and '\"verdict\": \"FAIL\"' in r.output, r.output; c=CliRunner().invoke(nus,['qc','tests/fixtures/nus/clean_peaks_synthetic','--format','json']); assert c.exit_code==0 and 'PASS' in c.output, c.output; print('qc ok')" && mypy src/lucy_ng/cli/nus.py</automated>
  </verify>
  <done>lucy nus qc runs against an arbitrary peaks-dir, --format json + text, exits non-zero on FAIL, thresholds overridable, import-safe; QC-02 CLI proof green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lucy nus pipeline &lt;expdir&gt; + write-boundary enforcement (D-07/QC-03)</name>
  <files>src/lucy_ng/cli/nus.py</files>
  <behavior>
    - pipeline runs reconstruct → bridge_peak_pick (STAGED, verdict-less) → run_qc_checks → bridge_peak_pick (FINAL, verdict-derived) → verdict branch.
    - The QcReport does NOT exist at first-pass bridge time; the payload that is finally written MUST carry the real verdict-derived confidence (D-06) and metadata block (D-05), so the payload is rebuilt with the verdict before any write.
    - PASS/PARTIAL: writes consumable analysis/nmr_peaks/*.json (final payload — reconstruction.qc_verdict == computed verdict, per-peak confidence == verdict-derived); exit 0 (PARTIAL prints a warning).
    - FAIL: writes NOTHING to analysis/nmr_peaks/; writes the (verdict-annotated) peaks + report to analysis/nus_recon/<expN>/qc_failed/; exits non-zero.
    - --format json emits a machine-readable summary (verdict, written paths or quarantine path).
    - Threshold + reconstruction knobs are CLI-overridable; deferred imports preserved.
  </behavior>
  <read_first>
    - src/lucy_ng/cli/nus.py reconstruct command (lines 146-254 — the knob flags + thin-wrapper shape to extend)
    - 99-CONTEXT.md D-07 (write boundary: PASS/PARTIAL write, FAIL quarantine + non-zero exit; case.md NOT touched), D-01 (PARTIAL passes with warning)
    - 99-RESEARCH.md Open Question 1 (keep qc.py pure; write/quarantine branching lives in cli/nus.py::pipeline), Assumptions A4 (qc_failed/ quarantine path)
    - src/lucy_ng/nus/runner.py reconstruct (lines 346-400 — the result contract: processed_spectrum, backend, smile_iterations, stage_dir)
    - tests/nus/test_write_boundary.py (Plan 01 stub — the write/quarantine/exit assertions to satisfy)
  </read_first>
  <action>
    Add a `pipeline` command to the `nus` group:
    - `@nus.command("pipeline")`, `@click.argument("expdir", type=click.Path(exists=True))`, reconstruction knobs mirroring reconstruct (`--iterations`,`--threshold`,`--virtual-echo/--no-virtual-echo`,`--f1-p0`,`--f1-p1`,`--f2-p0`,`--f2-p1`), QC threshold overrides (`--ridge-fail`,`--coverage-floor`,`--c13-tol`,`--h1-tol`), and the standard `--format` block.
    - Body (deferred imports: `from lucy_ng.nus.runner import NusRunner`; `from lucy_ng.nus.params import read_nus_params`; `from lucy_ng.nus.bridge import build_spectrum2d, bridge_peak_pick, write_peak_json`; `from lucy_ng.nus.qc import run_qc_checks, QcConfig`; `from lucy_ng.models.nus import QcVerdict`):
      1. `resolved = Path(expdir).resolve()`; `params = read_nus_params(resolved)`; `result = NusRunner().reconstruct(resolved, max_iter=iterations, threshold=threshold, virtual_echo=virtual_echo, f1_p0=..., f2_p0=...)`.
      2. Determine experiment_type from params (reuse the FnMODE/pulse-program → HSQC/HMBC/COSY mapping; a single-experiment expdir yields one type). Build spectrum via `build_spectrum2d(Path(result.processed_spectrum), params, experiment_type)`.
      3. STAGED (verdict-less) pass: call `bridge_peak_pick(spectrum, experiment=experiment_type, qc_report=None)` and `write_peak_json(<result.stage_dir>/"staged", experiment_type, payload)` for the peaks-only payload; then `report = run_qc_checks(<result.stage_dir>/"staged", config)` on the just-written staged peaks (so the SAME qc code path used by `lucy nus qc` gates the pipeline output — Open Question 2 / D-08 "calls the same qc code"). qc.py stays pure (verdict only, no writes) — all write/quarantine I/O is in this command body per the architecture map.
      4. CAUSAL RE-BUILD (fixes the ordering gap): the QcReport now exists, so build the FINAL payload WITH the verdict before any consumable write. CHOSEN APPROACH (a): re-invoke `bridge_peak_pick(spectrum, experiment=experiment_type, qc_report=report, recon_meta=<from result>)` — peak positions are deterministic for the same `spectrum`, so the re-pick reproduces identical cross-peaks but now stamps the D-05 `reconstruction` block (backend, iterations, `qc_verdict`, `violated_checks`, `thresholds_used`) and the D-06 verdict-derived per-peak `confidence`. Do NOT write the step-3 staged (verdict-less) payload to the consumable location — it exists only to feed `run_qc_checks`. (Alternative (b) — patching the staged dict in place — is acceptable if the executor prefers, but (a) is the plan of record because it reuses the bridge's own D-05/D-06 logic rather than duplicating it in the CLI.)
      5. WRITE BOUNDARY (D-07), acting on the FINAL verdict-annotated payload:
         - PASS or PARTIAL → write the FINAL payload's consumable JSON to `resolved / "analysis" / "nmr_peaks"`. PARTIAL → click.echo a warning to stderr naming the soft violations. exit 0.
         - FAIL → do NOT write to analysis/nmr_peaks/; write the FINAL (verdict-annotated) payload + `report.to_dict()` to `result.stage_dir / "qc_failed"` (mkdir parents); click.echo a clear error naming the critical violations; `raise SystemExit(1)`.
      6. `--format json` → emit `{verdict, written: [...], quarantine: <path or null>, report: report.to_dict()}`.
    - `case.md` and detection/fragments/lsd/ranking/cli/pick.py remain untouched (verify no import of them added beyond read-only reuse).
    - Optional: expose a thin `lucy nus peak-pick <expdir>` (bridge-only, no QC) — planner discretion; if included, follow the same deferred-import + --format json skeleton.
  </action>
  <acceptance_criteria>
    - `pytest tests/nus/test_write_boundary.py tests/nus/test_cli_pipeline.py -q` green.
    - FAIL branch (mocked reconstruct+bridge returning peaks that trip a critical check): nothing written under `<expdir>/analysis/nmr_peaks/`, a `qc_failed/` dir created under the stage dir, command exit code != 0.
    - PASS branch: consumable `analysis/nmr_peaks/*.json` written; exit code 0.
    - **Verdict-content assertion (D-05/D-06, closes the ordering gap):** the WRITTEN consumable file's `reconstruction.qc_verdict` EQUALS the verdict `run_qc_checks` computed for that run (not merely "a reconstruction block exists"); AND at least one written cross-peak's `confidence` EQUALS the verdict-derived value (`_confidence_for(verdict)`: PASS→"high", PARTIAL→"low"). Assert this by loading the file from disk after invocation and comparing to the report verdict, in `test_write_boundary.py` / `test_cli_pipeline.py`.
    - **Ordering assertion:** for a PARTIAL run, a written cross-peak `confidence` is "low" (proving the FINAL verdict-annotated payload was written, NOT the step-3 staged verdict-less one); for a PASS run it is "high".
    - PARTIAL branch: consumable peaks written AND a warning surfaced; exit code 0.
    - `lucy nus pipeline --help` shows reconstruction knobs + QC threshold overrides + --format.
    - Import-safety preserved: `grep -nE '^(from|import) lucy_ng\.nus' src/lucy_ng/cli/nus.py` returns 0.
    - case.md untouched: `git diff --exit-code .claude/commands/lucy-ng/case.md` returns 0; cli/pick.py untouched: `git diff --exit-code src/lucy_ng/cli/pick.py` returns 0.
    - `mypy src/lucy_ng/cli/nus.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_write_boundary.py tests/nus/test_cli_pipeline.py -q && ! grep -nE '^(from|import) lucy_ng\.nus' src/lucy_ng/cli/nus.py && git diff --exit-code src/lucy_ng/cli/pick.py && git diff --exit-code .claude/commands/lucy-ng/case.md && mypy src/lucy_ng/cli/nus.py</automated>
  </verify>
  <done>lucy nus pipeline wires reconstruct→staged-pick→QC→verdict-annotated-rebuild→write in one process; the WRITTEN consumable file carries the real verdict (D-05 reconstruction.qc_verdict) and verdict-derived per-peak confidence (D-06), asserted against the computed report; D-07 write boundary (PASS/PARTIAL write, FAIL quarantine+exit-1) enforced; case.md + cli/pick.py untouched; test_write_boundary.py + test_cli_pipeline.py green; mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `<expdir>`/`<peaks-dir>` CLI argument → filesystem operations | User/attacker-controlled path drives reconstruction, glob, and writes |
| QC verdict → write decision | The single gate deciding whether reconstructed peaks reach the CASE-consumable location (the milestone's crux barrier) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-12 | Integrity (crux) | A FAIL-verdict reconstruction's peaks reach `analysis/nmr_peaks/` and silently become hard LSD constraints | mitigate | D-07 write boundary: FAIL writes ONLY to `qc_failed/` quarantine + exit non-zero; Task 2 acceptance asserts nothing written under analysis/nmr_peaks/ on FAIL |
| T-99-13 | Elevation/Tampering | Quarantine or output path escaping the experiment dir (path traversal via crafted expdir) | mitigate | `Path(expdir).resolve()`; writes only under the resolved expdir's `analysis/` subtree and `result.stage_dir`; no string-interpolated paths |
| T-99-14 | Repudiation/Integrity | Second gate added to case.md drifts from the pipeline gate (two sources of truth) | accept | D-07 deliberately keeps a single pipeline-boundary barrier; case.md untouched (git diff == 0 asserted) — the untouched-case.md invariant is the design choice |
| T-99-01 | Integrity | qc command/pipeline treats a missing/empty peaks dir as clean → false PASS/exit-0 | mitigate | run_qc_checks (Plan 02) never PASSes on insufficient critical reference; pipeline gates on its returned verdict |
| T-99-06 | Tampering/Info-Disclosure | `<peaks-dir>` glob over arbitrary attacker paths | mitigate | `Path(peaks_dir).resolve()` before run_qc_checks; no shell=True; no new subprocess in cli additions |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; nothing to verify |
</threat_model>

<verification>
- `pytest tests/nus/test_write_boundary.py tests/nus/test_cli_pipeline.py -q` green.
- QC-02 CLI proof: `lucy nus qc known-bad → FAIL/exit≠0`, `clean → PASS/exit 0`.
- D-07 enforcement: FAIL quarantines + exits non-zero; PASS/PARTIAL write consumable peaks.
- case.md + cli/pick.py byte-unchanged; import-safety preserved.
- `pytest -q` full suite green; `mypy src/lucy_ng/cli/nus.py` clean.
</verification>

<success_criteria>
- PICK-02: `lucy nus pipeline <expdir>` reusable end-to-end command; every `lucy nus` subcommand supports `--format json`.
- QC-03: handoff refuses to start on FAIL (nothing consumable written, non-zero exit) — FIX-10 spirit extended to reconstruction-derived peaks.
- D-04/D-07/D-08 satisfied; `case.md` and the CASE pipeline unchanged.
</success_criteria>

<output>
Create `.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-04-SUMMARY.md` when done
</output>
