---
phase: 99-peak-pick-bridge-qc-gate-cli
plan: 03
type: execute
wave: 2
depends_on: [99-01]
files_modified:
  - src/lucy_ng/nus/bridge.py
  - src/lucy_ng/processing/edited_sign.py
  - src/lucy_ng/processing/__init__.py
autonomous: true
requirements: [PICK-01, PICK-03]
must_haves:
  truths:
    - "build_spectrum2d() constructs an in-memory Spectrum2D from a processed .ft2 using the ng.pipe idiom, preferring the calibrated F1 ppm sidecar when present"
    - "bridge_peak_pick() calls the existing PeakPicker2D unmodified (direct in-process call, no subprocess, no new picker)"
    - "The bridge transforms picker output into the per-experiment CASE schema (HSQC c13_ppm/h1_ppm/edited_sign/multiplicity_hint/confidence/note; HMBC rel_intensity/rank_in_carbon/suspected_1J_artifact; COSY h1a_ppm/h1b_ppm)"
    - "Each written peak JSON carries an additive top-level reconstruction metadata block (backend, iterations, QC verdict, violated checks, thresholds) and verdict-derived per-peak confidence (D-05/D-06)"
    - "cli/pick.py is byte-unchanged; the shared edited-sign detector lives in a new importable module"
  artifacts:
    - path: "src/lucy_ng/nus/bridge.py"
      provides: "build_spectrum2d + bridge_peak_pick + per-experiment JSON serialization + metadata block"
      exports: ["build_spectrum2d", "bridge_peak_pick"]
      min_lines: 120
    - path: "src/lucy_ng/processing/edited_sign.py"
      provides: "shared detect_multiplicity_edited() importable without touching cli/pick.py"
      contains: "def detect_multiplicity_edited"
  key_links:
    - from: "src/lucy_ng/nus/bridge.py"
      to: "lucy_ng.processing.PeakPicker2D.pick_peaks"
      via: "direct in-process call (mirrors _perform_ranking)"
      pattern: "PeakPicker2D.pick_peaks"
    - from: "src/lucy_ng/nus/bridge.py"
      to: "lucy_ng.processing.edited_sign.detect_multiplicity_edited"
      via: "import shared helper"
      pattern: "detect_multiplicity_edited"
---

<objective>
Build `nus/bridge.py`: the peak-pick bridge that constructs a `Spectrum2D` in memory from Phase-98's processed `.ft2` (mirroring `readers/bruker.py::read_2d()`, swapping `ng.bruker.*` for `ng.pipe.*`), calls the existing `PeakPicker2D.pick_peaks()` directly (mirroring `_perform_ranking()` — no subprocess, no new picker), and serializes the picked peaks into the per-experiment `analysis/nmr_peaks/*.json` schema the CASE `nmr-chemist` agent consumes. Each file carries the D-05 additive top-level reconstruction metadata block and D-06 verdict-derived per-peak confidence. Factor the multiplicity-edited detector into a new shared module so `cli/pick.py` stays byte-unchanged (HARD invariant).

Purpose: PICK-01 (schema-identical peak lists via direct Spectrum2D→PeakPicker2D call) + PICK-03 (reconstruction-quality metadata replacing the blanket `"confidence": "low"`). Independent of the QC engine's logic — the bridge accepts a QcReport/verdict as input; the pipeline (Plan 04) wires them.

Output: src/lucy_ng/nus/bridge.py, src/lucy_ng/processing/edited_sign.py, updated processing/__init__.py — making test_bridge.py + test_bridge_metadata.py GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-CONTEXT.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-RESEARCH.md
@.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-PATTERNS.md

<interfaces>
From src/lucy_ng/models/spectrum.py:
```python
class Spectrum2D(BaseModel):
    data; f1_ppm_scale; f2_ppm_scale  # F1=indirect(13C), F2=direct(1H)
    f1_nucleus; f2_nucleus; experiment_type; frequency; metadata
```
From src/lucy_ng/processing/peak_picker_2d.py:
```python
class PeakPicker2D:
    @staticmethod
    def pick_peaks(spectrum, threshold=None, min_separation=(3,3),
                   detect_negative=False, snr_floor=5.0, use_snr=True) -> PeakList2D
# PeakList2D.peaks: list[Peak2D(f1_position, f2_position, intensity, snr)]  (F1=13C, F2=1H)
```
From src/lucy_ng/cli/pick.py::_detect_multiplicity_edited (lines 22-55) — verbatim logic to REPLICATE in the new shared module (np.min(data) < -0.05*max_abs → (multiplicity_edited: bool, negative_count: int)).
From src/lucy_ng/models/nus.py (Plan 01): QcVerdict, QcReport.
From src/lucy_ng/nus/postprocess.py sidecar keys: processed_ppm_axis.json → {raw_ppm_axis, calibrated_ppm_axis, calibration_offset_ppm, reference_shifts, axis_size, ...}.

Target per-experiment CASE schema (verified on disk):
- HSQC cross_peak: {c13_ppm, h1_ppm, edited_sign, multiplicity_hint, confidence, note}
- HMBC cross_peak: {c13_ppm, h1_ppm, rel_intensity, rank_in_carbon, suspected_1J_artifact, confidence, note}
- COSY cross_peak: {h1a_ppm, h1b_ppm, rel_intensity, confidence, note}
- top-level: {experiment, caveat, n_cross_peaks, cross_peaks, <metadata block>}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared edited-sign helper + build_spectrum2d() from processed .ft2</name>
  <files>src/lucy_ng/processing/edited_sign.py, src/lucy_ng/processing/__init__.py, src/lucy_ng/nus/bridge.py</files>
  <read_first>
    - src/lucy_ng/cli/pick.py lines 22-55 (_detect_multiplicity_edited — the logic to replicate; DO NOT edit this file)
    - src/lucy_ng/readers/bruker.py read_2d (lines ~197-281 — the ng.bruker.guess_udic/uc_from_udic idiom to port to ng.pipe)
    - src/lucy_ng/nus/postprocess.py lines ~306-365 (_write_ppm_calibration_sidecar — the processed_ppm_axis.json keys)
    - src/lucy_ng/processing/__init__.py (existing exports to extend)
    - 99-PATTERNS.md lines 22-95 (bridge.py analog section, F1-axis-override note)
  </read_first>
  <action>
    HARD INVARIANT: do NOT modify src/lucy_ng/cli/pick.py. Instead:
    - Create `src/lucy_ng/processing/edited_sign.py` with a public `detect_multiplicity_edited(data: np.ndarray) -> tuple[bool, int]` replicating cli/pick.py's `_detect_multiplicity_edited` verbatim (empty/all-nonfinite/all-zero → (False,0); cutoff `-0.05*max_abs`; boolean derived from count). Add a module docstring noting it is the importable twin of cli/pick.py's private copy, intentionally duplicated to preserve the "cli/pick.py byte-unchanged" invariant; a future refactor may dedupe once the invariant is relaxed.
    - Export it from `processing/__init__.py` (`__all__` += "detect_multiplicity_edited").
    - Create `src/lucy_ng/nus/bridge.py` (`from __future__ import annotations`; `import json, nmrglue as ng, numpy as np`; `from pathlib import Path`; `from lucy_ng.models import Spectrum2D`).
    - Implement `build_spectrum2d(processed_ft2: Path, params, experiment_type: str) -> Spectrum2D`: `ng.pipe.read(str(processed_ft2))` → `ng.pipe.guess_udic(dic, data)` → `uc_f2 = ng.fileiobase.uc_from_udic(udic, dim=1)`; `f2_ppm_scale = np.array(uc_f2.ppm_scale())`. F1 axis: if `processed_ft2.parent / "processed_ppm_axis.json"` exists, read its `"calibrated_ppm_axis"` as f1_ppm_scale; else `uc_f1 = uc_from_udic(udic, dim=0)`; `f1_ppm_scale = np.array(uc_f1.ppm_scale())`. Build Spectrum2D with f1_nucleus=params.f1_nucleus (13C), f2_nucleus=params.f2_nucleus (1H), experiment_type, frequency=params.f2_sfo1. Wrap the `ng.pipe.read` / sidecar `json.loads` in a typed RuntimeError with a clear message (fail-loud convention, mirroring nus/runner.py::run_stage) rather than propagating a bare exception.
  </action>
  <acceptance_criteria>
    - cli/pick.py is byte-unchanged: `git diff --exit-code src/lucy_ng/cli/pick.py` returns 0.
    - `python -c "from lucy_ng.processing import detect_multiplicity_edited; import numpy as np; assert detect_multiplicity_edited(np.array([[1.0,-0.5],[0.2,0.1]]))[0] is True; assert detect_multiplicity_edited(np.zeros((2,2)))==(False,0)"` succeeds.
    - `detect_multiplicity_edited` output equals cli/pick.py's `_detect_multiplicity_edited` for a shared sample array (parity check in the test).
    - build_spectrum2d prefers `calibrated_ppm_axis` from the sidecar when it exists (test with a synthetic .ft2 + sidecar per test_bridge.py).
    - `mypy src/lucy_ng/nus/bridge.py src/lucy_ng/processing/edited_sign.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code src/lucy_ng/cli/pick.py && python -c "from lucy_ng.processing import detect_multiplicity_edited; import numpy as np; assert detect_multiplicity_edited(np.array([[1.0,-0.5]]))[0] is True; assert detect_multiplicity_edited(np.zeros((2,2)))==(False,0); print('ok')" && mypy src/lucy_ng/nus/bridge.py src/lucy_ng/processing/edited_sign.py</automated>
  </verify>
  <done>Shared detect_multiplicity_edited importable; cli/pick.py untouched; build_spectrum2d reads .ft2 with sidecar F1 override; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bridge_peak_pick() — per-experiment schema serialization + D-05/D-06 metadata</name>
  <files>src/lucy_ng/nus/bridge.py</files>
  <behavior>
    - HSQC output objects have exactly keys {c13_ppm, h1_ppm, edited_sign, multiplicity_hint, confidence, note}; c13_ppm==f1_position, h1_ppm==f2_position.
    - HMBC output objects have {c13_ppm, h1_ppm, rel_intensity, rank_in_carbon, suspected_1J_artifact, confidence, note}; rel_intensity==intensity/max|intensity|.
    - COSY output objects have {h1a_ppm, h1b_ppm, rel_intensity, confidence, note}.
    - Top-level file has {experiment, caveat, n_cross_peaks, cross_peaks, <metadata block>}.
    - Metadata block (e.g. "reconstruction") = {backend, iterations, qc_verdict, violated_checks, thresholds_used} — small, no per-check numeric traces.
    - confidence: PASS→"high" (or "medium"), PARTIAL→"low"; the bridge never writes for a FAIL verdict (that path is Plan 04's quarantine).
  </behavior>
  <read_first>
    - src/lucy_ng/cli/pick.py pick_hsqc/pick_hmbc/pick_2d (lines 205-352 — raw picker output shape to TRANSFORM, not copy)
    - 99-PATTERNS.md lines 69-95 (the CRITICAL per-peak-schema transform note + confidence mapping)
    - 99-CONTEXT.md D-05 (top-level additive block), D-06 (confidence from verdict), "byte-for-byte clarification"
    - 99-RESEARCH.md Pitfall 6 (LLM-only consumer → additive block provably safe; keep block small)
    - tests/nus/test_bridge.py + tests/nus/test_bridge_metadata.py (Plan 01 stubs — assertions to satisfy)
  </read_first>
  <action>
    Add to nus/bridge.py:
    - A small `ReconstructionMeta` dataclass or plain-dict builder capturing backend, iterations, qc_verdict (str), violated_checks (list[str]), thresholds_used (dict). Source backend/iterations from the Phase-98 `NusReconstructionResult` (`.backend`, `.smile_iterations`); qc fields from a `QcReport` passed in.
    - `_confidence_for(verdict: QcVerdict) -> str`: PASS→"high", PARTIAL→"low" (document the PASS→high vs medium choice; pick one, e.g. "high" for PASS). (FAIL never reaches here.)
    - `bridge_peak_pick(spectrum: Spectrum2D, *, experiment: str, qc_report=None, recon_meta=None, threshold=None, snr_floor=5.0) -> dict`: call `PeakPicker2D.pick_peaks(spectrum, use_snr=(threshold is None), threshold=threshold, snr_floor=snr_floor)`; for HSQC also call `detect_multiplicity_edited(spectrum.data)` to gate sign→multiplicity mapping (edited: positive→"CH_or_CH3", negative→"CH2"; not edited → "CH_or_CH2_or_CH3" ambiguous). Transform each Peak2D into the per-experiment dict (c13_ppm=f1_position, h1_ppm=f2_position; rel_intensity=intensity/max|intensity| with a zero-guard; rank_in_carbon computed per c13 group for HMBC; COSY maps f2_position→h1a_ppm and pairs by picker output — for a COSY plane F1 and F2 are both 1H so h1a_ppm=f1_position, h1b_ppm=f2_position). Attach confidence via `_confidence_for` and a `note` describing the real backend/QC state (NOT the home-IST caveat). Build the top-level dict {experiment, caveat (regenerated to reflect the real backend + verdict, or omitted), n_cross_peaks, cross_peaks, "reconstruction": <metadata block>}.
    - `write_peak_json(out_dir: Path, experiment: str, payload: dict) -> Path`: `json.dump` to `out_dir / f"{experiment}_..."`. Keep the write helper here but note the PASS/PARTIAL-vs-FAIL branch is Plan 04's responsibility (bridge only serializes+writes when asked).
  </action>
  <acceptance_criteria>
    - `pytest tests/nus/test_bridge.py tests/nus/test_bridge_metadata.py -q` green.
    - An HSQC payload's cross_peaks[0] has exactly the keys {c13_ppm,h1_ppm,edited_sign,multiplicity_hint,confidence,note} (set equality asserted in test).
    - HMBC cross_peaks carry {c13_ppm,h1_ppm,rel_intensity,rank_in_carbon,suspected_1J_artifact,confidence,note}; COSY carry {h1a_ppm,h1b_ppm,rel_intensity,confidence,note}.
    - Top-level payload contains a "reconstruction" block with keys backend, iterations, qc_verdict, violated_checks, thresholds_used.
    - confidence == "high" when qc_report.verdict==PASS, "low" when PARTIAL.
    - No blanket `"confidence": "low"` hardcoded for a PASS payload: `! grep -n '"confidence": "low"' src/lucy_ng/nus/bridge.py` OR the only occurrence is the PARTIAL branch mapping (verified by test, not grep alone).
    - `mypy src/lucy_ng/nus/bridge.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_bridge.py tests/nus/test_bridge_metadata.py -q && mypy src/lucy_ng/nus/bridge.py</automated>
  </verify>
  <done>bridge_peak_pick emits the three per-experiment schemas with the additive reconstruction metadata block and verdict-derived confidence; test_bridge.py + test_bridge_metadata.py green; mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processed `.ft2` file → in-memory Spectrum2D | Externally-produced (Phase-98 subprocess output) binary read into the picker |
| picked peaks → `analysis/nmr_peaks/*.json` → CASE LLM agent | The emitted JSON becomes the LLM's connectivity evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-08 | Integrity | Malformed/oversized metadata block burns CASE LLM attention or misleads it | mitigate | D-05 block kept small (backend/iterations/verdict/violated-check names/thresholds only — no per-check numeric traces); Task 2 acceptance bounds the keys |
| T-99-09 | Integrity | Bridge emits blanket `"confidence": "low"` regardless of real quality → CASE can't trust differentiated signal | mitigate | confidence derived from QcReport.verdict (D-06); test asserts PASS→high, PARTIAL→low |
| T-99-10 | Tampering | Corrupt/truncated `.ft2` silently yields a garbage Spectrum2D | mitigate | build_spectrum2d wraps ng.pipe.read + sidecar parse in a typed RuntimeError (fail-loud), never a bare traceback |
| T-99-11 | Integrity | Editing cli/pick.py to share the helper changes the frozen CASE picker behaviour | mitigate | New importable module instead; `git diff --exit-code src/lucy_ng/cli/pick.py` == 0 enforced |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; nothing to verify |
</threat_model>

<verification>
- `pytest tests/nus/test_bridge.py tests/nus/test_bridge_metadata.py -q` green.
- cli/pick.py byte-unchanged; shared helper importable and parity-checked.
- `pytest -q` full suite green; `mypy` clean on new files.
</verification>

<success_criteria>
- PICK-01: bridge builds Spectrum2D via ng.pipe idiom + direct PeakPicker2D call; emits the three per-experiment schemas (structurally identical per-peak keys).
- PICK-03: additive top-level reconstruction metadata block + verdict-derived confidence replacing blanket "low".
- HARD invariant preserved: cli/pick.py unchanged; detection/, fragments/, lsd/, ranking/ untouched.
</success_criteria>

<output>
Create `.planning/phases/99-peak-pick-bridge-qc-gate-cli/99-03-SUMMARY.md` when done
</output>
