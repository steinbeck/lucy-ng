---
phase: 100-cross-platform-hardening-end-to-end-validation
plan: 03
type: execute
wave: 2
depends_on: [100-01, 100-02]
files_modified:
  - .planning/phases/100-cross-platform-hardening-end-to-end-validation/VALIDATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [VAL-01, VAL-02]

user_setup:
  - service: NMRPipe + SMILE
    why: "Reconstruction backend for the real C20H32O2 exp2/3/4 NUS data (VAL-01/02); native macOS Apple-Silicon target, not yet installed on this machine (D-01)"
    env_vars: []
    dashboard_config:
      - task: "Download native mac11_arm64 NMRPipe build AND the separate SMILE companion plugin (plugin.smile.tZ), source nmrInit.mac11_arm64.com, add bin/ to PATH"
        location: "https://www.ibbr.umd.edu/nmrpipe/install (see docs/NUS-PORTABILITY.md)"

must_haves:
  truths:
    - "lucy nus check reports the NMRPipe+SMILE backend available AND the platform section clear of critical issues (exit 0) before the VAL run"
    - "C20H32O2 exp2/exp3/exp4 are reconstructed end-to-end via lucy nus pipeline and QC-graded"
    - "Each experiment's §8/QC verdict is recorded: PASS, or soft-only PARTIAL with a chemist confirmation line, or (after the D-04 tuning budget) a documented critical-FAIL limitation"
    - "A fresh /lucy-ng:case C20H32O2 on the newly reconstructed peaks is observed: LSD terminates (no ~10^6 explosion) and produces a finite, rankable candidate set (VAL-02 bar, D-03)"
    - "The known-bad QC-02 regression fixtures at tests/fixtures/nus/known_bad_peaks/ are byte-unchanged; the external root known-bad peak lists are archived before any overwrite"
    - "case.md and the 5-agent team stay byte-unchanged (git-diff-clean) — VAL-02 runs the orchestrator unmodified"
  artifacts:
    - path: ".planning/phases/100-cross-platform-hardening-end-to-end-validation/VALIDATION.md"
      provides: "Per-experiment §8/QC verdict (VAL-01) + CASE convergence result (VAL-02) + any documented limitation naming RECON-F1"
      contains: "VAL-01"
  key_links:
    - from: "lucy nus pipeline <expdir>"
      to: "lucy nus qc (PASS/PARTIAL/FAIL)"
      via: "the Phase-99 QC gate as the §8 machine judge (D-02)"
      pattern: "qc"
---

<objective>
The milestone-closing empirical proof. With NMRPipe+SMILE installed locally (native
Apple-Silicon, D-01), reconstruct the real C20H32O2 exp2 (COSY) / exp3 (HSQC) / exp4 (HMBC) via
the existing `lucy nus pipeline`, grade each against §8 through the existing Phase-99 QC gate
(VAL-01, D-02), then run a fresh unmodified `/lucy-ng:case C20H32O2` on the newly reconstructed
peaks and observe whether LSD terminates on a finite, rankable set (VAL-02, D-03 — the milestone's
actual success bar). Record everything in `VALIDATION.md`.

Purpose: prove the reconstruction fixed the connectivity gap that timed out the original
2026-07-09 run at ~10^6 candidates. No new code — this exercises the already-built Phase 97-99
pipeline and the untouched CASE orchestrator, and honestly records the result (D-04: a documented
backend limitation is a valid outcome; PORT already shipped in Plans 01/02).
Output: `VALIDATION.md` + committed real reconstructed peak JSONs + QC reports; a ROADMAP
limitation note only if the tuning budget is exhausted on a critical FAIL.

NOTE: this plan is `autonomous: false` — it interlocks a human install, a conditional chemist
visual confirm, and a multi-hour agentic CASE run. Do NOT auto-advance past its checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-cross-platform-hardening-end-to-end-validation/100-CONTEXT.md
@.planning/phases/100-cross-platform-hardening-end-to-end-validation/100-RESEARCH.md
@.planning/phases/100-cross-platform-hardening-end-to-end-validation/100-PATTERNS.md
@docs/NUS-PORTABILITY.md

<data_facts>
<!-- Verified external data layout (2026-07-18). -->
- Data root: /Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2/
- Raw Bruker NUS experiment dirs (each has ser + nuslist + acqus + acqu2s + pdata):
    <root>/2  = exp2 COSY   (FnMODE 1, QF/magnitude)
    <root>/3  = exp3 HSQC   (FnMODE 6, echo-antiecho)
    <root>/4  = exp4 HMBC   (FnMODE 6, echo-antiecho)
- The AUTHORITATIVE §8 quality gate + §10 ground-truth 1D shifts:
    <root>/analysis/NUS-RECONSTRUCTION-GUIDE.md  §8 and §10
- KNOWN-BAD home-IST peak lists (QC-02 FAIL regression floor) at the CONSUMABLE root location:
    <root>/analysis/nmr_peaks/{COSY_exp2,HSQC_exp3,HMBC_exp4}.json   ← archive before overwrite
- TRUSTED 1D reference the QC gate cross-checks against (DO NOT touch/overwrite):
    <root>/analysis/nmr_peaks/{13C_exp7_wide,13C_exp6_narrow,1H_exp1}.json   (+ NOESY_exp5.json)
- The repo regression fixture (SAFE — the automated suite reads ONLY this, never the external root):
    tests/fixtures/nus/known_bad_peaks/*.json   ← MUST stay byte-unchanged (RESEARCH Pitfall 5)

§8 essentials (RESEARCH VAL-01): ~17 protonated carbons each with one HSQC correlation (CH2 = two),
the 5 quaternaries 142.00 / 135.86 / 79.35 / 36.23 / 37.86 with NO 1-bond HSQC correlation, clean
edited signs, ridge-free HMBC with sharp gem-dimethyl correlations, a real aliphatic COSY network
(not just the OH-ridge at 5.32), signal-to-ridge better than the current home-IST lists. These are
exactly what the six nus/qc.py checks already encode — grade with the QC gate, do NOT invent a
seventh ad-hoc criterion (RESEARCH anti-pattern).
</data_facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install NMRPipe + SMILE locally, confirm lucy nus check green</name>
  <read_first>
    - docs/NUS-PORTABILITY.md (the macOS-arm64 install walkthrough written in Plan 02, incl. the separate SMILE-plugin step)
    - CLAUDE.md § Local prerequisites (the NMRPipe+SMILE bullet from Plan 02)
    - .planning/phases/100-.../100-RESEARCH.md § "VAL: NMRPipe+SMILE Install" facts, § "Common Pitfall 2" (SMILE separate download), § "Open Question 3" (X11/XQuartz only if a binary fails to launch)
  </read_first>
  <what-built>
    Plans 01/02 shipped the PORT-01 preflight (`lucy nus check` now reports the platform section and
    hard-blocks reconstruct/pipeline on critical gaps) and the `docs/NUS-PORTABILITY.md` install
    walkthrough. Nothing about the reconstruction backend itself is code in this repo — NMRPipe+SMILE
    is a manually installed external binary (D-01: install is not a code deliverable; it is
    registration-adjacent, edits .cshrc, and may require a logout/login — not automatable in an agent
    session, RESEARCH anti-pattern).
  </what-built>
  <how-to-verify>
    1. Follow docs/NUS-PORTABILITY.md: install the native `mac11_arm64` NMRPipe build, THEN separately
       install the SMILE companion plugin (`plugin.smile.tZ`), source `nmrInit.mac11_arm64.com`, add
       `bin/` to PATH (open a fresh shell / re-login so the sourced env takes effect).
    2. Run: `lucy nus check --format json`
    3. Expect: exit code 0; `status` == "available"; `smile_available` == true; `missing_tools` == [];
       the `platform` object shows `arch` "arm64" and `critical_platform_issues` == [].
    4. If `status` == "smile_plugin_missing": the base NMRPipe installed but the SEPARATE SMILE plugin
       was not — go back and install `plugin.smile.tZ` (RESEARCH Pitfall 2). If a binary fails to launch
       with an X11-library error, install XQuartz and retry (RESEARCH Open Question 3), even though
       nmrDraw itself is never used.
  </how-to-verify>
  <action>Human-only manual install (D-01 — not automatable; do NOT write an install script, RESEARCH anti-pattern). Guide the user through docs/NUS-PORTABILITY.md: install the native `mac11_arm64` NMRPipe build AND the SEPARATE `plugin.smile.tZ` SMILE companion, source `nmrInit.mac11_arm64.com`, add `bin/` to PATH in a fresh shell. Then run `lucy nus check --format json` and confirm `status`=="available", `smile_available`==true, `missing_tools`==[], `platform.critical_platform_issues`==[]. On `smile_plugin_missing` install the separate SMILE plugin (Pitfall 2); on an X11-library error install XQuartz and retry (Open Question 3).</action>
  <verify><human-check>`lucy nus check --format json` exits 0 with status "available", smile_available true, and platform.critical_platform_issues empty.</human-check></verify>
  <done>lucy nus check reports the NMRPipe+SMILE backend available and the platform section clear of critical issues; nmrPipe/bruk2pipe/nusExpand.tcl resolve and `nmrPipe -fn SMILE -help` does not report "unknown function".</done>
  <acceptance_criteria>
    - `lucy nus check --format json` exits 0 with `status":"available"`, `smile_available":true`, and `platform.critical_platform_issues` empty
    - `which nmrPipe bruk2pipe nusExpand.tcl` all resolve; `nmrPipe -fn SMILE -help` does not report "unknown function"
  </acceptance_criteria>
  <resume-signal>Type "installed" once `lucy nus check` reports the backend available with the platform section clear; or describe the failure (e.g. smile_plugin_missing, X11 error).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reconstruct exp2/3/4, grade against §8 via the QC gate, run the D-04 tuning budget if needed, commit evidence (VAL-01)</name>
  <read_first>
    - src/lucy_ng/cli/nus.py (the pipeline command body, lines ~367-613 — its write target under <expdir>/analysis and its D-07 FAIL-quarantine/exit-1 boundary; the qc command; the new --n-sigma flag from Plan 01)
    - <root>/analysis/NUS-RECONSTRUCTION-GUIDE.md §8 + §10 (the authoritative §8 gate + ground-truth shifts)
    - src/lucy_ng/nus/qc.py (run_qc_checks() PASS/PARTIAL/FAIL semantics; critical vs soft split — critical: quaternary_exclusion, ppm calibration, signal_to_ridge, hsqc_coverage; soft: edited_sign_consistency, cosy_diagonal_symmetry, per D-02)
    - .planning/phases/100-.../100-CONTEXT.md D-02/D-03/D-04 (acceptance thresholds + tuning budget + honest-stop)
    - .planning/phases/100-.../100-RESEARCH.md § "Pitfall 5" (known-bad fixture non-collision) + § "Pitfall 6" (chemist-confirm is a VALIDATION.md doc step, NOT new code/enum/flag)
  </read_first>
  <what-built>
    Claude drives the already-built pipeline (no new code). Steps Claude performs before the human
    verification:
    1. ARCHIVE (project-history preservation, RESEARCH Pitfall 5): copy — do NOT move — the three
       external root known-bad lists <root>/analysis/nmr_peaks/{COSY_exp2,HSQC_exp3,HMBC_exp4}.json to
       an explicit archive dir (e.g. <root>/analysis/nmr_peaks/known_bad_home_ist_archive/). Do NOT
       touch <root>/analysis/nmr_peaks/{13C_*,1H_exp1,NOESY_exp5}.json (trusted 1D reference). Do NOT
       touch tests/fixtures/nus/known_bad_peaks/ at all (repo regression floor — must stay byte-identical).
    2. RECONSTRUCT: run `lucy nus pipeline <root>/2 --format json`, `<root>/3`, `<root>/4` (COSY/HSQC/HMBC).
       Each runs params → schedule → reconstruct → process → peak-pick → QC → D-07 write/quarantine.
    3. GRADE: capture each experiment's QC verdict (the machine §8 judge, D-02) via the pipeline output
       and/or `lucy nus qc` on the produced peaks against the trusted 1D reference.
    4. TUNING BUDGET (D-04) — apply ONLY to an experiment whose baseline yields a CRITICAL QC FAIL, as a
       BOUNDED, pre-defined sweep, max 8 pipeline invocations for that experiment, in this order, stopping
       at the first PASS/soft-only-PARTIAL: (a) baseline defaults; (b) --threshold 0.9; (c) --threshold 0.95;
       (d) --n-sigma 4; (e) --n-sigma 3; (f) --no-virtual-echo (echo-antiecho HSQC/HMBC only) or toggle
       virtual-echo; (g) F1 phase sweep --f1-p0 0 then --f1-p0 180 (Phase-98 PROVISIONAL default); (h) the
       higher 33% sampling-density schedule variant if one exists for that experiment. After the 8-attempt
       cap with persistent critical FAIL → HONEST STOP for that experiment (do NOT drift further; do NOT
       pull in hmsIST/RECON-F1 — deferred, D-04).
    5. CONSOLIDATE: place the accepted reconstructed COSY_exp2/HSQC_exp3/HMBC_exp4 peak lists into the
       CONSUMABLE root <root>/analysis/nmr_peaks/ (overwriting the now-archived known-bad ones) so the
       VAL-02 CASE run consumes the NEW peaks. Leave the trusted 1D files in place.
    6. WRITE VALIDATION.md: create
       .planning/phases/100-.../VALIDATION.md with one `##` section per experiment (exp2/exp3/exp4)
       listing the §8 checklist outcome + QC verdict + which knob settings produced it + a link/path to the
       committed peak JSON; commit the real reconstructed peak JSONs + QC reports as durable evidence.
  </what-built>
  <how-to-verify>
    HUMAN (chemist) verification — required ONLY when an experiment lands PARTIAL with soft-only
    violations (D-02; RESEARCH Pitfall 6 — this is a one-off VALIDATION.md doc step, NOT a new CLI flag
    or QcVerdict state):
    1. Open the reconstructed HSQC/HMBC/COSY peak lists and §8 of NUS-RECONSTRUCTION-GUIDE.md side by side.
    2. Confirm per §8: ~17 protonated carbons each with a 1-bond HSQC correlation (CH2 = two); NONE of the
       5 quaternaries (142.00/135.86/79.35/36.23/37.86) show a 1-bond HSQC correlation; edited signs are
       consistent; HMBC is ridge-free with sharp gem-dimethyl correlations; the COSY shows a real aliphatic
       network beyond the OH ridge at 5.32.
    3. Record "chemist confirmed: usable for CASE" (or "not confirmed") as a line in VALIDATION.md for that
       experiment.
    A PASS verdict needs no human step. A CRITICAL FAIL after the tuning budget = documented limitation
    (record it in VALIDATION.md; Task 3 mirrors it into ROADMAP + names RECON-F1).
  </how-to-verify>
  <action>Claude drives the existing pipeline — no new code. Execute the six numbered steps in <what-built>: archive-copy the three external root known-bad lists (COSY_exp2/HSQC_exp3/HMBC_exp4) to a `known_bad_home_ist_archive/` dir; run `lucy nus pipeline <root>/2`, `<root>/3`, `<root>/4 --format json`; grade each via the emitted QC verdict / `lucy nus qc`; on a CRITICAL FAIL apply the bounded 8-attempt D-04 knob sweep (threshold 0.9/0.95 → n-sigma 4/3 → toggle virtual-echo → f1-p0 0/180 → 33% density) stopping at the first PASS/soft-PARTIAL, else HONEST STOP; consolidate accepted peak lists into `<root>/analysis/nmr_peaks/`; write VALIDATION.md with a per-experiment §8/QC section. Never touch the trusted 1D files or `tests/fixtures/nus/known_bad_peaks/`. Do NOT add a `--chemist-confirmed` flag or a new QcVerdict state (Pitfall 6 — the confirm is a VALIDATION.md line, not code).</action>
  <verify><human-check>VALIDATION.md records a §8/QC verdict for exp2/exp3/exp4 (PASS / soft-only PARTIAL + chemist-confirm line / documented critical-FAIL after the 8-attempt budget); `git status --porcelain tests/fixtures/nus/known_bad_peaks/` is empty.</human-check></verify>
  <done>VALIDATION.md records a §8 verdict for all three experiments, evidence (real peak JSONs + QC reports) committed, the external known-bad lists archived, the trusted 1D files and the repo regression fixtures byte-unchanged.</done>
  <acceptance_criteria>
    - `lucy nus pipeline <root>/2`, `<root>/3`, `<root>/4` each ran to completion (or produced a QC-FAIL quarantine + exit≠0 that was then addressed via the D-04 budget)
    - Each experiment has a recorded verdict in VALIDATION.md: PASS, or soft-only PARTIAL + a chemist-confirmation line, or a critical-FAIL documented limitation after the 8-attempt budget
    - `git status --porcelain tests/fixtures/nus/known_bad_peaks/` is EMPTY (regression floor byte-unchanged)
    - The three external root known-bad lists were archived (copied) before overwrite; the trusted 1D files (13C_*, 1H_exp1) are unchanged
    - Real reconstructed peak JSONs + QC reports committed as evidence alongside VALIDATION.md
  </acceptance_criteria>
  <resume-signal>Type "approved" once VALIDATION.md records a §8 verdict for all three experiments (PASS / soft-PARTIAL+confirm / documented critical-FAIL); or describe what to re-run.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fresh /lucy-ng:case C20H32O2 convergence run + record VAL-02 (and any documented limitation)</name>
  <read_first>
    - .claude/commands/lucy-ng/case.md (READ-ONLY — confirm it is byte-unchanged; VAL-02 runs it unmodified, the milestone's "CASE pipeline unchanged" invariant)
    - .planning/phases/100-.../100-CONTEXT.md D-03 (VAL-02 bar = LSD terminates + finite rankable set; correct structure top-ranked is a BONUS, not a condition)
    - .planning/phases/100-.../VALIDATION.md (the VAL-01 verdicts written in Task 2)
    - .planning/REQUIREMENTS.md (RECON-F1 — the deferred hmsIST fallback named as the tracked next step if VAL critically failed)
    - src/lucy_ng/lsd/runner.py (LSDRunner timeout behavior — "converges" is OBSERVED, not instrumented; RESEARCH Don't-Hand-Roll)
  </read_first>
  <what-built>
    VAL-01 produced the newly reconstructed, QC-graded peak lists in the consumable root
    <root>/analysis/nmr_peaks/. This task runs the real, UNMODIFIED CASE orchestrator against them to
    prove the reconstruction fixed the connectivity gap that exploded the original 2026-07-09 run to
    ~10^6 candidates. Claude launches the run; no code is added or instrumented (D-03: observe the
    existing machinery — case.md + LSDRunner + ranking — not a custom harness, RESEARCH Don't-Hand-Roll).
  </what-built>
  <how-to-verify>
    1. Confirm `git diff --stat .claude/commands/lucy-ng/ .claude/agents/` is EMPTY (case.md + team byte-unchanged).
    2. Run a fresh `/lucy-ng:case C20H32O2` on the newly reconstructed peak lists.
    3. Observe the VAL-02 bar (D-03): LSD RUNS TO COMPLETION (no ~10^6 timeout/explosion — the original
       failure mode) AND produces a FINITE, 13C-prediction-rankable candidate set
       (analysis/final_results.md written with a bounded candidate count). The correct C20H32O2 structure
       ranking top-N is a BONUS, note it if it happens, but it is NOT the pass condition.
    4. Record in VALIDATION.md a `## CASE Convergence (VAL-02)` section: did LSD terminate, the candidate-set
       size, run wall-clock, and (bonus) where the correct structure ranked if determinable.
    5. IF VAL-01 ended in a critical FAIL after the D-04 budget (no usable peaks): record the honest
       limitation in VALIDATION.md AND add a note to .planning/ROADMAP.md Phase 100 naming RECON-F1
       (hmsIST/mddnmr fallback) as the tracked next step (D-04). PORT (Plans 01/02) still shipped —
       the phase delivers regardless.
  </how-to-verify>
  <action>Confirm `git diff --stat .claude/commands/lucy-ng/ .claude/agents/` is empty (case.md + 5-agent team byte-unchanged), then run a fresh `/lucy-ng:case C20H32O2` on the consolidated reconstructed peaks in `<root>/analysis/nmr_peaks/`. Observe (do NOT instrument — D-03/RESEARCH Don't-Hand-Roll) whether LSDRunner terminates without the ~10^6 explosion and `analysis/final_results.md` lands a finite rankable candidate set. Write a `## CASE Convergence (VAL-02)` section into VALIDATION.md (LSD terminated y/n, candidate-set size, wall-clock, bonus rank of the correct structure). If VAL-01 ended in a post-budget critical FAIL, add a limitation note to `.planning/ROADMAP.md` Phase 100 naming RECON-F1 as the tracked next step.</action>
  <verify><human-check>VALIDATION.md `## CASE Convergence (VAL-02)` records LSD termination + candidate-set size (finite rankable set, or a documented limitation naming RECON-F1); `git diff --stat .claude/commands/lucy-ng/ .claude/agents/` is empty.</human-check></verify>
  <done>VAL-02 outcome recorded in VALIDATION.md (finite rankable set, or an honest documented limitation naming RECON-F1); case.md and the 5-agent team remain byte-unchanged.</done>
  <acceptance_criteria>
    - `git diff --stat .claude/commands/lucy-ng/ .claude/agents/` is empty (case.md + 5-agent team byte-unchanged)
    - VALIDATION.md `## CASE Convergence (VAL-02)` section records: LSD terminated (yes/no), candidate-set size (finite/explosion), and the run outcome
    - If VAL-02 met: LSD terminated on a finite rankable set (no ~10^6 explosion). If not met after VAL-01 critical FAIL: VALIDATION.md + ROADMAP document the limitation and name RECON-F1
  </acceptance_criteria>
  <resume-signal>Type "approved" once VALIDATION.md records the CASE convergence outcome (finite rankable set, or a documented limitation naming RECON-F1); or describe what to re-run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI (`lucy nus pipeline <expdir>`) → NMRPipe/SMILE/csh subprocess chain | developer-supplied experiment paths reach subprocess argv; external binaries execute csh-shebanged scripts |
| pipeline → filesystem (peak JSONs, quarantine, archive under the data tree) | file writes into the external data tree and the repo evidence dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05 | Tampering | `lucy nus pipeline` subprocess invocation of NMRPipe/SMILE/csh | mitigate | No new code — reuses the Phase-98 RECON-04 fail-loud `run_stage()` wrapper (fixed `list[str]` argv, no `shell=True`, exit-code + non-empty-output check); PORT-01 preflight (Plan 01) additionally hard-blocks before dispatch on a critical gap |
| T-100-06 | Tampering (data integrity) | overwrite of the known-bad regression fixtures / trusted 1D reference | mitigate | Archive-copy (never move) the external root known-bad lists before overwrite; NEVER touch `tests/fixtures/nus/known_bad_peaks/` (verified byte-unchanged via `git status`); leave the trusted 1D (13C/1H) files untouched (RESEARCH Pitfall 5) |
| T-100-07 | Elevation of Privilege | manually installed third-party NMRPipe binary from ibbr.umd.edu | transfer | Developer-performed manual install from the canonical vendor URL (documented, Plan 02); out of automation scope (D-01); lucy-ng only invokes the named CLI tools via fixed argv |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | None introduced — this plan runs existing CLI commands and writes docs/evidence. No package-manager surface; RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `lucy nus check` green (backend available + platform clear) gates the VAL run (Task 1).
- All three experiments graded against §8 via the QC gate; verdicts in VALIDATION.md (Task 2).
- Fresh unmodified `/lucy-ng:case C20H32O2` observed for convergence; VAL-02 recorded (Task 3).
- `tests/fixtures/nus/known_bad_peaks/` and `.claude/commands/lucy-ng/`+`.claude/agents/` byte-unchanged.
- NOTE: VAL-01/02 are backend-gated/manual (not CI) — their evidence (VALIDATION.md + committed peak JSONs/QC reports) IS the phase's milestone-closing proof, per D-01/D-02/D-03.
</verification>

<success_criteria>
- VAL-01: exp2/3/4 reconstructed and §8-graded (PASS / soft-only PARTIAL+chemist-confirm / documented critical-FAIL after the bounded D-04 budget).
- VAL-02: fresh `/lucy-ng:case C20H32O2` observed — LSD terminates on a finite rankable set (or an honest documented limitation naming RECON-F1).
- Regression floor + CASE skill untouched.
</success_criteria>

<output>
Create `.planning/phases/100-cross-platform-hardening-end-to-end-validation/100-03-SUMMARY.md` when done, and the durable evidence artifact `.planning/phases/100-cross-platform-hardening-end-to-end-validation/VALIDATION.md`.
</output>
