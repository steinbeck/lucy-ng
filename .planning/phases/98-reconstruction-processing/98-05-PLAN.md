---
phase: 98-reconstruction-processing
plan: 05
type: execute
wave: 4
depends_on: [98-02, 98-03, 98-04]
files_modified:
  - src/lucy_ng/nus/runner.py
  - tests/nus/test_reconstruct_orchestration.py
  - tests/nus/test_reconstruct_integration.py
autonomous: true
requirements: [RECON-01, RECON-02]

must_haves:
  truths:
    - "NusRunner.reconstruct(expdir) reads params/schedule once (never re-parses), owns the analysis/nus_recon/<expN>/ stage dir, calls backend.reconstruct() then postprocess, and returns a NusReconstructionResult"
    - "The F2-before-F1 processing plan is resolved as a hard precondition that RAISES before any subprocess is dispatched when unresolved (RECON-02 ordering gate)"
    - "A single backend+external-data-gated skipif integration test drives the real chain and SKIPS (not fails) when NMRPipe/SMILE or the external C20H32O2 data is absent"
  artifacts:
    - path: "src/lucy_ng/nus/runner.py"
      provides: "NusRunner class: reconstruct() orchestration + _resolve_f2_plan + _stage_dir"
      contains: "class NusRunner"
  key_links:
    - from: "src/lucy_ng/nus/runner.py"
      to: "lucy_ng.nus.params.read_nus_params / lucy_ng.nus.schedule.read_nus_schedule"
      via: "single parse at top of reconstruct() — never a second parse pass"
      pattern: "read_nus_params|read_nus_schedule"
    - from: "src/lucy_ng/nus/runner.py"
      to: "NmrPipeSmileBackend.reconstruct + postprocess.process_direct_then_indirect"
      via: "orchestration dispatch after the F2 gate"
      pattern: "reconstruct|process_direct_then_indirect"
---

<objective>
Assemble the top-level `NusRunner.reconstruct(expdir)` orchestration in `nus/runner.py`: resolve
params/schedule once, own the `analysis/nus_recon/<expN>/` stage-dir lifecycle (D-03 keep), enforce
the F2-before-F1 ordering gate as a hard precondition that raises BEFORE any subprocess runs
(RECON-02), dispatch `backend.reconstruct()` (Plan 03) then `postprocess.process_direct_then_indirect()`
(Plan 04), and return a `NusReconstructionResult`. Also fill in the one backend+external-data-gated
skipif integration test (D-04) that drives the real chain end-to-end.

Purpose: RECON-01 (the whole automatic chain from one entrypoint) + RECON-02 (F2-before-F1 hard
gate). Output: `NusRunner` in nus/runner.py + the real integration test body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-RESEARCH.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md
@src/lucy_ng/nus/runner.py

<interfaces>
From src/lucy_ng/nus/runner.py (Plan 02, same file — extend):
  run_stage(...), _ordering_for_fnmode(...), recipe_for_fnmode(...)

From src/lucy_ng/nus/backends/nmrpipe_smile.py (Plan 03):
  NmrPipeSmileBackend.reconstruct(self, expdir, params, schedule, stage_dir, *, max_iter, threshold, n_sigma, virtual_echo, f1_p0, f1_p1, timeout) -> dict[str, Path]

From src/lucy_ng/nus/postprocess.py (Plan 04):
  process_direct_then_indirect(reconstructed_fid, stage_dir, params, *, f2_p0, f2_p1, f1_p0, f1_p1, magnitude, timeout) -> Path

From src/lucy_ng/nus/params.py / schedule.py (Phase 97):
  read_nus_params(expdir) -> NusAcquisitionParams; read_nus_schedule(expdir) -> NusSchedule

From src/lucy_ng/models/nus.py (Plan 02): NusReconstructionResult
From src/lucy_ng/nus/backends/__init__.py (Phase 97): get_backend(name="nmrpipe_smile")

Integration-test skipif (98-PATTERNS.md § Test Patterns): gate on
  NmrPipeSmileBackend.is_available() AND external data path (env LUCY_NUS_TEST_DATA, default the C20H32O2 external dir) existing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NusRunner.reconstruct() orchestration + F2-before-F1 hard gate</name>
  <files>src/lucy_ng/nus/runner.py, tests/nus/test_reconstruct_orchestration.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Pattern 1 (F2-before-F1 explicit precondition) and § System Architecture Diagram (steps 1-7 of NusRunner.reconstruct)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "src/lucy_ng/nus/runner.py" (F2-gate raise, get_backend resolution instead of re-implementing discovery, analysis/nus_recon/<expN>/ lifecycle KEEP)
    - src/lucy_ng/lsd/runner.py lines 131-227 (LSDRunner __init__/run output_dir handling; but D-03 = keep, no rmtree)
    - src/lucy_ng/nus/runner.py (Plan 02 helpers already present in this file)
    - tests/nus/test_reconstruct_orchestration.py (Plan-01 stubs) + tests/nus/conftest.py (mock_run_stage)
  </read_first>
  <behavior>
    - reconstruct() calls read_nus_params + read_nus_schedule exactly once each
    - When the F2 processing plan cannot be resolved (mocked _resolve_f2_plan returns None), reconstruct() raises RuntimeError BEFORE any subprocess.run / run_stage / backend.reconstruct is called
    - On the happy path (backend + postprocess mocked), reconstruct() returns a NusReconstructionResult with success=True, stage_dir set to analysis/nus_recon/<expN>/, stage_outputs populated, processed_spectrum set
    - The stage dir is created and NOT deleted after the run
  </behavior>
  <action>
    Add `class NusRunner` to `src/lucy_ng/nus/runner.py`. `__init__(self, backend=None)` resolves the
    backend via `get_backend()` (deferred import; do NOT re-implement binary discovery — Phase-97
    NmrPipeSmileBackend already owns it). Add `_stage_dir(self, expdir) -> Path` returning
    `Path(expdir).resolve() / "analysis" / "nus_recon" / <expN>` and `mkdir(parents=True,
    exist_ok=True)` it (KEEP — no rmtree, D-03). Add `_resolve_f2_plan(self, params) ->
    F2Plan | None` returning the resolved direct-dimension phase/apod plan (deterministic constants,
    magnitude flag from the FnMODE recipe), or None if unresolvable. Implement `reconstruct(self,
    expdir, *, max_iter=500, threshold=0.8, n_sigma=5, virtual_echo=True, f1_p0=90.0, f1_p1=0.0,
    f2_p0, f2_p1, timeout=600) -> NusReconstructionResult` following the 98-RESEARCH.md architecture
    diagram steps 1-7: (1) read params/schedule once; (2) compute stage_dir; (3) HARD GATE — call
    `_resolve_f2_plan(params)`; if it returns None, `raise RuntimeError("F2 (direct-dimension)
    processing plan not resolved — refusing to start F1/SMILE reconstruction out of order (RECON-02
    hard gate).")` BEFORE any subprocess dispatch; (4) call `self.backend.reconstruct(...)`; (5) call
    `process_direct_then_indirect(...)` with the resolved F2/F1 phase + magnitude; (6) assemble and
    return NusReconstructionResult. Then implement the RECON-01/02 orchestration stubs against
    mocks — critically, assert that when `_resolve_f2_plan` is monkeypatched to return None, NO
    `subprocess.run`/`run_stage`/`backend.reconstruct` is called before the raise (reuse the
    `_fail_if_called` trick from conftest / tests/test_nus_backends.py).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/runner.py` contains `class NusRunner` and `def _resolve_f2_plan(`
    - `grep -c 'read_nus_params' src/lucy_ng/nus/runner.py` and `grep -c 'read_nus_schedule' src/lucy_ng/nus/runner.py` each return 1 (single parse, never a second pass)
    - `pytest tests/nus/test_reconstruct_orchestration.py -q` passes test_f2_before_f1_gate_raises_before_any_subprocess and test_reconstruct_returns_result_with_stage_paths (0 skipped)
    - The ordering-gate test proves the RuntimeError is raised with NO subprocess/backend call preceding it
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_orchestration.py -q</automated>
  </verify>
  <done>NusRunner.reconstruct orchestrates params→schedule→backend→postprocess with a single parse, keeps the stage dir, and enforces the F2-before-F1 gate as a raise-before-any-subprocess precondition; RECON-01/02 orchestration tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Real end-to-end skipif integration test (D-04)</name>
  <files>tests/nus/test_reconstruct_integration.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "Backend-gated real integration test" (the combined is_available() OR not data.exists() skipif condition)
    - .planning/phases/98-reconstruction-processing/98-VALIDATION.md § Manual-Only Verifications
    - tests/test_hmbc_peak_picking_integrity.py (external-path skipif precedent)
    - tests/nus/test_reconstruct_integration.py (the Plan-01 skipif stub to fill)
  </read_first>
  <action>
    Fill in the single skipif-guarded integration test body: resolve the external data dir from
    env `LUCY_NUS_TEST_DATA` (default the C20H32O2 external path), instantiate `NusRunner()`, call
    `reconstruct()` on exp3 (HSQC, echo-antiecho — the fully-worked high-confidence branch), and
    assert the returned NusReconstructionResult.success is True and the processed `.ft2` exists and
    is non-empty. Keep the `@pytest.mark.skipif(not NmrPipeSmileBackend.is_available() or not
    <data>.exists(), reason=...)` guard so it SKIPS cleanly in CI (no NMRPipe on the dev machine).
    Do NOT copy any `ser` binary into the repo. Add a comment that reconstruction-QUALITY judgement
    (fabricated-peak / ridge) is the Phase-99 QC gate + Phase-100 §8 validation, not this test.
  </action>
  <acceptance_criteria>
    - `tests/nus/test_reconstruct_integration.py` contains a `skipif` guard referencing `is_available()` and the external data path
    - `pytest tests/nus/test_reconstruct_integration.py -q` reports SKIPPED (not failed, not passed) on this backend-less machine
    - The test reads the external path from env `LUCY_NUS_TEST_DATA` with a default
    - No `ser` binary is added under tests/ (`git status --porcelain tests/ | grep -c ser` returns 0)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_integration.py -q -rs</automated>
  </verify>
  <done>One skipif-guarded end-to-end test drives the real chain for exp3 and skips cleanly when the backend/data is absent; no binaries committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/caller → NusRunner.reconstruct(expdir) | expdir is the only external input; resolved via Path(expdir).resolve() before any stage-dir derivation |
| NusRunner → analysis/nus_recon/<expN>/ | Stage dir derived from the resolved expdir; no user-string concatenation with `..` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-02 | Tampering | expdir → stage_dir path handling | mitigate | `_stage_dir` derives from `Path(expdir).resolve()`; intermediates written only under that resolved per-experiment subfolder (no traversal) |
| T-98-01 | Tampering | orchestrated subprocess chain | mitigate | All external calls go through run_stage (fixed argv, no shell=True) built by Plans 03/04 from typed params |
| T-98-SC | Tampering | pip installs | accept | No new package installs this phase |
</threat_model>

<verification>
- `pytest tests/nus/ -q -rs` green (orchestration tests pass; integration test SKIPPED)
- `mypy src/lucy_ng/nus/runner.py` passes
- `pytest -q` still green
</verification>

<success_criteria>
NusRunner.reconstruct runs the whole chain from one entrypoint with a single param/schedule parse,
a KEEP stage-dir lifecycle, and a hard F2-before-F1 gate that raises before any subprocess; the real
end-to-end chain is a single skipif integration test that skips cleanly without the backend.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-05-SUMMARY.md` when done
</output>
