---
phase: 98-reconstruction-processing
plan: 06
type: execute
wave: 5
depends_on: [98-05]
files_modified:
  - src/lucy_ng/cli/nus.py
  - tests/test_cli_nus.py
  - tests/nus/test_cli_reconstruct.py
autonomous: true
requirements: [RECON-05]

must_haves:
  truths:
    - "lucy nus reconstruct <expdir> exposes iteration count, threshold, and virtual-echo toggle as CLI flags with sane defaults"
    - "The flags thread through to the SMILE invocation; -maxIter is an upper bound and convergence stopping is -nSigma/-sigma based, not iteration-count-only"
    - "The reconstruct command supports --format json emitting NusReconstructionResult.to_dict()"
    - "The Phase-97 import-safety regression guard now expects reconstruct in the nus command set"
  artifacts:
    - path: "src/lucy_ng/cli/nus.py"
      provides: "reconstruct command (deferred imports, --format json, knob flags)"
      contains: "@nus.command(\"reconstruct\")"
  key_links:
    - from: "src/lucy_ng/cli/nus.py"
      to: "lucy_ng.nus.runner.NusRunner"
      via: "deferred import inside the reconstruct command body"
      pattern: "NusRunner"
---

<objective>
Add the `lucy nus reconstruct <expdir>` CLI command: the thin Click wrapper that instantiates
`NusRunner` (Plan 05) and exposes RECON-05's iteration/threshold/virtual-echo knobs (plus the D-02
phase-override flags) with sane defaults, `--format json` output, and the existing deferred-import /
`Path.resolve()` convention. Also perform the required companion edit to the Phase-97 import-safety
regression guard so it expects `reconstruct` in the registered command set.

Purpose: RECON-05 (knob CLI flags with convergence-based defaults). Output: `reconstruct` command
in cli/nus.py + updated companion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md
@src/lucy_ng/cli/nus.py

<interfaces>
From src/lucy_ng/nus/runner.py (Plan 05):
  class NusRunner: reconstruct(expdir, *, max_iter, threshold, n_sigma, virtual_echo, f1_p0, f1_p1, f2_p0, f2_p1, timeout) -> NusReconstructionResult
  NusReconstructionResult.to_dict()

Existing cli/nus.py convention (Phase 97, mirror exactly):
  @nus.command("schedule") + @click.argument("expdir", type=click.Path(exists=True))
  + --format text|json option, deferred `from lucy_ng.nus.* import ...` inside the body, Path(expdir).resolve()

Companion edit target (98-PATTERNS.md § cli/nus.py):
  tests/test_cli_nus.py::TestImportSafety::test_nus_group_help_lists_only_implemented_subcommands
  currently asserts set(nus.commands) == {"check","params","schedule"} — must become
  {"check","params","schedule","reconstruct"}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconstruct command with RECON-05 knob flags + phase overrides + --format json</name>
  <files>src/lucy_ng/cli/nus.py, tests/nus/test_cli_reconstruct.py</files>
  <read_first>
    - src/lucy_ng/cli/nus.py (whole file — the schedule command is the closest analog: expdir arg + --format + deferred import)
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Alternatives Considered (descriptive lucy-ng flag names mapped to SMILE -maxIter/-thresh/-EA) and § Recommended defaults
    - .planning/phases/98-reconstruction-processing/98-CONTEXT.md § D-02 (optional phase CLI override) and § Claude's Discretion (RECON-05 knob defaults)
    - tests/nus/test_cli_reconstruct.py (Plan-01 stubs) + tests/test_cli_nus.py (CliRunner shape)
  </read_first>
  <action>
    Add `@nus.command("reconstruct")` to `src/lucy_ng/cli/nus.py` with `@click.argument("expdir",
    type=click.Path(exists=True))`, the same `--format text|json` option, and RECON-05 knob options
    with descriptive lucy-ng names + defaults: `--iterations` (int, default 500, help notes it is an
    upper bound not the sole stopping rule), `--threshold` (float, default 0.8), and
    `--virtual-echo/--no-virtual-echo` (default on). Also add D-02 phase-override options
    `--f2-p0`/`--f2-p1`/`--f1-p0`/`--f1-p1` (floats, with the provisional defaults, help notes A2
    provisional). The command body uses a DEFERRED `from lucy_ng.nus.runner import NusRunner`,
    resolves `Path(expdir).resolve()`, calls `NusRunner().reconstruct(...)` threading the flags
    (map `--iterations`→max_iter, `--threshold`→threshold, `--virtual-echo`→virtual_echo), and emits
    `json.dumps(result.to_dict(), indent=2)` for `--format json` or a human summary otherwise. Then
    implement the RECON-05 stubs in test_cli_reconstruct.py: assert `--help` lists the knob flags and
    that (with NusRunner monkeypatched) the flag values thread through to the reconstruct() call /
    SMILE invocation.
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/cli/nus.py` contains `@nus.command("reconstruct")` and a deferred `from lucy_ng.nus.runner import NusRunner`
    - `lucy nus reconstruct --help` (via CliRunner) lists `--iterations`, `--threshold`, and `--virtual-echo/--no-virtual-echo`
    - `grep -c 'import lucy_ng.nus' src/lucy_ng/cli/nus.py` returns 0 outside command bodies (import-safety preserved — top-level import test still passes)
    - `pytest tests/nus/test_cli_reconstruct.py -q` passes test_reconstruct_help_lists_knob_flags and test_flags_thread_through_to_smile_invocation (0 skipped)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_cli_reconstruct.py -q</automated>
  </verify>
  <done>lucy nus reconstruct exposes the RECON-05 knobs + phase overrides with convergence-based defaults, threads them to NusRunner, supports --format json, and preserves import-safety.</done>
</task>

<task type="auto">
  <name>Task 2: Companion edit — update the import-safety regression guard</name>
  <files>tests/test_cli_nus.py</files>
  <read_first>
    - tests/test_cli_nus.py lines 138-147 (the hard-coded `set(nus.commands) == {"check","params","schedule"}` assertion + the --help substring checks)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § cli/nus.py (this edit is flagged MANDATORY — CI fails without it)
  </read_first>
  <action>
    Update `tests/test_cli_nus.py::TestImportSafety::test_nus_group_help_lists_only_implemented_subcommands`
    so the expected set becomes `{"check", "params", "schedule", "reconstruct"}` and add an assertion
    that `"reconstruct"` appears in the `--help` output. Do not weaken the test to a subset check —
    keep it an exact-set assertion (it is the D-02 "no dead stubs" guard) now that reconstruct is a
    real implemented command. Leave the top-level import-safety test
    (`test_no_top_level_nus_submodule_import`) unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c 'reconstruct' tests/test_cli_nus.py` is >= 2 (set membership + help assertion)
    - The assertion is still an exact-set equality (`set(nus.commands) == {...}`), not a subset check
    - `pytest tests/test_cli_nus.py -q` exits 0 (all Phase-97 + updated guard tests green)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_cli_nus.py -q</automated>
  </verify>
  <done>The D-02 no-dead-stubs regression guard expects reconstruct in the exact command set; the full cli_nus suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell user → `lucy nus reconstruct <expdir> [flags]` | expdir validated by `click.Path(exists=True)` then `Path(expdir).resolve()`; flag values are typed (int/float/bool) via Click, never raw strings interpolated into a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-05 | Input validation | reconstruct CLI argument/flags | mitigate | `click.Path(exists=True)` for expdir + typed `click.option` for knobs; the resolved path/typed values flow into NusRunner, no shell interpolation |
| T-98-02 | Tampering | expdir path handling | mitigate | `Path(expdir).resolve()` before use (existing cli/nus.py convention) |
| T-98-SC | Tampering | pip installs | accept | No new package installs; `[nus]` extra stays empty |
</threat_model>

<verification>
- `pytest tests/nus/test_cli_reconstruct.py tests/test_cli_nus.py -q` green (0 skipped in the new tests)
- `python -c "import lucy_ng.cli.nus"` exits 0 (import-safety preserved)
- `pytest -q` FULL suite green; the real integration test SKIPS (not fails)
- `mypy src/lucy_ng/cli/nus.py` passes
</verification>

<success_criteria>
lucy nus reconstruct exposes iteration/threshold/virtual-echo (+ phase-override) flags with sane
convergence-based defaults and --format json, threads them to NusRunner, preserves import-safety,
and the import-safety regression guard is updated to expect the new command.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-06-SUMMARY.md` when done
</output>
