---
phase: 98-reconstruction-processing
plan: 03
type: execute
wave: 3
depends_on: [98-02]
files_modified:
  - src/lucy_ng/nus/backends/nmrpipe_smile.py
  - tests/nus/test_reconstruct_chain.py
autonomous: true
requirements: [RECON-01, RECON-03]

must_haves:
  truths:
    - "NmrPipeSmileBackend.reconstruct() dispatches bruk2pipe / nusExpand.tcl / SMILE fully automatically with no GUI step, routing every call through run_stage()"
    - "Stage ORDER branches on FnMODE: echo-antiecho runs nusExpand.tcl before bruk2pipe; QF/magnitude runs bruk2pipe before nusExpand.tcl"
    - "bruk2pipe's F1 conversion size uses nus_td (full grid: 400/700/750), never f1_td or max(nuslist)+1"
    - "GRPDLY is removed via bruk2pipe's built-in -grpdly flag passing the exact non-integer acqus value"
    - "SMILE is invoked with -maxIter as an upper bound plus -nSigma/-sigma as the real convergence stopping rule; -EA is passed for echo-antiecho axes"
  artifacts:
    - path: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      provides: "reconstruct() method body"
      contains: "def reconstruct("
  key_links:
    - from: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      to: "lucy_ng.nus.runner.run_stage"
      via: "deferred import inside reconstruct() (avoids runner↔backend import cycle)"
      pattern: "run_stage"
    - from: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      to: "NusAcquisitionParams.nus_td"
      via: "bruk2pipe -yN/-yT sizing"
      pattern: "nus_td"
---

<objective>
Fill in `NmrPipeSmileBackend.reconstruct()`: the FnMODE-branched bruk2pipe → nusExpand.tcl → SMILE
stage chain that produces a reconstructed indirect-dimension FID under `analysis/nus_recon/<expN>/`,
fully automatically and headless. Every external call routes through the Plan-02 `run_stage()`
fail-loud wrapper. This plan builds the argv and stage sequencing; the direct-dimension
processing/phase/baseline stage is Plan 04, and the top-level orchestration + F2-before-F1 gate is
Plan 05.

Purpose: RECON-01 (the automatic backend chain) + RECON-03 (FnMODE-aware stage order from one
entrypoint). Output: `reconstruct()` body in nus/backends/nmrpipe_smile.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-RESEARCH.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md
@src/lucy_ng/nus/backends/nmrpipe_smile.py

<interfaces>
From src/lucy_ng/nus/runner.py (Plan 02):
  run_stage(name, argv: list[str], cwd: Path, expected_output: Path, timeout=600) -> None
  _ordering_for_fnmode(fnmode: int) -> "expand_first" | "convert_first"
  recipe_for_fnmode(fnmode: int) -> FnModeRecipe(stage_order, bruk2pipe_ymode, phase_sensitive, smile_ea)

From src/lucy_ng/nus/params.py / schedule.py (Phase 97 — consume, never re-parse):
  read_nus_params(expdir: Path) -> NusAcquisitionParams   # grpdly, decim, dspfvs, byte_order, dtype_code, nus_td, fnmode_f1, f1/f2 SFO1/SW_h/TD
  read_nus_schedule(expdir: Path) -> NusSchedule          # nuslist, nus_td, n_sampled

Existing in this file (Phase 97, keep): REQUIRED_TOOLS, missing_tools(), smile_plugin_available(), is_available(), diagnose().

SMILE-manual-verified defaults (98-RESEARCH.md § Recommended defaults / Code Examples):
  -maxIter 500 (upper bound only), -nSigma 5 (convergence stopping), -thresh 0.8, virtual echo default-ON for echo-antiecho (-EA).
  bruk2pipe echo-antiecho worked example uses -yMODE Echo-AntiEcho, -grpdly <exact acqus value>, -yN/-yT = full grid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconstruct() — echo-antiecho branch (expand-first) + SMILE knobs</name>
  <files>src/lucy_ng/nus/backends/nmrpipe_smile.py, tests/nus/test_reconstruct_chain.py</files>
  <read_first>
    - src/lucy_ng/nus/backends/nmrpipe_smile.py (whole file — extend the docstring + add the method; keep all Phase-97 classmethods)
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Critical Finding 1 (expand-before-convert for echo-antiecho), § Critical Finding 2 (nus_td not f1_td), § Code Examples (the bruk2pipe + SMILE argv), § Recommended defaults
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "reconstruct()" (deferred run_stage import, raise-not-return-False contract, REQUIRED_TOOLS pre-flight)
    - src/lucy_ng/lsd/runner.py lines 176-227 (output_dir.mkdir(parents=True, exist_ok=True) — stage_dir lifecycle, but D-03 default is KEEP, no rmtree)
    - tests/nus/test_reconstruct_chain.py + tests/nus/conftest.py (mock_run_stage recorder to assert dispatch order/argv)
  </read_first>
  <action>
    Add `reconstruct(self, expdir, params, schedule, stage_dir, *, max_iter=500, threshold=0.8,
    n_sigma=5, virtual_echo=True, f1_p0=90.0, f1_p1=0.0, timeout=600) -> dict[str, Path]` to
    `NmrPipeSmileBackend` (accepting the already-parsed params/schedule + an explicit stage_dir so
    the runner in Plan 05 owns path resolution). Pre-flight: raise `FileNotFoundError` if
    `missing_tools()` is non-empty (mirror LSDRunner). `mkdir(parents=True, exist_ok=True)` the
    stage_dir; do NOT delete it (D-03 keep). Import `run_stage` and `recipe_for_fnmode` via a
    DEFERRED import inside the method body (avoids the runner↔backend import cycle). For the
    echo-antiecho branch (`recipe.stage_order == "expand_first"`): (1) run_stage("nusExpand.tcl",
    [...`-mode bruker -sampleCount <n_sampled> -in ./ser -out ./ser_full -sample ./nuslist`...],
    cwd=stage_dir, expected_output=ser_full); (2) run_stage("bruk2pipe", [...`-in ./ser_full`,
    `-grpdly <params.grpdly exact>`, `-decim`, `-dspfvs`, `-yN`/`-yT` from `params.nus_td` (NOT
    f1_td), `-yMODE <recipe.bruk2pipe_ymode>`, x/y SW/OBS/CAR from params...],
    expected_output=test.fid); (3) run_stage for SMILE via `nmrPipe -fn SMILE -nDim 2 -sample
    nuslist -maxIter <max_iter> -nSigma <n_sigma> -thresh <threshold>` plus `-xP0 <f1_p0> -xP1
    <f1_p1>` and `-EA` when `recipe.smile_ea and virtual_echo`, expected_output=reconstructed.fid.
    Return a dict of stage name → output Path. Add a source comment that `-maxIter` is an upper
    bound and `-nSigma`/`-sigma` is the real stopping rule (RECON-05 rationale). Implement the
    RECON-01/03 echo-antiecho stubs in test_reconstruct_chain.py against `mock_run_stage`,
    asserting the dispatch ORDER is nusExpand.tcl → bruk2pipe → SMILE, that bruk2pipe argv contains
    `str(params.nus_td)` and not `str(params.f1_td)`, and that `-grpdly` carries the exact
    non-integer value.
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/backends/nmrpipe_smile.py` contains `def reconstruct(`
    - `grep -c 'shell=True' src/lucy_ng/nus/backends/nmrpipe_smile.py` returns 0
    - `pytest tests/nus/test_reconstruct_chain.py -q` passes test_echo_antiecho_dispatches_expand_then_convert_then_smile, test_smile_argv_carries_default_knobs, test_bruk2pipe_uses_nus_td_not_f1_td, test_grpdly_passed_exact_non_integer (0 skipped)
    - The all Phase-97 classmethods (missing_tools/smile_plugin_available/is_available/diagnose) remain unchanged (still present)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_chain.py -q</automated>
  </verify>
  <done>Echo-antiecho reconstruct chain dispatches expand→convert→SMILE via run_stage with correct nus_td sizing, exact GRPDLY, and convergence-based SMILE knobs; RECON-01/03 chain tests green.</done>
</task>

<task type="auto">
  <name>Task 2: reconstruct() — QF/magnitude COSY branch (convert-first, provisional + A1/A3 flagged)</name>
  <files>src/lucy_ng/nus/backends/nmrpipe_smile.py, tests/nus/test_reconstruct_chain.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Critical Finding 1 (COSY = convert-first, no fully-worked manual script), § Assumptions Log A1/A3, § Open Questions 1
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "No Analog Found" (COSY branch is an implementation-time spike, not a trusted hard-coded guess)
    - tests/nus/test_reconstruct_chain.py (add the QF-branch assertions)
  </read_first>
  <action>
    Add the QF/magnitude branch (`recipe.stage_order == "convert_first"`) to `reconstruct()`: run
    bruk2pipe FIRST (on `./ser`, `-yMODE <recipe.bruk2pipe_ymode>` — the provisional magnitude
    value flagged A3), then nusExpand.tcl on the resulting `.fid`, then SMILE without `-EA`
    (magnitude mode, `recipe.phase_sensitive is False`). Add a clear in-source docstring/comment
    block marking this branch PROVISIONAL per 98-RESEARCH.md Assumptions Log A1/A3: the exact
    convert-then-expand invocation for magnitude-mode COSY is not fully documented in the SMILE
    manual and MUST be empirically confirmed against the real C20H32O2 exp2 data (cross-checked via
    the Phase-99 COSY diagonal-symmetry gate) before the automated path is trusted unattended. Do
    NOT silently reduce scope — the branch is implemented, just annotated as needing an
    implementation-time spike. Implement the QF-branch stub in test_reconstruct_chain.py asserting
    `_ordering_for_fnmode(1) == "convert_first"` drives bruk2pipe→nusExpand→SMILE order and that no
    `-EA` flag appears in the SMILE argv for FnMODE 1.
  </action>
  <acceptance_criteria>
    - `reconstruct()` handles both `expand_first` and `convert_first` (grep both literals present in the file)
    - The QF branch is annotated with an "A1"/"A3" or "PROVISIONAL" source comment referencing empirical confirmation
    - `pytest tests/nus/test_reconstruct_chain.py -q` — QF-branch test asserts convert-first dispatch order and absence of `-EA` for FnMODE 1
    - `pytest tests/nus/test_fnmode_branching.py tests/nus/test_reconstruct_chain.py -q` all green (0 skipped)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_chain.py -q</automated>
  </verify>
  <done>Both FnMODE branches are implemented from one entrypoint; the QF branch is annotated PROVISIONAL (A1/A3) pending an empirical spike, not omitted; chain tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconstruct() → bruk2pipe/nusExpand.tcl/SMILE subprocess | External-tool invocation; argv built from typed Pydantic fields (params/schedule), never raw user strings |
| reconstruct() → analysis/nus_recon/<expN>/ filesystem | Intermediate files written under the caller-supplied resolved stage_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | bruk2pipe/nusExpand/SMILE argv | mitigate | run_stage fixed arg-list, never `shell=True` (grep-gated); values come from validated NusAcquisitionParams/NusSchedule, not shell interpolation |
| T-98-02 | Tampering | stage_dir write location | mitigate | stage_dir supplied resolved by the caller (Plan 05 uses Path(expdir).resolve()); reconstruct only mkdir/writes within it, no `..` concatenation |
| T-98-SC | Tampering | pip installs | accept | No new package installs; external tools runtime-detected like LSD |
</threat_model>

<verification>
- `pytest tests/nus/test_reconstruct_chain.py -q` green (0 skipped)
- `mypy src/lucy_ng/nus/backends/nmrpipe_smile.py` passes
- `pytest -q` still green
</verification>

<success_criteria>
reconstruct() runs bruk2pipe/nusExpand.tcl/SMILE automatically and headless, branches stage order
correctly by FnMODE, sizes bruk2pipe from nus_td, removes GRPDLY via the built-in flag, and drives
SMILE with an upper-bound -maxIter plus convergence-based -nSigma; the QF branch is implemented and
annotated PROVISIONAL per A1/A3.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-03-SUMMARY.md` when done
</output>
