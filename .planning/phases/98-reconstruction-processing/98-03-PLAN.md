---
phase: 98-reconstruction-processing
plan: 03
type: execute
wave: 3
depends_on: [98-02]
files_modified:
  - src/lucy_ng/nus/backends/nmrpipe_smile.py
  - tests/nus/test_reconstruct_chain.py
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03]

must_haves:
  truths:
    - "NmrPipeSmileBackend.convert() dispatches bruk2pipe / nusExpand.tcl fully automatically with no GUI step, routing every call through run_stage(), producing a converted FID under analysis/nus_recon/<expN>/"
    - "Conversion stage ORDER branches on FnMODE: echo-antiecho runs nusExpand.tcl before bruk2pipe; QF/magnitude runs bruk2pipe before nusExpand.tcl"
    - "NmrPipeSmileBackend.reconstruct_indirect() runs SMILE on the ALREADY F2-processed, transposed FID (SMILE's input is NOT a raw time-domain/converted FID — it is process_direct()'s output from Plan 04)"
    - "bruk2pipe's F1 conversion size uses nus_td (full grid: 400/700/750), never f1_td or max(nuslist)+1"
    - "GRPDLY is removed via bruk2pipe's built-in -grpdly flag passing the exact non-integer acqus value"
    - "SMILE is invoked with -maxIter as an upper bound plus -nSigma/-sigma as the real convergence stopping rule; -EA is passed for echo-antiecho axes only (never for QF/magnitude)"
  artifacts:
    - path: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      provides: "convert() + reconstruct_indirect() method bodies"
      contains: "def reconstruct_indirect("
  key_links:
    - from: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      to: "lucy_ng.nus.runner.run_stage"
      via: "deferred import inside convert()/reconstruct_indirect() (avoids runner↔backend import cycle)"
      pattern: "run_stage"
    - from: "src/lucy_ng/nus/backends/nmrpipe_smile.py"
      to: "NusAcquisitionParams.nus_td"
      via: "bruk2pipe -yN/-yT sizing"
      pattern: "nus_td"
---

<objective>
Fill in the backend's external-tool stages, split around the F2-processing step that SMILE
requires to run first (SMILE manual §4): `NmrPipeSmileBackend.convert()` (the FnMODE-branched
bruk2pipe ↔ nusExpand.tcl conversion → a converted FID) and `NmrPipeSmileBackend.reconstruct_indirect()`
(the SMILE call, consuming the ALREADY F2-processed + transposed FID produced by Plan 04's
`process_direct()`, → a reconstructed indirect-dimension file). Both write under
`analysis/nus_recon/<expN>/`, fully automatically and headless, routing every external call through
the Plan-02 `run_stage()` fail-loud wrapper.

Plan 04 supplies `process_direct()` (F2 apod/ZF/FT/PS/POLY/EXT + TP) which runs BETWEEN `convert()`
and `reconstruct_indirect()`; Plan 05's `NusRunner` orchestrates the four ordered stages and owns the
F2-before-F1 gate.

Purpose: RECON-01 (the automatic backend chain) + RECON-03 (FnMODE-aware conversion order from one
entrypoint) + RECON-02 (SMILE consumes the F2-processed FID, never a raw one — the physical F2-before-F1
ordering). Output: `convert()` + `reconstruct_indirect()` bodies in nus/backends/nmrpipe_smile.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-RESEARCH.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md
@src/lucy_ng/nus/backends/nmrpipe_smile.py

<interfaces>
From src/lucy_ng/nus/runner.py (Plan 02):
  run_stage(name, argv: list[str], cwd: Path, expected_output: Path, timeout=600) -> None
  _ordering_for_fnmode(fnmode: int) -> "expand_first" | "convert_first"
  recipe_for_fnmode(fnmode: int) -> FnModeRecipe(stage_order, bruk2pipe_ymode, phase_sensitive, smile_ea)

From src/lucy_ng/nus/params.py / schedule.py (Phase 97 — consume, never re-parse):
  read_nus_params(expdir: Path) -> NusAcquisitionParams   # grpdly, decim, dspfvs, byte_order, dtype_code, nus_td, fnmode_f1, f1/f2 SFO1/SW_h/TD
  read_nus_schedule(expdir: Path) -> NusSchedule          # nuslist, nus_td, n_sampled

From src/lucy_ng/nus/postprocess.py (Plan 04 — runs BETWEEN convert() and reconstruct_indirect()):
  process_direct(converted_fid, stage_dir, params, *, f2_p0, f2_p1, magnitude, timeout=600) -> Path
    # F2 apod/ZF/FT/PS/POLY/EXT + TP → f2_processed.fid  (this is reconstruct_indirect()'s input)

Existing in this file (Phase 97, keep): REQUIRED_TOOLS, missing_tools(), smile_plugin_available(), is_available(), diagnose().

Pipeline ORDER (SMILE manual §4/§6.1, RESEARCH.md Architecture Diagram):
  convert()  →  process_direct() [F2 + TP, Plan 04]  →  reconstruct_indirect() [SMILE]  →  process_indirect() [F1, Plan 04]
  SMILE runs on the TRANSPOSED, F2-PROCESSED FID — NOT immediately after conversion.

SMILE-manual-verified defaults (98-RESEARCH.md § Recommended defaults / Code Examples):
  -maxIter 500 (upper bound only), -nSigma 5 (convergence stopping), -thresh 0.8, virtual echo default-ON for echo-antiecho (-EA).
  bruk2pipe echo-antiecho worked example uses -yMODE Echo-AntiEcho, -grpdly <exact acqus value>, -yN/-yT = full grid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: convert() echo-antiecho branch (expand-first) + reconstruct_indirect() SMILE call</name>
  <files>src/lucy_ng/nus/backends/nmrpipe_smile.py, tests/nus/test_reconstruct_chain.py</files>
  <read_first>
    - src/lucy_ng/nus/backends/nmrpipe_smile.py (whole file — extend the docstring + add the methods; keep all Phase-97 classmethods)
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Critical Finding 1 (expand-before-convert for echo-antiecho), § Critical Finding 2 (nus_td not f1_td), § Code Examples (the bruk2pipe + SMILE argv; note the manual pipe processes F2 + TP BEFORE SMILE), § Recommended defaults, § System Architecture Diagram (steps 3 and 5)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "convert() + reconstruct_indirect()" (deferred run_stage import, raise-not-return-False contract, REQUIRED_TOOLS pre-flight, why two methods)
    - src/lucy_ng/lsd/runner.py lines 176-227 (output_dir.mkdir(parents=True, exist_ok=True) — stage_dir lifecycle, but D-03 default is KEEP, no rmtree)
    - tests/nus/test_reconstruct_chain.py + tests/nus/conftest.py (mock_run_stage recorder to assert dispatch order/argv)
  </read_first>
  <action>
    Add TWO methods to `NmrPipeSmileBackend`. Both import `run_stage` and `recipe_for_fnmode` via a
    DEFERRED import inside the method body (avoids the runner↔backend import cycle) and pre-flight by
    raising `FileNotFoundError` if `missing_tools()` is non-empty (mirror LSDRunner). Neither method
    deletes stage_dir (D-03 keep); `mkdir(parents=True, exist_ok=True)` it.

    (A) `convert(self, expdir, params, schedule, stage_dir, *, timeout=600) -> Path` — for the
    echo-antiecho branch (`recipe.stage_order == "expand_first"`): (1) run_stage("nusExpand.tcl",
    [...`-mode bruker -sampleCount <schedule.n_sampled> -in ./ser -out ./ser_full -sample ./nuslist`...],
    cwd=stage_dir, expected_output=ser_full); (2) run_stage("bruk2pipe", [...`-in ./ser_full`,
    `-grpdly <params.grpdly exact non-integer>`, `-decim`, `-dspfvs`, `-yN`/`-yT` from `params.nus_td`
    (NOT f1_td), `-yMODE <recipe.bruk2pipe_ymode>`, x/y SW/OBS/CAR from params...],
    expected_output=converted.fid). Return the converted.fid Path. Add a source comment that
    conversion produces a TIME-DOMAIN FID that MUST still be F2-processed + transposed (Plan 04
    `process_direct()`) BEFORE SMILE — convert() does NOT call SMILE.

    (B) `reconstruct_indirect(self, f2_processed_fid, stage_dir, params, schedule, *, max_iter=500,
    threshold=0.8, n_sigma=5, virtual_echo=True, f1_p0=90.0, f1_p1=0.0, timeout=600) -> Path` — run
    SMILE via `nmrPipe -fn SMILE -nDim 2 -sample nuslist -maxIter <max_iter> -nSigma <n_sigma> -thresh
    <threshold>` plus `-xP0 <f1_p0> -xP1 <f1_p1>` and `-EA` when `recipe.smile_ea and virtual_echo`,
    with the INPUT being `f2_processed_fid` (the transposed, F2-processed FID from Plan 04 —
    NAME THE PARAMETER so this is unmistakable; SMILE's input is NOT a raw converted FID),
    expected_output=reconstructed.ft1. Add a source comment that `-maxIter` is an upper bound and
    `-nSigma`/`-sigma` is the real stopping rule (RECON-05 rationale).

    Then implement the echo-antiecho stubs in test_reconstruct_chain.py against `mock_run_stage`:
    `test_echo_antiecho_convert_dispatches_expand_then_bruk2pipe` (convert() dispatch ORDER is
    nusExpand.tcl → bruk2pipe), `test_bruk2pipe_uses_nus_td_not_f1_td` (bruk2pipe argv contains
    `str(params.nus_td)` and not `str(params.f1_td)`), `test_grpdly_passed_exact_non_integer`,
    `test_smile_argv_carries_default_knobs` (reconstruct_indirect SMILE argv carries -maxIter/-nSigma/
    -thresh + -EA for FnMODE 6), and `test_smile_input_is_f2_processed_not_raw_converted_fid` (assert
    reconstruct_indirect's `-in`/input path is the f2_processed_fid argument, distinct from convert()'s
    converted.fid output).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/backends/nmrpipe_smile.py` contains `def convert(` and `def reconstruct_indirect(`
    - `grep -c 'shell=True' src/lucy_ng/nus/backends/nmrpipe_smile.py` returns 0
    - `grep -c 'def reconstruct(self' src/lucy_ng/nus/backends/nmrpipe_smile.py` returns 0 (no single convert-then-SMILE method — SMILE must not run immediately after conversion)
    - `pytest tests/nus/test_reconstruct_chain.py -q` passes test_echo_antiecho_convert_dispatches_expand_then_bruk2pipe, test_smile_argv_carries_default_knobs, test_bruk2pipe_uses_nus_td_not_f1_td, test_grpdly_passed_exact_non_integer, test_smile_input_is_f2_processed_not_raw_converted_fid (0 skipped)
    - All Phase-97 classmethods (missing_tools/smile_plugin_available/is_available/diagnose) remain unchanged (still present)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_chain.py -q</automated>
  </verify>
  <done>Echo-antiecho convert() dispatches expand→bruk2pipe via run_stage with correct nus_td sizing and exact GRPDLY; reconstruct_indirect() runs SMILE on the F2-processed transposed FID (not the raw converted FID) with convergence-based knobs; RECON-01/02/03 chain tests green.</done>
</task>

<task type="auto">
  <name>Task 2: convert() QF/magnitude COSY branch (convert-first, provisional + A1/A3 flagged)</name>
  <files>src/lucy_ng/nus/backends/nmrpipe_smile.py, tests/nus/test_reconstruct_chain.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Critical Finding 1 (COSY = convert-first, no fully-worked manual script), § Assumptions Log A1/A3, § Open Questions 1 (RESOLVED — deferred to implementation-time spike)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "No Analog Found" (COSY branch is an implementation-time spike, not a trusted hard-coded guess)
    - tests/nus/test_reconstruct_chain.py (add the QF-branch assertions)
  </read_first>
  <action>
    Add the QF/magnitude branch (`recipe.stage_order == "convert_first"`) to `convert()`: run
    bruk2pipe FIRST (on `./ser`, `-yMODE <recipe.bruk2pipe_ymode>` — the provisional magnitude value
    flagged A3), then nusExpand.tcl on the resulting `.fid`, returning the expanded converted.fid. Add
    a clear in-source docstring/comment block marking this branch PROVISIONAL per 98-RESEARCH.md
    Assumptions Log A1/A3: the exact convert-then-expand invocation for magnitude-mode COSY is not
    fully documented in the SMILE manual and MUST be empirically confirmed against the real C20H32O2
    exp2 data (cross-checked via the Phase-99 COSY diagonal-symmetry gate) before the automated path is
    trusted unattended. Do NOT silently reduce scope — the branch is implemented, just annotated as
    needing an implementation-time spike. For the QF branch, `reconstruct_indirect()` (Task 1) already
    omits `-EA` because `recipe.smile_ea is False` for REAL_FNMODES — no COSY-specific SMILE change
    needed here; the magnitude/no-phase handling for F2/F1 processing lives in Plan 04's `magnitude`
    flag. Implement the QF-branch stub in test_reconstruct_chain.py:
    `test_qf_convert_dispatches_bruk2pipe_then_expand` asserting `_ordering_for_fnmode(1) ==
    "convert_first"` drives bruk2pipe→nusExpand order in convert(), and add an assertion that
    reconstruct_indirect's SMILE argv carries no `-EA` flag for FnMODE 1.
  </action>
  <acceptance_criteria>
    - `convert()` handles both `expand_first` and `convert_first` (grep both literals present in the file)
    - The QF branch is annotated with an "A1"/"A3" or "PROVISIONAL" source comment referencing empirical confirmation
    - `pytest tests/nus/test_reconstruct_chain.py -q` — QF-branch test asserts convert-first dispatch order in convert() and absence of `-EA` in the SMILE argv for FnMODE 1
    - `pytest tests/nus/test_fnmode_branching.py tests/nus/test_reconstruct_chain.py -q` all green (0 skipped)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_reconstruct_chain.py -q</automated>
  </verify>
  <done>Both FnMODE conversion branches are implemented from one entrypoint; the QF branch is annotated PROVISIONAL (A1/A3) pending an empirical spike, not omitted; SMILE omits -EA for QF; chain tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| convert()/reconstruct_indirect() → bruk2pipe/nusExpand.tcl/SMILE subprocess | External-tool invocation; argv built from typed Pydantic fields (params/schedule), never raw user strings |
| convert()/reconstruct_indirect() → analysis/nus_recon/<expN>/ filesystem | Intermediate files written under the caller-supplied resolved stage_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | bruk2pipe/nusExpand/SMILE argv | mitigate | run_stage fixed arg-list, never `shell=True` (grep-gated); values come from validated NusAcquisitionParams/NusSchedule, not shell interpolation |
| T-98-02 | Tampering | stage_dir write location | mitigate | stage_dir supplied resolved by the caller (Plan 05 uses Path(expdir).resolve()); methods only mkdir/write within it, no `..` concatenation |
| T-98-SC | Tampering | pip installs | accept | No new package installs; external tools runtime-detected like LSD |
</threat_model>

<verification>
- `pytest tests/nus/test_reconstruct_chain.py -q` green (0 skipped)
- `mypy src/lucy_ng/nus/backends/nmrpipe_smile.py` passes
- `pytest -q` still green
</verification>

<success_criteria>
convert() runs bruk2pipe/nusExpand.tcl automatically and headless, branches conversion order correctly
by FnMODE, sizes bruk2pipe from nus_td, and removes GRPDLY via the built-in flag; reconstruct_indirect()
runs SMILE on the F2-processed transposed FID (never a raw converted FID) with an upper-bound -maxIter
plus convergence-based -nSigma and -EA only for echo-antiecho; the QF branch is implemented and annotated
PROVISIONAL per A1/A3.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-03-SUMMARY.md` when done
</output>
