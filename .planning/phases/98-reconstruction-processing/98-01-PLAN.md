---
phase: 98-reconstruction-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/nus/__init__.py
  - tests/nus/conftest.py
  - tests/nus/test_runner_faillloud.py
  - tests/nus/test_reconstruct_chain.py
  - tests/nus/test_processing_order.py
  - tests/nus/test_fnmode_branching.py
  - tests/nus/test_reconstruct_orchestration.py
  - tests/nus/test_cli_reconstruct.py
  - tests/nus/test_reconstruct_integration.py
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-05]

must_haves:
  truths:
    - "pytest collects tests/nus/ with zero import errors on a machine with no NMRPipe installed"
    - "Every RECON requirement has a named, skipped test stub whose docstring states the exact behavior the implementation plan must satisfy"
    - "conftest.py provides a run_stage mock seam and valid/truncated/empty fake-NMRPipe-intermediate factories without importing any not-yet-existing Phase-98 module at collection time"
  artifacts:
    - path: "tests/nus/conftest.py"
      provides: "Shared fixtures: run_stage mock seam, fake intermediate factories, Phase-97 fixture-dir accessors"
      min_lines: 40
    - path: "tests/nus/test_runner_faillloud.py"
      provides: "RECON-04 stub (exit-code + output-non-emptiness)"
      contains: "def test_"
    - path: "tests/nus/test_fnmode_branching.py"
      provides: "RECON-03 stub (stage-order + phase-mode per FnMODE)"
      contains: "def test_"
  key_links:
    - from: "tests/nus/conftest.py"
      to: "tests/fixtures/nus/{exp2_cosy,exp3_hsqc,exp4_hmbc}"
      via: "fixture-dir accessor fixtures (pure Path, no lucy import)"
      pattern: "fixtures/nus"
---

<objective>
Lay down the Nyquist Wave 0 test scaffolding for Phase 98: a collectable `tests/nus/`
package, a shared `conftest.py` (subprocess mock seam + fake-intermediate factories +
Phase-97 fixture accessors), and one RED-by-skip stub file per RECON requirement. No
production code — this plan exists so every later implementation plan turns a named,
pre-existing test from skipped to green rather than inventing coverage after the fact.

Purpose: satisfy the 98-VALIDATION.md coverage contract (every RECON requirement has a
CI-safe automated test; the real end-to-end chain is a separate skipif integration test).
Output: `tests/nus/__init__.py`, `tests/nus/conftest.py`, and 7 stub test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-VALIDATION.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md

<interfaces>
<!-- Contracts the stubs are written against; implemented in later plans. Executor must NOT -->
<!-- import these at collection time (modules do not exist yet in Wave 0) — defer all imports -->
<!-- into test bodies / fixture bodies. -->

Future module `src/lucy_ng/nus/runner.py` (Plan 02/05) will expose:
  run_stage(name: str, argv: list[str], cwd: Path, expected_output: Path, timeout: int = 600) -> None
  _ordering_for_fnmode(fnmode: int) -> str   # "expand_first" (COMPLEX_FNMODES) | "convert_first" (REAL_FNMODES); NotImplementedError otherwise
  class NusRunner  # NusRunner(...).reconstruct(expdir: Path) -> NusReconstructionResult

Future module `src/lucy_ng/nus/postprocess.py` (Plan 04) will expose the FT/phase/baseline
stage + a ppm-axis reversal/calibration helper.

Future CLI (Plan 06): `lucy nus reconstruct <expdir>` with flags --iterations/--threshold/
--virtual-echo|--no-virtual-echo (+ phase-override flags).

Existing (Phase 97, importable today): lucy_ng.nus.backends.NmrPipeSmileBackend
(.is_available()), tests/fixtures/nus/{exp2_cosy,exp3_hsqc,exp4_hmbc}/ (acqus/acqu2s/nuslist/pdata).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/nus package + conftest.py shared fixtures</name>
  <files>tests/nus/__init__.py, tests/nus/conftest.py</files>
  <read_first>
    - tests/__init__.py (confirms tests is an importable package — add the parallel tests/nus/__init__.py)
    - tests/test_nus_backends.py (the `_fail_if_called` assertion-in-mock trick and the fixed-arg-list capture pattern to reuse in the mock seam)
    - tests/test_lsd_runner.py lines 208-253 (monkeypatch.setattr(subprocess, "run", ...) shape)
    - tests/fixtures/nus/exp2_cosy/nuslist (0-based acquisition-order format the fake factories mimic)
    - .planning/phases/98-reconstruction-processing/98-VALIDATION.md § Wave 0 Requirements
  </read_first>
  <action>
    Create empty `tests/nus/__init__.py` (mirrors tests/__init__.py so the subdir is a package).
    Create `tests/nus/conftest.py` with NO top-level import of any `lucy_ng.nus.runner`/
    `lucy_ng.nus.postprocess` symbol (those modules do not exist in Wave 0 — importing them at
    collection time would break the whole tests/nus/ tree). Provide these pytest fixtures:
    (a) `nus_fixture_dir` — returns a callable mapping "exp2_cosy"/"exp3_hsqc"/"exp4_hmbc" to the
    resolved `tests/fixtures/nus/<name>` Path (pure pathlib, no lucy import);
    (b) `make_valid_intermediate` — factory writing a non-empty fake NMRPipe intermediate file
    under a tmp dir (arbitrary non-zero bytes; suffix-parameterized `.fid`/`.ft2`) for the
    exit-0-success path;
    (c) `make_empty_intermediate` and `make_truncated_intermediate` — factories writing a
    zero-byte and a deliberately-too-short file respectively, for the RECON-04 fail-loud tests;
    (d) `mock_run_stage` — a fixture that monkeypatches `lucy_ng.nus.runner.run_stage` BY STRING
    TARGET inside the fixture body (deferred, `raising=False`) with a recorder that captures each
    (name, argv, cwd, expected_output) call, so tests can assert dispatch order/argv without a
    real subprocess;
    (e) `mock_subprocess_run` — monkeypatches `subprocess.run` with a recorder returning a
    configurable `returncode`, mirroring tests/test_lsd_runner.py's shape.
    Do not implement any production logic here.
  </action>
  <acceptance_criteria>
    - `tests/nus/__init__.py` exists (may be empty)
    - `tests/nus/conftest.py` defines fixtures named `nus_fixture_dir`, `make_valid_intermediate`, `make_empty_intermediate`, `make_truncated_intermediate`, `mock_run_stage`, `mock_subprocess_run`
    - `grep -v '^#' tests/nus/conftest.py | grep -c 'import lucy_ng.nus.runner'` returns 0 (no top-level import of the not-yet-existing module)
    - `python -c "import ast,sys; ast.parse(open('tests/nus/conftest.py').read())"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/nus/conftest.py').read()); ast.parse(open('tests/nus/__init__.py').read())"</automated>
  </verify>
  <done>tests/nus/ is a collectable package with the six shared fixtures, importable on a machine with no NMRPipe.</done>
</task>

<task type="auto">
  <name>Task 2: Create one RED-by-skip stub file per RECON requirement</name>
  <files>tests/nus/test_runner_faillloud.py, tests/nus/test_reconstruct_chain.py, tests/nus/test_processing_order.py, tests/nus/test_fnmode_branching.py, tests/nus/test_reconstruct_orchestration.py, tests/nus/test_cli_reconstruct.py, tests/nus/test_reconstruct_integration.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-VALIDATION.md § Per-Task Verification Map (the requirement→file mapping)
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Validation Architecture → Phase Requirements → Test Map (exact test class/method names)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § Test Patterns (D-04) (analog test shapes + the skipif condition for the integration test)
    - tests/test_cli_nus.py (CliRunner().invoke(nus, [...]) shape for the CLI stub)
  </read_first>
  <action>
    Create 7 stub files. Each test function is decorated `@pytest.mark.skip(reason="Wave 0 stub —
    implemented in Plan NN")` and carries a docstring stating the exact behavior its implementing
    plan must satisfy (the RED contract). Defer all `lucy_ng.nus.*` imports into test bodies.
    - `test_runner_faillloud.py` (RECON-04, Plan 02): `test_nonzero_exit_raises`,
      `test_empty_output_raises`, `test_truncated_all_zero_output_raises`, `test_valid_output_passes`.
    - `test_fnmode_branching.py` (RECON-03, Plan 02): `test_echo_antiecho_expand_first` (FnMODE 6),
      `test_qf_convert_first` (FnMODE 1), `test_unknown_fnmode_raises` (NotImplementedError),
      plus a note that both 25%/33% densities share the branch (exp3 vs exp4).
    - `test_reconstruct_chain.py` (RECON-01/02/03, Plan 03): `test_echo_antiecho_convert_dispatches_expand_then_bruk2pipe`,
      `test_qf_convert_dispatches_bruk2pipe_then_expand`, `test_smile_argv_carries_default_knobs`,
      `test_bruk2pipe_uses_nus_td_not_f1_td`, `test_grpdly_passed_exact_non_integer`,
      `test_smile_input_is_f2_processed_not_raw_converted_fid` (SMILE consumes the transposed,
      F2-processed FID — NOT a raw time-domain/converted FID). NOTE: there is NO
      `dispatches_expand_then_convert_then_smile` test — SMILE does NOT run immediately after
      conversion; the direct (F2) dimension is processed + transposed first (see Plan 04 / Plan 05).
    - `test_processing_order.py` (RECON-02, Plan 04): `test_f2_direct_chain_runs_before_transpose_before_smile_input`
      (the F2 apod/ZF/FT/PS/POLY/EXT verb chain ends with `TP`, producing SMILE's input),
      `test_f2_phase_constants_thread_through`, `test_magnitude_branch_skips_phase`,
      `test_ppm_axes_reversed`, `test_ppm_calibrated_to_1d_reference`.
    - `test_reconstruct_orchestration.py` (RECON-01/02, Plan 05): `test_f2_before_f1_gate_raises_before_any_subprocess`,
      `test_orchestration_sequences_convert_then_direct_then_smile_then_indirect` (whole-pipeline
      order: convert → process_direct(F2)+TP → SMILE → process_indirect(F1)),
      `test_reconstruct_returns_result_with_stage_paths`.
    - `test_cli_reconstruct.py` (RECON-05, Plan 06): `test_reconstruct_help_lists_knob_flags`,
      `test_flags_thread_through_to_smile_invocation`.
    - `test_reconstruct_integration.py` (Manual-Only, Plan 05): ONE `@pytest.mark.skipif`-guarded
      end-to-end test gated on `NmrPipeSmileBackend.is_available()` AND the external-data path
      (env var `LUCY_NUS_TEST_DATA`, default the C20H32O2 external dir from 98-PATTERNS.md) existing;
      this test SKIPS (not fails) in CI — that is correct per D-04.
  </action>
  <acceptance_criteria>
    - All 7 files exist and each function is decorated with `pytest.mark.skip` or `pytest.mark.skipif`
    - `pytest tests/nus/ -q` exits 0 with every test reported as skipped and ZERO collection/import errors
    - `pytest tests/nus/ --collect-only -q` lists at least: test_nonzero_exit_raises, test_empty_output_raises, test_echo_antiecho_expand_first, test_qf_convert_first, test_unknown_fnmode_raises, test_f2_before_f1_gate_raises_before_any_subprocess, test_orchestration_sequences_convert_then_direct_then_smile_then_indirect, test_smile_input_is_f2_processed_not_raw_converted_fid, test_reconstruct_help_lists_knob_flags
    - `grep -l skipif tests/nus/test_reconstruct_integration.py` matches (integration test is skipif-guarded, not plain skip)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/ -q</automated>
  </verify>
  <done>Every RECON requirement has a named, skipped, docstring-specified stub; `pytest tests/nus/ -q` is green (all skipped) with no import errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → tmp filesystem | Fixtures write fake intermediates only under pytest `tmp_path`; no external/network input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | conftest fake-file factories | accept | Files written only under pytest `tmp_path`; no user-controlled path concatenation |
| T-98-SC | Tampering | pip installs | accept | No new package installs this phase (98-RESEARCH.md Package Legitimacy Audit: N/A; `[nus]` extra stays empty) |
</threat_model>

<verification>
- `pytest tests/nus/ -q` green (all skipped, zero import errors)
- `pytest -q` still green (no regression to the existing 1289-test suite)
</verification>

<success_criteria>
tests/nus/ is a collectable package; every RECON requirement has a named RED-by-skip stub; the
integration test is skipif-guarded; conftest provides the run_stage mock seam and fake-intermediate
factories with no top-level import of not-yet-existing Phase-98 modules.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-01-SUMMARY.md` when done
</output>
