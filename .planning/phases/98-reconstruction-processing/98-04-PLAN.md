---
phase: 98-reconstruction-processing
plan: 04
type: execute
wave: 3
depends_on: [98-02]
files_modified:
  - src/lucy_ng/nus/postprocess.py
  - tests/nus/test_processing_order.py
autonomous: true
requirements: [RECON-02]

must_haves:
  truths:
    - "The direct dimension (F2) is apodized, zero-filled, FT'd, phased and baseline-corrected as ONE run_stage-checked stage before any transpose/indirect processing"
    - "F2 phase (p0/p1) and F1 phase (xP0/xP1) are named, CLI-overridable constants — never a blind auto-phase search"
    - "Output ppm axes are reversed (highest ppm first) and calibrated against the §10 ground-truth 1D 13C shifts"
  artifacts:
    - path: "src/lucy_ng/nus/postprocess.py"
      provides: "FT/apod/phase/baseline stage + ppm-axis reversal/calibration helper"
      contains: "def process_direct_then_indirect("
      min_lines: 60
  key_links:
    - from: "src/lucy_ng/nus/postprocess.py"
      to: "lucy_ng.nus.runner.run_stage"
      via: "import — the post-processing stage's final .ft2 is run_stage-verified"
      pattern: "run_stage"
    - from: "src/lucy_ng/nus/postprocess.py"
      to: "NusAcquisitionParams.f2_sf / f2_offset / f1_sf / f1_offset"
      via: "ppm-scale calibration arithmetic"
      pattern: "f2_sf|f2_offset|f1_sf|f1_offset"
---

<objective>
Build `nus/postprocess.py`: the post-reconstruction processing stage (apodization / zero-fill /
FT / deterministic phase / baseline) plus the ppm-axis reversal and 1D-calibration helper. The
whole apod→ZF→FT→PS→POLY chain runs as ONE D-01 subprocess stage (idiomatic NMRPipe internal `|`
chaining is allowed within the stage), with the stage's final `processed.ft2` verified by the
Plan-02 `run_stage()` wrapper. Phase is deterministic and CLI-overridable — never a blind
auto-phase search (D-02, Pitfall 10).

Purpose: RECON-02 (F2-first processing on reversed, 1D-calibrated ppm axes). Output:
`src/lucy_ng/nus/postprocess.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-reconstruction-processing/98-CONTEXT.md
@.planning/phases/98-reconstruction-processing/98-RESEARCH.md
@.planning/phases/98-reconstruction-processing/98-PATTERNS.md

<interfaces>
From src/lucy_ng/nus/runner.py (Plan 02):
  run_stage(name, argv: list[str], cwd: Path, expected_output: Path, timeout=600) -> None

From src/lucy_ng/models/nus.py (Phase 97):
  NusAcquisitionParams.f2_sf, .f2_offset, .f1_sf, .f1_offset (calibration; may be None pre-processing)
  NusAcquisitionParams.f2_sfo1, .f2_sw_h, .f1_sfo1, .f1_sw_h, .f1_o1 (ppm-scale inputs)

§10 ground-truth 1D 13C shifts (calibration cross-check target), from
/Users/steinbeck/Dropbox/develop/data/nmrdata/active-lucy-ng-testprojects/C20H32O2/analysis/NUS-RECONSTRUCTION-GUIDE.md §10:
  142.00, 135.86, 79.35, 69.06, 67.06, 51.63, 37.86, 37.19, 36.23, 35.23, 34.21, 33.67,
  30.66, 29.77, 27.93, 27.15, 25.96, 23.43, 22.63, 21.78

Deterministic phase (D-02 / 98-RESEARCH.md Code Examples, provisional per Assumptions Log A2):
  F2: p0/p1 from the reliable 1D reference (empirically determined constant, e.g. -24/0 in the manual example)
  F1 echo-antiecho: xP0=90 / xP1=0 (manual TROSY example; A2-provisional for hsqcedetgpsp.3/hmbcetgpl3nd)
  COSY: magnitude mode, no phase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-processing stage (apod/ZF/FT/deterministic phase/baseline) as one run_stage-checked stage</name>
  <files>src/lucy_ng/nus/postprocess.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Code Examples (the SP/GMB→ZF→FT→PS→POLY chain + the F2-before-F1 order it encodes) and § Anti-Patterns (do NOT decompose into N single-verb subprocess calls)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "nus/postprocess.py" (one stage via a small .com script or piped Popen; run_stage on the final .ft2; deterministic-phase-as-constant rule)
    - src/lucy_ng/nus/runner.py (import run_stage — no circular risk: postprocess→runner only)
  </read_first>
  <action>
    Create `src/lucy_ng/nus/postprocess.py` with a `process_direct_then_indirect(reconstructed_fid:
    Path, stage_dir: Path, params: NusAcquisitionParams, *, f2_p0: float, f2_p1: float, f1_p0:
    float, f1_p1: float, magnitude: bool, timeout: int = 600) -> Path` function that builds the
    NMRPipe apod→ZF→FT→PS(f2_p0/f2_p1)→POLY direct-dimension chain, THEN transpose, THEN the
    indirect-dimension FT/PS(f1_p0/f1_p1) chain, as ONE subprocess stage (either by writing a small
    `.com` script into stage_dir and executing it with a fixed argv, or by chaining `nmrPipe`
    invocations via `subprocess.Popen` pipes) — and verify the final `processed.ft2` via
    `run_stage("postprocess", ...)` (or an equivalent final-output run_stage call). For
    `magnitude is True` (COSY), skip the phase-correction verbs and use magnitude-mode processing;
    phase values are passed-in constants, NEVER computed by a search. Add a source comment marking
    the F1 phase defaults PROVISIONAL per Assumptions Log A2. Do NOT split the NMRPipe verb chain
    into per-verb `subprocess.run` calls (98-RESEARCH.md Anti-Patterns).
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/postprocess.py` contains `def process_direct_then_indirect(`
    - `grep -c 'shell=True' src/lucy_ng/nus/postprocess.py` returns 0
    - The function imports and uses `run_stage` for the final `.ft2` verification
    - Phase parameters appear as function arguments (grep `f2_p0`, `f1_p0`), never derived by a loop/search over phase (no phase-optimizing `while` loop in the file)
    - `mypy src/lucy_ng/nus/postprocess.py` passes (strict)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('src/lucy_ng/nus/postprocess.py').read()); assert any(isinstance(n,ast.FunctionDef) and n.name=='process_direct_then_indirect' for n in ast.walk(t))"</automated>
  </verify>
  <done>The direct dimension is processed as one run_stage-checked stage with deterministic, argument-supplied phase (magnitude branch for COSY); no per-verb subprocess sprawl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ppm-axis reversal + 1D calibration cross-check (RECON-02)</name>
  <files>src/lucy_ng/nus/postprocess.py, tests/nus/test_processing_order.py</files>
  <read_first>
    - .planning/phases/98-reconstruction-processing/98-RESEARCH.md § Architectural Responsibility Map ("ppm-axis reversal + 1D calibration cross-check" = Application logic, simple arithmetic) and § Common Pitfalls (ppm calibration vs 1D reference)
    - .planning/phases/98-reconstruction-processing/98-PATTERNS.md § "ppm-axis reversal + 1D calibration cross-check"
    - src/lucy_ng/models/nus.py lines 72-77 (f2_sf/f2_offset/f1_sf/f1_offset fields)
    - tests/nus/test_processing_order.py (Plan-01 stubs test_ppm_axes_reversed, test_ppm_calibrated_to_1d_reference)
  </read_first>
  <behavior>
    - ppm_scale(sf, offset, size) returns a descending (reversed, highest-ppm-first) array of length `size`
    - calibration cross-check flags a systematic ppm offset when a reconstructed F2 carbon axis is compared against the §10 shift list beyond a tolerance
    - test_ppm_axes_reversed: the returned scale[0] > scale[-1] (reversed Bruker convention)
    - test_ppm_calibrated_to_1d_reference: a scale built from the exp3 F1 SF/OFFSET maps the known 1D shifts within tolerance; an artificially offset scale is detected
  </behavior>
  <action>
    Add a `ppm_scale(sf: float, offset: float, size: int) -> list[float]` (or numpy array) helper
    computing a reversed ppm axis from SF/OFFSET (Bruker convention: index 0 = highest ppm), and a
    `check_calibration(observed_ppms, reference_ppms=GUIDE_S10_C13, tol=...) -> bool` (or a small
    result object) that cross-checks reconstructed carbon shifts against the §10 ground-truth 1D
    list. These are pure arithmetic — no external tool. Then implement the two RECON-02 ppm stubs
    in test_processing_order.py against these helpers using the fixture SF/OFFSET values.
  </action>
  <acceptance_criteria>
    - `src/lucy_ng/nus/postprocess.py` contains `def ppm_scale(` and a calibration cross-check helper
    - `pytest tests/nus/test_processing_order.py -q` passes test_ppm_axes_reversed and test_ppm_calibrated_to_1d_reference (0 skipped)
    - The reversed-axis assertion holds: returned scale[0] > scale[-1]
    - `mypy src/lucy_ng/nus/postprocess.py` passes (strict)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/nus/test_processing_order.py -q</automated>
  </verify>
  <done>ppm axes are reversed and calibrated against the §10 1D reference; both RECON-02 ppm tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| postprocess → nmrPipe subprocess | External-tool invocation; the post-processing verb chain runs as one stage with a fixed argv / a script written into stage_dir |
| postprocess → stage_dir filesystem | Reads reconstructed.fid, writes processed.ft2 within the caller-supplied resolved stage_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | nmrPipe post-processing stage invocation | mitigate | No `shell=True` (grep-gated); if a `.com` script is written it lives under the resolved stage_dir and is executed with a fixed argv, phase/window values are typed floats not shell strings |
| T-98-02 | Tampering | processed.ft2 write location | mitigate | Written only under the caller-supplied resolved stage_dir; no user-string path concatenation |
| T-98-SC | Tampering | pip installs | accept | No new package installs; nmrglue/numpy already core |
</threat_model>

<verification>
- `pytest tests/nus/test_processing_order.py -q` green (0 skipped)
- `mypy src/lucy_ng/nus/postprocess.py` passes
- `pytest -q` still green
</verification>

<success_criteria>
Direct-dimension processing runs as one run_stage-checked stage with deterministic argument-supplied
phase (magnitude branch for COSY); ppm axes are reversed and calibrated against the §10 1D reference;
RECON-02 processing/ppm tests green.
</success_criteria>

<output>
Create `.planning/phases/98-reconstruction-processing/98-04-SUMMARY.md` when done
</output>
