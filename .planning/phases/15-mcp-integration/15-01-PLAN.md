---
phase: 15-mcp-integration
plan: 01
type: execute
---

<objective>
Update MCP `dereplicate_c13` tool to use SQLite database backend by default.

Purpose: Complete the v1.1 milestone by bringing database-backed dereplication to AI agents.
Output: MCP tool that auto-detects database, uses it when available, falls back to SD files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cli-integration/14-01-SUMMARY.md

**Key files:**
@src/lucy_ng/mcp/server.py
@src/lucy_ng/cli/dereplicate.py
@src/lucy_ng/database/query.py

**Tech stack available:**
- SQLite database via `DatabaseQueryService` (Phase 13)
- Database detection helpers from CLI module (Phase 14)
- FastMCP framework for MCP tools

**Established patterns:**
- Phase 14: Database auto-detection (env var → default location → SD file fallback)
- Phase 14: Informative messages about which backend is being used
- Phase 14: `Any` type for loader abstraction

**Constraining decisions:**
- Phase 14: Default database location is `data/reference/compounds.db`
- Phase 14: Environment variable `LUCY_DATABASE` for custom locations
- Phase 14: Use `DatabaseQueryService.get_by_formula()` - same interface as SD loaders
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP dereplicate_c13 to use database backend</name>
  <files>src/lucy_ng/mcp/server.py</files>
  <action>
  Modify `dereplicate_c13` MCP tool to mirror CLI behavior from Phase 14:

  1. Import database helpers from CLI module:
     - `from lucy_ng.cli.dereplicate import _find_database_path, _is_sqlite_database`
     - `from lucy_ng.database import DatabaseQueryService`

  2. Update the database discovery logic BEFORE SD file search:
     - If `database_path` provided and ends with `.db` → use DatabaseQueryService
     - If `database_path` is None → check `_find_database_path()` for auto-detected database
     - If database found → use DatabaseQueryService
     - Otherwise → fall back to existing SD file logic

  3. Add `database_type` field to return value:
     - "sqlite" when using DatabaseQueryService
     - "coconut_sd" or "nmrshiftdb_sd" when using SD files
     - Include compound count in return for SQLite databases

  4. Ensure DatabaseQueryService is properly closed after use (try/finally pattern)

  Key implementation notes:
  - Reuse `_find_database_path()` and `_is_sqlite_database()` from CLI - DO NOT duplicate logic
  - Return `database_type` and `compound_count` fields for agent transparency
  - Keep backward compatibility: existing parameters should work unchanged
  </action>
  <verify>
  python -c "from lucy_ng.mcp.server import dereplicate_c13; print(dereplicate_c13.__doc__)"
  ruff check src/lucy_ng/mcp/server.py
  mypy src/lucy_ng/mcp/server.py
  </verify>
  <done>
  - MCP tool uses database when present at `data/reference/compounds.db`
  - Falls back to SD files when database not found
  - Returns `database_type` field indicating backend used
  - All type checks and lint checks pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for database-backed MCP tool</name>
  <files>tests/test_mcp_server.py</files>
  <action>
  Add tests to verify database backend integration:

  1. Test database detection:
     - Test that `.db` path is recognized and uses DatabaseQueryService
     - Test that SD file path still works for backward compatibility

  2. Test auto-detection:
     - Mock `_find_database_path()` to return a database path
     - Verify DatabaseQueryService is used

  3. Test return value:
     - Verify `database_type` field is present
     - Verify `compound_count` field is present when using SQLite

  Use pytest fixtures with mocking for database path detection.
  Pattern follows existing MCP tests in the file.
  </action>
  <verify>pytest tests/test_mcp_server.py -v -k "dereplicate"</verify>
  <done>
  - Tests for database path detection
  - Tests for backward compatibility with SD files
  - Tests for new return fields
  - All tests pass
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `lucy dereplicate c13 --help` still works (no breakage)
- [ ] MCP tool uses database when present (check return value)
- [ ] MCP tool falls back to SD when database not present
- [ ] `pytest tests/test_mcp_server.py -v` passes
- [ ] `ruff check src/lucy_ng/mcp/server.py` - no linting errors
- [ ] `mypy src/lucy_ng/mcp/server.py` - no type errors
</verification>

<success_criteria>

- MCP tool seamlessly uses database when `data/reference/compounds.db` exists
- Fallback to SD files works when database not present
- No breaking changes to existing MCP interface
- Return value includes `database_type` for agent transparency
- All tests pass
- Phase 15 complete → v1.1 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/15-mcp-integration/15-01-SUMMARY.md` with:

# Phase 15-01 Summary: MCP Integration

**[One-liner describing what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `src/lucy_ng/mcp/server.py` - Database backend integration
- `tests/test_mcp_server.py` - MCP dereplication tests

## Decisions Made
[Any decisions made during implementation]

## Issues Encountered
[Problems and resolutions, or "None"]

## Milestone Complete
v1.1 Database-Backed Dereplication milestone is complete:
- Phase 11: Database Schema ✓
- Phase 12: Database Import ✓
- Phase 13: Database Query API ✓
- Phase 14: CLI Integration ✓
- Phase 15: MCP Integration ✓
</output>
